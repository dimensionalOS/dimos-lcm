"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

from io import BytesIO
import struct
import sys

import std_msgs


class EpisodeStatus(object):
    msg_name = "imitation_msgs.EpisodeStatus"

    __slots__ = [
        "header",
        "state",
        "episodes_saved",
        "episodes_discarded",
        "last_event",
        "task_label",
    ]

    __typenames__ = [
        "std_msgs.Header",
        "string",
        "int32_t",
        "int32_t",
        "string",
        "string",
    ]

    __dimensions__ = [None, None, None, None, None, None]

    header: std_msgs.Header
    state: "string"
    episodes_saved: "int32_t"
    episodes_discarded: "int32_t"
    last_event: "string"
    task_label: "string"

    def __init__(
        self,
        header=std_msgs.Header(),
        state="",
        episodes_saved=0,
        episodes_discarded=0,
        last_event="",
        task_label="",
    ):
        # LCM Type: std_msgs.Header
        self.header = header
        # LCM Type: string
        self.state = state
        # LCM Type: int32_t
        self.episodes_saved = episodes_saved
        # LCM Type: int32_t
        self.episodes_discarded = episodes_discarded
        # LCM Type: string
        self.last_event = last_event
        # LCM Type: string
        self.task_label = task_label

    def lcm_encode(self):
        buf = BytesIO()
        buf.write(EpisodeStatus._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        assert (
            self.header._get_packed_fingerprint()
            == std_msgs.Header._get_packed_fingerprint()
        )
        self.header._encode_one(buf)
        __state_encoded = self.state.encode("utf-8")
        buf.write(struct.pack(">I", len(__state_encoded) + 1))
        buf.write(__state_encoded)
        buf.write(b"\0")
        buf.write(struct.pack(">ii", self.episodes_saved, self.episodes_discarded))
        __last_event_encoded = self.last_event.encode("utf-8")
        buf.write(struct.pack(">I", len(__last_event_encoded) + 1))
        buf.write(__last_event_encoded)
        buf.write(b"\0")
        __task_label_encoded = self.task_label.encode("utf-8")
        buf.write(struct.pack(">I", len(__task_label_encoded) + 1))
        buf.write(__task_label_encoded)
        buf.write(b"\0")

    @classmethod
    def lcm_decode(cls, data: bytes):
        if hasattr(data, "read"):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != cls._get_packed_fingerprint():
            raise ValueError("Decode error")
        return cls._decode_one(buf)

    @classmethod
    def _decode_one(cls, buf):
        self = cls()
        self.header = cls._get_field_type("header")._decode_one(buf)
        __state_len = struct.unpack(">I", buf.read(4))[0]
        self.state = buf.read(__state_len)[:-1].decode("utf-8", "replace")
        self.episodes_saved, self.episodes_discarded = struct.unpack(">ii", buf.read(8))
        __last_event_len = struct.unpack(">I", buf.read(4))[0]
        self.last_event = buf.read(__last_event_len)[:-1].decode("utf-8", "replace")
        __task_label_len = struct.unpack(">I", buf.read(4))[0]
        self.task_label = buf.read(__task_label_len)[:-1].decode("utf-8", "replace")
        return self

    @classmethod
    def _get_field_type(cls, field_name):
        """Get the type for a field from annotations."""
        annotation = cls.__annotations__.get(field_name)
        if annotation is None:
            return None
        if isinstance(annotation, str):
            module = sys.modules[cls.__module__]
            if hasattr(module, annotation):
                return getattr(module, annotation)
            return None
        return annotation

    @classmethod
    def _get_hash_recursive(cls, parents):
        if cls in parents:
            return 0
        newparents = parents + [cls]
        tmphash = (
            0x3454DE950643DD2F + std_msgs.Header._get_hash_recursive(newparents)
        ) & 0xFFFFFFFFFFFFFFFF
        tmphash = (
            ((tmphash << 1) & 0xFFFFFFFFFFFFFFFF) + (tmphash >> 63)
        ) & 0xFFFFFFFFFFFFFFFF
        return tmphash

    _packed_fingerprint = None

    @classmethod
    def _get_packed_fingerprint(cls):
        if cls._packed_fingerprint is None:
            cls._packed_fingerprint = struct.pack(">Q", cls._get_hash_recursive([]))
        return cls._packed_fingerprint

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", cls._get_packed_fingerprint())[0]
