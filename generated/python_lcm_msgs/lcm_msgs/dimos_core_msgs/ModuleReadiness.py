"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

from io import BytesIO
import struct
import sys


class ModuleReadiness(object):
    msg_name = "dimos_core_msgs.ModuleReadiness"

    __slots__ = ["source", "generation", "sequence", "source_ts", "ready", "reason"]

    __typenames__ = ["string", "int64_t", "int64_t", "double", "boolean", "string"]

    __dimensions__ = [None, None, None, None, None, None]

    source: "string"
    generation: "int64_t"
    sequence: "int64_t"
    source_ts: "double"
    ready: "boolean"
    reason: "string"

    def __init__(
        self, source="", generation=0, sequence=0, source_ts=0.0, ready=False, reason=""
    ):
        # LCM Type: string
        self.source = source
        # LCM Type: int64_t
        self.generation = generation
        # LCM Type: int64_t
        self.sequence = sequence
        # LCM Type: double
        self.source_ts = source_ts
        # LCM Type: boolean
        self.ready = ready
        # LCM Type: string
        self.reason = reason

    def lcm_encode(self):
        buf = BytesIO()
        buf.write(ModuleReadiness._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        __source_encoded = self.source.encode("utf-8")
        buf.write(struct.pack(">I", len(__source_encoded) + 1))
        buf.write(__source_encoded)
        buf.write(b"\0")
        buf.write(
            struct.pack(
                ">qqdb", self.generation, self.sequence, self.source_ts, self.ready
            )
        )
        __reason_encoded = self.reason.encode("utf-8")
        buf.write(struct.pack(">I", len(__reason_encoded) + 1))
        buf.write(__reason_encoded)
        buf.write(b"\0")

    @classmethod
    def lcm_decode(cls, data: bytes):
        if hasattr(data, "read"):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != cls._get_packed_fingerprint():
            raise ValueError("Decode error")
        return cls._decode_one(buf)

    @classmethod
    def _decode_one(cls, buf):
        self = cls()
        __source_len = struct.unpack(">I", buf.read(4))[0]
        self.source = buf.read(__source_len)[:-1].decode("utf-8", "replace")
        self.generation, self.sequence, self.source_ts = struct.unpack(
            ">qqd", buf.read(24)
        )
        self.ready = bool(struct.unpack("b", buf.read(1))[0])
        __reason_len = struct.unpack(">I", buf.read(4))[0]
        self.reason = buf.read(__reason_len)[:-1].decode("utf-8", "replace")
        return self

    @classmethod
    def _get_field_type(cls, field_name):
        """Get the type for a field from annotations."""
        annotation = cls.__annotations__.get(field_name)
        if annotation is None:
            return None
        if isinstance(annotation, str):
            module = sys.modules[cls.__module__]
            if hasattr(module, annotation):
                return getattr(module, annotation)
            return None
        return annotation

    @classmethod
    def _get_hash_recursive(cls, parents):
        if cls in parents:
            return 0
        tmphash = (0x150D1BFA7D87808D) & 0xFFFFFFFFFFFFFFFF
        tmphash = (
            ((tmphash << 1) & 0xFFFFFFFFFFFFFFFF) + (tmphash >> 63)
        ) & 0xFFFFFFFFFFFFFFFF
        return tmphash

    _packed_fingerprint = None

    @classmethod
    def _get_packed_fingerprint(cls):
        if cls._packed_fingerprint is None:
            cls._packed_fingerprint = struct.pack(">Q", cls._get_hash_recursive([]))
        return cls._packed_fingerprint

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", cls._get_packed_fingerprint())[0]
