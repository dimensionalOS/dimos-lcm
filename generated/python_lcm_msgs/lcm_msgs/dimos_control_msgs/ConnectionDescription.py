"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

from io import BytesIO
import struct
import sys

from . import *
from .CommandProfile import CommandProfile
from .InterfaceDescription import InterfaceDescription
from .ResourceDescription import ResourceDescription


class ConnectionDescription(object):
    msg_name = "dimos_control_msgs.ConnectionDescription"

    __slots__ = [
        "source",
        "description_epoch",
        "resources_length",
        "interfaces_length",
        "command_profiles_length",
        "resources",
        "interfaces",
        "command_profiles",
        "expected_state_rate_hz",
        "state_stale_timeout_s",
        "command_watchdog_timeout_s",
        "activation_policy",
        "armed_idle_policy",
        "safe_stop_behavior",
        "process_loss_classification",
        "process_loss_evidence",
    ]

    __typenames__ = [
        "string",
        "int64_t",
        "int32_t",
        "int32_t",
        "int32_t",
        "ResourceDescription",
        "InterfaceDescription",
        "CommandProfile",
        "double",
        "double",
        "double",
        "int8_t",
        "int8_t",
        "string",
        "int8_t",
        "string",
    ]

    __dimensions__ = [
        None,
        None,
        None,
        None,
        None,
        ["resources_length"],
        ["interfaces_length"],
        ["command_profiles_length"],
        None,
        None,
        None,
        None,
        None,
        None,
        None,
        None,
    ]

    source: "string"
    description_epoch: "int64_t"
    resources_length: "int32_t"
    interfaces_length: "int32_t"
    command_profiles_length: "int32_t"
    resources: ResourceDescription
    interfaces: InterfaceDescription
    command_profiles: CommandProfile
    expected_state_rate_hz: "double"
    state_stale_timeout_s: "double"
    command_watchdog_timeout_s: "double"
    activation_policy: "int8_t"
    armed_idle_policy: "int8_t"
    safe_stop_behavior: "string"
    process_loss_classification: "int8_t"
    process_loss_evidence: "string"

    DIRECT = 1
    OPERATOR_CONFIRMED = 2
    APPLY_OMISSION = 1
    ARMED_IDLE_SAFE_STOP = 2
    NATIVE_WATCHDOG = 1
    EXTERNAL_SUPERVISOR = 2
    INTRINSICALLY_SAFE = 3
    SIMULATION = 4
    UNPROTECTED = 5

    def __init__(
        self,
        source="",
        description_epoch=0,
        resources_length=0,
        interfaces_length=0,
        command_profiles_length=0,
        resources=[],
        interfaces=[],
        command_profiles=[],
        expected_state_rate_hz=0.0,
        state_stale_timeout_s=0.0,
        command_watchdog_timeout_s=0.0,
        activation_policy=0,
        armed_idle_policy=0,
        safe_stop_behavior="",
        process_loss_classification=0,
        process_loss_evidence="",
    ):
        # LCM Type: string
        self.source = source
        # LCM Type: int64_t
        self.description_epoch = description_epoch
        # LCM Type: int32_t
        self.resources_length = resources_length
        # LCM Type: int32_t
        self.interfaces_length = interfaces_length
        # LCM Type: int32_t
        self.command_profiles_length = command_profiles_length
        # LCM Type: ResourceDescription[resources_length]
        self.resources = resources
        # LCM Type: InterfaceDescription[interfaces_length]
        self.interfaces = interfaces
        # LCM Type: CommandProfile[command_profiles_length]
        self.command_profiles = command_profiles
        # LCM Type: double
        self.expected_state_rate_hz = expected_state_rate_hz
        # LCM Type: double
        self.state_stale_timeout_s = state_stale_timeout_s
        # LCM Type: double
        self.command_watchdog_timeout_s = command_watchdog_timeout_s
        # LCM Type: int8_t
        self.activation_policy = activation_policy
        # LCM Type: int8_t
        self.armed_idle_policy = armed_idle_policy
        # LCM Type: string
        self.safe_stop_behavior = safe_stop_behavior
        # LCM Type: int8_t
        self.process_loss_classification = process_loss_classification
        # LCM Type: string
        self.process_loss_evidence = process_loss_evidence

    def lcm_encode(self):
        buf = BytesIO()
        buf.write(ConnectionDescription._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        __source_encoded = self.source.encode("utf-8")
        buf.write(struct.pack(">I", len(__source_encoded) + 1))
        buf.write(__source_encoded)
        buf.write(b"\0")
        buf.write(
            struct.pack(
                ">qiii",
                self.description_epoch,
                self.resources_length,
                self.interfaces_length,
                self.command_profiles_length,
            )
        )
        for i0 in range(self.resources_length):
            assert (
                self.resources[i0]._get_packed_fingerprint()
                == ResourceDescription._get_packed_fingerprint()
            )
            self.resources[i0]._encode_one(buf)
        for i0 in range(self.interfaces_length):
            assert (
                self.interfaces[i0]._get_packed_fingerprint()
                == InterfaceDescription._get_packed_fingerprint()
            )
            self.interfaces[i0]._encode_one(buf)
        for i0 in range(self.command_profiles_length):
            assert (
                self.command_profiles[i0]._get_packed_fingerprint()
                == CommandProfile._get_packed_fingerprint()
            )
            self.command_profiles[i0]._encode_one(buf)
        buf.write(
            struct.pack(
                ">dddbb",
                self.expected_state_rate_hz,
                self.state_stale_timeout_s,
                self.command_watchdog_timeout_s,
                self.activation_policy,
                self.armed_idle_policy,
            )
        )
        __safe_stop_behavior_encoded = self.safe_stop_behavior.encode("utf-8")
        buf.write(struct.pack(">I", len(__safe_stop_behavior_encoded) + 1))
        buf.write(__safe_stop_behavior_encoded)
        buf.write(b"\0")
        buf.write(struct.pack(">b", self.process_loss_classification))
        __process_loss_evidence_encoded = self.process_loss_evidence.encode("utf-8")
        buf.write(struct.pack(">I", len(__process_loss_evidence_encoded) + 1))
        buf.write(__process_loss_evidence_encoded)
        buf.write(b"\0")

    @classmethod
    def lcm_decode(cls, data: bytes):
        if hasattr(data, "read"):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != cls._get_packed_fingerprint():
            raise ValueError("Decode error")
        return cls._decode_one(buf)

    @classmethod
    def _decode_one(cls, buf):
        self = cls()
        __source_len = struct.unpack(">I", buf.read(4))[0]
        self.source = buf.read(__source_len)[:-1].decode("utf-8", "replace")
        (
            self.description_epoch,
            self.resources_length,
            self.interfaces_length,
            self.command_profiles_length,
        ) = struct.unpack(">qiii", buf.read(20))
        self.resources = []
        for i0 in range(self.resources_length):
            self.resources.append(cls._get_field_type("resources")._decode_one(buf))
        self.interfaces = []
        for i0 in range(self.interfaces_length):
            self.interfaces.append(cls._get_field_type("interfaces")._decode_one(buf))
        self.command_profiles = []
        for i0 in range(self.command_profiles_length):
            self.command_profiles.append(
                cls._get_field_type("command_profiles")._decode_one(buf)
            )
        (
            self.expected_state_rate_hz,
            self.state_stale_timeout_s,
            self.command_watchdog_timeout_s,
            self.activation_policy,
            self.armed_idle_policy,
        ) = struct.unpack(">dddbb", buf.read(26))
        __safe_stop_behavior_len = struct.unpack(">I", buf.read(4))[0]
        self.safe_stop_behavior = buf.read(__safe_stop_behavior_len)[:-1].decode(
            "utf-8", "replace"
        )
        self.process_loss_classification = struct.unpack(">b", buf.read(1))[0]
        __process_loss_evidence_len = struct.unpack(">I", buf.read(4))[0]
        self.process_loss_evidence = buf.read(__process_loss_evidence_len)[:-1].decode(
            "utf-8", "replace"
        )
        return self

    @classmethod
    def _get_field_type(cls, field_name):
        """Get the type for a field from annotations."""
        annotation = cls.__annotations__.get(field_name)
        if annotation is None:
            return None
        if isinstance(annotation, str):
            module = sys.modules[cls.__module__]
            if hasattr(module, annotation):
                return getattr(module, annotation)
            return None
        return annotation

    @classmethod
    def _get_hash_recursive(cls, parents):
        if cls in parents:
            return 0
        newparents = parents + [cls]
        tmphash = (
            0x31D30851F65C7D8C
            + ResourceDescription._get_hash_recursive(newparents)
            + InterfaceDescription._get_hash_recursive(newparents)
            + CommandProfile._get_hash_recursive(newparents)
        ) & 0xFFFFFFFFFFFFFFFF
        tmphash = (
            ((tmphash << 1) & 0xFFFFFFFFFFFFFFFF) + (tmphash >> 63)
        ) & 0xFFFFFFFFFFFFFFFF
        return tmphash

    _packed_fingerprint = None

    @classmethod
    def _get_packed_fingerprint(cls):
        if cls._packed_fingerprint is None:
            cls._packed_fingerprint = struct.pack(">Q", cls._get_hash_recursive([]))
        return cls._packed_fingerprint

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", cls._get_packed_fingerprint())[0]
