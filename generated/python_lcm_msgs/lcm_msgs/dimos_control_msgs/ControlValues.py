"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

from io import BytesIO
import struct
import sys


class ControlValues(object):
    msg_name = "dimos_control_msgs.ControlValues"

    __slots__ = [
        "source",
        "source_ts",
        "epoch",
        "sequence",
        "interface_names_length",
        "values_length",
        "interface_names",
        "values",
    ]

    __typenames__ = [
        "string",
        "double",
        "int64_t",
        "int64_t",
        "int32_t",
        "int32_t",
        "string",
        "double",
    ]

    __dimensions__ = [
        None,
        None,
        None,
        None,
        None,
        None,
        ["interface_names_length"],
        ["values_length"],
    ]

    source: "string"
    source_ts: "double"
    epoch: "int64_t"
    sequence: "int64_t"
    interface_names_length: "int32_t"
    values_length: "int32_t"
    interface_names: "string"
    values: "double"

    def __init__(
        self,
        source="",
        source_ts=0.0,
        epoch=0,
        sequence=0,
        interface_names_length=0,
        values_length=0,
        interface_names=[],
        values=[],
    ):
        # LCM Type: string
        self.source = source
        # LCM Type: double
        self.source_ts = source_ts
        # LCM Type: int64_t
        self.epoch = epoch
        # LCM Type: int64_t
        self.sequence = sequence
        # LCM Type: int32_t
        self.interface_names_length = interface_names_length
        # LCM Type: int32_t
        self.values_length = values_length
        # LCM Type: string[interface_names_length]
        self.interface_names = interface_names
        # LCM Type: double[values_length]
        self.values = values

    def lcm_encode(self):
        buf = BytesIO()
        buf.write(ControlValues._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        __source_encoded = self.source.encode("utf-8")
        buf.write(struct.pack(">I", len(__source_encoded) + 1))
        buf.write(__source_encoded)
        buf.write(b"\0")
        buf.write(
            struct.pack(
                ">dqqii",
                self.source_ts,
                self.epoch,
                self.sequence,
                self.interface_names_length,
                self.values_length,
            )
        )
        for i0 in range(self.interface_names_length):
            __interface_names_encoded = self.interface_names[i0].encode("utf-8")
            buf.write(struct.pack(">I", len(__interface_names_encoded) + 1))
            buf.write(__interface_names_encoded)
            buf.write(b"\0")
        buf.write(
            struct.pack(">%dd" % self.values_length, *self.values[: self.values_length])
        )

    @classmethod
    def lcm_decode(cls, data: bytes):
        if hasattr(data, "read"):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != cls._get_packed_fingerprint():
            raise ValueError("Decode error")
        return cls._decode_one(buf)

    @classmethod
    def _decode_one(cls, buf):
        self = cls()
        __source_len = struct.unpack(">I", buf.read(4))[0]
        self.source = buf.read(__source_len)[:-1].decode("utf-8", "replace")
        (
            self.source_ts,
            self.epoch,
            self.sequence,
            self.interface_names_length,
            self.values_length,
        ) = struct.unpack(">dqqii", buf.read(32))
        self.interface_names = []
        for i0 in range(self.interface_names_length):
            __interface_names_len = struct.unpack(">I", buf.read(4))[0]
            self.interface_names.append(
                buf.read(__interface_names_len)[:-1].decode("utf-8", "replace")
            )
        self.values = struct.unpack(
            ">%dd" % self.values_length, buf.read(self.values_length * 8)
        )
        return self

    @classmethod
    def _get_field_type(cls, field_name):
        """Get the type for a field from annotations."""
        annotation = cls.__annotations__.get(field_name)
        if annotation is None:
            return None
        if isinstance(annotation, str):
            module = sys.modules[cls.__module__]
            if hasattr(module, annotation):
                return getattr(module, annotation)
            return None
        return annotation

    @classmethod
    def _get_hash_recursive(cls, parents):
        if cls in parents:
            return 0
        tmphash = (0x98D9F64B18D5A4CF) & 0xFFFFFFFFFFFFFFFF
        tmphash = (
            ((tmphash << 1) & 0xFFFFFFFFFFFFFFFF) + (tmphash >> 63)
        ) & 0xFFFFFFFFFFFFFFFF
        return tmphash

    _packed_fingerprint = None

    @classmethod
    def _get_packed_fingerprint(cls):
        if cls._packed_fingerprint is None:
            cls._packed_fingerprint = struct.pack(">Q", cls._get_hash_recursive([]))
        return cls._packed_fingerprint

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", cls._get_packed_fingerprint())[0]
