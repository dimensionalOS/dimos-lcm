"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

from io import BytesIO
import struct
import sys


class ResourceDescription(object):
    msg_name = "dimos_control_msgs.ResourceDescription"

    __slots__ = ["name", "resource_type", "group_name"]

    __typenames__ = ["string", "int8_t", "string"]

    __dimensions__ = [None, None, None]

    name: "string"
    resource_type: "int8_t"
    group_name: "string"

    JOINT = 1
    GRIPPER = 2
    BASE = 3
    IMU = 4

    def __init__(self, name="", resource_type=0, group_name=""):
        # LCM Type: string
        self.name = name
        # LCM Type: int8_t
        self.resource_type = resource_type
        # LCM Type: string
        self.group_name = group_name

    def lcm_encode(self):
        buf = BytesIO()
        buf.write(ResourceDescription._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        __name_encoded = self.name.encode("utf-8")
        buf.write(struct.pack(">I", len(__name_encoded) + 1))
        buf.write(__name_encoded)
        buf.write(b"\0")
        buf.write(struct.pack(">b", self.resource_type))
        __group_name_encoded = self.group_name.encode("utf-8")
        buf.write(struct.pack(">I", len(__group_name_encoded) + 1))
        buf.write(__group_name_encoded)
        buf.write(b"\0")

    @classmethod
    def lcm_decode(cls, data: bytes):
        if hasattr(data, "read"):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != cls._get_packed_fingerprint():
            raise ValueError("Decode error")
        return cls._decode_one(buf)

    @classmethod
    def _decode_one(cls, buf):
        self = cls()
        __name_len = struct.unpack(">I", buf.read(4))[0]
        self.name = buf.read(__name_len)[:-1].decode("utf-8", "replace")
        self.resource_type = struct.unpack(">b", buf.read(1))[0]
        __group_name_len = struct.unpack(">I", buf.read(4))[0]
        self.group_name = buf.read(__group_name_len)[:-1].decode("utf-8", "replace")
        return self

    @classmethod
    def _get_field_type(cls, field_name):
        """Get the type for a field from annotations."""
        annotation = cls.__annotations__.get(field_name)
        if annotation is None:
            return None
        if isinstance(annotation, str):
            module = sys.modules[cls.__module__]
            if hasattr(module, annotation):
                return getattr(module, annotation)
            return None
        return annotation

    @classmethod
    def _get_hash_recursive(cls, parents):
        if cls in parents:
            return 0
        tmphash = (0x802E2D1F1CD1DF73) & 0xFFFFFFFFFFFFFFFF
        tmphash = (
            ((tmphash << 1) & 0xFFFFFFFFFFFFFFFF) + (tmphash >> 63)
        ) & 0xFFFFFFFFFFFFFFFF
        return tmphash

    _packed_fingerprint = None

    @classmethod
    def _get_packed_fingerprint(cls):
        if cls._packed_fingerprint is None:
            cls._packed_fingerprint = struct.pack(">Q", cls._get_hash_recursive([]))
        return cls._packed_fingerprint

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", cls._get_packed_fingerprint())[0]
