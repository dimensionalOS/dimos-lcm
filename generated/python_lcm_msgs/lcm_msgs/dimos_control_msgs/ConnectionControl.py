"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

from io import BytesIO
import struct
import sys


class ConnectionControl(object):
    msg_name = "dimos_control_msgs.ConnectionControl"

    __slots__ = [
        "target_sources_length",
        "target_sources",
        "operation_id",
        "operation",
        "proposed_control_epoch",
        "reason",
    ]

    __typenames__ = ["int32_t", "string", "int64_t", "int8_t", "int64_t", "string"]

    __dimensions__ = [None, ["target_sources_length"], None, None, None, None]

    target_sources_length: "int32_t"
    target_sources: "string"
    operation_id: "int64_t"
    operation: "int8_t"
    proposed_control_epoch: "int64_t"
    reason: "string"

    PREPARE_ARM = 1
    COMMIT_ARM = 2
    ABORT_ARM = 3
    SAFE_STOP = 4
    CLEAR_SAFE_STOP = 5
    ESTOP = 6
    CLEAR_ESTOP = 7

    def __init__(
        self,
        target_sources_length=0,
        target_sources=[],
        operation_id=0,
        operation=0,
        proposed_control_epoch=0,
        reason="",
    ):
        # LCM Type: int32_t
        self.target_sources_length = target_sources_length
        # LCM Type: string[target_sources_length]
        self.target_sources = target_sources
        # LCM Type: int64_t
        self.operation_id = operation_id
        # LCM Type: int8_t
        self.operation = operation
        # LCM Type: int64_t
        self.proposed_control_epoch = proposed_control_epoch
        # LCM Type: string
        self.reason = reason

    def lcm_encode(self):
        buf = BytesIO()
        buf.write(ConnectionControl._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">i", self.target_sources_length))
        for i0 in range(self.target_sources_length):
            __target_sources_encoded = self.target_sources[i0].encode("utf-8")
            buf.write(struct.pack(">I", len(__target_sources_encoded) + 1))
            buf.write(__target_sources_encoded)
            buf.write(b"\0")
        buf.write(
            struct.pack(
                ">qbq", self.operation_id, self.operation, self.proposed_control_epoch
            )
        )
        __reason_encoded = self.reason.encode("utf-8")
        buf.write(struct.pack(">I", len(__reason_encoded) + 1))
        buf.write(__reason_encoded)
        buf.write(b"\0")

    @classmethod
    def lcm_decode(cls, data: bytes):
        if hasattr(data, "read"):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != cls._get_packed_fingerprint():
            raise ValueError("Decode error")
        return cls._decode_one(buf)

    @classmethod
    def _decode_one(cls, buf):
        self = cls()
        self.target_sources_length = struct.unpack(">i", buf.read(4))[0]
        self.target_sources = []
        for i0 in range(self.target_sources_length):
            __target_sources_len = struct.unpack(">I", buf.read(4))[0]
            self.target_sources.append(
                buf.read(__target_sources_len)[:-1].decode("utf-8", "replace")
            )
        self.operation_id, self.operation, self.proposed_control_epoch = struct.unpack(
            ">qbq", buf.read(17)
        )
        __reason_len = struct.unpack(">I", buf.read(4))[0]
        self.reason = buf.read(__reason_len)[:-1].decode("utf-8", "replace")
        return self

    @classmethod
    def _get_field_type(cls, field_name):
        """Get the type for a field from annotations."""
        annotation = cls.__annotations__.get(field_name)
        if annotation is None:
            return None
        if isinstance(annotation, str):
            module = sys.modules[cls.__module__]
            if hasattr(module, annotation):
                return getattr(module, annotation)
            return None
        return annotation

    @classmethod
    def _get_hash_recursive(cls, parents):
        if cls in parents:
            return 0
        tmphash = (0xB82D9643472F465D) & 0xFFFFFFFFFFFFFFFF
        tmphash = (
            ((tmphash << 1) & 0xFFFFFFFFFFFFFFFF) + (tmphash >> 63)
        ) & 0xFFFFFFFFFFFFFFFF
        return tmphash

    _packed_fingerprint = None

    @classmethod
    def _get_packed_fingerprint(cls):
        if cls._packed_fingerprint is None:
            cls._packed_fingerprint = struct.pack(">Q", cls._get_hash_recursive([]))
        return cls._packed_fingerprint

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", cls._get_packed_fingerprint())[0]
