"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

from io import BytesIO
import struct
import sys


class CommandProfile(object):
    msg_name = "dimos_control_msgs.CommandProfile"

    __slots__ = [
        "name",
        "required_fields_length",
        "optional_fields_length",
        "required_fields",
        "optional_fields",
        "uniform_across_resources",
    ]

    __typenames__ = ["string", "int32_t", "int32_t", "string", "string", "boolean"]

    __dimensions__ = [
        None,
        None,
        None,
        ["required_fields_length"],
        ["optional_fields_length"],
        None,
    ]

    name: "string"
    required_fields_length: "int32_t"
    optional_fields_length: "int32_t"
    required_fields: "string"
    optional_fields: "string"
    uniform_across_resources: "boolean"

    def __init__(
        self,
        name="",
        required_fields_length=0,
        optional_fields_length=0,
        required_fields=[],
        optional_fields=[],
        uniform_across_resources=False,
    ):
        # LCM Type: string
        self.name = name
        # LCM Type: int32_t
        self.required_fields_length = required_fields_length
        # LCM Type: int32_t
        self.optional_fields_length = optional_fields_length
        # LCM Type: string[required_fields_length]
        self.required_fields = required_fields
        # LCM Type: string[optional_fields_length]
        self.optional_fields = optional_fields
        # LCM Type: boolean
        self.uniform_across_resources = uniform_across_resources

    def lcm_encode(self):
        buf = BytesIO()
        buf.write(CommandProfile._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        __name_encoded = self.name.encode("utf-8")
        buf.write(struct.pack(">I", len(__name_encoded) + 1))
        buf.write(__name_encoded)
        buf.write(b"\0")
        buf.write(
            struct.pack(">ii", self.required_fields_length, self.optional_fields_length)
        )
        for i0 in range(self.required_fields_length):
            __required_fields_encoded = self.required_fields[i0].encode("utf-8")
            buf.write(struct.pack(">I", len(__required_fields_encoded) + 1))
            buf.write(__required_fields_encoded)
            buf.write(b"\0")
        for i0 in range(self.optional_fields_length):
            __optional_fields_encoded = self.optional_fields[i0].encode("utf-8")
            buf.write(struct.pack(">I", len(__optional_fields_encoded) + 1))
            buf.write(__optional_fields_encoded)
            buf.write(b"\0")
        buf.write(struct.pack(">b", self.uniform_across_resources))

    @classmethod
    def lcm_decode(cls, data: bytes):
        if hasattr(data, "read"):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != cls._get_packed_fingerprint():
            raise ValueError("Decode error")
        return cls._decode_one(buf)

    @classmethod
    def _decode_one(cls, buf):
        self = cls()
        __name_len = struct.unpack(">I", buf.read(4))[0]
        self.name = buf.read(__name_len)[:-1].decode("utf-8", "replace")
        self.required_fields_length, self.optional_fields_length = struct.unpack(
            ">ii", buf.read(8)
        )
        self.required_fields = []
        for i0 in range(self.required_fields_length):
            __required_fields_len = struct.unpack(">I", buf.read(4))[0]
            self.required_fields.append(
                buf.read(__required_fields_len)[:-1].decode("utf-8", "replace")
            )
        self.optional_fields = []
        for i0 in range(self.optional_fields_length):
            __optional_fields_len = struct.unpack(">I", buf.read(4))[0]
            self.optional_fields.append(
                buf.read(__optional_fields_len)[:-1].decode("utf-8", "replace")
            )
        self.uniform_across_resources = bool(struct.unpack("b", buf.read(1))[0])
        return self

    @classmethod
    def _get_field_type(cls, field_name):
        """Get the type for a field from annotations."""
        annotation = cls.__annotations__.get(field_name)
        if annotation is None:
            return None
        if isinstance(annotation, str):
            module = sys.modules[cls.__module__]
            if hasattr(module, annotation):
                return getattr(module, annotation)
            return None
        return annotation

    @classmethod
    def _get_hash_recursive(cls, parents):
        if cls in parents:
            return 0
        tmphash = (0x38538E7A9D86EB4A) & 0xFFFFFFFFFFFFFFFF
        tmphash = (
            ((tmphash << 1) & 0xFFFFFFFFFFFFFFFF) + (tmphash >> 63)
        ) & 0xFFFFFFFFFFFFFFFF
        return tmphash

    _packed_fingerprint = None

    @classmethod
    def _get_packed_fingerprint(cls):
        if cls._packed_fingerprint is None:
            cls._packed_fingerprint = struct.pack(">Q", cls._get_hash_recursive([]))
        return cls._packed_fingerprint

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", cls._get_packed_fingerprint())[0]
