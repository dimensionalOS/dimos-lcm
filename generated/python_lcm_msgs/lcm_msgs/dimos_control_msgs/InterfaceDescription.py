"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

from io import BytesIO
import struct
import sys


class InterfaceDescription(object):
    msg_name = "dimos_control_msgs.InterfaceDescription"

    __slots__ = [
        "name",
        "resource",
        "field",
        "unit",
        "state",
        "command",
        "has_hard_min",
        "hard_min",
        "has_hard_max",
        "hard_max",
        "has_soft_min",
        "soft_min",
        "has_soft_max",
        "soft_max",
        "omission_policy",
    ]

    __typenames__ = [
        "string",
        "string",
        "string",
        "string",
        "boolean",
        "boolean",
        "boolean",
        "double",
        "boolean",
        "double",
        "boolean",
        "double",
        "boolean",
        "double",
        "int8_t",
    ]

    __dimensions__ = [
        None,
        None,
        None,
        None,
        None,
        None,
        None,
        None,
        None,
        None,
        None,
        None,
        None,
        None,
        None,
    ]

    name: "string"
    resource: "string"
    field: "string"
    unit: "string"
    state: "boolean"
    command: "boolean"
    has_hard_min: "boolean"
    hard_min: "double"
    has_hard_max: "boolean"
    hard_max: "double"
    has_soft_min: "boolean"
    soft_min: "double"
    has_soft_max: "boolean"
    soft_max: "double"
    omission_policy: "int8_t"

    HOLD_LAST = 1
    ZERO = 2
    SAFE_STOP = 3

    def __init__(
        self,
        name="",
        resource="",
        field="",
        unit="",
        state=False,
        command=False,
        has_hard_min=False,
        hard_min=0.0,
        has_hard_max=False,
        hard_max=0.0,
        has_soft_min=False,
        soft_min=0.0,
        has_soft_max=False,
        soft_max=0.0,
        omission_policy=0,
    ):
        # LCM Type: string
        self.name = name
        # LCM Type: string
        self.resource = resource
        # LCM Type: string
        self.field = field
        # LCM Type: string
        self.unit = unit
        # LCM Type: boolean
        self.state = state
        # LCM Type: boolean
        self.command = command
        # LCM Type: boolean
        self.has_hard_min = has_hard_min
        # LCM Type: double
        self.hard_min = hard_min
        # LCM Type: boolean
        self.has_hard_max = has_hard_max
        # LCM Type: double
        self.hard_max = hard_max
        # LCM Type: boolean
        self.has_soft_min = has_soft_min
        # LCM Type: double
        self.soft_min = soft_min
        # LCM Type: boolean
        self.has_soft_max = has_soft_max
        # LCM Type: double
        self.soft_max = soft_max
        # LCM Type: int8_t
        self.omission_policy = omission_policy

    def lcm_encode(self):
        buf = BytesIO()
        buf.write(InterfaceDescription._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        __name_encoded = self.name.encode("utf-8")
        buf.write(struct.pack(">I", len(__name_encoded) + 1))
        buf.write(__name_encoded)
        buf.write(b"\0")
        __resource_encoded = self.resource.encode("utf-8")
        buf.write(struct.pack(">I", len(__resource_encoded) + 1))
        buf.write(__resource_encoded)
        buf.write(b"\0")
        __field_encoded = self.field.encode("utf-8")
        buf.write(struct.pack(">I", len(__field_encoded) + 1))
        buf.write(__field_encoded)
        buf.write(b"\0")
        __unit_encoded = self.unit.encode("utf-8")
        buf.write(struct.pack(">I", len(__unit_encoded) + 1))
        buf.write(__unit_encoded)
        buf.write(b"\0")
        buf.write(
            struct.pack(
                ">bbbdbdbdbdb",
                self.state,
                self.command,
                self.has_hard_min,
                self.hard_min,
                self.has_hard_max,
                self.hard_max,
                self.has_soft_min,
                self.soft_min,
                self.has_soft_max,
                self.soft_max,
                self.omission_policy,
            )
        )

    @classmethod
    def lcm_decode(cls, data: bytes):
        if hasattr(data, "read"):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != cls._get_packed_fingerprint():
            raise ValueError("Decode error")
        return cls._decode_one(buf)

    @classmethod
    def _decode_one(cls, buf):
        self = cls()
        __name_len = struct.unpack(">I", buf.read(4))[0]
        self.name = buf.read(__name_len)[:-1].decode("utf-8", "replace")
        __resource_len = struct.unpack(">I", buf.read(4))[0]
        self.resource = buf.read(__resource_len)[:-1].decode("utf-8", "replace")
        __field_len = struct.unpack(">I", buf.read(4))[0]
        self.field = buf.read(__field_len)[:-1].decode("utf-8", "replace")
        __unit_len = struct.unpack(">I", buf.read(4))[0]
        self.unit = buf.read(__unit_len)[:-1].decode("utf-8", "replace")
        self.state = bool(struct.unpack("b", buf.read(1))[0])
        self.command = bool(struct.unpack("b", buf.read(1))[0])
        self.has_hard_min = bool(struct.unpack("b", buf.read(1))[0])
        self.hard_min = struct.unpack(">d", buf.read(8))[0]
        self.has_hard_max = bool(struct.unpack("b", buf.read(1))[0])
        self.hard_max = struct.unpack(">d", buf.read(8))[0]
        self.has_soft_min = bool(struct.unpack("b", buf.read(1))[0])
        self.soft_min = struct.unpack(">d", buf.read(8))[0]
        self.has_soft_max = bool(struct.unpack("b", buf.read(1))[0])
        self.soft_max, self.omission_policy = struct.unpack(">db", buf.read(9))
        return self

    @classmethod
    def _get_field_type(cls, field_name):
        """Get the type for a field from annotations."""
        annotation = cls.__annotations__.get(field_name)
        if annotation is None:
            return None
        if isinstance(annotation, str):
            module = sys.modules[cls.__module__]
            if hasattr(module, annotation):
                return getattr(module, annotation)
            return None
        return annotation

    @classmethod
    def _get_hash_recursive(cls, parents):
        if cls in parents:
            return 0
        tmphash = (0xBFB21E12C9A9BD0D) & 0xFFFFFFFFFFFFFFFF
        tmphash = (
            ((tmphash << 1) & 0xFFFFFFFFFFFFFFFF) + (tmphash >> 63)
        ) & 0xFFFFFFFFFFFFFFFF
        return tmphash

    _packed_fingerprint = None

    @classmethod
    def _get_packed_fingerprint(cls):
        if cls._packed_fingerprint is None:
            cls._packed_fingerprint = struct.pack(">Q", cls._get_hash_recursive([]))
        return cls._packed_fingerprint

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", cls._get_packed_fingerprint())[0]
