"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

from io import BytesIO
import struct
import sys

from . import *
from lcm_msgs import std_msgs
from lcm_msgs import uuid_msgs
from .BoundingBox import BoundingBox
from .KeyValue import KeyValue
from .WayPoint import WayPoint
from .RouteSegment import RouteSegment


class RouteNetwork(object):
    msg_name = "geographic_msgs.RouteNetwork"

    __slots__ = [
        "points_length",
        "segments_length",
        "props_length",
        "header",
        "id",
        "bounds",
        "points",
        "segments",
        "props",
    ]

    __typenames__ = [
        "int32_t",
        "int32_t",
        "int32_t",
        "std_msgs.Header",
        "uuid_msgs.UniqueID",
        "BoundingBox",
        "WayPoint",
        "RouteSegment",
        "KeyValue",
    ]

    __dimensions__ = [
        None,
        None,
        None,
        None,
        None,
        None,
        ["points_length"],
        ["segments_length"],
        ["props_length"],
    ]

    points_length: "int32_t"
    segments_length: "int32_t"
    props_length: "int32_t"
    header: std_msgs.Header
    id: uuid_msgs.UniqueID
    bounds: BoundingBox
    points: WayPoint
    segments: RouteSegment
    props: KeyValue

    def __init__(
        self,
        points_length=0,
        segments_length=0,
        props_length=0,
        header=std_msgs.Header(),
        id=uuid_msgs.UniqueID(),
        bounds=BoundingBox(),
        points=[],
        segments=[],
        props=[],
    ):
        # LCM Type: int32_t
        self.points_length = points_length
        # LCM Type: int32_t
        self.segments_length = segments_length
        # LCM Type: int32_t
        self.props_length = props_length
        # LCM Type: std_msgs.Header
        self.header = header
        # LCM Type: uuid_msgs.UniqueID
        self.id = id
        # LCM Type: BoundingBox
        self.bounds = bounds
        # LCM Type: WayPoint[points_length]
        self.points = points
        # LCM Type: RouteSegment[segments_length]
        self.segments = segments
        # LCM Type: KeyValue[props_length]
        self.props = props

    def lcm_encode(self):
        buf = BytesIO()
        buf.write(RouteNetwork._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(
            struct.pack(
                ">iii", self.points_length, self.segments_length, self.props_length
            )
        )
        assert (
            self.header._get_packed_fingerprint()
            == std_msgs.Header._get_packed_fingerprint()
        )
        self.header._encode_one(buf)
        assert (
            self.id._get_packed_fingerprint()
            == uuid_msgs.UniqueID._get_packed_fingerprint()
        )
        self.id._encode_one(buf)
        assert (
            self.bounds._get_packed_fingerprint()
            == BoundingBox._get_packed_fingerprint()
        )
        self.bounds._encode_one(buf)
        for i0 in range(self.points_length):
            assert (
                self.points[i0]._get_packed_fingerprint()
                == WayPoint._get_packed_fingerprint()
            )
            self.points[i0]._encode_one(buf)
        for i0 in range(self.segments_length):
            assert (
                self.segments[i0]._get_packed_fingerprint()
                == RouteSegment._get_packed_fingerprint()
            )
            self.segments[i0]._encode_one(buf)
        for i0 in range(self.props_length):
            assert (
                self.props[i0]._get_packed_fingerprint()
                == KeyValue._get_packed_fingerprint()
            )
            self.props[i0]._encode_one(buf)

    @classmethod
    def lcm_decode(cls, data: bytes):
        if hasattr(data, "read"):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != cls._get_packed_fingerprint():
            raise ValueError("Decode error")
        return cls._decode_one(buf)

    @classmethod
    def _decode_one(cls, buf):
        self = cls()
        self.points_length, self.segments_length, self.props_length = struct.unpack(
            ">iii", buf.read(12)
        )
        self.header = cls._get_field_type("header")._decode_one(buf)
        self.id = cls._get_field_type("id")._decode_one(buf)
        self.bounds = cls._get_field_type("bounds")._decode_one(buf)
        self.points = []
        for i0 in range(self.points_length):
            self.points.append(cls._get_field_type("points")._decode_one(buf))
        self.segments = []
        for i0 in range(self.segments_length):
            self.segments.append(cls._get_field_type("segments")._decode_one(buf))
        self.props = []
        for i0 in range(self.props_length):
            self.props.append(cls._get_field_type("props")._decode_one(buf))
        return self

    @classmethod
    def _get_field_type(cls, field_name):
        """Get the type for a field from annotations."""
        annotation = cls.__annotations__.get(field_name)
        if annotation is None:
            return None
        if isinstance(annotation, str):
            module = sys.modules[cls.__module__]
            if hasattr(module, annotation):
                return getattr(module, annotation)
            return None
        return annotation

    @classmethod
    def _get_hash_recursive(cls, parents):
        if cls in parents:
            return 0
        newparents = parents + [cls]
        tmphash = (
            0xF50B14F90FC9A672
            + std_msgs.Header._get_hash_recursive(newparents)
            + uuid_msgs.UniqueID._get_hash_recursive(newparents)
            + BoundingBox._get_hash_recursive(newparents)
            + WayPoint._get_hash_recursive(newparents)
            + RouteSegment._get_hash_recursive(newparents)
            + KeyValue._get_hash_recursive(newparents)
        ) & 0xFFFFFFFFFFFFFFFF
        tmphash = (
            ((tmphash << 1) & 0xFFFFFFFFFFFFFFFF) + (tmphash >> 63)
        ) & 0xFFFFFFFFFFFFFFFF
        return tmphash

    _packed_fingerprint = None

    @classmethod
    def _get_packed_fingerprint(cls):
        if cls._packed_fingerprint is None:
            cls._packed_fingerprint = struct.pack(">Q", cls._get_hash_recursive([]))
        return cls._packed_fingerprint

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", cls._get_packed_fingerprint())[0]
