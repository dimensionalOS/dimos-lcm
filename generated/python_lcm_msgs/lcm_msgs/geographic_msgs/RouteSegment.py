"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

from io import BytesIO
import struct
import sys

from . import *
from lcm_msgs import uuid_msgs
from .KeyValue import KeyValue


class RouteSegment(object):
    msg_name = "geographic_msgs.RouteSegment"

    __slots__ = ["props_length", "id", "start", "end", "props"]

    __typenames__ = [
        "int32_t",
        "uuid_msgs.UniqueID",
        "uuid_msgs.UniqueID",
        "uuid_msgs.UniqueID",
        "KeyValue",
    ]

    __dimensions__ = [None, None, None, None, ["props_length"]]

    props_length: "int32_t"
    id: uuid_msgs.UniqueID
    start: uuid_msgs.UniqueID
    end: uuid_msgs.UniqueID
    props: KeyValue

    def __init__(
        self,
        props_length=0,
        id=uuid_msgs.UniqueID(),
        start=uuid_msgs.UniqueID(),
        end=uuid_msgs.UniqueID(),
        props=[],
    ):
        # LCM Type: int32_t
        self.props_length = props_length
        # LCM Type: uuid_msgs.UniqueID
        self.id = id
        # LCM Type: uuid_msgs.UniqueID
        self.start = start
        # LCM Type: uuid_msgs.UniqueID
        self.end = end
        # LCM Type: KeyValue[props_length]
        self.props = props

    def lcm_encode(self):
        buf = BytesIO()
        buf.write(RouteSegment._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">i", self.props_length))
        assert (
            self.id._get_packed_fingerprint()
            == uuid_msgs.UniqueID._get_packed_fingerprint()
        )
        self.id._encode_one(buf)
        assert (
            self.start._get_packed_fingerprint()
            == uuid_msgs.UniqueID._get_packed_fingerprint()
        )
        self.start._encode_one(buf)
        assert (
            self.end._get_packed_fingerprint()
            == uuid_msgs.UniqueID._get_packed_fingerprint()
        )
        self.end._encode_one(buf)
        for i0 in range(self.props_length):
            assert (
                self.props[i0]._get_packed_fingerprint()
                == KeyValue._get_packed_fingerprint()
            )
            self.props[i0]._encode_one(buf)

    @classmethod
    def lcm_decode(cls, data: bytes):
        if hasattr(data, "read"):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != cls._get_packed_fingerprint():
            raise ValueError("Decode error")
        return cls._decode_one(buf)

    @classmethod
    def _decode_one(cls, buf):
        self = cls()
        self.props_length = struct.unpack(">i", buf.read(4))[0]
        self.id = cls._get_field_type("id")._decode_one(buf)
        self.start = cls._get_field_type("start")._decode_one(buf)
        self.end = cls._get_field_type("end")._decode_one(buf)
        self.props = []
        for i0 in range(self.props_length):
            self.props.append(cls._get_field_type("props")._decode_one(buf))
        return self

    @classmethod
    def _get_field_type(cls, field_name):
        """Get the type for a field from annotations."""
        annotation = cls.__annotations__.get(field_name)
        if annotation is None:
            return None
        if isinstance(annotation, str):
            module = sys.modules[cls.__module__]
            if hasattr(module, annotation):
                return getattr(module, annotation)
            return None
        return annotation

    @classmethod
    def _get_hash_recursive(cls, parents):
        if cls in parents:
            return 0
        newparents = parents + [cls]
        tmphash = (
            0x2757AFE165F2B650
            + uuid_msgs.UniqueID._get_hash_recursive(newparents)
            + uuid_msgs.UniqueID._get_hash_recursive(newparents)
            + uuid_msgs.UniqueID._get_hash_recursive(newparents)
            + KeyValue._get_hash_recursive(newparents)
        ) & 0xFFFFFFFFFFFFFFFF
        tmphash = (
            ((tmphash << 1) & 0xFFFFFFFFFFFFFFFF) + (tmphash >> 63)
        ) & 0xFFFFFFFFFFFFFFFF
        return tmphash

    _packed_fingerprint = None

    @classmethod
    def _get_packed_fingerprint(cls):
        if cls._packed_fingerprint is None:
            cls._packed_fingerprint = struct.pack(">Q", cls._get_hash_recursive([]))
        return cls._packed_fingerprint

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", cls._get_packed_fingerprint())[0]
