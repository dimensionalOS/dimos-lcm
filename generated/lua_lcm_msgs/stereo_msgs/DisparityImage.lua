--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local sensor_msgs_RegionOfInterest = require('sensor_msgs.RegionOfInterest')
local sensor_msgs_Image = require('sensor_msgs.Image')
local std_msgs_Header = require('std_msgs.Header')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local DisparityImage = {}
DisparityImage.__index = DisparityImage

DisparityImage.name = 'stereo_msgs.DisparityImage'
DisparityImage.packagename = 'stereo_msgs'
DisparityImage.shortname = 'DisparityImage'

function DisparityImage:new()

  local obj = {}

  obj.header = nil
  obj.image = nil
  obj.f = 0.0
  obj.T = 0.0
  obj.valid_window = nil
  obj.min_disparity = 0.0
  obj.max_disparity = 0.0
  obj.delta_d = 0.0

  setmetatable(obj, self)

  return obj
end

function DisparityImage._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == DisparityImage then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, DisparityImage)

  local hash = lcm._hash.new('0xdeb7a557a2b9258a')
    + std_msgs_Header._get_hash_recursive(newparents)
    + sensor_msgs_Image._get_hash_recursive(newparents)
    + sensor_msgs_RegionOfInterest._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

DisparityImage._packed_fingerprint = lcm._pack.pack('>X', DisparityImage._get_hash_recursive({}))

function DisparityImage:encode()

  return DisparityImage._packed_fingerprint .. self:_encode_one()
end

function DisparityImage:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.header:_encode_one())
  table.insert(buf_table, self.image:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>ff', self.f, self.T))
  table.insert(buf_table, self.valid_window:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>fff', self.min_disparity, self.max_disparity, self.delta_d))

  return table.concat(buf_table)
end

function DisparityImage.decode(data)

  if data:sub(1, 8) ~= DisparityImage._packed_fingerprint then
    error('bad fingerprint')
  end

  return DisparityImage._decode_one(data:sub(9))
end

function DisparityImage._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = DisparityImage:new()

  obj.header = std_msgs_Header._decode_one(data)
  obj.image = sensor_msgs_Image._decode_one(data)
  obj.f, obj.T = lcm._pack.unpack('>ff', data:read(8))
  obj.valid_window = sensor_msgs_RegionOfInterest._decode_one(data)
  obj.min_disparity, obj.max_disparity, obj.delta_d = lcm._pack.unpack('>fff', data:read(12))

  return obj
end

return DisparityImage

