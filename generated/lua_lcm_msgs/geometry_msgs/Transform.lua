--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geometry_msgs_Quaternion = require('geometry_msgs.Quaternion')
local geometry_msgs_Vector3 = require('geometry_msgs.Vector3')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Transform = {}
Transform.__index = Transform

Transform.name = 'geometry_msgs.Transform'
Transform.packagename = 'geometry_msgs'
Transform.shortname = 'Transform'

function Transform:new()

  local obj = {}

  obj.translation = nil
  obj.rotation = nil

  setmetatable(obj, self)

  return obj
end

function Transform._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Transform then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Transform)

  local hash = lcm._hash.new('0x1275bd1ccbdaf47f')
    + geometry_msgs_Vector3._get_hash_recursive(newparents)
    + geometry_msgs_Quaternion._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

Transform._packed_fingerprint = lcm._pack.pack('>X', Transform._get_hash_recursive({}))

function Transform:encode()

  return Transform._packed_fingerprint .. self:_encode_one()
end

function Transform:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.translation:_encode_one())
  table.insert(buf_table, self.rotation:_encode_one())

  return table.concat(buf_table)
end

function Transform.decode(data)

  if data:sub(1, 8) ~= Transform._packed_fingerprint then
    error('bad fingerprint')
  end

  return Transform._decode_one(data:sub(9))
end

function Transform._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Transform:new()

  obj.translation = geometry_msgs_Vector3._decode_one(data)
  obj.rotation = geometry_msgs_Quaternion._decode_one(data)

  return obj
end

return Transform

