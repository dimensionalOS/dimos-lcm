--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geometry_msgs_Vector3 = require('geometry_msgs.Vector3')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Wrench = {}
Wrench.__index = Wrench

Wrench.name = 'geometry_msgs.Wrench'
Wrench.packagename = 'geometry_msgs'
Wrench.shortname = 'Wrench'

function Wrench:new()

  local obj = {}

  obj.force = nil
  obj.torque = nil

  setmetatable(obj, self)

  return obj
end

function Wrench._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Wrench then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Wrench)

  local hash = lcm._hash.new('0x980c3aea984c9a6')
    + geometry_msgs_Vector3._get_hash_recursive(newparents)
    + geometry_msgs_Vector3._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

Wrench._packed_fingerprint = lcm._pack.pack('>X', Wrench._get_hash_recursive({}))

function Wrench:encode()

  return Wrench._packed_fingerprint .. self:_encode_one()
end

function Wrench:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.force:_encode_one())
  table.insert(buf_table, self.torque:_encode_one())

  return table.concat(buf_table)
end

function Wrench.decode(data)

  if data:sub(1, 8) ~= Wrench._packed_fingerprint then
    error('bad fingerprint')
  end

  return Wrench._decode_one(data:sub(9))
end

function Wrench._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Wrench:new()

  obj.force = geometry_msgs_Vector3._decode_one(data)
  obj.torque = geometry_msgs_Vector3._decode_one(data)

  return obj
end

return Wrench

