--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geometry_msgs_Accel = require('geometry_msgs.Accel')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local AccelWithCovariance = {}
AccelWithCovariance.__index = AccelWithCovariance

AccelWithCovariance.name = 'geometry_msgs.AccelWithCovariance'
AccelWithCovariance.packagename = 'geometry_msgs'
AccelWithCovariance.shortname = 'AccelWithCovariance'

function AccelWithCovariance:new()

  local obj = {}

  obj.accel = nil
  obj.covariance = {}
  for d0 = 1, 36 do
    obj.covariance[d0] = 0.0
  end

  setmetatable(obj, self)

  return obj
end

function AccelWithCovariance._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == AccelWithCovariance then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, AccelWithCovariance)

  local hash = lcm._hash.new('0x545f576dfe97951d')
    + geometry_msgs_Accel._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

AccelWithCovariance._packed_fingerprint = lcm._pack.pack('>X', AccelWithCovariance._get_hash_recursive({}))

function AccelWithCovariance:encode()

  return AccelWithCovariance._packed_fingerprint .. self:_encode_one()
end

function AccelWithCovariance:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.accel:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>36d', unpack(self.covariance)))

  return table.concat(buf_table)
end

function AccelWithCovariance.decode(data)

  if data:sub(1, 8) ~= AccelWithCovariance._packed_fingerprint then
    error('bad fingerprint')
  end

  return AccelWithCovariance._decode_one(data:sub(9))
end

function AccelWithCovariance._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = AccelWithCovariance:new()

  obj.accel = geometry_msgs_Accel._decode_one(data)
  obj.covariance = {lcm._pack.unpack('>36d', data:read(288))}

  return obj
end

return AccelWithCovariance

