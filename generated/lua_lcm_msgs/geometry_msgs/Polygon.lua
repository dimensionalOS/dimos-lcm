--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geometry_msgs_Point32 = require('geometry_msgs.Point32')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Polygon = {}
Polygon.__index = Polygon

Polygon.name = 'geometry_msgs.Polygon'
Polygon.packagename = 'geometry_msgs'
Polygon.shortname = 'Polygon'

function Polygon:new()

  local obj = {}

  obj.points_length = 0
  obj.points = {}

  setmetatable(obj, self)

  return obj
end

function Polygon._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Polygon then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Polygon)

  local hash = lcm._hash.new('0x5634733b354407e8')
    + geometry_msgs_Point32._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

Polygon._packed_fingerprint = lcm._pack.pack('>X', Polygon._get_hash_recursive({}))

function Polygon:encode()

  return Polygon._packed_fingerprint .. self:_encode_one()
end

function Polygon:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.points_length))
  for i0 = 1, self.points_length do
    table.insert(buf_table, self.points[i0]:_encode_one())
  end

  return table.concat(buf_table)
end

function Polygon.decode(data)

  if data:sub(1, 8) ~= Polygon._packed_fingerprint then
    error('bad fingerprint')
  end

  return Polygon._decode_one(data:sub(9))
end

function Polygon._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Polygon:new()

  obj.points_length = lcm._pack.unpack('>i', data:read(4))
  obj.points = {}
  for i0 = 1, obj.points_length do
    obj.points[i0] = geometry_msgs_Point32._decode_one(data)
  end

  return obj
end

return Polygon

