--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geometry_msgs_Transform = require('geometry_msgs.Transform')
local std_msgs_Header = require('std_msgs.Header')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local TransformStamped = {}
TransformStamped.__index = TransformStamped

TransformStamped.name = 'geometry_msgs.TransformStamped'
TransformStamped.packagename = 'geometry_msgs'
TransformStamped.shortname = 'TransformStamped'

function TransformStamped:new()

  local obj = {}

  obj.header = nil
  obj.child_frame_id = ''
  obj.transform = nil

  setmetatable(obj, self)

  return obj
end

function TransformStamped._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == TransformStamped then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, TransformStamped)

  local hash = lcm._hash.new('0xf694f4a6d8779002')
    + std_msgs_Header._get_hash_recursive(newparents)
    + geometry_msgs_Transform._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

TransformStamped._packed_fingerprint = lcm._pack.pack('>X', TransformStamped._get_hash_recursive({}))

function TransformStamped:encode()

  return TransformStamped._packed_fingerprint .. self:_encode_one()
end

function TransformStamped:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.header:_encode_one())
  local __child_frame_id_tmpstr = lcm._pack.prepare_string(self.child_frame_id)
  table.insert(buf_table, lcm._pack.pack('>I', #__child_frame_id_tmpstr + 1))
  table.insert(buf_table, __child_frame_id_tmpstr .. '\0')
  table.insert(buf_table, self.transform:_encode_one())

  return table.concat(buf_table)
end

function TransformStamped.decode(data)

  if data:sub(1, 8) ~= TransformStamped._packed_fingerprint then
    error('bad fingerprint')
  end

  return TransformStamped._decode_one(data:sub(9))
end

function TransformStamped._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = TransformStamped:new()

  obj.header = std_msgs_Header._decode_one(data)
  local __child_frame_id_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.child_frame_id = lcm._pack.prepare_string(data:read(__child_frame_id_tmpstrlen))
  obj.transform = geometry_msgs_Transform._decode_one(data)

  return obj
end

return TransformStamped

