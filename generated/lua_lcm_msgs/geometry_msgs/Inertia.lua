--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geometry_msgs_Vector3 = require('geometry_msgs.Vector3')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Inertia = {}
Inertia.__index = Inertia

Inertia.name = 'geometry_msgs.Inertia'
Inertia.packagename = 'geometry_msgs'
Inertia.shortname = 'Inertia'

function Inertia:new()

  local obj = {}

  obj.m = 0.0
  obj.com = nil
  obj.ixx = 0.0
  obj.ixy = 0.0
  obj.ixz = 0.0
  obj.iyy = 0.0
  obj.iyz = 0.0
  obj.izz = 0.0

  setmetatable(obj, self)

  return obj
end

function Inertia._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Inertia then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Inertia)

  local hash = lcm._hash.new('0xbeaa1a2a4c70b2e0')
    + geometry_msgs_Vector3._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

Inertia._packed_fingerprint = lcm._pack.pack('>X', Inertia._get_hash_recursive({}))

function Inertia:encode()

  return Inertia._packed_fingerprint .. self:_encode_one()
end

function Inertia:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>d', self.m))
  table.insert(buf_table, self.com:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>dddddd', self.ixx, self.ixy, self.ixz, self.iyy, self.iyz, self.izz))

  return table.concat(buf_table)
end

function Inertia.decode(data)

  if data:sub(1, 8) ~= Inertia._packed_fingerprint then
    error('bad fingerprint')
  end

  return Inertia._decode_one(data:sub(9))
end

function Inertia._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Inertia:new()

  obj.m = lcm._pack.unpack('>d', data:read(8))
  obj.com = geometry_msgs_Vector3._decode_one(data)
  obj.ixx, obj.ixy, obj.ixz, obj.iyy, obj.iyz, obj.izz = lcm._pack.unpack('>dddddd', data:read(48))

  return obj
end

return Inertia

