--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geometry_msgs_Pose = require('geometry_msgs.Pose')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local PoseWithCovariance = {}
PoseWithCovariance.__index = PoseWithCovariance

PoseWithCovariance.name = 'geometry_msgs.PoseWithCovariance'
PoseWithCovariance.packagename = 'geometry_msgs'
PoseWithCovariance.shortname = 'PoseWithCovariance'

function PoseWithCovariance:new()

  local obj = {}

  obj.pose = nil
  obj.covariance = {}
  for d0 = 1, 36 do
    obj.covariance[d0] = 0.0
  end

  setmetatable(obj, self)

  return obj
end

function PoseWithCovariance._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == PoseWithCovariance then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, PoseWithCovariance)

  local hash = lcm._hash.new('0x42dbdfaa69371237')
    + geometry_msgs_Pose._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

PoseWithCovariance._packed_fingerprint = lcm._pack.pack('>X', PoseWithCovariance._get_hash_recursive({}))

function PoseWithCovariance:encode()

  return PoseWithCovariance._packed_fingerprint .. self:_encode_one()
end

function PoseWithCovariance:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.pose:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>36d', unpack(self.covariance)))

  return table.concat(buf_table)
end

function PoseWithCovariance.decode(data)

  if data:sub(1, 8) ~= PoseWithCovariance._packed_fingerprint then
    error('bad fingerprint')
  end

  return PoseWithCovariance._decode_one(data:sub(9))
end

function PoseWithCovariance._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = PoseWithCovariance:new()

  obj.pose = geometry_msgs_Pose._decode_one(data)
  obj.covariance = {lcm._pack.unpack('>36d', data:read(288))}

  return obj
end

return PoseWithCovariance

