--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geometry_msgs_Vector3 = require('geometry_msgs.Vector3')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Accel = {}
Accel.__index = Accel

Accel.name = 'geometry_msgs.Accel'
Accel.packagename = 'geometry_msgs'
Accel.shortname = 'Accel'

function Accel:new()

  local obj = {}

  obj.linear = nil
  obj.angular = nil

  setmetatable(obj, self)

  return obj
end

function Accel._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Accel then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Accel)

  local hash = lcm._hash.new('0x3a4144772922add7')
    + geometry_msgs_Vector3._get_hash_recursive(newparents)
    + geometry_msgs_Vector3._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

Accel._packed_fingerprint = lcm._pack.pack('>X', Accel._get_hash_recursive({}))

function Accel:encode()

  return Accel._packed_fingerprint .. self:_encode_one()
end

function Accel:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.linear:_encode_one())
  table.insert(buf_table, self.angular:_encode_one())

  return table.concat(buf_table)
end

function Accel.decode(data)

  if data:sub(1, 8) ~= Accel._packed_fingerprint then
    error('bad fingerprint')
  end

  return Accel._decode_one(data:sub(9))
end

function Accel._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Accel:new()

  obj.linear = geometry_msgs_Vector3._decode_one(data)
  obj.angular = geometry_msgs_Vector3._decode_one(data)

  return obj
end

return Accel

