--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geometry_msgs_Inertia = require('geometry_msgs.Inertia')
local std_msgs_Header = require('std_msgs.Header')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local InertiaStamped = {}
InertiaStamped.__index = InertiaStamped

InertiaStamped.name = 'geometry_msgs.InertiaStamped'
InertiaStamped.packagename = 'geometry_msgs'
InertiaStamped.shortname = 'InertiaStamped'

function InertiaStamped:new()

  local obj = {}

  obj.header = nil
  obj.inertia = nil

  setmetatable(obj, self)

  return obj
end

function InertiaStamped._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == InertiaStamped then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, InertiaStamped)

  local hash = lcm._hash.new('0x3a39287c292abed7')
    + std_msgs_Header._get_hash_recursive(newparents)
    + geometry_msgs_Inertia._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

InertiaStamped._packed_fingerprint = lcm._pack.pack('>X', InertiaStamped._get_hash_recursive({}))

function InertiaStamped:encode()

  return InertiaStamped._packed_fingerprint .. self:_encode_one()
end

function InertiaStamped:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.header:_encode_one())
  table.insert(buf_table, self.inertia:_encode_one())

  return table.concat(buf_table)
end

function InertiaStamped.decode(data)

  if data:sub(1, 8) ~= InertiaStamped._packed_fingerprint then
    error('bad fingerprint')
  end

  return InertiaStamped._decode_one(data:sub(9))
end

function InertiaStamped._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = InertiaStamped:new()

  obj.header = std_msgs_Header._decode_one(data)
  obj.inertia = geometry_msgs_Inertia._decode_one(data)

  return obj
end

return InertiaStamped

