--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geometry_msgs_Point = require('geometry_msgs.Point')
local std_msgs_Header = require('std_msgs.Header')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local PointStamped = {}
PointStamped.__index = PointStamped

PointStamped.name = 'geometry_msgs.PointStamped'
PointStamped.packagename = 'geometry_msgs'
PointStamped.shortname = 'PointStamped'

function PointStamped:new()

  local obj = {}

  obj.header = nil
  obj.point = nil

  setmetatable(obj, self)

  return obj
end

function PointStamped._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == PointStamped then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, PointStamped)

  local hash = lcm._hash.new('0xf012413a2c8028c2')
    + std_msgs_Header._get_hash_recursive(newparents)
    + geometry_msgs_Point._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

PointStamped._packed_fingerprint = lcm._pack.pack('>X', PointStamped._get_hash_recursive({}))

function PointStamped:encode()

  return PointStamped._packed_fingerprint .. self:_encode_one()
end

function PointStamped:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.header:_encode_one())
  table.insert(buf_table, self.point:_encode_one())

  return table.concat(buf_table)
end

function PointStamped.decode(data)

  if data:sub(1, 8) ~= PointStamped._packed_fingerprint then
    error('bad fingerprint')
  end

  return PointStamped._decode_one(data:sub(9))
end

function PointStamped._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = PointStamped:new()

  obj.header = std_msgs_Header._decode_one(data)
  obj.point = geometry_msgs_Point._decode_one(data)

  return obj
end

return PointStamped

