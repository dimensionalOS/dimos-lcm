--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local vision_msgs_BoundingBox3D = require('vision_msgs.BoundingBox3D')
local std_msgs_Header = require('std_msgs.Header')
local vision_msgs_ObjectHypothesisWithPose = require('vision_msgs.ObjectHypothesisWithPose')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Detection3D = {}
Detection3D.__index = Detection3D

Detection3D.name = 'vision_msgs.Detection3D'
Detection3D.packagename = 'vision_msgs'
Detection3D.shortname = 'Detection3D'

function Detection3D:new()

  local obj = {}

  obj.results_length = 0
  obj.header = nil
  obj.results = {}
  obj.bbox = nil
  obj.id = ''

  setmetatable(obj, self)

  return obj
end

function Detection3D._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Detection3D then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Detection3D)

  local hash = lcm._hash.new('0x7c62020c10a78d22')
    + std_msgs_Header._get_hash_recursive(newparents)
    + vision_msgs_ObjectHypothesisWithPose._get_hash_recursive(newparents)
    + vision_msgs_BoundingBox3D._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

Detection3D._packed_fingerprint = lcm._pack.pack('>X', Detection3D._get_hash_recursive({}))

function Detection3D:encode()

  return Detection3D._packed_fingerprint .. self:_encode_one()
end

function Detection3D:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.results_length))
  table.insert(buf_table, self.header:_encode_one())
  for i0 = 1, self.results_length do
    table.insert(buf_table, self.results[i0]:_encode_one())
  end
  table.insert(buf_table, self.bbox:_encode_one())
  local __id_tmpstr = lcm._pack.prepare_string(self.id)
  table.insert(buf_table, lcm._pack.pack('>I', #__id_tmpstr + 1))
  table.insert(buf_table, __id_tmpstr .. '\0')

  return table.concat(buf_table)
end

function Detection3D.decode(data)

  if data:sub(1, 8) ~= Detection3D._packed_fingerprint then
    error('bad fingerprint')
  end

  return Detection3D._decode_one(data:sub(9))
end

function Detection3D._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Detection3D:new()

  obj.results_length = lcm._pack.unpack('>i', data:read(4))
  obj.header = std_msgs_Header._decode_one(data)
  obj.results = {}
  for i0 = 1, obj.results_length do
    obj.results[i0] = vision_msgs_ObjectHypothesisWithPose._decode_one(data)
  end
  obj.bbox = vision_msgs_BoundingBox3D._decode_one(data)
  local __id_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.id = lcm._pack.prepare_string(data:read(__id_tmpstrlen))

  return obj
end

return Detection3D

