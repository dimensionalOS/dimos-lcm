--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local VisionClass = {}
VisionClass.__index = VisionClass

VisionClass.name = 'vision_msgs.VisionClass'
VisionClass.packagename = 'vision_msgs'
VisionClass.shortname = 'VisionClass'

function VisionClass:new()

  local obj = {}

  obj.class_id = 0
  obj.class_name = ''

  setmetatable(obj, self)

  return obj
end

function VisionClass._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == VisionClass then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, VisionClass)

  local hash = lcm._hash.new('0xdc055c3dd46c2633')
  hash:rotate(1)

  return hash
end

VisionClass._packed_fingerprint = lcm._pack.pack('>X', VisionClass._get_hash_recursive({}))

function VisionClass:encode()

  return VisionClass._packed_fingerprint .. self:_encode_one()
end

function VisionClass:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>h', self.class_id))
  local __class_name_tmpstr = lcm._pack.prepare_string(self.class_name)
  table.insert(buf_table, lcm._pack.pack('>I', #__class_name_tmpstr + 1))
  table.insert(buf_table, __class_name_tmpstr .. '\0')

  return table.concat(buf_table)
end

function VisionClass.decode(data)

  if data:sub(1, 8) ~= VisionClass._packed_fingerprint then
    error('bad fingerprint')
  end

  return VisionClass._decode_one(data:sub(9))
end

function VisionClass._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = VisionClass:new()

  obj.class_id = lcm._pack.unpack('>h', data:read(2))
  local __class_name_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.class_name = lcm._pack.prepare_string(data:read(__class_name_tmpstrlen))

  return obj
end

return VisionClass

