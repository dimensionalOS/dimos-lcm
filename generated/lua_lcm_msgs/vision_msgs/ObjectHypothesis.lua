--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local ObjectHypothesis = {}
ObjectHypothesis.__index = ObjectHypothesis

ObjectHypothesis.name = 'vision_msgs.ObjectHypothesis'
ObjectHypothesis.packagename = 'vision_msgs'
ObjectHypothesis.shortname = 'ObjectHypothesis'

function ObjectHypothesis:new()

  local obj = {}

  obj.class_id = ''
  obj.score = 0.0

  setmetatable(obj, self)

  return obj
end

function ObjectHypothesis._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == ObjectHypothesis then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, ObjectHypothesis)

  local hash = lcm._hash.new('0x38c9acd543138b22')
  hash:rotate(1)

  return hash
end

ObjectHypothesis._packed_fingerprint = lcm._pack.pack('>X', ObjectHypothesis._get_hash_recursive({}))

function ObjectHypothesis:encode()

  return ObjectHypothesis._packed_fingerprint .. self:_encode_one()
end

function ObjectHypothesis:_encode_one()

  local buf_table = {}

  local __class_id_tmpstr = lcm._pack.prepare_string(self.class_id)
  table.insert(buf_table, lcm._pack.pack('>I', #__class_id_tmpstr + 1))
  table.insert(buf_table, __class_id_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack('>d', self.score))

  return table.concat(buf_table)
end

function ObjectHypothesis.decode(data)

  if data:sub(1, 8) ~= ObjectHypothesis._packed_fingerprint then
    error('bad fingerprint')
  end

  return ObjectHypothesis._decode_one(data:sub(9))
end

function ObjectHypothesis._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = ObjectHypothesis:new()

  local __class_id_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.class_id = lcm._pack.prepare_string(data:read(__class_id_tmpstrlen))
  obj.score = lcm._pack.unpack('>d', data:read(8))

  return obj
end

return ObjectHypothesis

