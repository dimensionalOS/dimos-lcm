--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local vision_msgs_ObjectHypothesis = require('vision_msgs.ObjectHypothesis')
local std_msgs_Header = require('std_msgs.Header')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Classification = {}
Classification.__index = Classification

Classification.name = 'vision_msgs.Classification'
Classification.packagename = 'vision_msgs'
Classification.shortname = 'Classification'

function Classification:new()

  local obj = {}

  obj.results_length = 0
  obj.header = nil
  obj.results = {}

  setmetatable(obj, self)

  return obj
end

function Classification._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Classification then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Classification)

  local hash = lcm._hash.new('0x60719354aae1b22a')
    + std_msgs_Header._get_hash_recursive(newparents)
    + vision_msgs_ObjectHypothesis._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

Classification._packed_fingerprint = lcm._pack.pack('>X', Classification._get_hash_recursive({}))

function Classification:encode()

  return Classification._packed_fingerprint .. self:_encode_one()
end

function Classification:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.results_length))
  table.insert(buf_table, self.header:_encode_one())
  for i0 = 1, self.results_length do
    table.insert(buf_table, self.results[i0]:_encode_one())
  end

  return table.concat(buf_table)
end

function Classification.decode(data)

  if data:sub(1, 8) ~= Classification._packed_fingerprint then
    error('bad fingerprint')
  end

  return Classification._decode_one(data:sub(9))
end

function Classification._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Classification:new()

  obj.results_length = lcm._pack.unpack('>i', data:read(4))
  obj.header = std_msgs_Header._decode_one(data)
  obj.results = {}
  for i0 = 1, obj.results_length do
    obj.results[i0] = vision_msgs_ObjectHypothesis._decode_one(data)
  end

  return obj
end

return Classification

