--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local vision_msgs_Point2D = require('vision_msgs.Point2D')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Pose2D = {}
Pose2D.__index = Pose2D

Pose2D.name = 'vision_msgs.Pose2D'
Pose2D.packagename = 'vision_msgs'
Pose2D.shortname = 'Pose2D'

function Pose2D:new()

  local obj = {}

  obj.position = nil
  obj.theta = 0.0

  setmetatable(obj, self)

  return obj
end

function Pose2D._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Pose2D then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Pose2D)

  local hash = lcm._hash.new('0x2da59bf5b18f540')
    + vision_msgs_Point2D._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

Pose2D._packed_fingerprint = lcm._pack.pack('>X', Pose2D._get_hash_recursive({}))

function Pose2D:encode()

  return Pose2D._packed_fingerprint .. self:_encode_one()
end

function Pose2D:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.position:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>d', self.theta))

  return table.concat(buf_table)
end

function Pose2D.decode(data)

  if data:sub(1, 8) ~= Pose2D._packed_fingerprint then
    error('bad fingerprint')
  end

  return Pose2D._decode_one(data:sub(9))
end

function Pose2D._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Pose2D:new()

  obj.position = vision_msgs_Point2D._decode_one(data)
  obj.theta = lcm._pack.unpack('>d', data:read(8))

  return obj
end

return Pose2D

