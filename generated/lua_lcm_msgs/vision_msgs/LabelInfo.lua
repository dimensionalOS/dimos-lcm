--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local vision_msgs_VisionClass = require('vision_msgs.VisionClass')
local std_msgs_Header = require('std_msgs.Header')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local LabelInfo = {}
LabelInfo.__index = LabelInfo

LabelInfo.name = 'vision_msgs.LabelInfo'
LabelInfo.packagename = 'vision_msgs'
LabelInfo.shortname = 'LabelInfo'

function LabelInfo:new()

  local obj = {}

  obj.class_map_length = 0
  obj.header = nil
  obj.class_map = {}
  obj.threshold = 0.0

  setmetatable(obj, self)

  return obj
end

function LabelInfo._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == LabelInfo then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, LabelInfo)

  local hash = lcm._hash.new('0x73231a5050580a67')
    + std_msgs_Header._get_hash_recursive(newparents)
    + vision_msgs_VisionClass._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

LabelInfo._packed_fingerprint = lcm._pack.pack('>X', LabelInfo._get_hash_recursive({}))

function LabelInfo:encode()

  return LabelInfo._packed_fingerprint .. self:_encode_one()
end

function LabelInfo:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.class_map_length))
  table.insert(buf_table, self.header:_encode_one())
  for i0 = 1, self.class_map_length do
    table.insert(buf_table, self.class_map[i0]:_encode_one())
  end
  table.insert(buf_table, lcm._pack.pack('>f', self.threshold))

  return table.concat(buf_table)
end

function LabelInfo.decode(data)

  if data:sub(1, 8) ~= LabelInfo._packed_fingerprint then
    error('bad fingerprint')
  end

  return LabelInfo._decode_one(data:sub(9))
end

function LabelInfo._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = LabelInfo:new()

  obj.class_map_length = lcm._pack.unpack('>i', data:read(4))
  obj.header = std_msgs_Header._decode_one(data)
  obj.class_map = {}
  for i0 = 1, obj.class_map_length do
    obj.class_map[i0] = vision_msgs_VisionClass._decode_one(data)
  end
  obj.threshold = lcm._pack.unpack('>f', data:read(4))

  return obj
end

return LabelInfo

