--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local vision_msgs_Pose2D = require('vision_msgs.Pose2D')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local BoundingBox2D = {}
BoundingBox2D.__index = BoundingBox2D

BoundingBox2D.name = 'vision_msgs.BoundingBox2D'
BoundingBox2D.packagename = 'vision_msgs'
BoundingBox2D.shortname = 'BoundingBox2D'

function BoundingBox2D:new()

  local obj = {}

  obj.center = nil
  obj.size_x = 0.0
  obj.size_y = 0.0

  setmetatable(obj, self)

  return obj
end

function BoundingBox2D._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == BoundingBox2D then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, BoundingBox2D)

  local hash = lcm._hash.new('0xe2274ad6240f1c77')
    + vision_msgs_Pose2D._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

BoundingBox2D._packed_fingerprint = lcm._pack.pack('>X', BoundingBox2D._get_hash_recursive({}))

function BoundingBox2D:encode()

  return BoundingBox2D._packed_fingerprint .. self:_encode_one()
end

function BoundingBox2D:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.center:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>dd', self.size_x, self.size_y))

  return table.concat(buf_table)
end

function BoundingBox2D.decode(data)

  if data:sub(1, 8) ~= BoundingBox2D._packed_fingerprint then
    error('bad fingerprint')
  end

  return BoundingBox2D._decode_one(data:sub(9))
end

function BoundingBox2D._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = BoundingBox2D:new()

  obj.center = vision_msgs_Pose2D._decode_one(data)
  obj.size_x, obj.size_y = lcm._pack.unpack('>dd', data:read(16))

  return obj
end

return BoundingBox2D

