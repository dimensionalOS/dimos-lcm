--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geometry_msgs_Pose = require('geometry_msgs.Pose')
local geometry_msgs_Vector3 = require('geometry_msgs.Vector3')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local BoundingBox3D = {}
BoundingBox3D.__index = BoundingBox3D

BoundingBox3D.name = 'vision_msgs.BoundingBox3D'
BoundingBox3D.packagename = 'vision_msgs'
BoundingBox3D.shortname = 'BoundingBox3D'

function BoundingBox3D:new()

  local obj = {}

  obj.center = nil
  obj.size = nil

  setmetatable(obj, self)

  return obj
end

function BoundingBox3D._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == BoundingBox3D then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, BoundingBox3D)

  local hash = lcm._hash.new('0xe10feec5cba89663')
    + geometry_msgs_Pose._get_hash_recursive(newparents)
    + geometry_msgs_Vector3._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

BoundingBox3D._packed_fingerprint = lcm._pack.pack('>X', BoundingBox3D._get_hash_recursive({}))

function BoundingBox3D:encode()

  return BoundingBox3D._packed_fingerprint .. self:_encode_one()
end

function BoundingBox3D:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.center:_encode_one())
  table.insert(buf_table, self.size:_encode_one())

  return table.concat(buf_table)
end

function BoundingBox3D.decode(data)

  if data:sub(1, 8) ~= BoundingBox3D._packed_fingerprint then
    error('bad fingerprint')
  end

  return BoundingBox3D._decode_one(data:sub(9))
end

function BoundingBox3D._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = BoundingBox3D:new()

  obj.center = geometry_msgs_Pose._decode_one(data)
  obj.size = geometry_msgs_Vector3._decode_one(data)

  return obj
end

return BoundingBox3D

