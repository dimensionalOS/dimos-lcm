--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geometry_msgs_PoseWithCovariance = require('geometry_msgs.PoseWithCovariance')
local vision_msgs_ObjectHypothesis = require('vision_msgs.ObjectHypothesis')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local ObjectHypothesisWithPose = {}
ObjectHypothesisWithPose.__index = ObjectHypothesisWithPose

ObjectHypothesisWithPose.name = 'vision_msgs.ObjectHypothesisWithPose'
ObjectHypothesisWithPose.packagename = 'vision_msgs'
ObjectHypothesisWithPose.shortname = 'ObjectHypothesisWithPose'

function ObjectHypothesisWithPose:new()

  local obj = {}

  obj.hypothesis = nil
  obj.pose = nil

  setmetatable(obj, self)

  return obj
end

function ObjectHypothesisWithPose._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == ObjectHypothesisWithPose then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, ObjectHypothesisWithPose)

  local hash = lcm._hash.new('0x65e1d44b451e8a8b')
    + vision_msgs_ObjectHypothesis._get_hash_recursive(newparents)
    + geometry_msgs_PoseWithCovariance._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

ObjectHypothesisWithPose._packed_fingerprint = lcm._pack.pack('>X', ObjectHypothesisWithPose._get_hash_recursive({}))

function ObjectHypothesisWithPose:encode()

  return ObjectHypothesisWithPose._packed_fingerprint .. self:_encode_one()
end

function ObjectHypothesisWithPose:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.hypothesis:_encode_one())
  table.insert(buf_table, self.pose:_encode_one())

  return table.concat(buf_table)
end

function ObjectHypothesisWithPose.decode(data)

  if data:sub(1, 8) ~= ObjectHypothesisWithPose._packed_fingerprint then
    error('bad fingerprint')
  end

  return ObjectHypothesisWithPose._decode_one(data:sub(9))
end

function ObjectHypothesisWithPose._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = ObjectHypothesisWithPose:new()

  obj.hypothesis = vision_msgs_ObjectHypothesis._decode_one(data)
  obj.pose = geometry_msgs_PoseWithCovariance._decode_one(data)

  return obj
end

return ObjectHypothesisWithPose

