--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Point2D = {}
Point2D.__index = Point2D

Point2D.name = 'vision_msgs.Point2D'
Point2D.packagename = 'vision_msgs'
Point2D.shortname = 'Point2D'

function Point2D:new()

  local obj = {}

  obj.x = 0.0
  obj.y = 0.0

  setmetatable(obj, self)

  return obj
end

function Point2D._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Point2D then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Point2D)

  local hash = lcm._hash.new('0xd259512e30b44885')
  hash:rotate(1)

  return hash
end

Point2D._packed_fingerprint = lcm._pack.pack('>X', Point2D._get_hash_recursive({}))

function Point2D:encode()

  return Point2D._packed_fingerprint .. self:_encode_one()
end

function Point2D:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>dd', self.x, self.y))

  return table.concat(buf_table)
end

function Point2D.decode(data)

  if data:sub(1, 8) ~= Point2D._packed_fingerprint then
    error('bad fingerprint')
  end

  return Point2D._decode_one(data:sub(9))
end

function Point2D._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Point2D:new()

  obj.x, obj.y = lcm._pack.unpack('>dd', data:read(16))

  return obj
end

return Point2D

