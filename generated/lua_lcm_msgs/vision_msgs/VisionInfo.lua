--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local std_msgs_Header = require('std_msgs.Header')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local VisionInfo = {}
VisionInfo.__index = VisionInfo

VisionInfo.name = 'vision_msgs.VisionInfo'
VisionInfo.packagename = 'vision_msgs'
VisionInfo.shortname = 'VisionInfo'

function VisionInfo:new()

  local obj = {}

  obj.header = nil
  obj.method = ''
  obj.database_location = ''
  obj.database_version = 0

  setmetatable(obj, self)

  return obj
end

function VisionInfo._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == VisionInfo then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, VisionInfo)

  local hash = lcm._hash.new('0x62c1cbfdc2184fbb')
    + std_msgs_Header._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

VisionInfo._packed_fingerprint = lcm._pack.pack('>X', VisionInfo._get_hash_recursive({}))

function VisionInfo:encode()

  return VisionInfo._packed_fingerprint .. self:_encode_one()
end

function VisionInfo:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.header:_encode_one())
  local __method_tmpstr = lcm._pack.prepare_string(self.method)
  table.insert(buf_table, lcm._pack.pack('>I', #__method_tmpstr + 1))
  table.insert(buf_table, __method_tmpstr .. '\0')
  local __database_location_tmpstr = lcm._pack.prepare_string(self.database_location)
  table.insert(buf_table, lcm._pack.pack('>I', #__database_location_tmpstr + 1))
  table.insert(buf_table, __database_location_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack('>i', self.database_version))

  return table.concat(buf_table)
end

function VisionInfo.decode(data)

  if data:sub(1, 8) ~= VisionInfo._packed_fingerprint then
    error('bad fingerprint')
  end

  return VisionInfo._decode_one(data:sub(9))
end

function VisionInfo._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = VisionInfo:new()

  obj.header = std_msgs_Header._decode_one(data)
  local __method_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.method = lcm._pack.prepare_string(data:read(__method_tmpstrlen))
  local __database_location_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.database_location = lcm._pack.prepare_string(data:read(__database_location_tmpstrlen))
  obj.database_version = lcm._pack.unpack('>i', data:read(4))

  return obj
end

return VisionInfo

