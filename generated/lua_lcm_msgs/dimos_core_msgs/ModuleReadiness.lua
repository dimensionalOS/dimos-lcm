--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local ModuleReadiness = {}
ModuleReadiness.__index = ModuleReadiness

ModuleReadiness.name = 'dimos_core_msgs.ModuleReadiness'
ModuleReadiness.packagename = 'dimos_core_msgs'
ModuleReadiness.shortname = 'ModuleReadiness'

function ModuleReadiness:new()

  local obj = {}

  obj.source = ''
  obj.generation = 0
  obj.sequence = 0
  obj.source_ts = 0.0
  obj.ready = false
  obj.reason = ''

  setmetatable(obj, self)

  return obj
end

function ModuleReadiness._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == ModuleReadiness then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, ModuleReadiness)

  local hash = lcm._hash.new('0x150d1bfa7d87808d')
  hash:rotate(1)

  return hash
end

ModuleReadiness._packed_fingerprint = lcm._pack.pack('>X', ModuleReadiness._get_hash_recursive({}))

function ModuleReadiness:encode()

  return ModuleReadiness._packed_fingerprint .. self:_encode_one()
end

function ModuleReadiness:_encode_one()

  local buf_table = {}

  local __source_tmpstr = lcm._pack.prepare_string(self.source)
  table.insert(buf_table, lcm._pack.pack('>I', #__source_tmpstr + 1))
  table.insert(buf_table, __source_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack('>qqd?', self.generation, self.sequence, self.source_ts, self.ready))
  local __reason_tmpstr = lcm._pack.prepare_string(self.reason)
  table.insert(buf_table, lcm._pack.pack('>I', #__reason_tmpstr + 1))
  table.insert(buf_table, __reason_tmpstr .. '\0')

  return table.concat(buf_table)
end

function ModuleReadiness.decode(data)

  if data:sub(1, 8) ~= ModuleReadiness._packed_fingerprint then
    error('bad fingerprint')
  end

  return ModuleReadiness._decode_one(data:sub(9))
end

function ModuleReadiness._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = ModuleReadiness:new()

  local __source_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.source = lcm._pack.prepare_string(data:read(__source_tmpstrlen))
  obj.generation, obj.sequence, obj.source_ts, obj.ready = lcm._pack.unpack('>qqd?', data:read(25))
  local __reason_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.reason = lcm._pack.prepare_string(data:read(__reason_tmpstrlen))

  return obj
end

return ModuleReadiness

