--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local UniqueID = {}
UniqueID.__index = UniqueID

UniqueID.name = 'uuid_msgs.UniqueID'
UniqueID.packagename = 'uuid_msgs'
UniqueID.shortname = 'UniqueID'

function UniqueID:new()

  local obj = {}

  obj.uuid = {}
  for d0 = 1, 16 do
    obj.uuid[d0] = 0
  end

  setmetatable(obj, self)

  return obj
end

function UniqueID._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == UniqueID then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, UniqueID)

  local hash = lcm._hash.new('0x697d4febd3ed5654')
  hash:rotate(1)

  return hash
end

UniqueID._packed_fingerprint = lcm._pack.pack('>X', UniqueID._get_hash_recursive({}))

function UniqueID:encode()

  return UniqueID._packed_fingerprint .. self:_encode_one()
end

function UniqueID:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>16B', unpack(self.uuid)))

  return table.concat(buf_table)
end

function UniqueID.decode(data)

  if data:sub(1, 8) ~= UniqueID._packed_fingerprint then
    error('bad fingerprint')
  end

  return UniqueID._decode_one(data:sub(9))
end

function UniqueID._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = UniqueID:new()

  obj.uuid = {lcm._pack.unpack('>16B', data:read(16))}

  return obj
end

return UniqueID

