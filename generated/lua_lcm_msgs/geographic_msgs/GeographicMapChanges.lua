--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geographic_msgs_GeographicMap = require('geographic_msgs.GeographicMap')
local uuid_msgs_UniqueID = require('uuid_msgs.UniqueID')
local std_msgs_Header = require('std_msgs.Header')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local GeographicMapChanges = {}
GeographicMapChanges.__index = GeographicMapChanges

GeographicMapChanges.name = 'geographic_msgs.GeographicMapChanges'
GeographicMapChanges.packagename = 'geographic_msgs'
GeographicMapChanges.shortname = 'GeographicMapChanges'

function GeographicMapChanges:new()

  local obj = {}

  obj.deletes_length = 0
  obj.header = nil
  obj.diffs = nil
  obj.deletes = {}

  setmetatable(obj, self)

  return obj
end

function GeographicMapChanges._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == GeographicMapChanges then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, GeographicMapChanges)

  local hash = lcm._hash.new('0x5d92adbb4d9db2cb')
    + std_msgs_Header._get_hash_recursive(newparents)
    + geographic_msgs_GeographicMap._get_hash_recursive(newparents)
    + uuid_msgs_UniqueID._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

GeographicMapChanges._packed_fingerprint = lcm._pack.pack('>X', GeographicMapChanges._get_hash_recursive({}))

function GeographicMapChanges:encode()

  return GeographicMapChanges._packed_fingerprint .. self:_encode_one()
end

function GeographicMapChanges:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.deletes_length))
  table.insert(buf_table, self.header:_encode_one())
  table.insert(buf_table, self.diffs:_encode_one())
  for i0 = 1, self.deletes_length do
    table.insert(buf_table, self.deletes[i0]:_encode_one())
  end

  return table.concat(buf_table)
end

function GeographicMapChanges.decode(data)

  if data:sub(1, 8) ~= GeographicMapChanges._packed_fingerprint then
    error('bad fingerprint')
  end

  return GeographicMapChanges._decode_one(data:sub(9))
end

function GeographicMapChanges._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = GeographicMapChanges:new()

  obj.deletes_length = lcm._pack.unpack('>i', data:read(4))
  obj.header = std_msgs_Header._decode_one(data)
  obj.diffs = geographic_msgs_GeographicMap._decode_one(data)
  obj.deletes = {}
  for i0 = 1, obj.deletes_length do
    obj.deletes[i0] = uuid_msgs_UniqueID._decode_one(data)
  end

  return obj
end

return GeographicMapChanges

