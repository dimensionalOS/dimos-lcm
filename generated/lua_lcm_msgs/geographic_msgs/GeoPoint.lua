--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local GeoPoint = {}
GeoPoint.__index = GeoPoint

GeoPoint.name = 'geographic_msgs.GeoPoint'
GeoPoint.packagename = 'geographic_msgs'
GeoPoint.shortname = 'GeoPoint'

function GeoPoint:new()

  local obj = {}

  obj.latitude = 0.0
  obj.longitude = 0.0
  obj.altitude = 0.0

  setmetatable(obj, self)

  return obj
end

function GeoPoint._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == GeoPoint then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, GeoPoint)

  local hash = lcm._hash.new('0xb84ee2b89179c84e')
  hash:rotate(1)

  return hash
end

GeoPoint._packed_fingerprint = lcm._pack.pack('>X', GeoPoint._get_hash_recursive({}))

function GeoPoint:encode()

  return GeoPoint._packed_fingerprint .. self:_encode_one()
end

function GeoPoint:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>ddd', self.latitude, self.longitude, self.altitude))

  return table.concat(buf_table)
end

function GeoPoint.decode(data)

  if data:sub(1, 8) ~= GeoPoint._packed_fingerprint then
    error('bad fingerprint')
  end

  return GeoPoint._decode_one(data:sub(9))
end

function GeoPoint._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = GeoPoint:new()

  obj.latitude, obj.longitude, obj.altitude = lcm._pack.unpack('>ddd', data:read(24))

  return obj
end

return GeoPoint

