--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local std_msgs_Header = require('std_msgs.Header')
local geographic_msgs_GeoPoseStamped = require('geographic_msgs.GeoPoseStamped')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local GeoPath = {}
GeoPath.__index = GeoPath

GeoPath.name = 'geographic_msgs.GeoPath'
GeoPath.packagename = 'geographic_msgs'
GeoPath.shortname = 'GeoPath'

function GeoPath:new()

  local obj = {}

  obj.poses_length = 0
  obj.header = nil
  obj.poses = {}

  setmetatable(obj, self)

  return obj
end

function GeoPath._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == GeoPath then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, GeoPath)

  local hash = lcm._hash.new('0xc779b6acc503055a')
    + std_msgs_Header._get_hash_recursive(newparents)
    + geographic_msgs_GeoPoseStamped._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

GeoPath._packed_fingerprint = lcm._pack.pack('>X', GeoPath._get_hash_recursive({}))

function GeoPath:encode()

  return GeoPath._packed_fingerprint .. self:_encode_one()
end

function GeoPath:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.poses_length))
  table.insert(buf_table, self.header:_encode_one())
  for i0 = 1, self.poses_length do
    table.insert(buf_table, self.poses[i0]:_encode_one())
  end

  return table.concat(buf_table)
end

function GeoPath.decode(data)

  if data:sub(1, 8) ~= GeoPath._packed_fingerprint then
    error('bad fingerprint')
  end

  return GeoPath._decode_one(data:sub(9))
end

function GeoPath._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = GeoPath:new()

  obj.poses_length = lcm._pack.unpack('>i', data:read(4))
  obj.header = std_msgs_Header._decode_one(data)
  obj.poses = {}
  for i0 = 1, obj.poses_length do
    obj.poses[i0] = geographic_msgs_GeoPoseStamped._decode_one(data)
  end

  return obj
end

return GeoPath

