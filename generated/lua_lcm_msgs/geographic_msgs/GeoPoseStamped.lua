--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geographic_msgs_GeoPose = require('geographic_msgs.GeoPose')
local std_msgs_Header = require('std_msgs.Header')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local GeoPoseStamped = {}
GeoPoseStamped.__index = GeoPoseStamped

GeoPoseStamped.name = 'geographic_msgs.GeoPoseStamped'
GeoPoseStamped.packagename = 'geographic_msgs'
GeoPoseStamped.shortname = 'GeoPoseStamped'

function GeoPoseStamped:new()

  local obj = {}

  obj.header = nil
  obj.pose = nil

  setmetatable(obj, self)

  return obj
end

function GeoPoseStamped._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == GeoPoseStamped then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, GeoPoseStamped)

  local hash = lcm._hash.new('0xe10feebec5c97663')
    + std_msgs_Header._get_hash_recursive(newparents)
    + geographic_msgs_GeoPose._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

GeoPoseStamped._packed_fingerprint = lcm._pack.pack('>X', GeoPoseStamped._get_hash_recursive({}))

function GeoPoseStamped:encode()

  return GeoPoseStamped._packed_fingerprint .. self:_encode_one()
end

function GeoPoseStamped:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.header:_encode_one())
  table.insert(buf_table, self.pose:_encode_one())

  return table.concat(buf_table)
end

function GeoPoseStamped.decode(data)

  if data:sub(1, 8) ~= GeoPoseStamped._packed_fingerprint then
    error('bad fingerprint')
  end

  return GeoPoseStamped._decode_one(data:sub(9))
end

function GeoPoseStamped._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = GeoPoseStamped:new()

  obj.header = std_msgs_Header._decode_one(data)
  obj.pose = geographic_msgs_GeoPose._decode_one(data)

  return obj
end

return GeoPoseStamped

