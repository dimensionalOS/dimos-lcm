--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geographic_msgs_GeoPoint = require('geographic_msgs.GeoPoint')
local geometry_msgs_Quaternion = require('geometry_msgs.Quaternion')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local GeoPose = {}
GeoPose.__index = GeoPose

GeoPose.name = 'geographic_msgs.GeoPose'
GeoPose.packagename = 'geographic_msgs'
GeoPose.shortname = 'GeoPose'

function GeoPose:new()

  local obj = {}

  obj.position = nil
  obj.orientation = nil

  setmetatable(obj, self)

  return obj
end

function GeoPose._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == GeoPose then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, GeoPose)

  local hash = lcm._hash.new('0x2d70dd60bd541272')
    + geographic_msgs_GeoPoint._get_hash_recursive(newparents)
    + geometry_msgs_Quaternion._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

GeoPose._packed_fingerprint = lcm._pack.pack('>X', GeoPose._get_hash_recursive({}))

function GeoPose:encode()

  return GeoPose._packed_fingerprint .. self:_encode_one()
end

function GeoPose:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.position:_encode_one())
  table.insert(buf_table, self.orientation:_encode_one())

  return table.concat(buf_table)
end

function GeoPose.decode(data)

  if data:sub(1, 8) ~= GeoPose._packed_fingerprint then
    error('bad fingerprint')
  end

  return GeoPose._decode_one(data:sub(9))
end

function GeoPose._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = GeoPose:new()

  obj.position = geographic_msgs_GeoPoint._decode_one(data)
  obj.orientation = geometry_msgs_Quaternion._decode_one(data)

  return obj
end

return GeoPose

