--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geographic_msgs_GeoPoint = require('geographic_msgs.GeoPoint')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local BoundingBox = {}
BoundingBox.__index = BoundingBox

BoundingBox.name = 'geographic_msgs.BoundingBox'
BoundingBox.packagename = 'geographic_msgs'
BoundingBox.shortname = 'BoundingBox'

function BoundingBox:new()

  local obj = {}

  obj.min_pt = nil
  obj.max_pt = nil

  setmetatable(obj, self)

  return obj
end

function BoundingBox._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == BoundingBox then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, BoundingBox)

  local hash = lcm._hash.new('0xecb7cbaa66bccde0')
    + geographic_msgs_GeoPoint._get_hash_recursive(newparents)
    + geographic_msgs_GeoPoint._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

BoundingBox._packed_fingerprint = lcm._pack.pack('>X', BoundingBox._get_hash_recursive({}))

function BoundingBox:encode()

  return BoundingBox._packed_fingerprint .. self:_encode_one()
end

function BoundingBox:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.min_pt:_encode_one())
  table.insert(buf_table, self.max_pt:_encode_one())

  return table.concat(buf_table)
end

function BoundingBox.decode(data)

  if data:sub(1, 8) ~= BoundingBox._packed_fingerprint then
    error('bad fingerprint')
  end

  return BoundingBox._decode_one(data:sub(9))
end

function BoundingBox._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = BoundingBox:new()

  obj.min_pt = geographic_msgs_GeoPoint._decode_one(data)
  obj.max_pt = geographic_msgs_GeoPoint._decode_one(data)

  return obj
end

return BoundingBox

