--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local std_msgs_Header = require('std_msgs.Header')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local JointState = {}
JointState.__index = JointState

JointState.name = 'sensor_msgs.JointState'
JointState.packagename = 'sensor_msgs'
JointState.shortname = 'JointState'

function JointState:new()

  local obj = {}

  obj.name_length = 0
  obj.position_length = 0
  obj.velocity_length = 0
  obj.effort_length = 0
  obj.header = nil
  obj.name = {}
  obj.position = {}
  obj.velocity = {}
  obj.effort = {}

  setmetatable(obj, self)

  return obj
end

function JointState._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == JointState then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, JointState)

  local hash = lcm._hash.new('0xb69cc30d0d7668fa')
    + std_msgs_Header._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

JointState._packed_fingerprint = lcm._pack.pack('>X', JointState._get_hash_recursive({}))

function JointState:encode()

  return JointState._packed_fingerprint .. self:_encode_one()
end

function JointState:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>iiii', self.name_length, self.position_length, self.velocity_length, self.effort_length))
  table.insert(buf_table, self.header:_encode_one())
  for i0 = 1, self.name_length do
    local __name_tmpstr = lcm._pack.prepare_string(self.name[i0])
    table.insert(buf_table, lcm._pack.pack('>I', #__name_tmpstr + 1))
    table.insert(buf_table, __name_tmpstr .. '\0')
  end
  table.insert(buf_table, lcm._pack.pack(string.format('>%dd', self.position_length), unpack(self.position)))
  table.insert(buf_table, lcm._pack.pack(string.format('>%dd', self.velocity_length), unpack(self.velocity)))
  table.insert(buf_table, lcm._pack.pack(string.format('>%dd', self.effort_length), unpack(self.effort)))

  return table.concat(buf_table)
end

function JointState.decode(data)

  if data:sub(1, 8) ~= JointState._packed_fingerprint then
    error('bad fingerprint')
  end

  return JointState._decode_one(data:sub(9))
end

function JointState._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = JointState:new()

  obj.name_length, obj.position_length, obj.velocity_length, obj.effort_length = lcm._pack.unpack('>iiii', data:read(16))
  obj.header = std_msgs_Header._decode_one(data)
  obj.name = {}
  for i0 = 1, obj.name_length do
    local __name_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
    obj.name[i0] = lcm._pack.prepare_string(data:read(__name_tmpstrlen))
  end
  obj.position = {lcm._pack.unpack(string.format('>%dd', obj.position_length), data:read(obj.position_length * 8))}
  obj.velocity = {lcm._pack.unpack(string.format('>%dd', obj.velocity_length), data:read(obj.velocity_length * 8))}
  obj.effort = {lcm._pack.unpack(string.format('>%dd', obj.effort_length), data:read(obj.effort_length * 8))}

  return obj
end

return JointState

