--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local LaserEcho = {}
LaserEcho.__index = LaserEcho

LaserEcho.name = 'sensor_msgs.LaserEcho'
LaserEcho.packagename = 'sensor_msgs'
LaserEcho.shortname = 'LaserEcho'

function LaserEcho:new()

  local obj = {}

  obj.echoes_length = 0
  obj.echoes = {}

  setmetatable(obj, self)

  return obj
end

function LaserEcho._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == LaserEcho then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, LaserEcho)

  local hash = lcm._hash.new('0xc45702116c7d0a2e')
  hash:rotate(1)

  return hash
end

LaserEcho._packed_fingerprint = lcm._pack.pack('>X', LaserEcho._get_hash_recursive({}))

function LaserEcho:encode()

  return LaserEcho._packed_fingerprint .. self:_encode_one()
end

function LaserEcho:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.echoes_length))
  table.insert(buf_table, lcm._pack.pack(string.format('>%df', self.echoes_length), unpack(self.echoes)))

  return table.concat(buf_table)
end

function LaserEcho.decode(data)

  if data:sub(1, 8) ~= LaserEcho._packed_fingerprint then
    error('bad fingerprint')
  end

  return LaserEcho._decode_one(data:sub(9))
end

function LaserEcho._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = LaserEcho:new()

  obj.echoes_length = lcm._pack.unpack('>i', data:read(4))
  obj.echoes = {lcm._pack.unpack(string.format('>%df', obj.echoes_length), data:read(obj.echoes_length * 4))}

  return obj
end

return LaserEcho

