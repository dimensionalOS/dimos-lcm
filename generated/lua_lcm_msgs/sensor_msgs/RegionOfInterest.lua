--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local RegionOfInterest = {}
RegionOfInterest.__index = RegionOfInterest

RegionOfInterest.name = 'sensor_msgs.RegionOfInterest'
RegionOfInterest.packagename = 'sensor_msgs'
RegionOfInterest.shortname = 'RegionOfInterest'

function RegionOfInterest:new()

  local obj = {}

  obj.x_offset = 0
  obj.y_offset = 0
  obj.height = 0
  obj.width = 0
  obj.do_rectify = false

  setmetatable(obj, self)

  return obj
end

function RegionOfInterest._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == RegionOfInterest then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, RegionOfInterest)

  local hash = lcm._hash.new('0x398a869d05983f0e')
  hash:rotate(1)

  return hash
end

RegionOfInterest._packed_fingerprint = lcm._pack.pack('>X', RegionOfInterest._get_hash_recursive({}))

function RegionOfInterest:encode()

  return RegionOfInterest._packed_fingerprint .. self:_encode_one()
end

function RegionOfInterest:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>iiii?', self.x_offset, self.y_offset, self.height, self.width, self.do_rectify))

  return table.concat(buf_table)
end

function RegionOfInterest.decode(data)

  if data:sub(1, 8) ~= RegionOfInterest._packed_fingerprint then
    error('bad fingerprint')
  end

  return RegionOfInterest._decode_one(data:sub(9))
end

function RegionOfInterest._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = RegionOfInterest:new()

  obj.x_offset, obj.y_offset, obj.height, obj.width, obj.do_rectify = lcm._pack.unpack('>iiii?', data:read(17))

  return obj
end

return RegionOfInterest

