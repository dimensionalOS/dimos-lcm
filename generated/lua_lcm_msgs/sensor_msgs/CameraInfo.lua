--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local sensor_msgs_RegionOfInterest = require('sensor_msgs.RegionOfInterest')
local std_msgs_Header = require('std_msgs.Header')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local CameraInfo = {}
CameraInfo.__index = CameraInfo

CameraInfo.name = 'sensor_msgs.CameraInfo'
CameraInfo.packagename = 'sensor_msgs'
CameraInfo.shortname = 'CameraInfo'

function CameraInfo:new()

  local obj = {}

  obj.D_length = 0
  obj.header = nil
  obj.height = 0
  obj.width = 0
  obj.distortion_model = ''
  obj.D = {}
  obj.K = {}
  for d0 = 1, 9 do
    obj.K[d0] = 0.0
  end
  obj.R = {}
  for d0 = 1, 9 do
    obj.R[d0] = 0.0
  end
  obj.P = {}
  for d0 = 1, 12 do
    obj.P[d0] = 0.0
  end
  obj.binning_x = 0
  obj.binning_y = 0
  obj.roi = nil

  setmetatable(obj, self)

  return obj
end

function CameraInfo._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == CameraInfo then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, CameraInfo)

  local hash = lcm._hash.new('0xb4ea6258bc6d0702')
    + std_msgs_Header._get_hash_recursive(newparents)
    + sensor_msgs_RegionOfInterest._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

CameraInfo._packed_fingerprint = lcm._pack.pack('>X', CameraInfo._get_hash_recursive({}))

function CameraInfo:encode()

  return CameraInfo._packed_fingerprint .. self:_encode_one()
end

function CameraInfo:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.D_length))
  table.insert(buf_table, self.header:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>ii', self.height, self.width))
  local __distortion_model_tmpstr = lcm._pack.prepare_string(self.distortion_model)
  table.insert(buf_table, lcm._pack.pack('>I', #__distortion_model_tmpstr + 1))
  table.insert(buf_table, __distortion_model_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack(string.format('>%dd', self.D_length), unpack(self.D)))
  table.insert(buf_table, lcm._pack.pack('>9d', unpack(self.K)))
  table.insert(buf_table, lcm._pack.pack('>9d', unpack(self.R)))
  table.insert(buf_table, lcm._pack.pack('>12d', unpack(self.P)))
  table.insert(buf_table, lcm._pack.pack('>ii', self.binning_x, self.binning_y))
  table.insert(buf_table, self.roi:_encode_one())

  return table.concat(buf_table)
end

function CameraInfo.decode(data)

  if data:sub(1, 8) ~= CameraInfo._packed_fingerprint then
    error('bad fingerprint')
  end

  return CameraInfo._decode_one(data:sub(9))
end

function CameraInfo._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = CameraInfo:new()

  obj.D_length = lcm._pack.unpack('>i', data:read(4))
  obj.header = std_msgs_Header._decode_one(data)
  obj.height, obj.width = lcm._pack.unpack('>ii', data:read(8))
  local __distortion_model_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.distortion_model = lcm._pack.prepare_string(data:read(__distortion_model_tmpstrlen))
  obj.D = {lcm._pack.unpack(string.format('>%dd', obj.D_length), data:read(obj.D_length * 8))}
  obj.K = {lcm._pack.unpack('>9d', data:read(72))}
  obj.R = {lcm._pack.unpack('>9d', data:read(72))}
  obj.P = {lcm._pack.unpack('>12d', data:read(96))}
  obj.binning_x, obj.binning_y = lcm._pack.unpack('>ii', data:read(8))
  obj.roi = sensor_msgs_RegionOfInterest._decode_one(data)

  return obj
end

return CameraInfo

