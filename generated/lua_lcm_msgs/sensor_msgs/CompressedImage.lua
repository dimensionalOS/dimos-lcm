--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local std_msgs_Header = require('std_msgs.Header')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local CompressedImage = {}
CompressedImage.__index = CompressedImage

CompressedImage.name = 'sensor_msgs.CompressedImage'
CompressedImage.packagename = 'sensor_msgs'
CompressedImage.shortname = 'CompressedImage'

function CompressedImage:new()

  local obj = {}

  obj.data_length = 0
  obj.header = nil
  obj.format = ''
  obj.data = {}

  setmetatable(obj, self)

  return obj
end

function CompressedImage._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == CompressedImage then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, CompressedImage)

  local hash = lcm._hash.new('0xac8cf79b462311d9')
    + std_msgs_Header._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

CompressedImage._packed_fingerprint = lcm._pack.pack('>X', CompressedImage._get_hash_recursive({}))

function CompressedImage:encode()

  return CompressedImage._packed_fingerprint .. self:_encode_one()
end

function CompressedImage:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.data_length))
  table.insert(buf_table, self.header:_encode_one())
  local __format_tmpstr = lcm._pack.prepare_string(self.format)
  table.insert(buf_table, lcm._pack.pack('>I', #__format_tmpstr + 1))
  table.insert(buf_table, __format_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack(string.format('>%dB', self.data_length), unpack(self.data)))

  return table.concat(buf_table)
end

function CompressedImage.decode(data)

  if data:sub(1, 8) ~= CompressedImage._packed_fingerprint then
    error('bad fingerprint')
  end

  return CompressedImage._decode_one(data:sub(9))
end

function CompressedImage._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = CompressedImage:new()

  obj.data_length = lcm._pack.unpack('>i', data:read(4))
  obj.header = std_msgs_Header._decode_one(data)
  local __format_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.format = lcm._pack.prepare_string(data:read(__format_tmpstrlen))
  obj.data = {lcm._pack.unpack(string.format('>%dB', obj.data_length), data:read(obj.data_length))}

  return obj
end

return CompressedImage

