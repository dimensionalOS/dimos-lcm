--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local std_msgs_Header = require('std_msgs.Header')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Joy = {}
Joy.__index = Joy

Joy.name = 'sensor_msgs.Joy'
Joy.packagename = 'sensor_msgs'
Joy.shortname = 'Joy'

function Joy:new()

  local obj = {}

  obj.axes_length = 0
  obj.buttons_length = 0
  obj.header = nil
  obj.axes = {}
  obj.buttons = {}

  setmetatable(obj, self)

  return obj
end

function Joy._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Joy then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Joy)

  local hash = lcm._hash.new('0x209d2c19077a692f')
    + std_msgs_Header._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

Joy._packed_fingerprint = lcm._pack.pack('>X', Joy._get_hash_recursive({}))

function Joy:encode()

  return Joy._packed_fingerprint .. self:_encode_one()
end

function Joy:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>ii', self.axes_length, self.buttons_length))
  table.insert(buf_table, self.header:_encode_one())
  table.insert(buf_table, lcm._pack.pack(string.format('>%df', self.axes_length), unpack(self.axes)))
  table.insert(buf_table, lcm._pack.pack(string.format('>%di', self.buttons_length), unpack(self.buttons)))

  return table.concat(buf_table)
end

function Joy.decode(data)

  if data:sub(1, 8) ~= Joy._packed_fingerprint then
    error('bad fingerprint')
  end

  return Joy._decode_one(data:sub(9))
end

function Joy._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Joy:new()

  obj.axes_length, obj.buttons_length = lcm._pack.unpack('>ii', data:read(8))
  obj.header = std_msgs_Header._decode_one(data)
  obj.axes = {lcm._pack.unpack(string.format('>%df', obj.axes_length), data:read(obj.axes_length * 4))}
  obj.buttons = {lcm._pack.unpack(string.format('>%di', obj.buttons_length), data:read(obj.buttons_length * 4))}

  return obj
end

return Joy

