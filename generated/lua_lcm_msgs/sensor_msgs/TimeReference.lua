--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local std_msgs_Header = require('std_msgs.Header')
local std_msgs_Time = require('std_msgs.Time')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local TimeReference = {}
TimeReference.__index = TimeReference

TimeReference.name = 'sensor_msgs.TimeReference'
TimeReference.packagename = 'sensor_msgs'
TimeReference.shortname = 'TimeReference'

function TimeReference:new()

  local obj = {}

  obj.header = nil
  obj.time_ref = nil
  obj.source = ''

  setmetatable(obj, self)

  return obj
end

function TimeReference._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == TimeReference then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, TimeReference)

  local hash = lcm._hash.new('0x1a6b1c8c5abae6db')
    + std_msgs_Header._get_hash_recursive(newparents)
    + std_msgs_Time._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

TimeReference._packed_fingerprint = lcm._pack.pack('>X', TimeReference._get_hash_recursive({}))

function TimeReference:encode()

  return TimeReference._packed_fingerprint .. self:_encode_one()
end

function TimeReference:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.header:_encode_one())
  table.insert(buf_table, self.time_ref:_encode_one())
  local __source_tmpstr = lcm._pack.prepare_string(self.source)
  table.insert(buf_table, lcm._pack.pack('>I', #__source_tmpstr + 1))
  table.insert(buf_table, __source_tmpstr .. '\0')

  return table.concat(buf_table)
end

function TimeReference.decode(data)

  if data:sub(1, 8) ~= TimeReference._packed_fingerprint then
    error('bad fingerprint')
  end

  return TimeReference._decode_one(data:sub(9))
end

function TimeReference._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = TimeReference:new()

  obj.header = std_msgs_Header._decode_one(data)
  obj.time_ref = std_msgs_Time._decode_one(data)
  local __source_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.source = lcm._pack.prepare_string(data:read(__source_tmpstrlen))

  return obj
end

return TimeReference

