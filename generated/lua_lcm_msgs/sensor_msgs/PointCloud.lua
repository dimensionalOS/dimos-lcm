--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local sensor_msgs_ChannelFloat32 = require('sensor_msgs.ChannelFloat32')
local std_msgs_Header = require('std_msgs.Header')
local geometry_msgs_Point32 = require('geometry_msgs.Point32')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local PointCloud = {}
PointCloud.__index = PointCloud

PointCloud.name = 'sensor_msgs.PointCloud'
PointCloud.packagename = 'sensor_msgs'
PointCloud.shortname = 'PointCloud'

function PointCloud:new()

  local obj = {}

  obj.points_length = 0
  obj.channels_length = 0
  obj.header = nil
  obj.points = {}
  obj.channels = {}

  setmetatable(obj, self)

  return obj
end

function PointCloud._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == PointCloud then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, PointCloud)

  local hash = lcm._hash.new('0x3ff7dc99e345164e')
    + std_msgs_Header._get_hash_recursive(newparents)
    + geometry_msgs_Point32._get_hash_recursive(newparents)
    + sensor_msgs_ChannelFloat32._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

PointCloud._packed_fingerprint = lcm._pack.pack('>X', PointCloud._get_hash_recursive({}))

function PointCloud:encode()

  return PointCloud._packed_fingerprint .. self:_encode_one()
end

function PointCloud:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>ii', self.points_length, self.channels_length))
  table.insert(buf_table, self.header:_encode_one())
  for i0 = 1, self.points_length do
    table.insert(buf_table, self.points[i0]:_encode_one())
  end
  for i0 = 1, self.channels_length do
    table.insert(buf_table, self.channels[i0]:_encode_one())
  end

  return table.concat(buf_table)
end

function PointCloud.decode(data)

  if data:sub(1, 8) ~= PointCloud._packed_fingerprint then
    error('bad fingerprint')
  end

  return PointCloud._decode_one(data:sub(9))
end

function PointCloud._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = PointCloud:new()

  obj.points_length, obj.channels_length = lcm._pack.unpack('>ii', data:read(8))
  obj.header = std_msgs_Header._decode_one(data)
  obj.points = {}
  for i0 = 1, obj.points_length do
    obj.points[i0] = geometry_msgs_Point32._decode_one(data)
  end
  obj.channels = {}
  for i0 = 1, obj.channels_length do
    obj.channels[i0] = sensor_msgs_ChannelFloat32._decode_one(data)
  end

  return obj
end

return PointCloud

