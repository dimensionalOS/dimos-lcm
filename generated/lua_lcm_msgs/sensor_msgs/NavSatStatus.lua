--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local NavSatStatus = {}
NavSatStatus.__index = NavSatStatus

  NavSatStatus.STATUS_NO_FIX = -1
  NavSatStatus.STATUS_FIX = 0
  NavSatStatus.STATUS_SBAS_FIX = 1
  NavSatStatus.STATUS_GBAS_FIX = 2
  NavSatStatus.SERVICE_GPS = 1
  NavSatStatus.SERVICE_GLONASS = 2
  NavSatStatus.SERVICE_COMPASS = 4
  NavSatStatus.SERVICE_GALILEO = 8

NavSatStatus.name = 'sensor_msgs.NavSatStatus'
NavSatStatus.packagename = 'sensor_msgs'
NavSatStatus.shortname = 'NavSatStatus'

function NavSatStatus:new()

  local obj = {}

  obj.status = 0
  obj.service = 0

  setmetatable(obj, self)

  return obj
end

function NavSatStatus._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == NavSatStatus then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, NavSatStatus)

  local hash = lcm._hash.new('0x76b236592075c1db')
  hash:rotate(1)

  return hash
end

NavSatStatus._packed_fingerprint = lcm._pack.pack('>X', NavSatStatus._get_hash_recursive({}))

function NavSatStatus:encode()

  return NavSatStatus._packed_fingerprint .. self:_encode_one()
end

function NavSatStatus:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>bh', self.status, self.service))

  return table.concat(buf_table)
end

function NavSatStatus.decode(data)

  if data:sub(1, 8) ~= NavSatStatus._packed_fingerprint then
    error('bad fingerprint')
  end

  return NavSatStatus._decode_one(data:sub(9))
end

function NavSatStatus._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = NavSatStatus:new()

  obj.status, obj.service = lcm._pack.unpack('>bh', data:read(3))

  return obj
end

return NavSatStatus

