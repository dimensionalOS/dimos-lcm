--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local ChannelFloat32 = {}
ChannelFloat32.__index = ChannelFloat32

ChannelFloat32.name = 'sensor_msgs.ChannelFloat32'
ChannelFloat32.packagename = 'sensor_msgs'
ChannelFloat32.shortname = 'ChannelFloat32'

function ChannelFloat32:new()

  local obj = {}

  obj.values_length = 0
  obj.name = ''
  obj.values = {}

  setmetatable(obj, self)

  return obj
end

function ChannelFloat32._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == ChannelFloat32 then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, ChannelFloat32)

  local hash = lcm._hash.new('0x8cbe448953ebf073')
  hash:rotate(1)

  return hash
end

ChannelFloat32._packed_fingerprint = lcm._pack.pack('>X', ChannelFloat32._get_hash_recursive({}))

function ChannelFloat32:encode()

  return ChannelFloat32._packed_fingerprint .. self:_encode_one()
end

function ChannelFloat32:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.values_length))
  local __name_tmpstr = lcm._pack.prepare_string(self.name)
  table.insert(buf_table, lcm._pack.pack('>I', #__name_tmpstr + 1))
  table.insert(buf_table, __name_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack(string.format('>%df', self.values_length), unpack(self.values)))

  return table.concat(buf_table)
end

function ChannelFloat32.decode(data)

  if data:sub(1, 8) ~= ChannelFloat32._packed_fingerprint then
    error('bad fingerprint')
  end

  return ChannelFloat32._decode_one(data:sub(9))
end

function ChannelFloat32._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = ChannelFloat32:new()

  obj.values_length = lcm._pack.unpack('>i', data:read(4))
  local __name_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.name = lcm._pack.prepare_string(data:read(__name_tmpstrlen))
  obj.values = {lcm._pack.unpack(string.format('>%df', obj.values_length), data:read(obj.values_length * 4))}

  return obj
end

return ChannelFloat32

