--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local sensor_msgs_PointField = require('sensor_msgs.PointField')
local std_msgs_Header = require('std_msgs.Header')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local PointCloud2 = {}
PointCloud2.__index = PointCloud2

PointCloud2.name = 'sensor_msgs.PointCloud2'
PointCloud2.packagename = 'sensor_msgs'
PointCloud2.shortname = 'PointCloud2'

function PointCloud2:new()

  local obj = {}

  obj.fields_length = 0
  obj.data_length = 0
  obj.header = nil
  obj.height = 0
  obj.width = 0
  obj.fields = {}
  obj.is_bigendian = false
  obj.point_step = 0
  obj.row_step = 0
  obj.data = {}
  obj.is_dense = false

  setmetatable(obj, self)

  return obj
end

function PointCloud2._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == PointCloud2 then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, PointCloud2)

  local hash = lcm._hash.new('0xeabe7183c4d74215')
    + std_msgs_Header._get_hash_recursive(newparents)
    + sensor_msgs_PointField._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

PointCloud2._packed_fingerprint = lcm._pack.pack('>X', PointCloud2._get_hash_recursive({}))

function PointCloud2:encode()

  return PointCloud2._packed_fingerprint .. self:_encode_one()
end

function PointCloud2:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>ii', self.fields_length, self.data_length))
  table.insert(buf_table, self.header:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>ii', self.height, self.width))
  for i0 = 1, self.fields_length do
    table.insert(buf_table, self.fields[i0]:_encode_one())
  end
  table.insert(buf_table, lcm._pack.pack('>?ii', self.is_bigendian, self.point_step, self.row_step))
  table.insert(buf_table, lcm._pack.pack(string.format('>%dB', self.data_length), unpack(self.data)))
  table.insert(buf_table, lcm._pack.pack('>?', self.is_dense))

  return table.concat(buf_table)
end

function PointCloud2.decode(data)

  if data:sub(1, 8) ~= PointCloud2._packed_fingerprint then
    error('bad fingerprint')
  end

  return PointCloud2._decode_one(data:sub(9))
end

function PointCloud2._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = PointCloud2:new()

  obj.fields_length, obj.data_length = lcm._pack.unpack('>ii', data:read(8))
  obj.header = std_msgs_Header._decode_one(data)
  obj.height, obj.width = lcm._pack.unpack('>ii', data:read(8))
  obj.fields = {}
  for i0 = 1, obj.fields_length do
    obj.fields[i0] = sensor_msgs_PointField._decode_one(data)
  end
  obj.is_bigendian, obj.point_step, obj.row_step = lcm._pack.unpack('>?ii', data:read(9))
  obj.data = {lcm._pack.unpack(string.format('>%dB', obj.data_length), data:read(obj.data_length))}
  obj.is_dense = lcm._pack.unpack('>?', data:read(1))

  return obj
end

return PointCloud2

