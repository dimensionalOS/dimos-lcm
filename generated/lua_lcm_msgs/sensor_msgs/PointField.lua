--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local PointField = {}
PointField.__index = PointField

  PointField.INT8 = 1
  PointField.UINT8 = 2
  PointField.INT16 = 3
  PointField.UINT16 = 4
  PointField.INT32 = 5
  PointField.UINT32 = 6
  PointField.FLOAT32 = 7
  PointField.FLOAT64 = 8

PointField.name = 'sensor_msgs.PointField'
PointField.packagename = 'sensor_msgs'
PointField.shortname = 'PointField'

function PointField:new()

  local obj = {}

  obj.name = ''
  obj.offset = 0
  obj.datatype = 0
  obj.count = 0

  setmetatable(obj, self)

  return obj
end

function PointField._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == PointField then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, PointField)

  local hash = lcm._hash.new('0x702e0e03f04285d7')
  hash:rotate(1)

  return hash
end

PointField._packed_fingerprint = lcm._pack.pack('>X', PointField._get_hash_recursive({}))

function PointField:encode()

  return PointField._packed_fingerprint .. self:_encode_one()
end

function PointField:_encode_one()

  local buf_table = {}

  local __name_tmpstr = lcm._pack.prepare_string(self.name)
  table.insert(buf_table, lcm._pack.pack('>I', #__name_tmpstr + 1))
  table.insert(buf_table, __name_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack('>iBi', self.offset, self.datatype, self.count))

  return table.concat(buf_table)
end

function PointField.decode(data)

  if data:sub(1, 8) ~= PointField._packed_fingerprint then
    error('bad fingerprint')
  end

  return PointField._decode_one(data:sub(9))
end

function PointField._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = PointField:new()

  local __name_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.name = lcm._pack.prepare_string(data:read(__name_tmpstrlen))
  obj.offset, obj.datatype, obj.count = lcm._pack.unpack('>iBi', data:read(9))

  return obj
end

return PointField

