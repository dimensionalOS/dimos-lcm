--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local std_msgs_Header = require('std_msgs.Header')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Range = {}
Range.__index = Range

  Range.ULTRASOUND = 0
  Range.INFRARED = 1

Range.name = 'sensor_msgs.Range'
Range.packagename = 'sensor_msgs'
Range.shortname = 'Range'

function Range:new()

  local obj = {}

  obj.header = nil
  obj.radiation_type = 0
  obj.field_of_view = 0.0
  obj.min_range = 0.0
  obj.max_range = 0.0
  obj.range = 0.0

  setmetatable(obj, self)

  return obj
end

function Range._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Range then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Range)

  local hash = lcm._hash.new('0xaaf9249a1465f86a')
    + std_msgs_Header._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

Range._packed_fingerprint = lcm._pack.pack('>X', Range._get_hash_recursive({}))

function Range:encode()

  return Range._packed_fingerprint .. self:_encode_one()
end

function Range:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.header:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>Bffff', self.radiation_type, self.field_of_view, self.min_range, self.max_range, self.range))

  return table.concat(buf_table)
end

function Range.decode(data)

  if data:sub(1, 8) ~= Range._packed_fingerprint then
    error('bad fingerprint')
  end

  return Range._decode_one(data:sub(9))
end

function Range._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Range:new()

  obj.header = std_msgs_Header._decode_one(data)
  obj.radiation_type, obj.field_of_view, obj.min_range, obj.max_range, obj.range = lcm._pack.unpack('>Bffff', data:read(17))

  return obj
end

return Range

