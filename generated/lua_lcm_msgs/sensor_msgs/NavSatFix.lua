--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local sensor_msgs_NavSatStatus = require('sensor_msgs.NavSatStatus')
local std_msgs_Header = require('std_msgs.Header')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local NavSatFix = {}
NavSatFix.__index = NavSatFix

  NavSatFix.COVARIANCE_TYPE_UNKNOWN = 0
  NavSatFix.COVARIANCE_TYPE_APPROXIMATED = 1
  NavSatFix.COVARIANCE_TYPE_DIAGONAL_KNOWN = 2
  NavSatFix.COVARIANCE_TYPE_KNOWN = 3

NavSatFix.name = 'sensor_msgs.NavSatFix'
NavSatFix.packagename = 'sensor_msgs'
NavSatFix.shortname = 'NavSatFix'

function NavSatFix:new()

  local obj = {}

  obj.header = nil
  obj.status = nil
  obj.latitude = 0.0
  obj.longitude = 0.0
  obj.altitude = 0.0
  obj.position_covariance = {}
  for d0 = 1, 9 do
    obj.position_covariance[d0] = 0.0
  end
  obj.position_covariance_type = 0

  setmetatable(obj, self)

  return obj
end

function NavSatFix._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == NavSatFix then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, NavSatFix)

  local hash = lcm._hash.new('0x4a84d20526d9067a')
    + std_msgs_Header._get_hash_recursive(newparents)
    + sensor_msgs_NavSatStatus._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

NavSatFix._packed_fingerprint = lcm._pack.pack('>X', NavSatFix._get_hash_recursive({}))

function NavSatFix:encode()

  return NavSatFix._packed_fingerprint .. self:_encode_one()
end

function NavSatFix:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.header:_encode_one())
  table.insert(buf_table, self.status:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>ddd', self.latitude, self.longitude, self.altitude))
  table.insert(buf_table, lcm._pack.pack('>9d', unpack(self.position_covariance)))
  table.insert(buf_table, lcm._pack.pack('>B', self.position_covariance_type))

  return table.concat(buf_table)
end

function NavSatFix.decode(data)

  if data:sub(1, 8) ~= NavSatFix._packed_fingerprint then
    error('bad fingerprint')
  end

  return NavSatFix._decode_one(data:sub(9))
end

function NavSatFix._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = NavSatFix:new()

  obj.header = std_msgs_Header._decode_one(data)
  obj.status = sensor_msgs_NavSatStatus._decode_one(data)
  obj.latitude, obj.longitude, obj.altitude = lcm._pack.unpack('>ddd', data:read(24))
  obj.position_covariance = {lcm._pack.unpack('>9d', data:read(72))}
  obj.position_covariance_type = lcm._pack.unpack('>B', data:read(1))

  return obj
end

return NavSatFix

