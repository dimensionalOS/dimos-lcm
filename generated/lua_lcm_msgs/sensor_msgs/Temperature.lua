--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local std_msgs_Header = require('std_msgs.Header')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Temperature = {}
Temperature.__index = Temperature

Temperature.name = 'sensor_msgs.Temperature'
Temperature.packagename = 'sensor_msgs'
Temperature.shortname = 'Temperature'

function Temperature:new()

  local obj = {}

  obj.header = nil
  obj.temperature = 0.0
  obj.variance = 0.0

  setmetatable(obj, self)

  return obj
end

function Temperature._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Temperature then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Temperature)

  local hash = lcm._hash.new('0x39cc1dc52d6ca13d')
    + std_msgs_Header._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

Temperature._packed_fingerprint = lcm._pack.pack('>X', Temperature._get_hash_recursive({}))

function Temperature:encode()

  return Temperature._packed_fingerprint .. self:_encode_one()
end

function Temperature:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.header:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>dd', self.temperature, self.variance))

  return table.concat(buf_table)
end

function Temperature.decode(data)

  if data:sub(1, 8) ~= Temperature._packed_fingerprint then
    error('bad fingerprint')
  end

  return Temperature._decode_one(data:sub(9))
end

function Temperature._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Temperature:new()

  obj.header = std_msgs_Header._decode_one(data)
  obj.temperature, obj.variance = lcm._pack.unpack('>dd', data:read(16))

  return obj
end

return Temperature

