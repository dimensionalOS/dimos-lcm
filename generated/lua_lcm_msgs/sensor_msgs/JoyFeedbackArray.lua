--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local sensor_msgs_JoyFeedback = require('sensor_msgs.JoyFeedback')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local JoyFeedbackArray = {}
JoyFeedbackArray.__index = JoyFeedbackArray

JoyFeedbackArray.name = 'sensor_msgs.JoyFeedbackArray'
JoyFeedbackArray.packagename = 'sensor_msgs'
JoyFeedbackArray.shortname = 'JoyFeedbackArray'

function JoyFeedbackArray:new()

  local obj = {}

  obj.array_length = 0
  obj.array = {}

  setmetatable(obj, self)

  return obj
end

function JoyFeedbackArray._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == JoyFeedbackArray then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, JoyFeedbackArray)

  local hash = lcm._hash.new('0xde2a78c20ffefa04')
    + sensor_msgs_JoyFeedback._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

JoyFeedbackArray._packed_fingerprint = lcm._pack.pack('>X', JoyFeedbackArray._get_hash_recursive({}))

function JoyFeedbackArray:encode()

  return JoyFeedbackArray._packed_fingerprint .. self:_encode_one()
end

function JoyFeedbackArray:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.array_length))
  for i0 = 1, self.array_length do
    table.insert(buf_table, self.array[i0]:_encode_one())
  end

  return table.concat(buf_table)
end

function JoyFeedbackArray.decode(data)

  if data:sub(1, 8) ~= JoyFeedbackArray._packed_fingerprint then
    error('bad fingerprint')
  end

  return JoyFeedbackArray._decode_one(data:sub(9))
end

function JoyFeedbackArray._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = JoyFeedbackArray:new()

  obj.array_length = lcm._pack.unpack('>i', data:read(4))
  obj.array = {}
  for i0 = 1, obj.array_length do
    obj.array[i0] = sensor_msgs_JoyFeedback._decode_one(data)
  end

  return obj
end

return JoyFeedbackArray

