--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local std_msgs_Header = require('std_msgs.Header')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local BatteryState = {}
BatteryState.__index = BatteryState

  BatteryState.POWER_SUPPLY_STATUS_UNKNOWN = 0
  BatteryState.POWER_SUPPLY_STATUS_CHARGING = 1
  BatteryState.POWER_SUPPLY_STATUS_DISCHARGING = 2
  BatteryState.POWER_SUPPLY_STATUS_NOT_CHARGING = 3
  BatteryState.POWER_SUPPLY_STATUS_FULL = 4
  BatteryState.POWER_SUPPLY_HEALTH_UNKNOWN = 0
  BatteryState.POWER_SUPPLY_HEALTH_GOOD = 1
  BatteryState.POWER_SUPPLY_HEALTH_OVERHEAT = 2
  BatteryState.POWER_SUPPLY_HEALTH_DEAD = 3
  BatteryState.POWER_SUPPLY_HEALTH_OVERVOLTAGE = 4
  BatteryState.POWER_SUPPLY_HEALTH_UNSPEC_FAILURE = 5
  BatteryState.POWER_SUPPLY_HEALTH_COLD = 6
  BatteryState.POWER_SUPPLY_HEALTH_WATCHDOG_TIMER_EXPIRE = 7
  BatteryState.POWER_SUPPLY_HEALTH_SAFETY_TIMER_EXPIRE = 8
  BatteryState.POWER_SUPPLY_TECHNOLOGY_UNKNOWN = 0
  BatteryState.POWER_SUPPLY_TECHNOLOGY_NIMH = 1
  BatteryState.POWER_SUPPLY_TECHNOLOGY_LION = 2
  BatteryState.POWER_SUPPLY_TECHNOLOGY_LIPO = 3
  BatteryState.POWER_SUPPLY_TECHNOLOGY_LIFE = 4
  BatteryState.POWER_SUPPLY_TECHNOLOGY_NICD = 5
  BatteryState.POWER_SUPPLY_TECHNOLOGY_LIMN = 6

BatteryState.name = 'sensor_msgs.BatteryState'
BatteryState.packagename = 'sensor_msgs'
BatteryState.shortname = 'BatteryState'

function BatteryState:new()

  local obj = {}

  obj.cell_voltage_length = 0
  obj.cell_temperature_length = 0
  obj.header = nil
  obj.voltage = 0.0
  obj.temperature = 0.0
  obj.current = 0.0
  obj.charge = 0.0
  obj.capacity = 0.0
  obj.design_capacity = 0.0
  obj.percentage = 0.0
  obj.power_supply_status = 0
  obj.power_supply_health = 0
  obj.power_supply_technology = 0
  obj.present = false
  obj.cell_voltage = {}
  obj.cell_temperature = {}
  obj.location = ''
  obj.serial_number = ''

  setmetatable(obj, self)

  return obj
end

function BatteryState._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == BatteryState then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, BatteryState)

  local hash = lcm._hash.new('0x8f419fb94c3b774d')
    + std_msgs_Header._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

BatteryState._packed_fingerprint = lcm._pack.pack('>X', BatteryState._get_hash_recursive({}))

function BatteryState:encode()

  return BatteryState._packed_fingerprint .. self:_encode_one()
end

function BatteryState:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>ii', self.cell_voltage_length, self.cell_temperature_length))
  table.insert(buf_table, self.header:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>fffffffBBB?', self.voltage, self.temperature, self.current, self.charge, self.capacity, self.design_capacity, self.percentage, self.power_supply_status, self.power_supply_health, self.power_supply_technology, self.present))
  table.insert(buf_table, lcm._pack.pack(string.format('>%df', self.cell_voltage_length), unpack(self.cell_voltage)))
  table.insert(buf_table, lcm._pack.pack(string.format('>%df', self.cell_temperature_length), unpack(self.cell_temperature)))
  local __location_tmpstr = lcm._pack.prepare_string(self.location)
  table.insert(buf_table, lcm._pack.pack('>I', #__location_tmpstr + 1))
  table.insert(buf_table, __location_tmpstr .. '\0')
  local __serial_number_tmpstr = lcm._pack.prepare_string(self.serial_number)
  table.insert(buf_table, lcm._pack.pack('>I', #__serial_number_tmpstr + 1))
  table.insert(buf_table, __serial_number_tmpstr .. '\0')

  return table.concat(buf_table)
end

function BatteryState.decode(data)

  if data:sub(1, 8) ~= BatteryState._packed_fingerprint then
    error('bad fingerprint')
  end

  return BatteryState._decode_one(data:sub(9))
end

function BatteryState._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = BatteryState:new()

  obj.cell_voltage_length, obj.cell_temperature_length = lcm._pack.unpack('>ii', data:read(8))
  obj.header = std_msgs_Header._decode_one(data)
  obj.voltage, obj.temperature, obj.current, obj.charge, obj.capacity, obj.design_capacity, obj.percentage, obj.power_supply_status, obj.power_supply_health, obj.power_supply_technology, obj.present = lcm._pack.unpack('>fffffffBBB?', data:read(32))
  obj.cell_voltage = {lcm._pack.unpack(string.format('>%df', obj.cell_voltage_length), data:read(obj.cell_voltage_length * 4))}
  obj.cell_temperature = {lcm._pack.unpack(string.format('>%df', obj.cell_temperature_length), data:read(obj.cell_temperature_length * 4))}
  local __location_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.location = lcm._pack.prepare_string(data:read(__location_tmpstrlen))
  local __serial_number_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.serial_number = lcm._pack.prepare_string(data:read(__serial_number_tmpstrlen))

  return obj
end

return BatteryState

