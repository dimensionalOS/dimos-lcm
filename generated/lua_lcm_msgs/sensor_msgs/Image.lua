--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local std_msgs_Header = require('std_msgs.Header')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Image = {}
Image.__index = Image

Image.name = 'sensor_msgs.Image'
Image.packagename = 'sensor_msgs'
Image.shortname = 'Image'

function Image:new()

  local obj = {}

  obj.data_length = 0
  obj.header = nil
  obj.height = 0
  obj.width = 0
  obj.encoding = ''
  obj.is_bigendian = 0
  obj.step = 0
  obj.data = {}

  setmetatable(obj, self)

  return obj
end

function Image._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Image then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Image)

  local hash = lcm._hash.new('0x79d36c21d3c16094')
    + std_msgs_Header._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

Image._packed_fingerprint = lcm._pack.pack('>X', Image._get_hash_recursive({}))

function Image:encode()

  return Image._packed_fingerprint .. self:_encode_one()
end

function Image:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.data_length))
  table.insert(buf_table, self.header:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>ii', self.height, self.width))
  local __encoding_tmpstr = lcm._pack.prepare_string(self.encoding)
  table.insert(buf_table, lcm._pack.pack('>I', #__encoding_tmpstr + 1))
  table.insert(buf_table, __encoding_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack('>Bi', self.is_bigendian, self.step))
  table.insert(buf_table, lcm._pack.pack(string.format('>%dB', self.data_length), unpack(self.data)))

  return table.concat(buf_table)
end

function Image.decode(data)

  if data:sub(1, 8) ~= Image._packed_fingerprint then
    error('bad fingerprint')
  end

  return Image._decode_one(data:sub(9))
end

function Image._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Image:new()

  obj.data_length = lcm._pack.unpack('>i', data:read(4))
  obj.header = std_msgs_Header._decode_one(data)
  obj.height, obj.width = lcm._pack.unpack('>ii', data:read(8))
  local __encoding_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.encoding = lcm._pack.prepare_string(data:read(__encoding_tmpstrlen))
  obj.is_bigendian, obj.step = lcm._pack.unpack('>Bi', data:read(5))
  obj.data = {lcm._pack.unpack(string.format('>%dB', obj.data_length), data:read(obj.data_length))}

  return obj
end

return Image

