--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local builtin_interfaces_Time = require('builtin_interfaces.Time')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Log = {}
Log.__index = Log

  Log.UNKNOWN = 0
  Log.DEBUG = 1
  Log.INFO = 2
  Log.WARNING = 3
  Log.ERROR = 4
  Log.FATAL = 5

Log.name = 'foxglove_msgs.Log'
Log.packagename = 'foxglove_msgs'
Log.shortname = 'Log'

function Log:new()

  local obj = {}

  obj.timestamp = nil
  obj.level = 0
  obj.message = ''
  obj.name = ''
  obj.file = ''
  obj.line = 0

  setmetatable(obj, self)

  return obj
end

function Log._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Log then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Log)

  local hash = lcm._hash.new('0x623dae456e90a1c7')
    + builtin_interfaces_Time._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

Log._packed_fingerprint = lcm._pack.pack('>X', Log._get_hash_recursive({}))

function Log:encode()

  return Log._packed_fingerprint .. self:_encode_one()
end

function Log:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.timestamp:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>B', self.level))
  local __message_tmpstr = lcm._pack.prepare_string(self.message)
  table.insert(buf_table, lcm._pack.pack('>I', #__message_tmpstr + 1))
  table.insert(buf_table, __message_tmpstr .. '\0')
  local __name_tmpstr = lcm._pack.prepare_string(self.name)
  table.insert(buf_table, lcm._pack.pack('>I', #__name_tmpstr + 1))
  table.insert(buf_table, __name_tmpstr .. '\0')
  local __file_tmpstr = lcm._pack.prepare_string(self.file)
  table.insert(buf_table, lcm._pack.pack('>I', #__file_tmpstr + 1))
  table.insert(buf_table, __file_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack('>i', self.line))

  return table.concat(buf_table)
end

function Log.decode(data)

  if data:sub(1, 8) ~= Log._packed_fingerprint then
    error('bad fingerprint')
  end

  return Log._decode_one(data:sub(9))
end

function Log._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Log:new()

  obj.timestamp = builtin_interfaces_Time._decode_one(data)
  obj.level = lcm._pack.unpack('>B', data:read(1))
  local __message_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.message = lcm._pack.prepare_string(data:read(__message_tmpstrlen))
  local __name_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.name = lcm._pack.prepare_string(data:read(__name_tmpstrlen))
  local __file_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.file = lcm._pack.prepare_string(data:read(__file_tmpstrlen))
  obj.line = lcm._pack.unpack('>i', data:read(4))

  return obj
end

return Log

