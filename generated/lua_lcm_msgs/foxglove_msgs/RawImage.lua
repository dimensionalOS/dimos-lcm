--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local builtin_interfaces_Time = require('builtin_interfaces.Time')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local RawImage = {}
RawImage.__index = RawImage

RawImage.name = 'foxglove_msgs.RawImage'
RawImage.packagename = 'foxglove_msgs'
RawImage.shortname = 'RawImage'

function RawImage:new()

  local obj = {}

  obj.data_length = 0
  obj.timestamp = nil
  obj.frame_id = ''
  obj.width = 0
  obj.height = 0
  obj.encoding = ''
  obj.step = 0
  obj.data = {}

  setmetatable(obj, self)

  return obj
end

function RawImage._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == RawImage then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, RawImage)

  local hash = lcm._hash.new('0x8dc0da8aa491bb39')
    + builtin_interfaces_Time._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

RawImage._packed_fingerprint = lcm._pack.pack('>X', RawImage._get_hash_recursive({}))

function RawImage:encode()

  return RawImage._packed_fingerprint .. self:_encode_one()
end

function RawImage:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.data_length))
  table.insert(buf_table, self.timestamp:_encode_one())
  local __frame_id_tmpstr = lcm._pack.prepare_string(self.frame_id)
  table.insert(buf_table, lcm._pack.pack('>I', #__frame_id_tmpstr + 1))
  table.insert(buf_table, __frame_id_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack('>ii', self.width, self.height))
  local __encoding_tmpstr = lcm._pack.prepare_string(self.encoding)
  table.insert(buf_table, lcm._pack.pack('>I', #__encoding_tmpstr + 1))
  table.insert(buf_table, __encoding_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack('>i', self.step))
  table.insert(buf_table, lcm._pack.pack(string.format('>%dB', self.data_length), unpack(self.data)))

  return table.concat(buf_table)
end

function RawImage.decode(data)

  if data:sub(1, 8) ~= RawImage._packed_fingerprint then
    error('bad fingerprint')
  end

  return RawImage._decode_one(data:sub(9))
end

function RawImage._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = RawImage:new()

  obj.data_length = lcm._pack.unpack('>i', data:read(4))
  obj.timestamp = builtin_interfaces_Time._decode_one(data)
  local __frame_id_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.frame_id = lcm._pack.prepare_string(data:read(__frame_id_tmpstrlen))
  obj.width, obj.height = lcm._pack.unpack('>ii', data:read(8))
  local __encoding_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.encoding = lcm._pack.prepare_string(data:read(__encoding_tmpstrlen))
  obj.step = lcm._pack.unpack('>i', data:read(4))
  obj.data = {lcm._pack.unpack(string.format('>%dB', obj.data_length), data:read(obj.data_length))}

  return obj
end

return RawImage

