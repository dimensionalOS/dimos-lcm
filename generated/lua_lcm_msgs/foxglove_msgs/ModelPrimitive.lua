--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geometry_msgs_Pose = require('geometry_msgs.Pose')
local geometry_msgs_Vector3 = require('geometry_msgs.Vector3')
local foxglove_msgs_Color = require('foxglove_msgs.Color')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local ModelPrimitive = {}
ModelPrimitive.__index = ModelPrimitive

ModelPrimitive.name = 'foxglove_msgs.ModelPrimitive'
ModelPrimitive.packagename = 'foxglove_msgs'
ModelPrimitive.shortname = 'ModelPrimitive'

function ModelPrimitive:new()

  local obj = {}

  obj.data_length = 0
  obj.pose = nil
  obj.scale = nil
  obj.color = nil
  obj.override_color = false
  obj.url = ''
  obj.media_type = ''
  obj.data = {}

  setmetatable(obj, self)

  return obj
end

function ModelPrimitive._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == ModelPrimitive then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, ModelPrimitive)

  local hash = lcm._hash.new('0x23cd41ba898fa1fa')
    + geometry_msgs_Pose._get_hash_recursive(newparents)
    + geometry_msgs_Vector3._get_hash_recursive(newparents)
    + foxglove_msgs_Color._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

ModelPrimitive._packed_fingerprint = lcm._pack.pack('>X', ModelPrimitive._get_hash_recursive({}))

function ModelPrimitive:encode()

  return ModelPrimitive._packed_fingerprint .. self:_encode_one()
end

function ModelPrimitive:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.data_length))
  table.insert(buf_table, self.pose:_encode_one())
  table.insert(buf_table, self.scale:_encode_one())
  table.insert(buf_table, self.color:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>?', self.override_color))
  local __url_tmpstr = lcm._pack.prepare_string(self.url)
  table.insert(buf_table, lcm._pack.pack('>I', #__url_tmpstr + 1))
  table.insert(buf_table, __url_tmpstr .. '\0')
  local __media_type_tmpstr = lcm._pack.prepare_string(self.media_type)
  table.insert(buf_table, lcm._pack.pack('>I', #__media_type_tmpstr + 1))
  table.insert(buf_table, __media_type_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack(string.format('>%dB', self.data_length), unpack(self.data)))

  return table.concat(buf_table)
end

function ModelPrimitive.decode(data)

  if data:sub(1, 8) ~= ModelPrimitive._packed_fingerprint then
    error('bad fingerprint')
  end

  return ModelPrimitive._decode_one(data:sub(9))
end

function ModelPrimitive._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = ModelPrimitive:new()

  obj.data_length = lcm._pack.unpack('>i', data:read(4))
  obj.pose = geometry_msgs_Pose._decode_one(data)
  obj.scale = geometry_msgs_Vector3._decode_one(data)
  obj.color = foxglove_msgs_Color._decode_one(data)
  obj.override_color = lcm._pack.unpack('>?', data:read(1))
  local __url_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.url = lcm._pack.prepare_string(data:read(__url_tmpstrlen))
  local __media_type_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.media_type = lcm._pack.prepare_string(data:read(__media_type_tmpstrlen))
  obj.data = {lcm._pack.unpack(string.format('>%dB', obj.data_length), data:read(obj.data_length))}

  return obj
end

return ModelPrimitive

