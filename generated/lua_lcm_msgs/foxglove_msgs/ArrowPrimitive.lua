--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geometry_msgs_Pose = require('geometry_msgs.Pose')
local foxglove_msgs_Color = require('foxglove_msgs.Color')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local ArrowPrimitive = {}
ArrowPrimitive.__index = ArrowPrimitive

ArrowPrimitive.name = 'foxglove_msgs.ArrowPrimitive'
ArrowPrimitive.packagename = 'foxglove_msgs'
ArrowPrimitive.shortname = 'ArrowPrimitive'

function ArrowPrimitive:new()

  local obj = {}

  obj.pose = nil
  obj.shaft_length = 0.0
  obj.shaft_diameter = 0.0
  obj.head_length = 0.0
  obj.head_diameter = 0.0
  obj.color = nil

  setmetatable(obj, self)

  return obj
end

function ArrowPrimitive._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == ArrowPrimitive then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, ArrowPrimitive)

  local hash = lcm._hash.new('0xb1f9d32968e60fbb')
    + geometry_msgs_Pose._get_hash_recursive(newparents)
    + foxglove_msgs_Color._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

ArrowPrimitive._packed_fingerprint = lcm._pack.pack('>X', ArrowPrimitive._get_hash_recursive({}))

function ArrowPrimitive:encode()

  return ArrowPrimitive._packed_fingerprint .. self:_encode_one()
end

function ArrowPrimitive:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.pose:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>dddd', self.shaft_length, self.shaft_diameter, self.head_length, self.head_diameter))
  table.insert(buf_table, self.color:_encode_one())

  return table.concat(buf_table)
end

function ArrowPrimitive.decode(data)

  if data:sub(1, 8) ~= ArrowPrimitive._packed_fingerprint then
    error('bad fingerprint')
  end

  return ArrowPrimitive._decode_one(data:sub(9))
end

function ArrowPrimitive._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = ArrowPrimitive:new()

  obj.pose = geometry_msgs_Pose._decode_one(data)
  obj.shaft_length, obj.shaft_diameter, obj.head_length, obj.head_diameter = lcm._pack.unpack('>dddd', data:read(32))
  obj.color = foxglove_msgs_Color._decode_one(data)

  return obj
end

return ArrowPrimitive

