--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geometry_msgs_Pose = require('geometry_msgs.Pose')
local geometry_msgs_Point = require('geometry_msgs.Point')
local foxglove_msgs_Color = require('foxglove_msgs.Color')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local LinePrimitive = {}
LinePrimitive.__index = LinePrimitive

  LinePrimitive.LINE_STRIP = 0
  LinePrimitive.LINE_LOOP = 1
  LinePrimitive.LINE_LIST = 2

LinePrimitive.name = 'foxglove_msgs.LinePrimitive'
LinePrimitive.packagename = 'foxglove_msgs'
LinePrimitive.shortname = 'LinePrimitive'

function LinePrimitive:new()

  local obj = {}

  obj.points_length = 0
  obj.colors_length = 0
  obj.indices_length = 0
  obj.type = 0
  obj.pose = nil
  obj.thickness = 0.0
  obj.scale_invariant = false
  obj.points = {}
  obj.color = nil
  obj.colors = {}
  obj.indices = {}

  setmetatable(obj, self)

  return obj
end

function LinePrimitive._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == LinePrimitive then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, LinePrimitive)

  local hash = lcm._hash.new('0x3b39f8eb653b3cd3')
    + geometry_msgs_Pose._get_hash_recursive(newparents)
    + geometry_msgs_Point._get_hash_recursive(newparents)
    + foxglove_msgs_Color._get_hash_recursive(newparents)
    + foxglove_msgs_Color._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

LinePrimitive._packed_fingerprint = lcm._pack.pack('>X', LinePrimitive._get_hash_recursive({}))

function LinePrimitive:encode()

  return LinePrimitive._packed_fingerprint .. self:_encode_one()
end

function LinePrimitive:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>iiiB', self.points_length, self.colors_length, self.indices_length, self.type))
  table.insert(buf_table, self.pose:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>d?', self.thickness, self.scale_invariant))
  for i0 = 1, self.points_length do
    table.insert(buf_table, self.points[i0]:_encode_one())
  end
  table.insert(buf_table, self.color:_encode_one())
  for i0 = 1, self.colors_length do
    table.insert(buf_table, self.colors[i0]:_encode_one())
  end
  table.insert(buf_table, lcm._pack.pack(string.format('>%di', self.indices_length), unpack(self.indices)))

  return table.concat(buf_table)
end

function LinePrimitive.decode(data)

  if data:sub(1, 8) ~= LinePrimitive._packed_fingerprint then
    error('bad fingerprint')
  end

  return LinePrimitive._decode_one(data:sub(9))
end

function LinePrimitive._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = LinePrimitive:new()

  obj.points_length, obj.colors_length, obj.indices_length, obj.type = lcm._pack.unpack('>iiiB', data:read(13))
  obj.pose = geometry_msgs_Pose._decode_one(data)
  obj.thickness, obj.scale_invariant = lcm._pack.unpack('>d?', data:read(9))
  obj.points = {}
  for i0 = 1, obj.points_length do
    obj.points[i0] = geometry_msgs_Point._decode_one(data)
  end
  obj.color = foxglove_msgs_Color._decode_one(data)
  obj.colors = {}
  for i0 = 1, obj.colors_length do
    obj.colors[i0] = foxglove_msgs_Color._decode_one(data)
  end
  obj.indices = {lcm._pack.unpack(string.format('>%di', obj.indices_length), data:read(obj.indices_length * 4))}

  return obj
end

return LinePrimitive

