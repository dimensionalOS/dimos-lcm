--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local foxglove_msgs_SceneEntityDeletion = require('foxglove_msgs.SceneEntityDeletion')
local foxglove_msgs_SceneEntity = require('foxglove_msgs.SceneEntity')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local SceneUpdate = {}
SceneUpdate.__index = SceneUpdate

SceneUpdate.name = 'foxglove_msgs.SceneUpdate'
SceneUpdate.packagename = 'foxglove_msgs'
SceneUpdate.shortname = 'SceneUpdate'

function SceneUpdate:new()

  local obj = {}

  obj.deletions_length = 0
  obj.entities_length = 0
  obj.deletions = {}
  obj.entities = {}

  setmetatable(obj, self)

  return obj
end

function SceneUpdate._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == SceneUpdate then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, SceneUpdate)

  local hash = lcm._hash.new('0x8f9d4ee9e2a92d31')
    + foxglove_msgs_SceneEntityDeletion._get_hash_recursive(newparents)
    + foxglove_msgs_SceneEntity._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

SceneUpdate._packed_fingerprint = lcm._pack.pack('>X', SceneUpdate._get_hash_recursive({}))

function SceneUpdate:encode()

  return SceneUpdate._packed_fingerprint .. self:_encode_one()
end

function SceneUpdate:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>ii', self.deletions_length, self.entities_length))
  for i0 = 1, self.deletions_length do
    table.insert(buf_table, self.deletions[i0]:_encode_one())
  end
  for i0 = 1, self.entities_length do
    table.insert(buf_table, self.entities[i0]:_encode_one())
  end

  return table.concat(buf_table)
end

function SceneUpdate.decode(data)

  if data:sub(1, 8) ~= SceneUpdate._packed_fingerprint then
    error('bad fingerprint')
  end

  return SceneUpdate._decode_one(data:sub(9))
end

function SceneUpdate._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = SceneUpdate:new()

  obj.deletions_length, obj.entities_length = lcm._pack.unpack('>ii', data:read(8))
  obj.deletions = {}
  for i0 = 1, obj.deletions_length do
    obj.deletions[i0] = foxglove_msgs_SceneEntityDeletion._decode_one(data)
  end
  obj.entities = {}
  for i0 = 1, obj.entities_length do
    obj.entities[i0] = foxglove_msgs_SceneEntity._decode_one(data)
  end

  return obj
end

return SceneUpdate

