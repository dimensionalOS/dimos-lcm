--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Color = {}
Color.__index = Color

Color.name = 'foxglove_msgs.Color'
Color.packagename = 'foxglove_msgs'
Color.shortname = 'Color'

function Color:new()

  local obj = {}

  obj.r = 0.0
  obj.g = 0.0
  obj.b = 0.0
  obj.a = 0.0

  setmetatable(obj, self)

  return obj
end

function Color._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Color then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Color)

  local hash = lcm._hash.new('0x998136169753c441')
  hash:rotate(1)

  return hash
end

Color._packed_fingerprint = lcm._pack.pack('>X', Color._get_hash_recursive({}))

function Color:encode()

  return Color._packed_fingerprint .. self:_encode_one()
end

function Color:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>dddd', self.r, self.g, self.b, self.a))

  return table.concat(buf_table)
end

function Color.decode(data)

  if data:sub(1, 8) ~= Color._packed_fingerprint then
    error('bad fingerprint')
  end

  return Color._decode_one(data:sub(9))
end

function Color._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Color:new()

  obj.r, obj.g, obj.b, obj.a = lcm._pack.unpack('>dddd', data:read(32))

  return obj
end

return Color

