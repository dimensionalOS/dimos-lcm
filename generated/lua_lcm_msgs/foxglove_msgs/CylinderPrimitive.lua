--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geometry_msgs_Pose = require('geometry_msgs.Pose')
local geometry_msgs_Vector3 = require('geometry_msgs.Vector3')
local foxglove_msgs_Color = require('foxglove_msgs.Color')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local CylinderPrimitive = {}
CylinderPrimitive.__index = CylinderPrimitive

CylinderPrimitive.name = 'foxglove_msgs.CylinderPrimitive'
CylinderPrimitive.packagename = 'foxglove_msgs'
CylinderPrimitive.shortname = 'CylinderPrimitive'

function CylinderPrimitive:new()

  local obj = {}

  obj.pose = nil
  obj.size = nil
  obj.bottom_scale = 0.0
  obj.top_scale = 0.0
  obj.color = nil

  setmetatable(obj, self)

  return obj
end

function CylinderPrimitive._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == CylinderPrimitive then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, CylinderPrimitive)

  local hash = lcm._hash.new('0xa52103034bfe0bac')
    + geometry_msgs_Pose._get_hash_recursive(newparents)
    + geometry_msgs_Vector3._get_hash_recursive(newparents)
    + foxglove_msgs_Color._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

CylinderPrimitive._packed_fingerprint = lcm._pack.pack('>X', CylinderPrimitive._get_hash_recursive({}))

function CylinderPrimitive:encode()

  return CylinderPrimitive._packed_fingerprint .. self:_encode_one()
end

function CylinderPrimitive:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.pose:_encode_one())
  table.insert(buf_table, self.size:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>dd', self.bottom_scale, self.top_scale))
  table.insert(buf_table, self.color:_encode_one())

  return table.concat(buf_table)
end

function CylinderPrimitive.decode(data)

  if data:sub(1, 8) ~= CylinderPrimitive._packed_fingerprint then
    error('bad fingerprint')
  end

  return CylinderPrimitive._decode_one(data:sub(9))
end

function CylinderPrimitive._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = CylinderPrimitive:new()

  obj.pose = geometry_msgs_Pose._decode_one(data)
  obj.size = geometry_msgs_Vector3._decode_one(data)
  obj.bottom_scale, obj.top_scale = lcm._pack.unpack('>dd', data:read(16))
  obj.color = foxglove_msgs_Color._decode_one(data)

  return obj
end

return CylinderPrimitive

