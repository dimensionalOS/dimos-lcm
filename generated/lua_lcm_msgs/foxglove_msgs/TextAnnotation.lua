--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local builtin_interfaces_Time = require('builtin_interfaces.Time')
local foxglove_msgs_Color = require('foxglove_msgs.Color')
local foxglove_msgs_Point2 = require('foxglove_msgs.Point2')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local TextAnnotation = {}
TextAnnotation.__index = TextAnnotation

TextAnnotation.name = 'foxglove_msgs.TextAnnotation'
TextAnnotation.packagename = 'foxglove_msgs'
TextAnnotation.shortname = 'TextAnnotation'

function TextAnnotation:new()

  local obj = {}

  obj.timestamp = nil
  obj.position = nil
  obj.text = ''
  obj.font_size = 0.0
  obj.text_color = nil
  obj.background_color = nil

  setmetatable(obj, self)

  return obj
end

function TextAnnotation._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == TextAnnotation then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, TextAnnotation)

  local hash = lcm._hash.new('0x1354af1f564701e9')
    + builtin_interfaces_Time._get_hash_recursive(newparents)
    + foxglove_msgs_Point2._get_hash_recursive(newparents)
    + foxglove_msgs_Color._get_hash_recursive(newparents)
    + foxglove_msgs_Color._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

TextAnnotation._packed_fingerprint = lcm._pack.pack('>X', TextAnnotation._get_hash_recursive({}))

function TextAnnotation:encode()

  return TextAnnotation._packed_fingerprint .. self:_encode_one()
end

function TextAnnotation:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.timestamp:_encode_one())
  table.insert(buf_table, self.position:_encode_one())
  local __text_tmpstr = lcm._pack.prepare_string(self.text)
  table.insert(buf_table, lcm._pack.pack('>I', #__text_tmpstr + 1))
  table.insert(buf_table, __text_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack('>d', self.font_size))
  table.insert(buf_table, self.text_color:_encode_one())
  table.insert(buf_table, self.background_color:_encode_one())

  return table.concat(buf_table)
end

function TextAnnotation.decode(data)

  if data:sub(1, 8) ~= TextAnnotation._packed_fingerprint then
    error('bad fingerprint')
  end

  return TextAnnotation._decode_one(data:sub(9))
end

function TextAnnotation._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = TextAnnotation:new()

  obj.timestamp = builtin_interfaces_Time._decode_one(data)
  obj.position = foxglove_msgs_Point2._decode_one(data)
  local __text_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.text = lcm._pack.prepare_string(data:read(__text_tmpstrlen))
  obj.font_size = lcm._pack.unpack('>d', data:read(8))
  obj.text_color = foxglove_msgs_Color._decode_one(data)
  obj.background_color = foxglove_msgs_Color._decode_one(data)

  return obj
end

return TextAnnotation

