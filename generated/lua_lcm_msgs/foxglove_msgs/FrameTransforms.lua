--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local foxglove_msgs_FrameTransform = require('foxglove_msgs.FrameTransform')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local FrameTransforms = {}
FrameTransforms.__index = FrameTransforms

FrameTransforms.name = 'foxglove_msgs.FrameTransforms'
FrameTransforms.packagename = 'foxglove_msgs'
FrameTransforms.shortname = 'FrameTransforms'

function FrameTransforms:new()

  local obj = {}

  obj.transforms_length = 0
  obj.transforms = {}

  setmetatable(obj, self)

  return obj
end

function FrameTransforms._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == FrameTransforms then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, FrameTransforms)

  local hash = lcm._hash.new('0x37bc5cbce50a5ce2')
    + foxglove_msgs_FrameTransform._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

FrameTransforms._packed_fingerprint = lcm._pack.pack('>X', FrameTransforms._get_hash_recursive({}))

function FrameTransforms:encode()

  return FrameTransforms._packed_fingerprint .. self:_encode_one()
end

function FrameTransforms:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.transforms_length))
  for i0 = 1, self.transforms_length do
    table.insert(buf_table, self.transforms[i0]:_encode_one())
  end

  return table.concat(buf_table)
end

function FrameTransforms.decode(data)

  if data:sub(1, 8) ~= FrameTransforms._packed_fingerprint then
    error('bad fingerprint')
  end

  return FrameTransforms._decode_one(data:sub(9))
end

function FrameTransforms._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = FrameTransforms:new()

  obj.transforms_length = lcm._pack.unpack('>i', data:read(4))
  obj.transforms = {}
  for i0 = 1, obj.transforms_length do
    obj.transforms[i0] = foxglove_msgs_FrameTransform._decode_one(data)
  end

  return obj
end

return FrameTransforms

