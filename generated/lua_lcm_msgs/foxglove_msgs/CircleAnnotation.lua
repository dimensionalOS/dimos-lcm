--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local builtin_interfaces_Time = require('builtin_interfaces.Time')
local foxglove_msgs_Color = require('foxglove_msgs.Color')
local foxglove_msgs_Point2 = require('foxglove_msgs.Point2')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local CircleAnnotation = {}
CircleAnnotation.__index = CircleAnnotation

CircleAnnotation.name = 'foxglove_msgs.CircleAnnotation'
CircleAnnotation.packagename = 'foxglove_msgs'
CircleAnnotation.shortname = 'CircleAnnotation'

function CircleAnnotation:new()

  local obj = {}

  obj.timestamp = nil
  obj.position = nil
  obj.diameter = 0.0
  obj.thickness = 0.0
  obj.fill_color = nil
  obj.outline_color = nil

  setmetatable(obj, self)

  return obj
end

function CircleAnnotation._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == CircleAnnotation then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, CircleAnnotation)

  local hash = lcm._hash.new('0x5a3219098258887c')
    + builtin_interfaces_Time._get_hash_recursive(newparents)
    + foxglove_msgs_Point2._get_hash_recursive(newparents)
    + foxglove_msgs_Color._get_hash_recursive(newparents)
    + foxglove_msgs_Color._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

CircleAnnotation._packed_fingerprint = lcm._pack.pack('>X', CircleAnnotation._get_hash_recursive({}))

function CircleAnnotation:encode()

  return CircleAnnotation._packed_fingerprint .. self:_encode_one()
end

function CircleAnnotation:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.timestamp:_encode_one())
  table.insert(buf_table, self.position:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>dd', self.diameter, self.thickness))
  table.insert(buf_table, self.fill_color:_encode_one())
  table.insert(buf_table, self.outline_color:_encode_one())

  return table.concat(buf_table)
end

function CircleAnnotation.decode(data)

  if data:sub(1, 8) ~= CircleAnnotation._packed_fingerprint then
    error('bad fingerprint')
  end

  return CircleAnnotation._decode_one(data:sub(9))
end

function CircleAnnotation._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = CircleAnnotation:new()

  obj.timestamp = builtin_interfaces_Time._decode_one(data)
  obj.position = foxglove_msgs_Point2._decode_one(data)
  obj.diameter, obj.thickness = lcm._pack.unpack('>dd', data:read(16))
  obj.fill_color = foxglove_msgs_Color._decode_one(data)
  obj.outline_color = foxglove_msgs_Color._decode_one(data)

  return obj
end

return CircleAnnotation

