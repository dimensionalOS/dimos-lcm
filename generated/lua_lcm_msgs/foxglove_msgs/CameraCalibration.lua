--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local builtin_interfaces_Time = require('builtin_interfaces.Time')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local CameraCalibration = {}
CameraCalibration.__index = CameraCalibration

CameraCalibration.name = 'foxglove_msgs.CameraCalibration'
CameraCalibration.packagename = 'foxglove_msgs'
CameraCalibration.shortname = 'CameraCalibration'

function CameraCalibration:new()

  local obj = {}

  obj.d_length = 0
  obj.timestamp = nil
  obj.frame_id = ''
  obj.width = 0
  obj.height = 0
  obj.distortion_model = ''
  obj.d = {}
  obj.k = {}
  for d0 = 1, 9 do
    obj.k[d0] = 0.0
  end
  obj.r = {}
  for d0 = 1, 9 do
    obj.r[d0] = 0.0
  end
  obj.p = {}
  for d0 = 1, 12 do
    obj.p[d0] = 0.0
  end

  setmetatable(obj, self)

  return obj
end

function CameraCalibration._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == CameraCalibration then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, CameraCalibration)

  local hash = lcm._hash.new('0x89c275083a857ce2')
    + builtin_interfaces_Time._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

CameraCalibration._packed_fingerprint = lcm._pack.pack('>X', CameraCalibration._get_hash_recursive({}))

function CameraCalibration:encode()

  return CameraCalibration._packed_fingerprint .. self:_encode_one()
end

function CameraCalibration:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.d_length))
  table.insert(buf_table, self.timestamp:_encode_one())
  local __frame_id_tmpstr = lcm._pack.prepare_string(self.frame_id)
  table.insert(buf_table, lcm._pack.pack('>I', #__frame_id_tmpstr + 1))
  table.insert(buf_table, __frame_id_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack('>ii', self.width, self.height))
  local __distortion_model_tmpstr = lcm._pack.prepare_string(self.distortion_model)
  table.insert(buf_table, lcm._pack.pack('>I', #__distortion_model_tmpstr + 1))
  table.insert(buf_table, __distortion_model_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack(string.format('>%dd', self.d_length), unpack(self.d)))
  table.insert(buf_table, lcm._pack.pack('>9d', unpack(self.k)))
  table.insert(buf_table, lcm._pack.pack('>9d', unpack(self.r)))
  table.insert(buf_table, lcm._pack.pack('>12d', unpack(self.p)))

  return table.concat(buf_table)
end

function CameraCalibration.decode(data)

  if data:sub(1, 8) ~= CameraCalibration._packed_fingerprint then
    error('bad fingerprint')
  end

  return CameraCalibration._decode_one(data:sub(9))
end

function CameraCalibration._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = CameraCalibration:new()

  obj.d_length = lcm._pack.unpack('>i', data:read(4))
  obj.timestamp = builtin_interfaces_Time._decode_one(data)
  local __frame_id_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.frame_id = lcm._pack.prepare_string(data:read(__frame_id_tmpstrlen))
  obj.width, obj.height = lcm._pack.unpack('>ii', data:read(8))
  local __distortion_model_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.distortion_model = lcm._pack.prepare_string(data:read(__distortion_model_tmpstrlen))
  obj.d = {lcm._pack.unpack(string.format('>%dd', obj.d_length), data:read(obj.d_length * 8))}
  obj.k = {lcm._pack.unpack('>9d', data:read(72))}
  obj.r = {lcm._pack.unpack('>9d', data:read(72))}
  obj.p = {lcm._pack.unpack('>12d', data:read(96))}

  return obj
end

return CameraCalibration

