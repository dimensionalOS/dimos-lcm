--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geometry_msgs_Pose = require('geometry_msgs.Pose')
local builtin_interfaces_Time = require('builtin_interfaces.Time')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local PosesInFrame = {}
PosesInFrame.__index = PosesInFrame

PosesInFrame.name = 'foxglove_msgs.PosesInFrame'
PosesInFrame.packagename = 'foxglove_msgs'
PosesInFrame.shortname = 'PosesInFrame'

function PosesInFrame:new()

  local obj = {}

  obj.poses_length = 0
  obj.timestamp = nil
  obj.frame_id = ''
  obj.poses = {}

  setmetatable(obj, self)

  return obj
end

function PosesInFrame._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == PosesInFrame then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, PosesInFrame)

  local hash = lcm._hash.new('0x65f6cee7d8076f4b')
    + builtin_interfaces_Time._get_hash_recursive(newparents)
    + geometry_msgs_Pose._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

PosesInFrame._packed_fingerprint = lcm._pack.pack('>X', PosesInFrame._get_hash_recursive({}))

function PosesInFrame:encode()

  return PosesInFrame._packed_fingerprint .. self:_encode_one()
end

function PosesInFrame:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.poses_length))
  table.insert(buf_table, self.timestamp:_encode_one())
  local __frame_id_tmpstr = lcm._pack.prepare_string(self.frame_id)
  table.insert(buf_table, lcm._pack.pack('>I', #__frame_id_tmpstr + 1))
  table.insert(buf_table, __frame_id_tmpstr .. '\0')
  for i0 = 1, self.poses_length do
    table.insert(buf_table, self.poses[i0]:_encode_one())
  end

  return table.concat(buf_table)
end

function PosesInFrame.decode(data)

  if data:sub(1, 8) ~= PosesInFrame._packed_fingerprint then
    error('bad fingerprint')
  end

  return PosesInFrame._decode_one(data:sub(9))
end

function PosesInFrame._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = PosesInFrame:new()

  obj.poses_length = lcm._pack.unpack('>i', data:read(4))
  obj.timestamp = builtin_interfaces_Time._decode_one(data)
  local __frame_id_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.frame_id = lcm._pack.prepare_string(data:read(__frame_id_tmpstrlen))
  obj.poses = {}
  for i0 = 1, obj.poses_length do
    obj.poses[i0] = geometry_msgs_Pose._decode_one(data)
  end

  return obj
end

return PosesInFrame

