--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local KeyValuePair = {}
KeyValuePair.__index = KeyValuePair

KeyValuePair.name = 'foxglove_msgs.KeyValuePair'
KeyValuePair.packagename = 'foxglove_msgs'
KeyValuePair.shortname = 'KeyValuePair'

function KeyValuePair:new()

  local obj = {}

  obj.key = ''
  obj.value = ''

  setmetatable(obj, self)

  return obj
end

function KeyValuePair._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == KeyValuePair then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, KeyValuePair)

  local hash = lcm._hash.new('0x97574015d52eedde')
  hash:rotate(1)

  return hash
end

KeyValuePair._packed_fingerprint = lcm._pack.pack('>X', KeyValuePair._get_hash_recursive({}))

function KeyValuePair:encode()

  return KeyValuePair._packed_fingerprint .. self:_encode_one()
end

function KeyValuePair:_encode_one()

  local buf_table = {}

  local __key_tmpstr = lcm._pack.prepare_string(self.key)
  table.insert(buf_table, lcm._pack.pack('>I', #__key_tmpstr + 1))
  table.insert(buf_table, __key_tmpstr .. '\0')
  local __value_tmpstr = lcm._pack.prepare_string(self.value)
  table.insert(buf_table, lcm._pack.pack('>I', #__value_tmpstr + 1))
  table.insert(buf_table, __value_tmpstr .. '\0')

  return table.concat(buf_table)
end

function KeyValuePair.decode(data)

  if data:sub(1, 8) ~= KeyValuePair._packed_fingerprint then
    error('bad fingerprint')
  end

  return KeyValuePair._decode_one(data:sub(9))
end

function KeyValuePair._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = KeyValuePair:new()

  local __key_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.key = lcm._pack.prepare_string(data:read(__key_tmpstrlen))
  local __value_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.value = lcm._pack.prepare_string(data:read(__value_tmpstrlen))

  return obj
end

return KeyValuePair

