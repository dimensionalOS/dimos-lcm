--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local GeoJSON = {}
GeoJSON.__index = GeoJSON

GeoJSON.name = 'foxglove_msgs.GeoJSON'
GeoJSON.packagename = 'foxglove_msgs'
GeoJSON.shortname = 'GeoJSON'

function GeoJSON:new()

  local obj = {}

  obj.geojson = ''

  setmetatable(obj, self)

  return obj
end

function GeoJSON._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == GeoJSON then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, GeoJSON)

  local hash = lcm._hash.new('0x15423f51794b2ebc')
  hash:rotate(1)

  return hash
end

GeoJSON._packed_fingerprint = lcm._pack.pack('>X', GeoJSON._get_hash_recursive({}))

function GeoJSON:encode()

  return GeoJSON._packed_fingerprint .. self:_encode_one()
end

function GeoJSON:_encode_one()

  local buf_table = {}

  local __geojson_tmpstr = lcm._pack.prepare_string(self.geojson)
  table.insert(buf_table, lcm._pack.pack('>I', #__geojson_tmpstr + 1))
  table.insert(buf_table, __geojson_tmpstr .. '\0')

  return table.concat(buf_table)
end

function GeoJSON.decode(data)

  if data:sub(1, 8) ~= GeoJSON._packed_fingerprint then
    error('bad fingerprint')
  end

  return GeoJSON._decode_one(data:sub(9))
end

function GeoJSON._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = GeoJSON:new()

  local __geojson_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.geojson = lcm._pack.prepare_string(data:read(__geojson_tmpstrlen))

  return obj
end

return GeoJSON

