--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geometry_msgs_Quaternion = require('geometry_msgs.Quaternion')
local geometry_msgs_Vector3 = require('geometry_msgs.Vector3')
local builtin_interfaces_Time = require('builtin_interfaces.Time')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local FrameTransform = {}
FrameTransform.__index = FrameTransform

FrameTransform.name = 'foxglove_msgs.FrameTransform'
FrameTransform.packagename = 'foxglove_msgs'
FrameTransform.shortname = 'FrameTransform'

function FrameTransform:new()

  local obj = {}

  obj.timestamp = nil
  obj.parent_frame_id = ''
  obj.child_frame_id = ''
  obj.translation = nil
  obj.rotation = nil

  setmetatable(obj, self)

  return obj
end

function FrameTransform._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == FrameTransform then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, FrameTransform)

  local hash = lcm._hash.new('0x16cb223d6329d1f0')
    + builtin_interfaces_Time._get_hash_recursive(newparents)
    + geometry_msgs_Vector3._get_hash_recursive(newparents)
    + geometry_msgs_Quaternion._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

FrameTransform._packed_fingerprint = lcm._pack.pack('>X', FrameTransform._get_hash_recursive({}))

function FrameTransform:encode()

  return FrameTransform._packed_fingerprint .. self:_encode_one()
end

function FrameTransform:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.timestamp:_encode_one())
  local __parent_frame_id_tmpstr = lcm._pack.prepare_string(self.parent_frame_id)
  table.insert(buf_table, lcm._pack.pack('>I', #__parent_frame_id_tmpstr + 1))
  table.insert(buf_table, __parent_frame_id_tmpstr .. '\0')
  local __child_frame_id_tmpstr = lcm._pack.prepare_string(self.child_frame_id)
  table.insert(buf_table, lcm._pack.pack('>I', #__child_frame_id_tmpstr + 1))
  table.insert(buf_table, __child_frame_id_tmpstr .. '\0')
  table.insert(buf_table, self.translation:_encode_one())
  table.insert(buf_table, self.rotation:_encode_one())

  return table.concat(buf_table)
end

function FrameTransform.decode(data)

  if data:sub(1, 8) ~= FrameTransform._packed_fingerprint then
    error('bad fingerprint')
  end

  return FrameTransform._decode_one(data:sub(9))
end

function FrameTransform._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = FrameTransform:new()

  obj.timestamp = builtin_interfaces_Time._decode_one(data)
  local __parent_frame_id_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.parent_frame_id = lcm._pack.prepare_string(data:read(__parent_frame_id_tmpstrlen))
  local __child_frame_id_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.child_frame_id = lcm._pack.prepare_string(data:read(__child_frame_id_tmpstrlen))
  obj.translation = geometry_msgs_Vector3._decode_one(data)
  obj.rotation = geometry_msgs_Quaternion._decode_one(data)

  return obj
end

return FrameTransform

