--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local builtin_interfaces_Time = require('builtin_interfaces.Time')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local LocationFix = {}
LocationFix.__index = LocationFix

  LocationFix.UNKNOWN = 0
  LocationFix.APPROXIMATED = 1
  LocationFix.DIAGONAL_KNOWN = 2
  LocationFix.KNOWN = 3

LocationFix.name = 'foxglove_msgs.LocationFix'
LocationFix.packagename = 'foxglove_msgs'
LocationFix.shortname = 'LocationFix'

function LocationFix:new()

  local obj = {}

  obj.timestamp = nil
  obj.frame_id = ''
  obj.latitude = 0.0
  obj.longitude = 0.0
  obj.altitude = 0.0
  obj.position_covariance = {}
  for d0 = 1, 9 do
    obj.position_covariance[d0] = 0.0
  end
  obj.position_covariance_type = 0

  setmetatable(obj, self)

  return obj
end

function LocationFix._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == LocationFix then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, LocationFix)

  local hash = lcm._hash.new('0xc7c172f7d2332f54')
    + builtin_interfaces_Time._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

LocationFix._packed_fingerprint = lcm._pack.pack('>X', LocationFix._get_hash_recursive({}))

function LocationFix:encode()

  return LocationFix._packed_fingerprint .. self:_encode_one()
end

function LocationFix:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.timestamp:_encode_one())
  local __frame_id_tmpstr = lcm._pack.prepare_string(self.frame_id)
  table.insert(buf_table, lcm._pack.pack('>I', #__frame_id_tmpstr + 1))
  table.insert(buf_table, __frame_id_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack('>ddd', self.latitude, self.longitude, self.altitude))
  table.insert(buf_table, lcm._pack.pack('>9d', unpack(self.position_covariance)))
  table.insert(buf_table, lcm._pack.pack('>B', self.position_covariance_type))

  return table.concat(buf_table)
end

function LocationFix.decode(data)

  if data:sub(1, 8) ~= LocationFix._packed_fingerprint then
    error('bad fingerprint')
  end

  return LocationFix._decode_one(data:sub(9))
end

function LocationFix._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = LocationFix:new()

  obj.timestamp = builtin_interfaces_Time._decode_one(data)
  local __frame_id_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.frame_id = lcm._pack.prepare_string(data:read(__frame_id_tmpstrlen))
  obj.latitude, obj.longitude, obj.altitude = lcm._pack.unpack('>ddd', data:read(24))
  obj.position_covariance = {lcm._pack.unpack('>9d', data:read(72))}
  obj.position_covariance_type = lcm._pack.unpack('>B', data:read(1))

  return obj
end

return LocationFix

