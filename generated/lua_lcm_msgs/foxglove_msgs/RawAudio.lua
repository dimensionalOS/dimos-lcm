--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local builtin_interfaces_Time = require('builtin_interfaces.Time')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local RawAudio = {}
RawAudio.__index = RawAudio

RawAudio.name = 'foxglove_msgs.RawAudio'
RawAudio.packagename = 'foxglove_msgs'
RawAudio.shortname = 'RawAudio'

function RawAudio:new()

  local obj = {}

  obj.data_length = 0
  obj.timestamp = nil
  obj.data = {}
  obj.format = ''
  obj.sample_rate = 0
  obj.number_of_channels = 0

  setmetatable(obj, self)

  return obj
end

function RawAudio._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == RawAudio then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, RawAudio)

  local hash = lcm._hash.new('0xc7f2a980cd122f7d')
    + builtin_interfaces_Time._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

RawAudio._packed_fingerprint = lcm._pack.pack('>X', RawAudio._get_hash_recursive({}))

function RawAudio:encode()

  return RawAudio._packed_fingerprint .. self:_encode_one()
end

function RawAudio:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.data_length))
  table.insert(buf_table, self.timestamp:_encode_one())
  table.insert(buf_table, lcm._pack.pack(string.format('>%dB', self.data_length), unpack(self.data)))
  local __format_tmpstr = lcm._pack.prepare_string(self.format)
  table.insert(buf_table, lcm._pack.pack('>I', #__format_tmpstr + 1))
  table.insert(buf_table, __format_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack('>ii', self.sample_rate, self.number_of_channels))

  return table.concat(buf_table)
end

function RawAudio.decode(data)

  if data:sub(1, 8) ~= RawAudio._packed_fingerprint then
    error('bad fingerprint')
  end

  return RawAudio._decode_one(data:sub(9))
end

function RawAudio._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = RawAudio:new()

  obj.data_length = lcm._pack.unpack('>i', data:read(4))
  obj.timestamp = builtin_interfaces_Time._decode_one(data)
  obj.data = {lcm._pack.unpack(string.format('>%dB', obj.data_length), data:read(obj.data_length))}
  local __format_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.format = lcm._pack.prepare_string(data:read(__format_tmpstrlen))
  obj.sample_rate, obj.number_of_channels = lcm._pack.unpack('>ii', data:read(8))

  return obj
end

return RawAudio

