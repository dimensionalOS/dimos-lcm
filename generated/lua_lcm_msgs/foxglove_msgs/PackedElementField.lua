--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local PackedElementField = {}
PackedElementField.__index = PackedElementField

  PackedElementField.UNKNOWN = 0
  PackedElementField.UINT8 = 1
  PackedElementField.INT8 = 2
  PackedElementField.UINT16 = 3
  PackedElementField.INT16 = 4
  PackedElementField.UINT32 = 5
  PackedElementField.INT32 = 6
  PackedElementField.FLOAT32 = 7
  PackedElementField.FLOAT64 = 8

PackedElementField.name = 'foxglove_msgs.PackedElementField'
PackedElementField.packagename = 'foxglove_msgs'
PackedElementField.shortname = 'PackedElementField'

function PackedElementField:new()

  local obj = {}

  obj.name = ''
  obj.offset = 0
  obj.type = 0

  setmetatable(obj, self)

  return obj
end

function PackedElementField._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == PackedElementField then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, PackedElementField)

  local hash = lcm._hash.new('0x47c85ba0fafa16b8')
  hash:rotate(1)

  return hash
end

PackedElementField._packed_fingerprint = lcm._pack.pack('>X', PackedElementField._get_hash_recursive({}))

function PackedElementField:encode()

  return PackedElementField._packed_fingerprint .. self:_encode_one()
end

function PackedElementField:_encode_one()

  local buf_table = {}

  local __name_tmpstr = lcm._pack.prepare_string(self.name)
  table.insert(buf_table, lcm._pack.pack('>I', #__name_tmpstr + 1))
  table.insert(buf_table, __name_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack('>iB', self.offset, self.type))

  return table.concat(buf_table)
end

function PackedElementField.decode(data)

  if data:sub(1, 8) ~= PackedElementField._packed_fingerprint then
    error('bad fingerprint')
  end

  return PackedElementField._decode_one(data:sub(9))
end

function PackedElementField._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = PackedElementField:new()

  local __name_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.name = lcm._pack.prepare_string(data:read(__name_tmpstrlen))
  obj.offset, obj.type = lcm._pack.unpack('>iB', data:read(5))

  return obj
end

return PackedElementField

