--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local foxglove_msgs_PointsAnnotation = require('foxglove_msgs.PointsAnnotation')
local foxglove_msgs_CircleAnnotation = require('foxglove_msgs.CircleAnnotation')
local foxglove_msgs_TextAnnotation = require('foxglove_msgs.TextAnnotation')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local ImageAnnotations = {}
ImageAnnotations.__index = ImageAnnotations

ImageAnnotations.name = 'foxglove_msgs.ImageAnnotations'
ImageAnnotations.packagename = 'foxglove_msgs'
ImageAnnotations.shortname = 'ImageAnnotations'

function ImageAnnotations:new()

  local obj = {}

  obj.circles_length = 0
  obj.points_length = 0
  obj.texts_length = 0
  obj.circles = {}
  obj.points = {}
  obj.texts = {}

  setmetatable(obj, self)

  return obj
end

function ImageAnnotations._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == ImageAnnotations then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, ImageAnnotations)

  local hash = lcm._hash.new('0x8b3a52c632c59b07')
    + foxglove_msgs_CircleAnnotation._get_hash_recursive(newparents)
    + foxglove_msgs_PointsAnnotation._get_hash_recursive(newparents)
    + foxglove_msgs_TextAnnotation._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

ImageAnnotations._packed_fingerprint = lcm._pack.pack('>X', ImageAnnotations._get_hash_recursive({}))

function ImageAnnotations:encode()

  return ImageAnnotations._packed_fingerprint .. self:_encode_one()
end

function ImageAnnotations:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>iii', self.circles_length, self.points_length, self.texts_length))
  for i0 = 1, self.circles_length do
    table.insert(buf_table, self.circles[i0]:_encode_one())
  end
  for i0 = 1, self.points_length do
    table.insert(buf_table, self.points[i0]:_encode_one())
  end
  for i0 = 1, self.texts_length do
    table.insert(buf_table, self.texts[i0]:_encode_one())
  end

  return table.concat(buf_table)
end

function ImageAnnotations.decode(data)

  if data:sub(1, 8) ~= ImageAnnotations._packed_fingerprint then
    error('bad fingerprint')
  end

  return ImageAnnotations._decode_one(data:sub(9))
end

function ImageAnnotations._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = ImageAnnotations:new()

  obj.circles_length, obj.points_length, obj.texts_length = lcm._pack.unpack('>iii', data:read(12))
  obj.circles = {}
  for i0 = 1, obj.circles_length do
    obj.circles[i0] = foxglove_msgs_CircleAnnotation._decode_one(data)
  end
  obj.points = {}
  for i0 = 1, obj.points_length do
    obj.points[i0] = foxglove_msgs_PointsAnnotation._decode_one(data)
  end
  obj.texts = {}
  for i0 = 1, obj.texts_length do
    obj.texts[i0] = foxglove_msgs_TextAnnotation._decode_one(data)
  end

  return obj
end

return ImageAnnotations

