--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local builtin_interfaces_Time = require('builtin_interfaces.Time')
local foxglove_msgs_Color = require('foxglove_msgs.Color')
local foxglove_msgs_Point2 = require('foxglove_msgs.Point2')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local PointsAnnotation = {}
PointsAnnotation.__index = PointsAnnotation

  PointsAnnotation.UNKNOWN = 0
  PointsAnnotation.POINTS = 1
  PointsAnnotation.LINE_LOOP = 2
  PointsAnnotation.LINE_STRIP = 3
  PointsAnnotation.LINE_LIST = 4

PointsAnnotation.name = 'foxglove_msgs.PointsAnnotation'
PointsAnnotation.packagename = 'foxglove_msgs'
PointsAnnotation.shortname = 'PointsAnnotation'

function PointsAnnotation:new()

  local obj = {}

  obj.points_length = 0
  obj.outline_colors_length = 0
  obj.timestamp = nil
  obj.type = 0
  obj.points = {}
  obj.outline_color = nil
  obj.outline_colors = {}
  obj.fill_color = nil
  obj.thickness = 0.0

  setmetatable(obj, self)

  return obj
end

function PointsAnnotation._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == PointsAnnotation then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, PointsAnnotation)

  local hash = lcm._hash.new('0x97465363cc7c2a18')
    + builtin_interfaces_Time._get_hash_recursive(newparents)
    + foxglove_msgs_Point2._get_hash_recursive(newparents)
    + foxglove_msgs_Color._get_hash_recursive(newparents)
    + foxglove_msgs_Color._get_hash_recursive(newparents)
    + foxglove_msgs_Color._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

PointsAnnotation._packed_fingerprint = lcm._pack.pack('>X', PointsAnnotation._get_hash_recursive({}))

function PointsAnnotation:encode()

  return PointsAnnotation._packed_fingerprint .. self:_encode_one()
end

function PointsAnnotation:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>ii', self.points_length, self.outline_colors_length))
  table.insert(buf_table, self.timestamp:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>B', self.type))
  for i0 = 1, self.points_length do
    table.insert(buf_table, self.points[i0]:_encode_one())
  end
  table.insert(buf_table, self.outline_color:_encode_one())
  for i0 = 1, self.outline_colors_length do
    table.insert(buf_table, self.outline_colors[i0]:_encode_one())
  end
  table.insert(buf_table, self.fill_color:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>d', self.thickness))

  return table.concat(buf_table)
end

function PointsAnnotation.decode(data)

  if data:sub(1, 8) ~= PointsAnnotation._packed_fingerprint then
    error('bad fingerprint')
  end

  return PointsAnnotation._decode_one(data:sub(9))
end

function PointsAnnotation._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = PointsAnnotation:new()

  obj.points_length, obj.outline_colors_length = lcm._pack.unpack('>ii', data:read(8))
  obj.timestamp = builtin_interfaces_Time._decode_one(data)
  obj.type = lcm._pack.unpack('>B', data:read(1))
  obj.points = {}
  for i0 = 1, obj.points_length do
    obj.points[i0] = foxglove_msgs_Point2._decode_one(data)
  end
  obj.outline_color = foxglove_msgs_Color._decode_one(data)
  obj.outline_colors = {}
  for i0 = 1, obj.outline_colors_length do
    obj.outline_colors[i0] = foxglove_msgs_Color._decode_one(data)
  end
  obj.fill_color = foxglove_msgs_Color._decode_one(data)
  obj.thickness = lcm._pack.unpack('>d', data:read(8))

  return obj
end

return PointsAnnotation

