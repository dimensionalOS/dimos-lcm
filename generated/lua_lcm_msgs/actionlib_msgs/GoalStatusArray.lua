--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local actionlib_msgs_GoalStatus = require('actionlib_msgs.GoalStatus')
local std_msgs_Header = require('std_msgs.Header')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local GoalStatusArray = {}
GoalStatusArray.__index = GoalStatusArray

GoalStatusArray.name = 'actionlib_msgs.GoalStatusArray'
GoalStatusArray.packagename = 'actionlib_msgs'
GoalStatusArray.shortname = 'GoalStatusArray'

function GoalStatusArray:new()

  local obj = {}

  obj.status_list_length = 0
  obj.header = nil
  obj.status_list = {}

  setmetatable(obj, self)

  return obj
end

function GoalStatusArray._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == GoalStatusArray then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, GoalStatusArray)

  local hash = lcm._hash.new('0xf5c35b2e7eed0fa')
    + std_msgs_Header._get_hash_recursive(newparents)
    + actionlib_msgs_GoalStatus._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

GoalStatusArray._packed_fingerprint = lcm._pack.pack('>X', GoalStatusArray._get_hash_recursive({}))

function GoalStatusArray:encode()

  return GoalStatusArray._packed_fingerprint .. self:_encode_one()
end

function GoalStatusArray:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.status_list_length))
  table.insert(buf_table, self.header:_encode_one())
  for i0 = 1, self.status_list_length do
    table.insert(buf_table, self.status_list[i0]:_encode_one())
  end

  return table.concat(buf_table)
end

function GoalStatusArray.decode(data)

  if data:sub(1, 8) ~= GoalStatusArray._packed_fingerprint then
    error('bad fingerprint')
  end

  return GoalStatusArray._decode_one(data:sub(9))
end

function GoalStatusArray._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = GoalStatusArray:new()

  obj.status_list_length = lcm._pack.unpack('>i', data:read(4))
  obj.header = std_msgs_Header._decode_one(data)
  obj.status_list = {}
  for i0 = 1, obj.status_list_length do
    obj.status_list[i0] = actionlib_msgs_GoalStatus._decode_one(data)
  end

  return obj
end

return GoalStatusArray

