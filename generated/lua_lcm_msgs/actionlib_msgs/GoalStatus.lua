--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local actionlib_msgs_GoalID = require('actionlib_msgs.GoalID')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local GoalStatus = {}
GoalStatus.__index = GoalStatus

  GoalStatus.PENDING = 0
  GoalStatus.ACTIVE = 1
  GoalStatus.PREEMPTED = 2
  GoalStatus.SUCCEEDED = 3
  GoalStatus.ABORTED = 4
  GoalStatus.REJECTED = 5
  GoalStatus.PREEMPTING = 6
  GoalStatus.RECALLING = 7
  GoalStatus.RECALLED = 8
  GoalStatus.LOST = 9

GoalStatus.name = 'actionlib_msgs.GoalStatus'
GoalStatus.packagename = 'actionlib_msgs'
GoalStatus.shortname = 'GoalStatus'

function GoalStatus:new()

  local obj = {}

  obj.goal_id = nil
  obj.status = 0
  obj.text = ''

  setmetatable(obj, self)

  return obj
end

function GoalStatus._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == GoalStatus then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, GoalStatus)

  local hash = lcm._hash.new('0xc0b4e95febdcd994')
    + actionlib_msgs_GoalID._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

GoalStatus._packed_fingerprint = lcm._pack.pack('>X', GoalStatus._get_hash_recursive({}))

function GoalStatus:encode()

  return GoalStatus._packed_fingerprint .. self:_encode_one()
end

function GoalStatus:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.goal_id:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>B', self.status))
  local __text_tmpstr = lcm._pack.prepare_string(self.text)
  table.insert(buf_table, lcm._pack.pack('>I', #__text_tmpstr + 1))
  table.insert(buf_table, __text_tmpstr .. '\0')

  return table.concat(buf_table)
end

function GoalStatus.decode(data)

  if data:sub(1, 8) ~= GoalStatus._packed_fingerprint then
    error('bad fingerprint')
  end

  return GoalStatus._decode_one(data:sub(9))
end

function GoalStatus._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = GoalStatus:new()

  obj.goal_id = actionlib_msgs_GoalID._decode_one(data)
  obj.status = lcm._pack.unpack('>B', data:read(1))
  local __text_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.text = lcm._pack.prepare_string(data:read(__text_tmpstrlen))

  return obj
end

return GoalStatus

