--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local std_msgs_Time = require('std_msgs.Time')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local GoalID = {}
GoalID.__index = GoalID

GoalID.name = 'actionlib_msgs.GoalID'
GoalID.packagename = 'actionlib_msgs'
GoalID.shortname = 'GoalID'

function GoalID:new()

  local obj = {}

  obj.stamp = nil
  obj.id = ''

  setmetatable(obj, self)

  return obj
end

function GoalID._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == GoalID then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, GoalID)

  local hash = lcm._hash.new('0xef36683ef0767e95')
    + std_msgs_Time._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

GoalID._packed_fingerprint = lcm._pack.pack('>X', GoalID._get_hash_recursive({}))

function GoalID:encode()

  return GoalID._packed_fingerprint .. self:_encode_one()
end

function GoalID:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.stamp:_encode_one())
  local __id_tmpstr = lcm._pack.prepare_string(self.id)
  table.insert(buf_table, lcm._pack.pack('>I', #__id_tmpstr + 1))
  table.insert(buf_table, __id_tmpstr .. '\0')

  return table.concat(buf_table)
end

function GoalID.decode(data)

  if data:sub(1, 8) ~= GoalID._packed_fingerprint then
    error('bad fingerprint')
  end

  return GoalID._decode_one(data:sub(9))
end

function GoalID._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = GoalID:new()

  obj.stamp = std_msgs_Time._decode_one(data)
  local __id_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.id = lcm._pack.prepare_string(data:read(__id_tmpstrlen))

  return obj
end

return GoalID

