--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geometry_msgs_Point = require('geometry_msgs.Point')
local std_msgs_Header = require('std_msgs.Header')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local GridCells = {}
GridCells.__index = GridCells

GridCells.name = 'nav_msgs.GridCells'
GridCells.packagename = 'nav_msgs'
GridCells.shortname = 'GridCells'

function GridCells:new()

  local obj = {}

  obj.cells_length = 0
  obj.header = nil
  obj.cell_width = 0.0
  obj.cell_height = 0.0
  obj.cells = {}

  setmetatable(obj, self)

  return obj
end

function GridCells._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == GridCells then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, GridCells)

  local hash = lcm._hash.new('0xb855326533062750')
    + std_msgs_Header._get_hash_recursive(newparents)
    + geometry_msgs_Point._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

GridCells._packed_fingerprint = lcm._pack.pack('>X', GridCells._get_hash_recursive({}))

function GridCells:encode()

  return GridCells._packed_fingerprint .. self:_encode_one()
end

function GridCells:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.cells_length))
  table.insert(buf_table, self.header:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>ff', self.cell_width, self.cell_height))
  for i0 = 1, self.cells_length do
    table.insert(buf_table, self.cells[i0]:_encode_one())
  end

  return table.concat(buf_table)
end

function GridCells.decode(data)

  if data:sub(1, 8) ~= GridCells._packed_fingerprint then
    error('bad fingerprint')
  end

  return GridCells._decode_one(data:sub(9))
end

function GridCells._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = GridCells:new()

  obj.cells_length = lcm._pack.unpack('>i', data:read(4))
  obj.header = std_msgs_Header._decode_one(data)
  obj.cell_width, obj.cell_height = lcm._pack.unpack('>ff', data:read(8))
  obj.cells = {}
  for i0 = 1, obj.cells_length do
    obj.cells[i0] = geometry_msgs_Point._decode_one(data)
  end

  return obj
end

return GridCells

