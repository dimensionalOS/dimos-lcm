--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geometry_msgs_Pose = require('geometry_msgs.Pose')
local std_msgs_Time = require('std_msgs.Time')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local MapMetaData = {}
MapMetaData.__index = MapMetaData

MapMetaData.name = 'nav_msgs.MapMetaData'
MapMetaData.packagename = 'nav_msgs'
MapMetaData.shortname = 'MapMetaData'

function MapMetaData:new()

  local obj = {}

  obj.map_load_time = nil
  obj.resolution = 0.0
  obj.width = 0
  obj.height = 0
  obj.origin = nil

  setmetatable(obj, self)

  return obj
end

function MapMetaData._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == MapMetaData then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, MapMetaData)

  local hash = lcm._hash.new('0x3245f3cdb468ba93')
    + std_msgs_Time._get_hash_recursive(newparents)
    + geometry_msgs_Pose._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

MapMetaData._packed_fingerprint = lcm._pack.pack('>X', MapMetaData._get_hash_recursive({}))

function MapMetaData:encode()

  return MapMetaData._packed_fingerprint .. self:_encode_one()
end

function MapMetaData:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.map_load_time:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>fii', self.resolution, self.width, self.height))
  table.insert(buf_table, self.origin:_encode_one())

  return table.concat(buf_table)
end

function MapMetaData.decode(data)

  if data:sub(1, 8) ~= MapMetaData._packed_fingerprint then
    error('bad fingerprint')
  end

  return MapMetaData._decode_one(data:sub(9))
end

function MapMetaData._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = MapMetaData:new()

  obj.map_load_time = std_msgs_Time._decode_one(data)
  obj.resolution, obj.width, obj.height = lcm._pack.unpack('>fii', data:read(12))
  obj.origin = geometry_msgs_Pose._decode_one(data)

  return obj
end

return MapMetaData

