--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local std_msgs_Header = require('std_msgs.Header')
local nav_msgs_MapMetaData = require('nav_msgs.MapMetaData')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local OccupancyGrid = {}
OccupancyGrid.__index = OccupancyGrid

OccupancyGrid.name = 'nav_msgs.OccupancyGrid'
OccupancyGrid.packagename = 'nav_msgs'
OccupancyGrid.shortname = 'OccupancyGrid'

function OccupancyGrid:new()

  local obj = {}

  obj.data_length = 0
  obj.header = nil
  obj.info = nil
  obj.data = {}

  setmetatable(obj, self)

  return obj
end

function OccupancyGrid._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == OccupancyGrid then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, OccupancyGrid)

  local hash = lcm._hash.new('0x9e67f3f149308d87')
    + std_msgs_Header._get_hash_recursive(newparents)
    + nav_msgs_MapMetaData._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

OccupancyGrid._packed_fingerprint = lcm._pack.pack('>X', OccupancyGrid._get_hash_recursive({}))

function OccupancyGrid:encode()

  return OccupancyGrid._packed_fingerprint .. self:_encode_one()
end

function OccupancyGrid:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.data_length))
  table.insert(buf_table, self.header:_encode_one())
  table.insert(buf_table, self.info:_encode_one())
  table.insert(buf_table, lcm._pack.pack(string.format('>%db', self.data_length), unpack(self.data)))

  return table.concat(buf_table)
end

function OccupancyGrid.decode(data)

  if data:sub(1, 8) ~= OccupancyGrid._packed_fingerprint then
    error('bad fingerprint')
  end

  return OccupancyGrid._decode_one(data:sub(9))
end

function OccupancyGrid._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = OccupancyGrid:new()

  obj.data_length = lcm._pack.unpack('>i', data:read(4))
  obj.header = std_msgs_Header._decode_one(data)
  obj.info = nav_msgs_MapMetaData._decode_one(data)
  obj.data = {lcm._pack.unpack(string.format('>%db', obj.data_length), data:read(obj.data_length))}

  return obj
end

return OccupancyGrid

