--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Time = {}
Time.__index = Time

Time.name = 'builtin_interfaces.Time'
Time.packagename = 'builtin_interfaces'
Time.shortname = 'Time'

function Time:new()

  local obj = {}

  obj.sec = 0
  obj.nanosec = 0

  setmetatable(obj, self)

  return obj
end

function Time._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Time then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Time)

  local hash = lcm._hash.new('0x263f39efa2a9af63')
  hash:rotate(1)

  return hash
end

Time._packed_fingerprint = lcm._pack.pack('>X', Time._get_hash_recursive({}))

function Time:encode()

  return Time._packed_fingerprint .. self:_encode_one()
end

function Time:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>ii', self.sec, self.nanosec))

  return table.concat(buf_table)
end

function Time.decode(data)

  if data:sub(1, 8) ~= Time._packed_fingerprint then
    error('bad fingerprint')
  end

  return Time._decode_one(data:sub(9))
end

function Time._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Time:new()

  obj.sec, obj.nanosec = lcm._pack.unpack('>ii', data:read(8))

  return obj
end

return Time

