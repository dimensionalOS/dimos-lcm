--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Int16 = {}
Int16.__index = Int16

Int16.name = 'std_msgs.Int16'
Int16.packagename = 'std_msgs'
Int16.shortname = 'Int16'

function Int16:new()

  local obj = {}

  obj.data = 0

  setmetatable(obj, self)

  return obj
end

function Int16._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Int16 then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Int16)

  local hash = lcm._hash.new('0x165e7cf9f948811f')
  hash:rotate(1)

  return hash
end

Int16._packed_fingerprint = lcm._pack.pack('>X', Int16._get_hash_recursive({}))

function Int16:encode()

  return Int16._packed_fingerprint .. self:_encode_one()
end

function Int16:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>h', self.data))

  return table.concat(buf_table)
end

function Int16.decode(data)

  if data:sub(1, 8) ~= Int16._packed_fingerprint then
    error('bad fingerprint')
  end

  return Int16._decode_one(data:sub(9))
end

function Int16._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Int16:new()

  obj.data = lcm._pack.unpack('>h', data:read(2))

  return obj
end

return Int16

