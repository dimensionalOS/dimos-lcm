--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local MultiArrayDimension = {}
MultiArrayDimension.__index = MultiArrayDimension

MultiArrayDimension.name = 'std_msgs.MultiArrayDimension'
MultiArrayDimension.packagename = 'std_msgs'
MultiArrayDimension.shortname = 'MultiArrayDimension'

function MultiArrayDimension:new()

  local obj = {}

  obj.label = ''
  obj.size = 0
  obj.stride = 0

  setmetatable(obj, self)

  return obj
end

function MultiArrayDimension._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == MultiArrayDimension then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, MultiArrayDimension)

  local hash = lcm._hash.new('0xcec1b9e9d868ef97')
  hash:rotate(1)

  return hash
end

MultiArrayDimension._packed_fingerprint = lcm._pack.pack('>X', MultiArrayDimension._get_hash_recursive({}))

function MultiArrayDimension:encode()

  return MultiArrayDimension._packed_fingerprint .. self:_encode_one()
end

function MultiArrayDimension:_encode_one()

  local buf_table = {}

  local __label_tmpstr = lcm._pack.prepare_string(self.label)
  table.insert(buf_table, lcm._pack.pack('>I', #__label_tmpstr + 1))
  table.insert(buf_table, __label_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack('>ii', self.size, self.stride))

  return table.concat(buf_table)
end

function MultiArrayDimension.decode(data)

  if data:sub(1, 8) ~= MultiArrayDimension._packed_fingerprint then
    error('bad fingerprint')
  end

  return MultiArrayDimension._decode_one(data:sub(9))
end

function MultiArrayDimension._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = MultiArrayDimension:new()

  local __label_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.label = lcm._pack.prepare_string(data:read(__label_tmpstrlen))
  obj.size, obj.stride = lcm._pack.unpack('>ii', data:read(8))

  return obj
end

return MultiArrayDimension

