--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local std_msgs_Time = require('std_msgs.Time')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Header = {}
Header.__index = Header

Header.name = 'std_msgs.Header'
Header.packagename = 'std_msgs'
Header.shortname = 'Header'

function Header:new()

  local obj = {}

  obj.seq = 0
  obj.stamp = nil
  obj.frame_id = ''

  setmetatable(obj, self)

  return obj
end

function Header._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Header then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Header)

  local hash = lcm._hash.new('0xdbb33f5b4c19b8ea')
    + std_msgs_Time._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

Header._packed_fingerprint = lcm._pack.pack('>X', Header._get_hash_recursive({}))

function Header:encode()

  return Header._packed_fingerprint .. self:_encode_one()
end

function Header:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.seq))
  table.insert(buf_table, self.stamp:_encode_one())
  local __frame_id_tmpstr = lcm._pack.prepare_string(self.frame_id)
  table.insert(buf_table, lcm._pack.pack('>I', #__frame_id_tmpstr + 1))
  table.insert(buf_table, __frame_id_tmpstr .. '\0')

  return table.concat(buf_table)
end

function Header.decode(data)

  if data:sub(1, 8) ~= Header._packed_fingerprint then
    error('bad fingerprint')
  end

  return Header._decode_one(data:sub(9))
end

function Header._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Header:new()

  obj.seq = lcm._pack.unpack('>i', data:read(4))
  obj.stamp = std_msgs_Time._decode_one(data)
  local __frame_id_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.frame_id = lcm._pack.prepare_string(data:read(__frame_id_tmpstrlen))

  return obj
end

return Header

