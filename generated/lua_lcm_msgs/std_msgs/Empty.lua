--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Empty = {}
Empty.__index = Empty

Empty.name = 'std_msgs.Empty'
Empty.packagename = 'std_msgs'
Empty.shortname = 'Empty'

function Empty:new()

  local obj = {}

  setmetatable(obj, self)

  return obj
end

function Empty._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Empty then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Empty)

  local hash = lcm._hash.new('0x12345678')
  hash:rotate(1)

  return hash
end

Empty._packed_fingerprint = lcm._pack.pack('>X', Empty._get_hash_recursive({}))

function Empty:encode()

  return Empty._packed_fingerprint .. self:_encode_one()
end

function Empty:_encode_one()

  -- nothing to do
end
function Empty.decode(data)

  if data:sub(1, 8) ~= Empty._packed_fingerprint then
    error('bad fingerprint')
  end

  return Empty._decode_one(data:sub(9))
end

function Empty._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Empty:new()


  return obj
end

return Empty

