--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local String = {}
String.__index = String

String.name = 'std_msgs.String'
String.packagename = 'std_msgs'
String.shortname = 'String'

function String:new()

  local obj = {}

  obj.data = ''

  setmetatable(obj, self)

  return obj
end

function String._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == String then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, String)

  local hash = lcm._hash.new('0x90df9b84cdceaf0a')
  hash:rotate(1)

  return hash
end

String._packed_fingerprint = lcm._pack.pack('>X', String._get_hash_recursive({}))

function String:encode()

  return String._packed_fingerprint .. self:_encode_one()
end

function String:_encode_one()

  local buf_table = {}

  local __data_tmpstr = lcm._pack.prepare_string(self.data)
  table.insert(buf_table, lcm._pack.pack('>I', #__data_tmpstr + 1))
  table.insert(buf_table, __data_tmpstr .. '\0')

  return table.concat(buf_table)
end

function String.decode(data)

  if data:sub(1, 8) ~= String._packed_fingerprint then
    error('bad fingerprint')
  end

  return String._decode_one(data:sub(9))
end

function String._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = String:new()

  local __data_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.data = lcm._pack.prepare_string(data:read(__data_tmpstrlen))

  return obj
end

return String

