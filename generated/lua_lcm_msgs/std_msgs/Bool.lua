--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Bool = {}
Bool.__index = Bool

Bool.name = 'std_msgs.Bool'
Bool.packagename = 'std_msgs'
Bool.shortname = 'Bool'

function Bool:new()

  local obj = {}

  obj.data = false

  setmetatable(obj, self)

  return obj
end

function Bool._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Bool then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Bool)

  local hash = lcm._hash.new('0xf5f7835284a871f')
  hash:rotate(1)

  return hash
end

Bool._packed_fingerprint = lcm._pack.pack('>X', Bool._get_hash_recursive({}))

function Bool:encode()

  return Bool._packed_fingerprint .. self:_encode_one()
end

function Bool:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>?', self.data))

  return table.concat(buf_table)
end

function Bool.decode(data)

  if data:sub(1, 8) ~= Bool._packed_fingerprint then
    error('bad fingerprint')
  end

  return Bool._decode_one(data:sub(9))
end

function Bool._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Bool:new()

  obj.data = lcm._pack.unpack('>?', data:read(1))

  return obj
end

return Bool

