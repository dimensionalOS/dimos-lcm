--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Float64 = {}
Float64.__index = Float64

Float64.name = 'std_msgs.Float64'
Float64.packagename = 'std_msgs'
Float64.shortname = 'Float64'

function Float64:new()

  local obj = {}

  obj.data = 0.0

  setmetatable(obj, self)

  return obj
end

function Float64._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Float64 then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Float64)

  local hash = lcm._hash.new('0x90eea081d4d0b10a')
  hash:rotate(1)

  return hash
end

Float64._packed_fingerprint = lcm._pack.pack('>X', Float64._get_hash_recursive({}))

function Float64:encode()

  return Float64._packed_fingerprint .. self:_encode_one()
end

function Float64:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>d', self.data))

  return table.concat(buf_table)
end

function Float64.decode(data)

  if data:sub(1, 8) ~= Float64._packed_fingerprint then
    error('bad fingerprint')
  end

  return Float64._decode_one(data:sub(9))
end

function Float64._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Float64:new()

  obj.data = lcm._pack.unpack('>d', data:read(8))

  return obj
end

return Float64

