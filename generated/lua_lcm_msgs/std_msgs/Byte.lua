--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Byte = {}
Byte.__index = Byte

Byte.name = 'std_msgs.Byte'
Byte.packagename = 'std_msgs'
Byte.shortname = 'Byte'

function Byte:new()

  local obj = {}

  obj.data = 0

  setmetatable(obj, self)

  return obj
end

function Byte._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Byte then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Byte)

  local hash = lcm._hash.new('0x90e9a182fedda20a')
  hash:rotate(1)

  return hash
end

Byte._packed_fingerprint = lcm._pack.pack('>X', Byte._get_hash_recursive({}))

function Byte:encode()

  return Byte._packed_fingerprint .. self:_encode_one()
end

function Byte:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>b', self.data))

  return table.concat(buf_table)
end

function Byte.decode(data)

  if data:sub(1, 8) ~= Byte._packed_fingerprint then
    error('bad fingerprint')
  end

  return Byte._decode_one(data:sub(9))
end

function Byte._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Byte:new()

  obj.data = lcm._pack.unpack('>b', data:read(1))

  return obj
end

return Byte

