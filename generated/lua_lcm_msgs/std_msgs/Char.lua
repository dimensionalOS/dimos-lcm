--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Char = {}
Char.__index = Char

Char.name = 'std_msgs.Char'
Char.packagename = 'std_msgs'
Char.shortname = 'Char'

function Char:new()

  local obj = {}

  obj.data = 0

  setmetatable(obj, self)

  return obj
end

function Char._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Char then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Char)

  local hash = lcm._hash.new('0x74856d0f697d2dc2')
  hash:rotate(1)

  return hash
end

Char._packed_fingerprint = lcm._pack.pack('>X', Char._get_hash_recursive({}))

function Char:encode()

  return Char._packed_fingerprint .. self:_encode_one()
end

function Char:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>B', self.data))

  return table.concat(buf_table)
end

function Char.decode(data)

  if data:sub(1, 8) ~= Char._packed_fingerprint then
    error('bad fingerprint')
  end

  return Char._decode_one(data:sub(9))
end

function Char._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Char:new()

  obj.data = lcm._pack.unpack('>B', data:read(1))

  return obj
end

return Char

