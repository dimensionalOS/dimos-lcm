--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local std_msgs_MultiArrayDimension = require('std_msgs.MultiArrayDimension')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local MultiArrayLayout = {}
MultiArrayLayout.__index = MultiArrayLayout

MultiArrayLayout.name = 'std_msgs.MultiArrayLayout'
MultiArrayLayout.packagename = 'std_msgs'
MultiArrayLayout.shortname = 'MultiArrayLayout'

function MultiArrayLayout:new()

  local obj = {}

  obj.dim_length = 0
  obj.dim = {}
  obj.data_offset = 0

  setmetatable(obj, self)

  return obj
end

function MultiArrayLayout._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == MultiArrayLayout then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, MultiArrayLayout)

  local hash = lcm._hash.new('0xbf4b5363481d321')
    + std_msgs_MultiArrayDimension._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

MultiArrayLayout._packed_fingerprint = lcm._pack.pack('>X', MultiArrayLayout._get_hash_recursive({}))

function MultiArrayLayout:encode()

  return MultiArrayLayout._packed_fingerprint .. self:_encode_one()
end

function MultiArrayLayout:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.dim_length))
  for i0 = 1, self.dim_length do
    table.insert(buf_table, self.dim[i0]:_encode_one())
  end
  table.insert(buf_table, lcm._pack.pack('>i', self.data_offset))

  return table.concat(buf_table)
end

function MultiArrayLayout.decode(data)

  if data:sub(1, 8) ~= MultiArrayLayout._packed_fingerprint then
    error('bad fingerprint')
  end

  return MultiArrayLayout._decode_one(data:sub(9))
end

function MultiArrayLayout._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = MultiArrayLayout:new()

  obj.dim_length = lcm._pack.unpack('>i', data:read(4))
  obj.dim = {}
  for i0 = 1, obj.dim_length do
    obj.dim[i0] = std_msgs_MultiArrayDimension._decode_one(data)
  end
  obj.data_offset = lcm._pack.unpack('>i', data:read(4))

  return obj
end

return MultiArrayLayout

