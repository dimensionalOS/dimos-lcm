--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Int64 = {}
Int64.__index = Int64

Int64.name = 'std_msgs.Int64'
Int64.packagename = 'std_msgs'
Int64.shortname = 'Int64'

function Int64:new()

  local obj = {}

  obj.data = 0

  setmetatable(obj, self)

  return obj
end

function Int64._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Int64 then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Int64)

  local hash = lcm._hash.new('0x165e7cfef748811f')
  hash:rotate(1)

  return hash
end

Int64._packed_fingerprint = lcm._pack.pack('>X', Int64._get_hash_recursive({}))

function Int64:encode()

  return Int64._packed_fingerprint .. self:_encode_one()
end

function Int64:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>q', self.data))

  return table.concat(buf_table)
end

function Int64.decode(data)

  if data:sub(1, 8) ~= Int64._packed_fingerprint then
    error('bad fingerprint')
  end

  return Int64._decode_one(data:sub(9))
end

function Int64._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Int64:new()

  obj.data = lcm._pack.unpack('>q', data:read(8))

  return obj
end

return Int64

