--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Int32 = {}
Int32.__index = Int32

Int32.name = 'std_msgs.Int32'
Int32.packagename = 'std_msgs'
Int32.shortname = 'Int32'

function Int32:new()

  local obj = {}

  obj.data = 0

  setmetatable(obj, self)

  return obj
end

function Int32._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Int32 then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Int32)

  local hash = lcm._hash.new('0x165e7cfbf548811f')
  hash:rotate(1)

  return hash
end

Int32._packed_fingerprint = lcm._pack.pack('>X', Int32._get_hash_recursive({}))

function Int32:encode()

  return Int32._packed_fingerprint .. self:_encode_one()
end

function Int32:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.data))

  return table.concat(buf_table)
end

function Int32.decode(data)

  if data:sub(1, 8) ~= Int32._packed_fingerprint then
    error('bad fingerprint')
  end

  return Int32._decode_one(data:sub(9))
end

function Int32._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Int32:new()

  obj.data = lcm._pack.unpack('>i', data:read(4))

  return obj
end

return Int32

