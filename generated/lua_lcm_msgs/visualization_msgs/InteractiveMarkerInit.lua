--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local visualization_msgs_InteractiveMarker = require('visualization_msgs.InteractiveMarker')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local InteractiveMarkerInit = {}
InteractiveMarkerInit.__index = InteractiveMarkerInit

InteractiveMarkerInit.name = 'visualization_msgs.InteractiveMarkerInit'
InteractiveMarkerInit.packagename = 'visualization_msgs'
InteractiveMarkerInit.shortname = 'InteractiveMarkerInit'

function InteractiveMarkerInit:new()

  local obj = {}

  obj.markers_length = 0
  obj.server_id = ''
  obj.seq_num = 0
  obj.markers = {}

  setmetatable(obj, self)

  return obj
end

function InteractiveMarkerInit._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == InteractiveMarkerInit then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, InteractiveMarkerInit)

  local hash = lcm._hash.new('0xd2afaf11cff61d9')
    + visualization_msgs_InteractiveMarker._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

InteractiveMarkerInit._packed_fingerprint = lcm._pack.pack('>X', InteractiveMarkerInit._get_hash_recursive({}))

function InteractiveMarkerInit:encode()

  return InteractiveMarkerInit._packed_fingerprint .. self:_encode_one()
end

function InteractiveMarkerInit:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.markers_length))
  local __server_id_tmpstr = lcm._pack.prepare_string(self.server_id)
  table.insert(buf_table, lcm._pack.pack('>I', #__server_id_tmpstr + 1))
  table.insert(buf_table, __server_id_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack('>q', self.seq_num))
  for i0 = 1, self.markers_length do
    table.insert(buf_table, self.markers[i0]:_encode_one())
  end

  return table.concat(buf_table)
end

function InteractiveMarkerInit.decode(data)

  if data:sub(1, 8) ~= InteractiveMarkerInit._packed_fingerprint then
    error('bad fingerprint')
  end

  return InteractiveMarkerInit._decode_one(data:sub(9))
end

function InteractiveMarkerInit._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = InteractiveMarkerInit:new()

  obj.markers_length = lcm._pack.unpack('>i', data:read(4))
  local __server_id_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.server_id = lcm._pack.prepare_string(data:read(__server_id_tmpstrlen))
  obj.seq_num = lcm._pack.unpack('>q', data:read(8))
  obj.markers = {}
  for i0 = 1, obj.markers_length do
    obj.markers[i0] = visualization_msgs_InteractiveMarker._decode_one(data)
  end

  return obj
end

return InteractiveMarkerInit

