--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local MenuEntry = {}
MenuEntry.__index = MenuEntry

  MenuEntry.FEEDBACK = 0
  MenuEntry.ROSRUN = 1
  MenuEntry.ROSLAUNCH = 2

MenuEntry.name = 'visualization_msgs.MenuEntry'
MenuEntry.packagename = 'visualization_msgs'
MenuEntry.shortname = 'MenuEntry'

function MenuEntry:new()

  local obj = {}

  obj.id = 0
  obj.parent_id = 0
  obj.title = ''
  obj.command = ''
  obj.command_type = 0

  setmetatable(obj, self)

  return obj
end

function MenuEntry._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == MenuEntry then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, MenuEntry)

  local hash = lcm._hash.new('0x667b2d15ef03e972')
  hash:rotate(1)

  return hash
end

MenuEntry._packed_fingerprint = lcm._pack.pack('>X', MenuEntry._get_hash_recursive({}))

function MenuEntry:encode()

  return MenuEntry._packed_fingerprint .. self:_encode_one()
end

function MenuEntry:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>ii', self.id, self.parent_id))
  local __title_tmpstr = lcm._pack.prepare_string(self.title)
  table.insert(buf_table, lcm._pack.pack('>I', #__title_tmpstr + 1))
  table.insert(buf_table, __title_tmpstr .. '\0')
  local __command_tmpstr = lcm._pack.prepare_string(self.command)
  table.insert(buf_table, lcm._pack.pack('>I', #__command_tmpstr + 1))
  table.insert(buf_table, __command_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack('>B', self.command_type))

  return table.concat(buf_table)
end

function MenuEntry.decode(data)

  if data:sub(1, 8) ~= MenuEntry._packed_fingerprint then
    error('bad fingerprint')
  end

  return MenuEntry._decode_one(data:sub(9))
end

function MenuEntry._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = MenuEntry:new()

  obj.id, obj.parent_id = lcm._pack.unpack('>ii', data:read(8))
  local __title_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.title = lcm._pack.prepare_string(data:read(__title_tmpstrlen))
  local __command_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.command = lcm._pack.prepare_string(data:read(__command_tmpstrlen))
  obj.command_type = lcm._pack.unpack('>B', data:read(1))

  return obj
end

return MenuEntry

