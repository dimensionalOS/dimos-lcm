--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geometry_msgs_Pose = require('geometry_msgs.Pose')
local visualization_msgs_MenuEntry = require('visualization_msgs.MenuEntry')
local std_msgs_Header = require('std_msgs.Header')
local visualization_msgs_InteractiveMarkerControl = require('visualization_msgs.InteractiveMarkerControl')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local InteractiveMarker = {}
InteractiveMarker.__index = InteractiveMarker

InteractiveMarker.name = 'visualization_msgs.InteractiveMarker'
InteractiveMarker.packagename = 'visualization_msgs'
InteractiveMarker.shortname = 'InteractiveMarker'

function InteractiveMarker:new()

  local obj = {}

  obj.menu_entries_length = 0
  obj.controls_length = 0
  obj.header = nil
  obj.pose = nil
  obj.name = ''
  obj.description = ''
  obj.scale = 0.0
  obj.menu_entries = {}
  obj.controls = {}

  setmetatable(obj, self)

  return obj
end

function InteractiveMarker._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == InteractiveMarker then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, InteractiveMarker)

  local hash = lcm._hash.new('0xe9ed8e5f9a72545f')
    + std_msgs_Header._get_hash_recursive(newparents)
    + geometry_msgs_Pose._get_hash_recursive(newparents)
    + visualization_msgs_MenuEntry._get_hash_recursive(newparents)
    + visualization_msgs_InteractiveMarkerControl._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

InteractiveMarker._packed_fingerprint = lcm._pack.pack('>X', InteractiveMarker._get_hash_recursive({}))

function InteractiveMarker:encode()

  return InteractiveMarker._packed_fingerprint .. self:_encode_one()
end

function InteractiveMarker:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>ii', self.menu_entries_length, self.controls_length))
  table.insert(buf_table, self.header:_encode_one())
  table.insert(buf_table, self.pose:_encode_one())
  local __name_tmpstr = lcm._pack.prepare_string(self.name)
  table.insert(buf_table, lcm._pack.pack('>I', #__name_tmpstr + 1))
  table.insert(buf_table, __name_tmpstr .. '\0')
  local __description_tmpstr = lcm._pack.prepare_string(self.description)
  table.insert(buf_table, lcm._pack.pack('>I', #__description_tmpstr + 1))
  table.insert(buf_table, __description_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack('>f', self.scale))
  for i0 = 1, self.menu_entries_length do
    table.insert(buf_table, self.menu_entries[i0]:_encode_one())
  end
  for i0 = 1, self.controls_length do
    table.insert(buf_table, self.controls[i0]:_encode_one())
  end

  return table.concat(buf_table)
end

function InteractiveMarker.decode(data)

  if data:sub(1, 8) ~= InteractiveMarker._packed_fingerprint then
    error('bad fingerprint')
  end

  return InteractiveMarker._decode_one(data:sub(9))
end

function InteractiveMarker._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = InteractiveMarker:new()

  obj.menu_entries_length, obj.controls_length = lcm._pack.unpack('>ii', data:read(8))
  obj.header = std_msgs_Header._decode_one(data)
  obj.pose = geometry_msgs_Pose._decode_one(data)
  local __name_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.name = lcm._pack.prepare_string(data:read(__name_tmpstrlen))
  local __description_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.description = lcm._pack.prepare_string(data:read(__description_tmpstrlen))
  obj.scale = lcm._pack.unpack('>f', data:read(4))
  obj.menu_entries = {}
  for i0 = 1, obj.menu_entries_length do
    obj.menu_entries[i0] = visualization_msgs_MenuEntry._decode_one(data)
  end
  obj.controls = {}
  for i0 = 1, obj.controls_length do
    obj.controls[i0] = visualization_msgs_InteractiveMarkerControl._decode_one(data)
  end

  return obj
end

return InteractiveMarker

