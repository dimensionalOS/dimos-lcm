--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geometry_msgs_Pose = require('geometry_msgs.Pose')
local std_msgs_Header = require('std_msgs.Header')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local InteractiveMarkerPose = {}
InteractiveMarkerPose.__index = InteractiveMarkerPose

InteractiveMarkerPose.name = 'visualization_msgs.InteractiveMarkerPose'
InteractiveMarkerPose.packagename = 'visualization_msgs'
InteractiveMarkerPose.shortname = 'InteractiveMarkerPose'

function InteractiveMarkerPose:new()

  local obj = {}

  obj.header = nil
  obj.pose = nil
  obj.name = ''

  setmetatable(obj, self)

  return obj
end

function InteractiveMarkerPose._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == InteractiveMarkerPose then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, InteractiveMarkerPose)

  local hash = lcm._hash.new('0x8c873ae70410464d')
    + std_msgs_Header._get_hash_recursive(newparents)
    + geometry_msgs_Pose._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

InteractiveMarkerPose._packed_fingerprint = lcm._pack.pack('>X', InteractiveMarkerPose._get_hash_recursive({}))

function InteractiveMarkerPose:encode()

  return InteractiveMarkerPose._packed_fingerprint .. self:_encode_one()
end

function InteractiveMarkerPose:_encode_one()

  local buf_table = {}

  table.insert(buf_table, self.header:_encode_one())
  table.insert(buf_table, self.pose:_encode_one())
  local __name_tmpstr = lcm._pack.prepare_string(self.name)
  table.insert(buf_table, lcm._pack.pack('>I', #__name_tmpstr + 1))
  table.insert(buf_table, __name_tmpstr .. '\0')

  return table.concat(buf_table)
end

function InteractiveMarkerPose.decode(data)

  if data:sub(1, 8) ~= InteractiveMarkerPose._packed_fingerprint then
    error('bad fingerprint')
  end

  return InteractiveMarkerPose._decode_one(data:sub(9))
end

function InteractiveMarkerPose._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = InteractiveMarkerPose:new()

  obj.header = std_msgs_Header._decode_one(data)
  obj.pose = geometry_msgs_Pose._decode_one(data)
  local __name_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.name = lcm._pack.prepare_string(data:read(__name_tmpstrlen))

  return obj
end

return InteractiveMarkerPose

