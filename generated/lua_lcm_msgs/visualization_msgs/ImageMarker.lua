--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local std_msgs_ColorRGBA = require('std_msgs.ColorRGBA')
local geometry_msgs_Point = require('geometry_msgs.Point')
local std_msgs_Duration = require('std_msgs.Duration')
local std_msgs_Header = require('std_msgs.Header')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local ImageMarker = {}
ImageMarker.__index = ImageMarker

  ImageMarker.CIRCLE = 0
  ImageMarker.LINE_STRIP = 1
  ImageMarker.LINE_LIST = 2
  ImageMarker.POLYGON = 3
  ImageMarker.POINTS = 4
  ImageMarker.ADD = 0
  ImageMarker.REMOVE = 1

ImageMarker.name = 'visualization_msgs.ImageMarker'
ImageMarker.packagename = 'visualization_msgs'
ImageMarker.shortname = 'ImageMarker'

function ImageMarker:new()

  local obj = {}

  obj.points_length = 0
  obj.outline_colors_length = 0
  obj.header = nil
  obj.ns = ''
  obj.id = 0
  obj.type = 0
  obj.action = 0
  obj.position = nil
  obj.scale = 0.0
  obj.outline_color = nil
  obj.filled = 0
  obj.fill_color = nil
  obj.lifetime = nil
  obj.points = {}
  obj.outline_colors = {}

  setmetatable(obj, self)

  return obj
end

function ImageMarker._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == ImageMarker then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, ImageMarker)

  local hash = lcm._hash.new('0x3a3ea371b474d924')
    + std_msgs_Header._get_hash_recursive(newparents)
    + geometry_msgs_Point._get_hash_recursive(newparents)
    + std_msgs_ColorRGBA._get_hash_recursive(newparents)
    + std_msgs_ColorRGBA._get_hash_recursive(newparents)
    + std_msgs_Duration._get_hash_recursive(newparents)
    + geometry_msgs_Point._get_hash_recursive(newparents)
    + std_msgs_ColorRGBA._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

ImageMarker._packed_fingerprint = lcm._pack.pack('>X', ImageMarker._get_hash_recursive({}))

function ImageMarker:encode()

  return ImageMarker._packed_fingerprint .. self:_encode_one()
end

function ImageMarker:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>ii', self.points_length, self.outline_colors_length))
  table.insert(buf_table, self.header:_encode_one())
  local __ns_tmpstr = lcm._pack.prepare_string(self.ns)
  table.insert(buf_table, lcm._pack.pack('>I', #__ns_tmpstr + 1))
  table.insert(buf_table, __ns_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack('>iii', self.id, self.type, self.action))
  table.insert(buf_table, self.position:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>f', self.scale))
  table.insert(buf_table, self.outline_color:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>B', self.filled))
  table.insert(buf_table, self.fill_color:_encode_one())
  table.insert(buf_table, self.lifetime:_encode_one())
  for i0 = 1, self.points_length do
    table.insert(buf_table, self.points[i0]:_encode_one())
  end
  for i0 = 1, self.outline_colors_length do
    table.insert(buf_table, self.outline_colors[i0]:_encode_one())
  end

  return table.concat(buf_table)
end

function ImageMarker.decode(data)

  if data:sub(1, 8) ~= ImageMarker._packed_fingerprint then
    error('bad fingerprint')
  end

  return ImageMarker._decode_one(data:sub(9))
end

function ImageMarker._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = ImageMarker:new()

  obj.points_length, obj.outline_colors_length = lcm._pack.unpack('>ii', data:read(8))
  obj.header = std_msgs_Header._decode_one(data)
  local __ns_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.ns = lcm._pack.prepare_string(data:read(__ns_tmpstrlen))
  obj.id, obj.type, obj.action = lcm._pack.unpack('>iii', data:read(12))
  obj.position = geometry_msgs_Point._decode_one(data)
  obj.scale = lcm._pack.unpack('>f', data:read(4))
  obj.outline_color = std_msgs_ColorRGBA._decode_one(data)
  obj.filled = lcm._pack.unpack('>B', data:read(1))
  obj.fill_color = std_msgs_ColorRGBA._decode_one(data)
  obj.lifetime = std_msgs_Duration._decode_one(data)
  obj.points = {}
  for i0 = 1, obj.points_length do
    obj.points[i0] = geometry_msgs_Point._decode_one(data)
  end
  obj.outline_colors = {}
  for i0 = 1, obj.outline_colors_length do
    obj.outline_colors[i0] = std_msgs_ColorRGBA._decode_one(data)
  end

  return obj
end

return ImageMarker

