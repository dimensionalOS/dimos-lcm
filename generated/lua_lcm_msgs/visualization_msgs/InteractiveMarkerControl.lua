--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geometry_msgs_Quaternion = require('geometry_msgs.Quaternion')
local visualization_msgs_Marker = require('visualization_msgs.Marker')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local InteractiveMarkerControl = {}
InteractiveMarkerControl.__index = InteractiveMarkerControl

  InteractiveMarkerControl.INHERIT = 0
  InteractiveMarkerControl.FIXED = 1
  InteractiveMarkerControl.VIEW_FACING = 2
  InteractiveMarkerControl.NONE = 0
  InteractiveMarkerControl.MENU = 1
  InteractiveMarkerControl.BUTTON = 2
  InteractiveMarkerControl.MOVE_AXIS = 3
  InteractiveMarkerControl.MOVE_PLANE = 4
  InteractiveMarkerControl.ROTATE_AXIS = 5
  InteractiveMarkerControl.MOVE_ROTATE = 6
  InteractiveMarkerControl.MOVE_3D = 7
  InteractiveMarkerControl.ROTATE_3D = 8
  InteractiveMarkerControl.MOVE_ROTATE_3D = 9

InteractiveMarkerControl.name = 'visualization_msgs.InteractiveMarkerControl'
InteractiveMarkerControl.packagename = 'visualization_msgs'
InteractiveMarkerControl.shortname = 'InteractiveMarkerControl'

function InteractiveMarkerControl:new()

  local obj = {}

  obj.markers_length = 0
  obj.name = ''
  obj.orientation = nil
  obj.orientation_mode = 0
  obj.interaction_mode = 0
  obj.always_visible = false
  obj.markers = {}
  obj.independent_marker_orientation = false
  obj.description = ''

  setmetatable(obj, self)

  return obj
end

function InteractiveMarkerControl._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == InteractiveMarkerControl then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, InteractiveMarkerControl)

  local hash = lcm._hash.new('0x9e1db7d7740da609')
    + geometry_msgs_Quaternion._get_hash_recursive(newparents)
    + visualization_msgs_Marker._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

InteractiveMarkerControl._packed_fingerprint = lcm._pack.pack('>X', InteractiveMarkerControl._get_hash_recursive({}))

function InteractiveMarkerControl:encode()

  return InteractiveMarkerControl._packed_fingerprint .. self:_encode_one()
end

function InteractiveMarkerControl:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.markers_length))
  local __name_tmpstr = lcm._pack.prepare_string(self.name)
  table.insert(buf_table, lcm._pack.pack('>I', #__name_tmpstr + 1))
  table.insert(buf_table, __name_tmpstr .. '\0')
  table.insert(buf_table, self.orientation:_encode_one())
  table.insert(buf_table, lcm._pack.pack('>BB?', self.orientation_mode, self.interaction_mode, self.always_visible))
  for i0 = 1, self.markers_length do
    table.insert(buf_table, self.markers[i0]:_encode_one())
  end
  table.insert(buf_table, lcm._pack.pack('>?', self.independent_marker_orientation))
  local __description_tmpstr = lcm._pack.prepare_string(self.description)
  table.insert(buf_table, lcm._pack.pack('>I', #__description_tmpstr + 1))
  table.insert(buf_table, __description_tmpstr .. '\0')

  return table.concat(buf_table)
end

function InteractiveMarkerControl.decode(data)

  if data:sub(1, 8) ~= InteractiveMarkerControl._packed_fingerprint then
    error('bad fingerprint')
  end

  return InteractiveMarkerControl._decode_one(data:sub(9))
end

function InteractiveMarkerControl._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = InteractiveMarkerControl:new()

  obj.markers_length = lcm._pack.unpack('>i', data:read(4))
  local __name_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.name = lcm._pack.prepare_string(data:read(__name_tmpstrlen))
  obj.orientation = geometry_msgs_Quaternion._decode_one(data)
  obj.orientation_mode, obj.interaction_mode, obj.always_visible = lcm._pack.unpack('>BB?', data:read(3))
  obj.markers = {}
  for i0 = 1, obj.markers_length do
    obj.markers[i0] = visualization_msgs_Marker._decode_one(data)
  end
  obj.independent_marker_orientation = lcm._pack.unpack('>?', data:read(1))
  local __description_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.description = lcm._pack.prepare_string(data:read(__description_tmpstrlen))

  return obj
end

return InteractiveMarkerControl

