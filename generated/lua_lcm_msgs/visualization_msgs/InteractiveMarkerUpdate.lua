--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local visualization_msgs_InteractiveMarker = require('visualization_msgs.InteractiveMarker')
local visualization_msgs_InteractiveMarkerPose = require('visualization_msgs.InteractiveMarkerPose')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local InteractiveMarkerUpdate = {}
InteractiveMarkerUpdate.__index = InteractiveMarkerUpdate

  InteractiveMarkerUpdate.KEEP_ALIVE = 0
  InteractiveMarkerUpdate.UPDATE = 1

InteractiveMarkerUpdate.name = 'visualization_msgs.InteractiveMarkerUpdate'
InteractiveMarkerUpdate.packagename = 'visualization_msgs'
InteractiveMarkerUpdate.shortname = 'InteractiveMarkerUpdate'

function InteractiveMarkerUpdate:new()

  local obj = {}

  obj.markers_length = 0
  obj.poses_length = 0
  obj.erases_length = 0
  obj.server_id = ''
  obj.seq_num = 0
  obj.type = 0
  obj.markers = {}
  obj.poses = {}
  obj.erases = {}

  setmetatable(obj, self)

  return obj
end

function InteractiveMarkerUpdate._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == InteractiveMarkerUpdate then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, InteractiveMarkerUpdate)

  local hash = lcm._hash.new('0xf6eebb4c4a608b4c')
    + visualization_msgs_InteractiveMarker._get_hash_recursive(newparents)
    + visualization_msgs_InteractiveMarkerPose._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

InteractiveMarkerUpdate._packed_fingerprint = lcm._pack.pack('>X', InteractiveMarkerUpdate._get_hash_recursive({}))

function InteractiveMarkerUpdate:encode()

  return InteractiveMarkerUpdate._packed_fingerprint .. self:_encode_one()
end

function InteractiveMarkerUpdate:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>iii', self.markers_length, self.poses_length, self.erases_length))
  local __server_id_tmpstr = lcm._pack.prepare_string(self.server_id)
  table.insert(buf_table, lcm._pack.pack('>I', #__server_id_tmpstr + 1))
  table.insert(buf_table, __server_id_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack('>qB', self.seq_num, self.type))
  for i0 = 1, self.markers_length do
    table.insert(buf_table, self.markers[i0]:_encode_one())
  end
  for i0 = 1, self.poses_length do
    table.insert(buf_table, self.poses[i0]:_encode_one())
  end
  for i0 = 1, self.erases_length do
    local __erases_tmpstr = lcm._pack.prepare_string(self.erases[i0])
    table.insert(buf_table, lcm._pack.pack('>I', #__erases_tmpstr + 1))
    table.insert(buf_table, __erases_tmpstr .. '\0')
  end

  return table.concat(buf_table)
end

function InteractiveMarkerUpdate.decode(data)

  if data:sub(1, 8) ~= InteractiveMarkerUpdate._packed_fingerprint then
    error('bad fingerprint')
  end

  return InteractiveMarkerUpdate._decode_one(data:sub(9))
end

function InteractiveMarkerUpdate._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = InteractiveMarkerUpdate:new()

  obj.markers_length, obj.poses_length, obj.erases_length = lcm._pack.unpack('>iii', data:read(12))
  local __server_id_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.server_id = lcm._pack.prepare_string(data:read(__server_id_tmpstrlen))
  obj.seq_num, obj.type = lcm._pack.unpack('>qB', data:read(9))
  obj.markers = {}
  for i0 = 1, obj.markers_length do
    obj.markers[i0] = visualization_msgs_InteractiveMarker._decode_one(data)
  end
  obj.poses = {}
  for i0 = 1, obj.poses_length do
    obj.poses[i0] = visualization_msgs_InteractiveMarkerPose._decode_one(data)
  end
  obj.erases = {}
  for i0 = 1, obj.erases_length do
    local __erases_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
    obj.erases[i0] = lcm._pack.prepare_string(data:read(__erases_tmpstrlen))
  end

  return obj
end

return InteractiveMarkerUpdate

