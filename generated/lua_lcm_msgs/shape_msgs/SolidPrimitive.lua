--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local SolidPrimitive = {}
SolidPrimitive.__index = SolidPrimitive

  SolidPrimitive.BOX = 1
  SolidPrimitive.SPHERE = 2
  SolidPrimitive.CYLINDER = 3
  SolidPrimitive.CONE = 4
  SolidPrimitive.BOX_X = 0
  SolidPrimitive.BOX_Y = 1
  SolidPrimitive.BOX_Z = 2
  SolidPrimitive.SPHERE_RADIUS = 0
  SolidPrimitive.CYLINDER_HEIGHT = 0
  SolidPrimitive.CYLINDER_RADIUS = 1
  SolidPrimitive.CONE_HEIGHT = 0
  SolidPrimitive.CONE_RADIUS = 1

SolidPrimitive.name = 'shape_msgs.SolidPrimitive'
SolidPrimitive.packagename = 'shape_msgs'
SolidPrimitive.shortname = 'SolidPrimitive'

function SolidPrimitive:new()

  local obj = {}

  obj.dimensions_length = 0
  obj.type = 0
  obj.dimensions = {}

  setmetatable(obj, self)

  return obj
end

function SolidPrimitive._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == SolidPrimitive then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, SolidPrimitive)

  local hash = lcm._hash.new('0xb8a8e7178cab07ac')
  hash:rotate(1)

  return hash
end

SolidPrimitive._packed_fingerprint = lcm._pack.pack('>X', SolidPrimitive._get_hash_recursive({}))

function SolidPrimitive:encode()

  return SolidPrimitive._packed_fingerprint .. self:_encode_one()
end

function SolidPrimitive:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>iB', self.dimensions_length, self.type))
  table.insert(buf_table, lcm._pack.pack(string.format('>%dd', self.dimensions_length), unpack(self.dimensions)))

  return table.concat(buf_table)
end

function SolidPrimitive.decode(data)

  if data:sub(1, 8) ~= SolidPrimitive._packed_fingerprint then
    error('bad fingerprint')
  end

  return SolidPrimitive._decode_one(data:sub(9))
end

function SolidPrimitive._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = SolidPrimitive:new()

  obj.dimensions_length, obj.type = lcm._pack.unpack('>iB', data:read(5))
  obj.dimensions = {lcm._pack.unpack(string.format('>%dd', obj.dimensions_length), data:read(obj.dimensions_length * 8))}

  return obj
end

return SolidPrimitive

