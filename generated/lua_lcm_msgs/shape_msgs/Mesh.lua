--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local geometry_msgs_Point = require('geometry_msgs.Point')
local shape_msgs_MeshTriangle = require('shape_msgs.MeshTriangle')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Mesh = {}
Mesh.__index = Mesh

Mesh.name = 'shape_msgs.Mesh'
Mesh.packagename = 'shape_msgs'
Mesh.shortname = 'Mesh'

function Mesh:new()

  local obj = {}

  obj.triangles_length = 0
  obj.vertices_length = 0
  obj.triangles = {}
  obj.vertices = {}

  setmetatable(obj, self)

  return obj
end

function Mesh._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Mesh then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Mesh)

  local hash = lcm._hash.new('0xdc739fb8d2f81ab9')
    + shape_msgs_MeshTriangle._get_hash_recursive(newparents)
    + geometry_msgs_Point._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

Mesh._packed_fingerprint = lcm._pack.pack('>X', Mesh._get_hash_recursive({}))

function Mesh:encode()

  return Mesh._packed_fingerprint .. self:_encode_one()
end

function Mesh:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>ii', self.triangles_length, self.vertices_length))
  for i0 = 1, self.triangles_length do
    table.insert(buf_table, self.triangles[i0]:_encode_one())
  end
  for i0 = 1, self.vertices_length do
    table.insert(buf_table, self.vertices[i0]:_encode_one())
  end

  return table.concat(buf_table)
end

function Mesh.decode(data)

  if data:sub(1, 8) ~= Mesh._packed_fingerprint then
    error('bad fingerprint')
  end

  return Mesh._decode_one(data:sub(9))
end

function Mesh._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Mesh:new()

  obj.triangles_length, obj.vertices_length = lcm._pack.unpack('>ii', data:read(8))
  obj.triangles = {}
  for i0 = 1, obj.triangles_length do
    obj.triangles[i0] = shape_msgs_MeshTriangle._decode_one(data)
  end
  obj.vertices = {}
  for i0 = 1, obj.vertices_length do
    obj.vertices[i0] = geometry_msgs_Point._decode_one(data)
  end

  return obj
end

return Mesh

