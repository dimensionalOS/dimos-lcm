--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local Plane = {}
Plane.__index = Plane

Plane.name = 'shape_msgs.Plane'
Plane.packagename = 'shape_msgs'
Plane.shortname = 'Plane'

function Plane:new()

  local obj = {}

  obj.coef = {}
  for d0 = 1, 4 do
    obj.coef[d0] = 0.0
  end

  setmetatable(obj, self)

  return obj
end

function Plane._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == Plane then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, Plane)

  local hash = lcm._hash.new('0x7e294b30ea2123f2')
  hash:rotate(1)

  return hash
end

Plane._packed_fingerprint = lcm._pack.pack('>X', Plane._get_hash_recursive({}))

function Plane:encode()

  return Plane._packed_fingerprint .. self:_encode_one()
end

function Plane:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>4d', unpack(self.coef)))

  return table.concat(buf_table)
end

function Plane.decode(data)

  if data:sub(1, 8) ~= Plane._packed_fingerprint then
    error('bad fingerprint')
  end

  return Plane._decode_one(data:sub(9))
end

function Plane._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = Plane:new()

  obj.coef = {lcm._pack.unpack('>4d', data:read(32))}

  return obj
end

return Plane

