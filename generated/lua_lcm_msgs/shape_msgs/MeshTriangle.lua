--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local MeshTriangle = {}
MeshTriangle.__index = MeshTriangle

MeshTriangle.name = 'shape_msgs.MeshTriangle'
MeshTriangle.packagename = 'shape_msgs'
MeshTriangle.shortname = 'MeshTriangle'

function MeshTriangle:new()

  local obj = {}

  obj.vertex_indices = {}
  for d0 = 1, 3 do
    obj.vertex_indices[d0] = 0
  end

  setmetatable(obj, self)

  return obj
end

function MeshTriangle._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == MeshTriangle then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, MeshTriangle)

  local hash = lcm._hash.new('0x2ff7303a4eea918d')
  hash:rotate(1)

  return hash
end

MeshTriangle._packed_fingerprint = lcm._pack.pack('>X', MeshTriangle._get_hash_recursive({}))

function MeshTriangle:encode()

  return MeshTriangle._packed_fingerprint .. self:_encode_one()
end

function MeshTriangle:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>3i', unpack(self.vertex_indices)))

  return table.concat(buf_table)
end

function MeshTriangle.decode(data)

  if data:sub(1, 8) ~= MeshTriangle._packed_fingerprint then
    error('bad fingerprint')
  end

  return MeshTriangle._decode_one(data:sub(9))
end

function MeshTriangle._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = MeshTriangle:new()

  obj.vertex_indices = {lcm._pack.unpack('>3i', data:read(12))}

  return obj
end

return MeshTriangle

