--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local dimos_control_msgs_ResourceDescription = require('dimos_control_msgs.ResourceDescription')
local dimos_control_msgs_InterfaceDescription = require('dimos_control_msgs.InterfaceDescription')
local dimos_control_msgs_CommandProfile = require('dimos_control_msgs.CommandProfile')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local ConnectionDescription = {}
ConnectionDescription.__index = ConnectionDescription

  ConnectionDescription.DIRECT = 1
  ConnectionDescription.OPERATOR_CONFIRMED = 2
  ConnectionDescription.APPLY_OMISSION = 1
  ConnectionDescription.ARMED_IDLE_SAFE_STOP = 2
  ConnectionDescription.NATIVE_WATCHDOG = 1
  ConnectionDescription.EXTERNAL_SUPERVISOR = 2
  ConnectionDescription.INTRINSICALLY_SAFE = 3
  ConnectionDescription.SIMULATION = 4
  ConnectionDescription.UNPROTECTED = 5

ConnectionDescription.name = 'dimos_control_msgs.ConnectionDescription'
ConnectionDescription.packagename = 'dimos_control_msgs'
ConnectionDescription.shortname = 'ConnectionDescription'

function ConnectionDescription:new()

  local obj = {}

  obj.source = ''
  obj.description_epoch = 0
  obj.resources_length = 0
  obj.interfaces_length = 0
  obj.command_profiles_length = 0
  obj.resources = {}
  obj.interfaces = {}
  obj.command_profiles = {}
  obj.expected_state_rate_hz = 0.0
  obj.state_stale_timeout_s = 0.0
  obj.command_watchdog_timeout_s = 0.0
  obj.activation_policy = 0
  obj.armed_idle_policy = 0
  obj.safe_stop_behavior = ''
  obj.process_loss_classification = 0
  obj.process_loss_evidence = ''

  setmetatable(obj, self)

  return obj
end

function ConnectionDescription._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == ConnectionDescription then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, ConnectionDescription)

  local hash = lcm._hash.new('0x31d30851f65c7d8c')
    + dimos_control_msgs_ResourceDescription._get_hash_recursive(newparents)
    + dimos_control_msgs_InterfaceDescription._get_hash_recursive(newparents)
    + dimos_control_msgs_CommandProfile._get_hash_recursive(newparents)
  hash:rotate(1)

  return hash
end

ConnectionDescription._packed_fingerprint = lcm._pack.pack('>X', ConnectionDescription._get_hash_recursive({}))

function ConnectionDescription:encode()

  return ConnectionDescription._packed_fingerprint .. self:_encode_one()
end

function ConnectionDescription:_encode_one()

  local buf_table = {}

  local __source_tmpstr = lcm._pack.prepare_string(self.source)
  table.insert(buf_table, lcm._pack.pack('>I', #__source_tmpstr + 1))
  table.insert(buf_table, __source_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack('>qiii', self.description_epoch, self.resources_length, self.interfaces_length, self.command_profiles_length))
  for i0 = 1, self.resources_length do
    table.insert(buf_table, self.resources[i0]:_encode_one())
  end
  for i0 = 1, self.interfaces_length do
    table.insert(buf_table, self.interfaces[i0]:_encode_one())
  end
  for i0 = 1, self.command_profiles_length do
    table.insert(buf_table, self.command_profiles[i0]:_encode_one())
  end
  table.insert(buf_table, lcm._pack.pack('>dddbb', self.expected_state_rate_hz, self.state_stale_timeout_s, self.command_watchdog_timeout_s, self.activation_policy, self.armed_idle_policy))
  local __safe_stop_behavior_tmpstr = lcm._pack.prepare_string(self.safe_stop_behavior)
  table.insert(buf_table, lcm._pack.pack('>I', #__safe_stop_behavior_tmpstr + 1))
  table.insert(buf_table, __safe_stop_behavior_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack('>b', self.process_loss_classification))
  local __process_loss_evidence_tmpstr = lcm._pack.prepare_string(self.process_loss_evidence)
  table.insert(buf_table, lcm._pack.pack('>I', #__process_loss_evidence_tmpstr + 1))
  table.insert(buf_table, __process_loss_evidence_tmpstr .. '\0')

  return table.concat(buf_table)
end

function ConnectionDescription.decode(data)

  if data:sub(1, 8) ~= ConnectionDescription._packed_fingerprint then
    error('bad fingerprint')
  end

  return ConnectionDescription._decode_one(data:sub(9))
end

function ConnectionDescription._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = ConnectionDescription:new()

  local __source_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.source = lcm._pack.prepare_string(data:read(__source_tmpstrlen))
  obj.description_epoch, obj.resources_length, obj.interfaces_length, obj.command_profiles_length = lcm._pack.unpack('>qiii', data:read(20))
  obj.resources = {}
  for i0 = 1, obj.resources_length do
    obj.resources[i0] = dimos_control_msgs_ResourceDescription._decode_one(data)
  end
  obj.interfaces = {}
  for i0 = 1, obj.interfaces_length do
    obj.interfaces[i0] = dimos_control_msgs_InterfaceDescription._decode_one(data)
  end
  obj.command_profiles = {}
  for i0 = 1, obj.command_profiles_length do
    obj.command_profiles[i0] = dimos_control_msgs_CommandProfile._decode_one(data)
  end
  obj.expected_state_rate_hz, obj.state_stale_timeout_s, obj.command_watchdog_timeout_s, obj.activation_policy, obj.armed_idle_policy = lcm._pack.unpack('>dddbb', data:read(26))
  local __safe_stop_behavior_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.safe_stop_behavior = lcm._pack.prepare_string(data:read(__safe_stop_behavior_tmpstrlen))
  obj.process_loss_classification = lcm._pack.unpack('>b', data:read(1))
  local __process_loss_evidence_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.process_loss_evidence = lcm._pack.prepare_string(data:read(__process_loss_evidence_tmpstrlen))

  return obj
end

return ConnectionDescription

