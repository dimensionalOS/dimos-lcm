--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local ConnectionControl = {}
ConnectionControl.__index = ConnectionControl

  ConnectionControl.PREPARE_ARM = 1
  ConnectionControl.COMMIT_ARM = 2
  ConnectionControl.ABORT_ARM = 3
  ConnectionControl.SAFE_STOP = 4
  ConnectionControl.CLEAR_SAFE_STOP = 5
  ConnectionControl.ESTOP = 6
  ConnectionControl.CLEAR_ESTOP = 7

ConnectionControl.name = 'dimos_control_msgs.ConnectionControl'
ConnectionControl.packagename = 'dimos_control_msgs'
ConnectionControl.shortname = 'ConnectionControl'

function ConnectionControl:new()

  local obj = {}

  obj.target_sources_length = 0
  obj.target_sources = {}
  obj.operation_id = 0
  obj.operation = 0
  obj.proposed_control_epoch = 0
  obj.reason = ''

  setmetatable(obj, self)

  return obj
end

function ConnectionControl._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == ConnectionControl then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, ConnectionControl)

  local hash = lcm._hash.new('0xb82d9643472f465d')
  hash:rotate(1)

  return hash
end

ConnectionControl._packed_fingerprint = lcm._pack.pack('>X', ConnectionControl._get_hash_recursive({}))

function ConnectionControl:encode()

  return ConnectionControl._packed_fingerprint .. self:_encode_one()
end

function ConnectionControl:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>i', self.target_sources_length))
  for i0 = 1, self.target_sources_length do
    local __target_sources_tmpstr = lcm._pack.prepare_string(self.target_sources[i0])
    table.insert(buf_table, lcm._pack.pack('>I', #__target_sources_tmpstr + 1))
    table.insert(buf_table, __target_sources_tmpstr .. '\0')
  end
  table.insert(buf_table, lcm._pack.pack('>qbq', self.operation_id, self.operation, self.proposed_control_epoch))
  local __reason_tmpstr = lcm._pack.prepare_string(self.reason)
  table.insert(buf_table, lcm._pack.pack('>I', #__reason_tmpstr + 1))
  table.insert(buf_table, __reason_tmpstr .. '\0')

  return table.concat(buf_table)
end

function ConnectionControl.decode(data)

  if data:sub(1, 8) ~= ConnectionControl._packed_fingerprint then
    error('bad fingerprint')
  end

  return ConnectionControl._decode_one(data:sub(9))
end

function ConnectionControl._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = ConnectionControl:new()

  obj.target_sources_length = lcm._pack.unpack('>i', data:read(4))
  obj.target_sources = {}
  for i0 = 1, obj.target_sources_length do
    local __target_sources_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
    obj.target_sources[i0] = lcm._pack.prepare_string(data:read(__target_sources_tmpstrlen))
  end
  obj.operation_id, obj.operation, obj.proposed_control_epoch = lcm._pack.unpack('>qbq', data:read(17))
  local __reason_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.reason = lcm._pack.prepare_string(data:read(__reason_tmpstrlen))

  return obj
end

return ConnectionControl

