--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local CommandProfile = {}
CommandProfile.__index = CommandProfile

CommandProfile.name = 'dimos_control_msgs.CommandProfile'
CommandProfile.packagename = 'dimos_control_msgs'
CommandProfile.shortname = 'CommandProfile'

function CommandProfile:new()

  local obj = {}

  obj.name = ''
  obj.required_fields_length = 0
  obj.optional_fields_length = 0
  obj.required_fields = {}
  obj.optional_fields = {}
  obj.uniform_across_resources = false

  setmetatable(obj, self)

  return obj
end

function CommandProfile._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == CommandProfile then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, CommandProfile)

  local hash = lcm._hash.new('0x38538e7a9d86eb4a')
  hash:rotate(1)

  return hash
end

CommandProfile._packed_fingerprint = lcm._pack.pack('>X', CommandProfile._get_hash_recursive({}))

function CommandProfile:encode()

  return CommandProfile._packed_fingerprint .. self:_encode_one()
end

function CommandProfile:_encode_one()

  local buf_table = {}

  local __name_tmpstr = lcm._pack.prepare_string(self.name)
  table.insert(buf_table, lcm._pack.pack('>I', #__name_tmpstr + 1))
  table.insert(buf_table, __name_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack('>ii', self.required_fields_length, self.optional_fields_length))
  for i0 = 1, self.required_fields_length do
    local __required_fields_tmpstr = lcm._pack.prepare_string(self.required_fields[i0])
    table.insert(buf_table, lcm._pack.pack('>I', #__required_fields_tmpstr + 1))
    table.insert(buf_table, __required_fields_tmpstr .. '\0')
  end
  for i0 = 1, self.optional_fields_length do
    local __optional_fields_tmpstr = lcm._pack.prepare_string(self.optional_fields[i0])
    table.insert(buf_table, lcm._pack.pack('>I', #__optional_fields_tmpstr + 1))
    table.insert(buf_table, __optional_fields_tmpstr .. '\0')
  end
  table.insert(buf_table, lcm._pack.pack('>?', self.uniform_across_resources))

  return table.concat(buf_table)
end

function CommandProfile.decode(data)

  if data:sub(1, 8) ~= CommandProfile._packed_fingerprint then
    error('bad fingerprint')
  end

  return CommandProfile._decode_one(data:sub(9))
end

function CommandProfile._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = CommandProfile:new()

  local __name_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.name = lcm._pack.prepare_string(data:read(__name_tmpstrlen))
  obj.required_fields_length, obj.optional_fields_length = lcm._pack.unpack('>ii', data:read(8))
  obj.required_fields = {}
  for i0 = 1, obj.required_fields_length do
    local __required_fields_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
    obj.required_fields[i0] = lcm._pack.prepare_string(data:read(__required_fields_tmpstrlen))
  end
  obj.optional_fields = {}
  for i0 = 1, obj.optional_fields_length do
    local __optional_fields_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
    obj.optional_fields[i0] = lcm._pack.prepare_string(data:read(__optional_fields_tmpstrlen))
  end
  obj.uniform_across_resources = lcm._pack.unpack('>?', data:read(1))

  return obj
end

return CommandProfile

