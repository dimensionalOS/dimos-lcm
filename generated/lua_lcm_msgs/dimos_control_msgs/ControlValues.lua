--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local ControlValues = {}
ControlValues.__index = ControlValues

ControlValues.name = 'dimos_control_msgs.ControlValues'
ControlValues.packagename = 'dimos_control_msgs'
ControlValues.shortname = 'ControlValues'

function ControlValues:new()

  local obj = {}

  obj.source = ''
  obj.source_ts = 0.0
  obj.epoch = 0
  obj.sequence = 0
  obj.interface_names_length = 0
  obj.values_length = 0
  obj.interface_names = {}
  obj.values = {}

  setmetatable(obj, self)

  return obj
end

function ControlValues._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == ControlValues then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, ControlValues)

  local hash = lcm._hash.new('0x98d9f64b18d5a4cf')
  hash:rotate(1)

  return hash
end

ControlValues._packed_fingerprint = lcm._pack.pack('>X', ControlValues._get_hash_recursive({}))

function ControlValues:encode()

  return ControlValues._packed_fingerprint .. self:_encode_one()
end

function ControlValues:_encode_one()

  local buf_table = {}

  local __source_tmpstr = lcm._pack.prepare_string(self.source)
  table.insert(buf_table, lcm._pack.pack('>I', #__source_tmpstr + 1))
  table.insert(buf_table, __source_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack('>dqqii', self.source_ts, self.epoch, self.sequence, self.interface_names_length, self.values_length))
  for i0 = 1, self.interface_names_length do
    local __interface_names_tmpstr = lcm._pack.prepare_string(self.interface_names[i0])
    table.insert(buf_table, lcm._pack.pack('>I', #__interface_names_tmpstr + 1))
    table.insert(buf_table, __interface_names_tmpstr .. '\0')
  end
  table.insert(buf_table, lcm._pack.pack(string.format('>%dd', self.values_length), unpack(self.values)))

  return table.concat(buf_table)
end

function ControlValues.decode(data)

  if data:sub(1, 8) ~= ControlValues._packed_fingerprint then
    error('bad fingerprint')
  end

  return ControlValues._decode_one(data:sub(9))
end

function ControlValues._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = ControlValues:new()

  local __source_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.source = lcm._pack.prepare_string(data:read(__source_tmpstrlen))
  obj.source_ts, obj.epoch, obj.sequence, obj.interface_names_length, obj.values_length = lcm._pack.unpack('>dqqii', data:read(32))
  obj.interface_names = {}
  for i0 = 1, obj.interface_names_length do
    local __interface_names_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
    obj.interface_names[i0] = lcm._pack.prepare_string(data:read(__interface_names_tmpstrlen))
  end
  obj.values = {lcm._pack.unpack(string.format('>%dd', obj.values_length), data:read(obj.values_length * 8))}

  return obj
end

return ControlValues

