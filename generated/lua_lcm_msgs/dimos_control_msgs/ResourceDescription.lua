--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local ResourceDescription = {}
ResourceDescription.__index = ResourceDescription

  ResourceDescription.JOINT = 1
  ResourceDescription.GRIPPER = 2
  ResourceDescription.BASE = 3
  ResourceDescription.IMU = 4

ResourceDescription.name = 'dimos_control_msgs.ResourceDescription'
ResourceDescription.packagename = 'dimos_control_msgs'
ResourceDescription.shortname = 'ResourceDescription'

function ResourceDescription:new()

  local obj = {}

  obj.name = ''
  obj.resource_type = 0
  obj.group_name = ''

  setmetatable(obj, self)

  return obj
end

function ResourceDescription._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == ResourceDescription then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, ResourceDescription)

  local hash = lcm._hash.new('0x802e2d1f1cd1df73')
  hash:rotate(1)

  return hash
end

ResourceDescription._packed_fingerprint = lcm._pack.pack('>X', ResourceDescription._get_hash_recursive({}))

function ResourceDescription:encode()

  return ResourceDescription._packed_fingerprint .. self:_encode_one()
end

function ResourceDescription:_encode_one()

  local buf_table = {}

  local __name_tmpstr = lcm._pack.prepare_string(self.name)
  table.insert(buf_table, lcm._pack.pack('>I', #__name_tmpstr + 1))
  table.insert(buf_table, __name_tmpstr .. '\0')
  table.insert(buf_table, lcm._pack.pack('>b', self.resource_type))
  local __group_name_tmpstr = lcm._pack.prepare_string(self.group_name)
  table.insert(buf_table, lcm._pack.pack('>I', #__group_name_tmpstr + 1))
  table.insert(buf_table, __group_name_tmpstr .. '\0')

  return table.concat(buf_table)
end

function ResourceDescription.decode(data)

  if data:sub(1, 8) ~= ResourceDescription._packed_fingerprint then
    error('bad fingerprint')
  end

  return ResourceDescription._decode_one(data:sub(9))
end

function ResourceDescription._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = ResourceDescription:new()

  local __name_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.name = lcm._pack.prepare_string(data:read(__name_tmpstrlen))
  obj.resource_type = lcm._pack.unpack('>b', data:read(1))
  local __group_name_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.group_name = lcm._pack.prepare_string(data:read(__group_name_tmpstrlen))

  return obj
end

return ResourceDescription

