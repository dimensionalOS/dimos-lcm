--[[
LCM type definitions
This file automatically generated by lcm 1.5.2.
DO NOT MODIFY BY HAND!!!!
--]]

local lcm = require('lcm')

local setmetatable = setmetatable
local ipairs = ipairs
local table = table
local string = string
local unpack = unpack or table.unpack

-- buffer helper for decoding
local _buffer_helper = {}
_buffer_helper.__index = _buffer_helper

function _buffer_helper:new(data_str)

  local obj = {buffer = data_str, index = 1}
  setmetatable(obj, self)

  return obj
end

function _buffer_helper:read(n_bytes)

  local partial = self.buffer:sub(self.index, self.index + n_bytes - 1)
  self.index = self.index + n_bytes

  if self.index > #self.buffer + 1 then
    error('buffer ran out of bytes')
  end

  return partial
end

local TF2Error = {}
TF2Error.__index = TF2Error

  TF2Error.NO_ERROR = 0
  TF2Error.LOOKUP_ERROR = 1
  TF2Error.CONNECTIVITY_ERROR = 2
  TF2Error.EXTRAPOLATION_ERROR = 3
  TF2Error.INVALID_ARGUMENT_ERROR = 4
  TF2Error.TIMEOUT_ERROR = 5
  TF2Error.TRANSFORM_ERROR = 6

TF2Error.name = 'tf2_msgs.TF2Error'
TF2Error.packagename = 'tf2_msgs'
TF2Error.shortname = 'TF2Error'

function TF2Error:new()

  local obj = {}

  obj.error = 0
  obj.error_string = ''

  setmetatable(obj, self)

  return obj
end

function TF2Error._get_hash_recursive(parents)

  local newparents = {}

  for _, v in ipairs(parents) do
    if v == TF2Error then return lcm._hash.new('0x0') end
    table.insert(newparents, v)
  end

  table.insert(newparents, TF2Error)

  local hash = lcm._hash.new('0x1e8ff0d80d02ee55')
  hash:rotate(1)

  return hash
end

TF2Error._packed_fingerprint = lcm._pack.pack('>X', TF2Error._get_hash_recursive({}))

function TF2Error:encode()

  return TF2Error._packed_fingerprint .. self:_encode_one()
end

function TF2Error:_encode_one()

  local buf_table = {}

  table.insert(buf_table, lcm._pack.pack('>B', self.error))
  local __error_string_tmpstr = lcm._pack.prepare_string(self.error_string)
  table.insert(buf_table, lcm._pack.pack('>I', #__error_string_tmpstr + 1))
  table.insert(buf_table, __error_string_tmpstr .. '\0')

  return table.concat(buf_table)
end

function TF2Error.decode(data)

  if data:sub(1, 8) ~= TF2Error._packed_fingerprint then
    error('bad fingerprint')
  end

  return TF2Error._decode_one(data:sub(9))
end

function TF2Error._decode_one(data)

  if not data.read then
    data = _buffer_helper:new(data)
  end

  local obj = TF2Error:new()

  obj.error = lcm._pack.unpack('>B', data:read(1))
  local __error_string_tmpstrlen = lcm._pack.unpack('>I', data:read(4))
  obj.error_string = lcm._pack.prepare_string(data:read(__error_string_tmpstrlen))

  return obj
end

return TF2Error

