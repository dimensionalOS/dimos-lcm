// Auto-generated by lcm-ts. DO NOT EDIT.

import { MeshTriangle } from "./MeshTriangle.ts";
import { Point } from "../geometry_msgs/Point.ts";

export class Mesh {
  static readonly _HASH = 0xdc739fb8d2f81ab9n;
  static readonly _NAME = "shape_msgs.Mesh";
  private static _packedFingerprint: bigint | null = null;

  triangles_length: number;
  vertices_length: number;
  triangles: MeshTriangle[];
  vertices: Point[];

  constructor(init?: Partial<Mesh>) {
    this.triangles_length = init?.triangles_length ?? 0;
    this.vertices_length = init?.vertices_length ?? 0;
    this.triangles = init?.triangles ?? [];
    this.vertices = init?.vertices ?? [];
  }

  static decode(data: Uint8Array): Mesh {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = Mesh._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Mesh();
    ({ offset } = Mesh._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Mesh): { offset: number } {
    target.triangles_length = view.getInt32(offset, false);
    offset += 4;
    target.vertices_length = view.getInt32(offset, false);
    offset += 4;
    target.triangles = new Array(target.triangles_length);
    for (let i0 = 0; i0 < target.triangles_length; i0++) {
      target.triangles[i0] = new MeshTriangle();
      ({ offset } = MeshTriangle._decodeOne(view, offset, target.triangles[i0]));
    }
    target.vertices = new Array(target.vertices_length);
    for (let i0 = 0; i0 < target.vertices_length; i0++) {
      target.vertices[i0] = new Point();
      ({ offset } = Point._decodeOne(view, offset, target.vertices[i0]));
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, Mesh._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.triangles_length, false);
    offset += 4;
    view.setInt32(offset, this.vertices_length, false);
    offset += 4;
    for (let i0 = 0; i0 < this.triangles_length; i0++) {
      offset = this.triangles[i0]._encodeOne(view, offset);
    }
    for (let i0 = 0; i0 < this.vertices_length; i0++) {
      offset = this.vertices[i0]._encodeOne(view, offset);
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    for (let i0 = 0; i0 < this.triangles_length; i0++) {
      size += this.triangles[i0]._encodedSize();
    }
    for (let i0 = 0; i0 < this.vertices_length; i0++) {
      size += this.vertices[i0]._encodedSize();
    }
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(Mesh)) return 0n;
    const newparents = [...parents, Mesh];
    let tmphash = Mesh._HASH;
    tmphash = (tmphash + MeshTriangle._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Point._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (Mesh._packedFingerprint === null) {
      Mesh._packedFingerprint = Mesh._getHashRecursive([]);
    }
    return Mesh._packedFingerprint;
  }
}
