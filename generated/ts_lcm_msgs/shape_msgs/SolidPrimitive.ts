// Auto-generated by lcm-ts. DO NOT EDIT.

export class SolidPrimitive {
  static readonly _HASH = 0xb8a8e7178cab07acn;
  static readonly _NAME = "shape_msgs.SolidPrimitive";
  private static _packedFingerprint: bigint | null = null;

  static readonly BOX = 1;
  static readonly SPHERE = 2;
  static readonly CYLINDER = 3;
  static readonly CONE = 4;
  static readonly BOX_X = 0;
  static readonly BOX_Y = 1;
  static readonly BOX_Z = 2;
  static readonly SPHERE_RADIUS = 0;
  static readonly CYLINDER_HEIGHT = 0;
  static readonly CYLINDER_RADIUS = 1;
  static readonly CONE_HEIGHT = 0;
  static readonly CONE_RADIUS = 1;

  dimensions_length: number;
  type: number;
  dimensions: number[];

  constructor(init?: Partial<SolidPrimitive>) {
    this.dimensions_length = init?.dimensions_length ?? 0;
    this.type = init?.type ?? 0;
    this.dimensions = init?.dimensions ?? [];
  }

  static decode(data: Uint8Array): SolidPrimitive {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = SolidPrimitive._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new SolidPrimitive();
    ({ offset } = SolidPrimitive._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: SolidPrimitive): { offset: number } {
    target.dimensions_length = view.getInt32(offset, false);
    offset += 4;
    target.type = view.getUint8(offset);
    offset += 1;
    target.dimensions = new Array(target.dimensions_length);
    for (let i0 = 0; i0 < target.dimensions_length; i0++) {
      target.dimensions[i0] = view.getFloat64(offset, false);
      offset += 8;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, SolidPrimitive._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.dimensions_length, false);
    offset += 4;
    view.setUint8(offset, this.type);
    offset += 1;
    for (let i0 = 0; i0 < this.dimensions_length; i0++) {
      view.setFloat64(offset, this.dimensions[i0], false);
      offset += 8;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 1;
    size += this.dimensions_length * 8;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(SolidPrimitive)) return 0n;
    const newparents = [...parents, SolidPrimitive];
    let tmphash = SolidPrimitive._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (SolidPrimitive._packedFingerprint === null) {
      SolidPrimitive._packedFingerprint = SolidPrimitive._getHashRecursive([]);
    }
    return SolidPrimitive._packedFingerprint;
  }
}
