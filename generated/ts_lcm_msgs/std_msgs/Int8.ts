// Auto-generated by lcm-ts. DO NOT EDIT.

export class Int8 {
  static readonly _HASH = 0x90e9a182fedda20an;
  static readonly _NAME = "std_msgs.Int8";
  private static _packedFingerprint: bigint | null = null;

  data: number;

  constructor(init?: Partial<Int8>) {
    this.data = init?.data ?? 0;
  }

  static decode(data: Uint8Array): Int8 {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = Int8._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Int8();
    ({ offset } = Int8._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Int8): { offset: number } {
    target.data = view.getInt8(offset);
    offset += 1;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, Int8._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt8(offset, this.data);
    offset += 1;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 1;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(Int8)) return 0n;
    const newparents = [...parents, Int8];
    let tmphash = Int8._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (Int8._packedFingerprint === null) {
      Int8._packedFingerprint = Int8._getHashRecursive([]);
    }
    return Int8._packedFingerprint;
  }
}
