// Auto-generated by lcm-ts. DO NOT EDIT.

export class Empty {
  static readonly _HASH = 0xcee65c8adae0e8f2n;
  static readonly _NAME = "std_msgs.Empty";


  constructor(init?: Partial<Empty>) {
  }

  static decode(data: Uint8Array): Empty {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== Empty._HASH) {
      throw new Error(`Hash mismatch: expected ${Empty._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Empty();
    ({ offset } = Empty._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Empty): { offset: number } {
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, Empty._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    return size;
  }
}
