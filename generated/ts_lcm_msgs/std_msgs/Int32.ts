// Auto-generated by lcm-ts. DO NOT EDIT.

export class Int32 {
  static readonly _HASH = 0x165e7cfbf548811fn;
  static readonly _NAME = "std_msgs.Int32";
  private static _packedFingerprint: bigint | null = null;

  data: number;

  constructor(init?: Partial<Int32>) {
    this.data = init?.data ?? 0;
  }

  static decode(data: Uint8Array): Int32 {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = Int32._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Int32();
    ({ offset } = Int32._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Int32): { offset: number } {
    target.data = view.getInt32(offset, false);
    offset += 4;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, Int32._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.data, false);
    offset += 4;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(Int32)) return 0n;
    const newparents = [...parents, Int32];
    let tmphash = Int32._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (Int32._packedFingerprint === null) {
      Int32._packedFingerprint = Int32._getHashRecursive([]);
    }
    return Int32._packedFingerprint;
  }
}
