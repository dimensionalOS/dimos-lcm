// Auto-generated by lcm-ts. DO NOT EDIT.

export class Char {
  static readonly _HASH = 0x74856d0f697d2dc2n;
  static readonly _NAME = "std_msgs.Char";
  private static _packedFingerprint: bigint | null = null;

  data: number;

  constructor(init?: Partial<Char>) {
    this.data = init?.data ?? 0;
  }

  static decode(data: Uint8Array): Char {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = Char._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Char();
    ({ offset } = Char._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Char): { offset: number } {
    target.data = view.getUint8(offset);
    offset += 1;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, Char._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setUint8(offset, this.data);
    offset += 1;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 1;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(Char)) return 0n;
    const newparents = [...parents, Char];
    let tmphash = Char._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (Char._packedFingerprint === null) {
      Char._packedFingerprint = Char._getHashRecursive([]);
    }
    return Char._packedFingerprint;
  }
}
