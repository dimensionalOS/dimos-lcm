// Auto-generated by lcm-ts. DO NOT EDIT.

import { MultiArrayDimension } from "./MultiArrayDimension.ts";

export class MultiArrayLayout {
  static readonly _HASH = 0xc2bedecccce6cae8n;
  static readonly _NAME = "std_msgs.MultiArrayLayout";

  dim_length: number;
  dim: MultiArrayDimension[];
  data_offset: number;

  constructor(init?: Partial<MultiArrayLayout>) {
    this.dim_length = init?.dim_length ?? 0;
    this.dim = init?.dim ?? [];
    this.data_offset = init?.data_offset ?? 0;
  }

  static decode(data: Uint8Array): MultiArrayLayout {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== MultiArrayLayout._HASH) {
      throw new Error(`Hash mismatch: expected ${MultiArrayLayout._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new MultiArrayLayout();
    ({ offset } = MultiArrayLayout._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: MultiArrayLayout): { offset: number } {
    target.dim_length = view.getInt32(offset, false);
    offset += 4;
    target.dim = new Array(target.dim_length);
    for (let i0 = 0; i0 < target.dim_length; i0++) {
      target.dim[i0] = new MultiArrayDimension();
      ({ offset } = MultiArrayDimension._decodeOne(view, offset, target.dim[i0]));
    }
    target.data_offset = view.getInt32(offset, false);
    offset += 4;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, MultiArrayLayout._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.dim_length, false);
    offset += 4;
    for (let i0 = 0; i0 < this.dim_length; i0++) {
      offset = this.dim[i0]._encodeOne(view, offset);
    }
    view.setInt32(offset, this.data_offset, false);
    offset += 4;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    for (let i0 = 0; i0 < this.dim_length; i0++) {
      size += this.dim[i0]._encodedSize();
    }
    size += 4;
    return size;
  }
}
