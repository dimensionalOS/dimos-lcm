// Auto-generated by lcm-ts. DO NOT EDIT.

import { Time } from "./Time.ts";

export class Header {
  static readonly _HASH = 0xdbb33f5b4c19b8ean;
  static readonly _NAME = "std_msgs.Header";
  private static _packedFingerprint: bigint | null = null;

  seq: number;
  stamp: Time;
  frame_id: string;

  constructor(init?: Partial<Header>) {
    this.seq = init?.seq ?? 0;
    this.stamp = init?.stamp ?? new Time();
    this.frame_id = init?.frame_id ?? "";
  }

  static decode(data: Uint8Array): Header {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = Header._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Header();
    ({ offset } = Header._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Header): { offset: number } {
    target.seq = view.getInt32(offset, false);
    offset += 4;
    target.stamp = new Time();
    ({ offset } = Time._decodeOne(view, offset, target.stamp));
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.frame_id = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, Header._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.seq, false);
    offset += 4;
    offset = this.stamp._encodeOne(view, offset);
    {
      const encoded = new TextEncoder().encode(this.frame_id);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += this.stamp._encodedSize();
    size += 4 + new TextEncoder().encode(this.frame_id).length + 1;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(Header)) return 0n;
    const newparents = [...parents, Header];
    let tmphash = Header._HASH;
    tmphash = (tmphash + Time._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (Header._packedFingerprint === null) {
      Header._packedFingerprint = Header._getHashRecursive([]);
    }
    return Header._packedFingerprint;
  }
}
