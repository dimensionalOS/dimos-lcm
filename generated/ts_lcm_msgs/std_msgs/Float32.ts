// Auto-generated by lcm-ts. DO NOT EDIT.

export class Float32 {
  static readonly _HASH = 0x856e135c782a36e9n;
  static readonly _NAME = "std_msgs.Float32";
  private static _packedFingerprint: bigint | null = null;

  data: number;

  constructor(init?: Partial<Float32>) {
    this.data = init?.data ?? 0;
  }

  static decode(data: Uint8Array): Float32 {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = Float32._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Float32();
    ({ offset } = Float32._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Float32): { offset: number } {
    target.data = view.getFloat32(offset, false);
    offset += 4;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, Float32._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setFloat32(offset, this.data, false);
    offset += 4;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(Float32)) return 0n;
    const newparents = [...parents, Float32];
    let tmphash = Float32._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (Float32._packedFingerprint === null) {
      Float32._packedFingerprint = Float32._getHashRecursive([]);
    }
    return Float32._packedFingerprint;
  }
}
