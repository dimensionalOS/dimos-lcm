// Auto-generated by lcm-ts. DO NOT EDIT.

export class ColorRGBA {
  static readonly _HASH = 0xe8c4ccd8dec2e8c2n;
  static readonly _NAME = "std_msgs.ColorRGBA";

  r: number;
  g: number;
  b: number;
  a: number;

  constructor(init?: Partial<ColorRGBA>) {
    this.r = init?.r ?? 0;
    this.g = init?.g ?? 0;
    this.b = init?.b ?? 0;
    this.a = init?.a ?? 0;
  }

  static decode(data: Uint8Array): ColorRGBA {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== ColorRGBA._HASH) {
      throw new Error(`Hash mismatch: expected ${ColorRGBA._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new ColorRGBA();
    ({ offset } = ColorRGBA._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: ColorRGBA): { offset: number } {
    target.r = view.getFloat32(offset, false);
    offset += 4;
    target.g = view.getFloat32(offset, false);
    offset += 4;
    target.b = view.getFloat32(offset, false);
    offset += 4;
    target.a = view.getFloat32(offset, false);
    offset += 4;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, ColorRGBA._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setFloat32(offset, this.r, false);
    offset += 4;
    view.setFloat32(offset, this.g, false);
    offset += 4;
    view.setFloat32(offset, this.b, false);
    offset += 4;
    view.setFloat32(offset, this.a, false);
    offset += 4;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    size += 4;
    size += 4;
    return size;
  }
}
