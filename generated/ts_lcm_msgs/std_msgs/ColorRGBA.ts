// Auto-generated by lcm-ts. DO NOT EDIT.

export class ColorRGBA {
  static readonly _HASH = 0x3719400fc6e3011an;
  static readonly _NAME = "std_msgs.ColorRGBA";
  private static _packedFingerprint: bigint | null = null;

  r: number;
  g: number;
  b: number;
  a: number;

  constructor(init?: Partial<ColorRGBA>) {
    this.r = init?.r ?? 0;
    this.g = init?.g ?? 0;
    this.b = init?.b ?? 0;
    this.a = init?.a ?? 0;
  }

  static decode(data: Uint8Array): ColorRGBA {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = ColorRGBA._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new ColorRGBA();
    ({ offset } = ColorRGBA._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: ColorRGBA): { offset: number } {
    target.r = view.getFloat32(offset, false);
    offset += 4;
    target.g = view.getFloat32(offset, false);
    offset += 4;
    target.b = view.getFloat32(offset, false);
    offset += 4;
    target.a = view.getFloat32(offset, false);
    offset += 4;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, ColorRGBA._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setFloat32(offset, this.r, false);
    offset += 4;
    view.setFloat32(offset, this.g, false);
    offset += 4;
    view.setFloat32(offset, this.b, false);
    offset += 4;
    view.setFloat32(offset, this.a, false);
    offset += 4;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    size += 4;
    size += 4;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(ColorRGBA)) return 0n;
    const newparents = [...parents, ColorRGBA];
    let tmphash = ColorRGBA._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (ColorRGBA._packedFingerprint === null) {
      ColorRGBA._packedFingerprint = ColorRGBA._getHashRecursive([]);
    }
    return ColorRGBA._packedFingerprint;
  }
}
