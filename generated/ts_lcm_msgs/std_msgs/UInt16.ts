// Auto-generated by lcm-ts. DO NOT EDIT.

export class UInt16 {
  static readonly _HASH = 0x165e7cf9f948811fn;
  static readonly _NAME = "std_msgs.UInt16";
  private static _packedFingerprint: bigint | null = null;

  data: number;

  constructor(init?: Partial<UInt16>) {
    this.data = init?.data ?? 0;
  }

  static decode(data: Uint8Array): UInt16 {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = UInt16._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new UInt16();
    ({ offset } = UInt16._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: UInt16): { offset: number } {
    target.data = view.getInt16(offset, false);
    offset += 2;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, UInt16._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt16(offset, this.data, false);
    offset += 2;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 2;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(UInt16)) return 0n;
    const newparents = [...parents, UInt16];
    let tmphash = UInt16._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (UInt16._packedFingerprint === null) {
      UInt16._packedFingerprint = UInt16._getHashRecursive([]);
    }
    return UInt16._packedFingerprint;
  }
}
