// Auto-generated by lcm-ts. DO NOT EDIT.

import { MultiArrayLayout } from "./MultiArrayLayout.ts";

export class UInt8MultiArray {
  static readonly _HASH = 0xf2e8cac8c2e8c200n;
  static readonly _NAME = "std_msgs.UInt8MultiArray";

  data_length: number;
  layout: MultiArrayLayout;
  data: Uint8Array;

  constructor(init?: Partial<UInt8MultiArray>) {
    this.data_length = init?.data_length ?? 0;
    this.layout = init?.layout ?? new MultiArrayLayout();
    this.data = init?.data ?? new Uint8Array(0);
  }

  static decode(data: Uint8Array): UInt8MultiArray {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== UInt8MultiArray._HASH) {
      throw new Error(`Hash mismatch: expected ${UInt8MultiArray._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new UInt8MultiArray();
    ({ offset } = UInt8MultiArray._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: UInt8MultiArray): { offset: number } {
    target.data_length = view.getInt32(offset, false);
    offset += 4;
    target.layout = new MultiArrayLayout();
    ({ offset } = MultiArrayLayout._decodeOne(view, offset, target.layout));
    target.data = new Uint8Array(view.buffer, view.byteOffset + offset, target.data_length);
    offset += target.data_length;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, UInt8MultiArray._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.data_length, false);
    offset += 4;
    offset = this.layout._encodeOne(view, offset);
    new Uint8Array(view.buffer, view.byteOffset + offset).set(this.data.subarray(0, this.data_length));
    offset += this.data_length;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += this.layout._encodedSize();
    size += this.data_length * 1;
    return size;
  }
}
