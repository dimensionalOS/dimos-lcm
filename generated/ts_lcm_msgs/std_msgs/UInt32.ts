// Auto-generated by lcm-ts. DO NOT EDIT.

export class UInt32 {
  static readonly _HASH = 0x6664bee8c8c2e8c2n;
  static readonly _NAME = "std_msgs.UInt32";

  data: number;

  constructor(init?: Partial<UInt32>) {
    this.data = init?.data ?? 0;
  }

  static decode(data: Uint8Array): UInt32 {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== UInt32._HASH) {
      throw new Error(`Hash mismatch: expected ${UInt32._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new UInt32();
    ({ offset } = UInt32._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: UInt32): { offset: number } {
    target.data = view.getInt32(offset, false);
    offset += 4;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, UInt32._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.data, false);
    offset += 4;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    return size;
  }
}
