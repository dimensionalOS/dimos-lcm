// Auto-generated by lcm-ts. DO NOT EDIT.

export class Bool {
  static readonly _HASH = 0xf5f7835284a871fn;
  static readonly _NAME = "std_msgs.Bool";
  private static _packedFingerprint: bigint | null = null;

  data: boolean;

  constructor(init?: Partial<Bool>) {
    this.data = init?.data ?? false;
  }

  static decode(data: Uint8Array): Bool {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = Bool._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Bool();
    ({ offset } = Bool._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Bool): { offset: number } {
    target.data = view.getInt8(offset) !== 0;
    offset += 1;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, Bool._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt8(offset, this.data ? 1 : 0);
    offset += 1;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 1;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(Bool)) return 0n;
    const newparents = [...parents, Bool];
    let tmphash = Bool._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (Bool._packedFingerprint === null) {
      Bool._packedFingerprint = Bool._getHashRecursive([]);
    }
    return Bool._packedFingerprint;
  }
}
