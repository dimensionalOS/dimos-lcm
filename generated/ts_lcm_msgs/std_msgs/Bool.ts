// Auto-generated by lcm-ts. DO NOT EDIT.

export class Bool {
  static readonly _HASH = 0xd8cac2dcc8c2e8c2n;
  static readonly _NAME = "std_msgs.Bool";

  data: boolean;

  constructor(init?: Partial<Bool>) {
    this.data = init?.data ?? false;
  }

  static decode(data: Uint8Array): Bool {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== Bool._HASH) {
      throw new Error(`Hash mismatch: expected ${Bool._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Bool();
    ({ offset } = Bool._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Bool): { offset: number } {
    target.data = view.getInt8(offset) !== 0;
    offset += 1;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, Bool._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt8(offset, this.data ? 1 : 0);
    offset += 1;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 1;
    return size;
  }
}
