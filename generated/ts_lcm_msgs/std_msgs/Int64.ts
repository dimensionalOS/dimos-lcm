// Auto-generated by lcm-ts. DO NOT EDIT.

export class Int64 {
  static readonly _HASH = 0x165e7cfef748811fn;
  static readonly _NAME = "std_msgs.Int64";
  private static _packedFingerprint: bigint | null = null;

  data: bigint;

  constructor(init?: Partial<Int64>) {
    this.data = init?.data ?? 0n;
  }

  static decode(data: Uint8Array): Int64 {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = Int64._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Int64();
    ({ offset } = Int64._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Int64): { offset: number } {
    target.data = view.getBigInt64(offset, false);
    offset += 8;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, Int64._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setBigInt64(offset, this.data, false);
    offset += 8;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 8;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(Int64)) return 0n;
    const newparents = [...parents, Int64];
    let tmphash = Int64._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (Int64._packedFingerprint === null) {
      Int64._packedFingerprint = Int64._getHashRecursive([]);
    }
    return Int64._packedFingerprint;
  }
}
