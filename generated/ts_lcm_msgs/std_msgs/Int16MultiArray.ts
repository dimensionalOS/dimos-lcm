// Auto-generated by lcm-ts. DO NOT EDIT.

import { MultiArrayLayout } from "./MultiArrayLayout.ts";

export class Int16MultiArray {
  static readonly _HASH = 0xaa51366c8f222c76n;
  static readonly _NAME = "std_msgs.Int16MultiArray";
  private static _packedFingerprint: bigint | null = null;

  data_length: number;
  layout: MultiArrayLayout;
  data: number[];

  constructor(init?: Partial<Int16MultiArray>) {
    this.data_length = init?.data_length ?? 0;
    this.layout = init?.layout ?? new MultiArrayLayout();
    this.data = init?.data ?? [];
  }

  static decode(data: Uint8Array): Int16MultiArray {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = Int16MultiArray._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Int16MultiArray();
    ({ offset } = Int16MultiArray._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Int16MultiArray): { offset: number } {
    target.data_length = view.getInt32(offset, false);
    offset += 4;
    target.layout = new MultiArrayLayout();
    ({ offset } = MultiArrayLayout._decodeOne(view, offset, target.layout));
    target.data = new Array(target.data_length);
    for (let i0 = 0; i0 < target.data_length; i0++) {
      target.data[i0] = view.getInt16(offset, false);
      offset += 2;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, Int16MultiArray._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.data_length, false);
    offset += 4;
    offset = this.layout._encodeOne(view, offset);
    for (let i0 = 0; i0 < this.data_length; i0++) {
      view.setInt16(offset, this.data[i0], false);
      offset += 2;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += this.layout._encodedSize();
    size += this.data_length * 2;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(Int16MultiArray)) return 0n;
    const newparents = [...parents, Int16MultiArray];
    let tmphash = Int16MultiArray._HASH;
    tmphash = (tmphash + MultiArrayLayout._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (Int16MultiArray._packedFingerprint === null) {
      Int16MultiArray._packedFingerprint = Int16MultiArray._getHashRecursive([]);
    }
    return Int16MultiArray._packedFingerprint;
  }
}
