// Auto-generated by lcm-ts. DO NOT EDIT.

export class Duration {
  static readonly _HASH = 0xde1d24a3a8ecb648n;
  static readonly _NAME = "std_msgs.Duration";
  private static _packedFingerprint: bigint | null = null;

  sec: number;
  nsec: number;

  constructor(init?: Partial<Duration>) {
    this.sec = init?.sec ?? 0;
    this.nsec = init?.nsec ?? 0;
  }

  static decode(data: Uint8Array): Duration {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = Duration._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Duration();
    ({ offset } = Duration._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Duration): { offset: number } {
    target.sec = view.getInt32(offset, false);
    offset += 4;
    target.nsec = view.getInt32(offset, false);
    offset += 4;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, Duration._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.sec, false);
    offset += 4;
    view.setInt32(offset, this.nsec, false);
    offset += 4;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(Duration)) return 0n;
    const newparents = [...parents, Duration];
    let tmphash = Duration._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (Duration._packedFingerprint === null) {
      Duration._packedFingerprint = Duration._getHashRecursive([]);
    }
    return Duration._packedFingerprint;
  }
}
