// Auto-generated by lcm-ts. DO NOT EDIT.

export class Int16 {
  static readonly _HASH = 0x626cbee8c8c2e8c2n;
  static readonly _NAME = "std_msgs.Int16";

  data: number;

  constructor(init?: Partial<Int16>) {
    this.data = init?.data ?? 0;
  }

  static decode(data: Uint8Array): Int16 {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== Int16._HASH) {
      throw new Error(`Hash mismatch: expected ${Int16._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Int16();
    ({ offset } = Int16._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Int16): { offset: number } {
    target.data = view.getInt16(offset, false);
    offset += 2;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, Int16._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt16(offset, this.data, false);
    offset += 2;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 2;
    return size;
  }
}
