// Auto-generated by lcm-ts. DO NOT EDIT.

export class Byte {
  static readonly _HASH = 0x90e9a182fedda20an;
  static readonly _NAME = "std_msgs.Byte";
  private static _packedFingerprint: bigint | null = null;

  data: number;

  constructor(init?: Partial<Byte>) {
    this.data = init?.data ?? 0;
  }

  static decode(data: Uint8Array): Byte {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = Byte._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Byte();
    ({ offset } = Byte._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Byte): { offset: number } {
    target.data = view.getInt8(offset);
    offset += 1;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, Byte._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt8(offset, this.data);
    offset += 1;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 1;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(Byte)) return 0n;
    const newparents = [...parents, Byte];
    let tmphash = Byte._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (Byte._packedFingerprint === null) {
      Byte._packedFingerprint = Byte._getHashRecursive([]);
    }
    return Byte._packedFingerprint;
  }
}
