// Auto-generated by lcm-ts. DO NOT EDIT.

import { MultiArrayLayout } from "./MultiArrayLayout.ts";

export class ByteMultiArray {
  static readonly _HASH = 0x70bee8c8c2e8c200n;
  static readonly _NAME = "std_msgs.ByteMultiArray";

  data_length: number;
  layout: MultiArrayLayout;
  data: number[];

  constructor(init?: Partial<ByteMultiArray>) {
    this.data_length = init?.data_length ?? 0;
    this.layout = init?.layout ?? new MultiArrayLayout();
    this.data = init?.data ?? [];
  }

  static decode(data: Uint8Array): ByteMultiArray {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== ByteMultiArray._HASH) {
      throw new Error(`Hash mismatch: expected ${ByteMultiArray._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new ByteMultiArray();
    ({ offset } = ByteMultiArray._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: ByteMultiArray): { offset: number } {
    target.data_length = view.getInt32(offset, false);
    offset += 4;
    target.layout = new MultiArrayLayout();
    ({ offset } = MultiArrayLayout._decodeOne(view, offset, target.layout));
    target.data = new Array(target.data_length);
    for (let i0 = 0; i0 < target.data_length; i0++) {
      target.data[i0] = view.getInt8(offset);
      offset += 1;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, ByteMultiArray._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.data_length, false);
    offset += 4;
    offset = this.layout._encodeOne(view, offset);
    for (let i0 = 0; i0 < this.data_length; i0++) {
      view.setInt8(offset, this.data[i0]);
      offset += 1;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += this.layout._encodedSize();
    size += this.data_length * 1;
    return size;
  }
}
