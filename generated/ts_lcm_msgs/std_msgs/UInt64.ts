// Auto-generated by lcm-ts. DO NOT EDIT.

export class UInt64 {
  static readonly _HASH = 0x6c68bee8c8c2e8c2n;
  static readonly _NAME = "std_msgs.UInt64";

  data: bigint;

  constructor(init?: Partial<UInt64>) {
    this.data = init?.data ?? 0n;
  }

  static decode(data: Uint8Array): UInt64 {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== UInt64._HASH) {
      throw new Error(`Hash mismatch: expected ${UInt64._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new UInt64();
    ({ offset } = UInt64._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: UInt64): { offset: number } {
    target.data = view.getBigInt64(offset, false);
    offset += 8;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, UInt64._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setBigInt64(offset, this.data, false);
    offset += 8;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 8;
    return size;
  }
}
