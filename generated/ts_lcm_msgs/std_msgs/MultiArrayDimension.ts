// Auto-generated by lcm-ts. DO NOT EDIT.

export class MultiArrayDimension {
  static readonly _HASH = 0xcec1b9e9d868ef97n;
  static readonly _NAME = "std_msgs.MultiArrayDimension";
  private static _packedFingerprint: bigint | null = null;

  label: string;
  size: number;
  stride: number;

  constructor(init?: Partial<MultiArrayDimension>) {
    this.label = init?.label ?? "";
    this.size = init?.size ?? 0;
    this.stride = init?.stride ?? 0;
  }

  static decode(data: Uint8Array): MultiArrayDimension {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = MultiArrayDimension._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new MultiArrayDimension();
    ({ offset } = MultiArrayDimension._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: MultiArrayDimension): { offset: number } {
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.label = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    target.size = view.getInt32(offset, false);
    offset += 4;
    target.stride = view.getInt32(offset, false);
    offset += 4;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, MultiArrayDimension._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    {
      const encoded = new TextEncoder().encode(this.label);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    view.setInt32(offset, this.size, false);
    offset += 4;
    view.setInt32(offset, this.stride, false);
    offset += 4;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4 + new TextEncoder().encode(this.label).length + 1;
    size += 4;
    size += 4;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(MultiArrayDimension)) return 0n;
    const newparents = [...parents, MultiArrayDimension];
    let tmphash = MultiArrayDimension._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (MultiArrayDimension._packedFingerprint === null) {
      MultiArrayDimension._packedFingerprint = MultiArrayDimension._getHashRecursive([]);
    }
    return MultiArrayDimension._packedFingerprint;
  }
}
