// Auto-generated by lcm-ts. DO NOT EDIT.

import { Transform } from "../geometry_msgs/Transform.ts";
import { Twist } from "../geometry_msgs/Twist.ts";
import { Duration } from "../std_msgs/Duration.ts";

export class MultiDOFJointTrajectoryPoint {
  static readonly _HASH = 0x6afb214a921f4cfan;
  static readonly _NAME = "trajectory_msgs.MultiDOFJointTrajectoryPoint";
  private static _packedFingerprint: bigint | null = null;

  transforms_length: number;
  velocities_length: number;
  accelerations_length: number;
  transforms: Transform[];
  velocities: Twist[];
  accelerations: Twist[];
  time_from_start: Duration;

  constructor(init?: Partial<MultiDOFJointTrajectoryPoint>) {
    this.transforms_length = init?.transforms_length ?? 0;
    this.velocities_length = init?.velocities_length ?? 0;
    this.accelerations_length = init?.accelerations_length ?? 0;
    this.transforms = init?.transforms ?? [];
    this.velocities = init?.velocities ?? [];
    this.accelerations = init?.accelerations ?? [];
    this.time_from_start = init?.time_from_start ?? new Duration();
  }

  static decode(data: Uint8Array): MultiDOFJointTrajectoryPoint {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = MultiDOFJointTrajectoryPoint._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new MultiDOFJointTrajectoryPoint();
    ({ offset } = MultiDOFJointTrajectoryPoint._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: MultiDOFJointTrajectoryPoint): { offset: number } {
    target.transforms_length = view.getInt32(offset, false);
    offset += 4;
    target.velocities_length = view.getInt32(offset, false);
    offset += 4;
    target.accelerations_length = view.getInt32(offset, false);
    offset += 4;
    target.transforms = new Array(target.transforms_length);
    for (let i0 = 0; i0 < target.transforms_length; i0++) {
      target.transforms[i0] = new Transform();
      ({ offset } = Transform._decodeOne(view, offset, target.transforms[i0]));
    }
    target.velocities = new Array(target.velocities_length);
    for (let i0 = 0; i0 < target.velocities_length; i0++) {
      target.velocities[i0] = new Twist();
      ({ offset } = Twist._decodeOne(view, offset, target.velocities[i0]));
    }
    target.accelerations = new Array(target.accelerations_length);
    for (let i0 = 0; i0 < target.accelerations_length; i0++) {
      target.accelerations[i0] = new Twist();
      ({ offset } = Twist._decodeOne(view, offset, target.accelerations[i0]));
    }
    target.time_from_start = new Duration();
    ({ offset } = Duration._decodeOne(view, offset, target.time_from_start));
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, MultiDOFJointTrajectoryPoint._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.transforms_length, false);
    offset += 4;
    view.setInt32(offset, this.velocities_length, false);
    offset += 4;
    view.setInt32(offset, this.accelerations_length, false);
    offset += 4;
    for (let i0 = 0; i0 < this.transforms_length; i0++) {
      offset = this.transforms[i0]._encodeOne(view, offset);
    }
    for (let i0 = 0; i0 < this.velocities_length; i0++) {
      offset = this.velocities[i0]._encodeOne(view, offset);
    }
    for (let i0 = 0; i0 < this.accelerations_length; i0++) {
      offset = this.accelerations[i0]._encodeOne(view, offset);
    }
    offset = this.time_from_start._encodeOne(view, offset);
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    size += 4;
    for (let i0 = 0; i0 < this.transforms_length; i0++) {
      size += this.transforms[i0]._encodedSize();
    }
    for (let i0 = 0; i0 < this.velocities_length; i0++) {
      size += this.velocities[i0]._encodedSize();
    }
    for (let i0 = 0; i0 < this.accelerations_length; i0++) {
      size += this.accelerations[i0]._encodedSize();
    }
    size += this.time_from_start._encodedSize();
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(MultiDOFJointTrajectoryPoint)) return 0n;
    const newparents = [...parents, MultiDOFJointTrajectoryPoint];
    let tmphash = MultiDOFJointTrajectoryPoint._HASH;
    tmphash = (tmphash + Transform._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Twist._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Twist._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Duration._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (MultiDOFJointTrajectoryPoint._packedFingerprint === null) {
      MultiDOFJointTrajectoryPoint._packedFingerprint = MultiDOFJointTrajectoryPoint._getHashRecursive([]);
    }
    return MultiDOFJointTrajectoryPoint._packedFingerprint;
  }
}
