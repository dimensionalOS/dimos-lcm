// Auto-generated by lcm-ts. DO NOT EDIT.

import { Duration } from "../std_msgs/Duration.ts";

export class JointTrajectoryPoint {
  static readonly _HASH = 0xdedabee6e8c2e4e8n;
  static readonly _NAME = "trajectory_msgs.JointTrajectoryPoint";

  positions_length: number;
  velocities_length: number;
  accelerations_length: number;
  effort_length: number;
  positions: number[];
  velocities: number[];
  accelerations: number[];
  effort: number[];
  time_from_start: Duration;

  constructor(init?: Partial<JointTrajectoryPoint>) {
    this.positions_length = init?.positions_length ?? 0;
    this.velocities_length = init?.velocities_length ?? 0;
    this.accelerations_length = init?.accelerations_length ?? 0;
    this.effort_length = init?.effort_length ?? 0;
    this.positions = init?.positions ?? [];
    this.velocities = init?.velocities ?? [];
    this.accelerations = init?.accelerations ?? [];
    this.effort = init?.effort ?? [];
    this.time_from_start = init?.time_from_start ?? new Duration();
  }

  static decode(data: Uint8Array): JointTrajectoryPoint {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== JointTrajectoryPoint._HASH) {
      throw new Error(`Hash mismatch: expected ${JointTrajectoryPoint._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new JointTrajectoryPoint();
    ({ offset } = JointTrajectoryPoint._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: JointTrajectoryPoint): { offset: number } {
    target.positions_length = view.getInt32(offset, false);
    offset += 4;
    target.velocities_length = view.getInt32(offset, false);
    offset += 4;
    target.accelerations_length = view.getInt32(offset, false);
    offset += 4;
    target.effort_length = view.getInt32(offset, false);
    offset += 4;
    target.positions = new Array(target.positions_length);
    for (let i0 = 0; i0 < target.positions_length; i0++) {
      target.positions[i0] = view.getFloat64(offset, false);
      offset += 8;
    }
    target.velocities = new Array(target.velocities_length);
    for (let i0 = 0; i0 < target.velocities_length; i0++) {
      target.velocities[i0] = view.getFloat64(offset, false);
      offset += 8;
    }
    target.accelerations = new Array(target.accelerations_length);
    for (let i0 = 0; i0 < target.accelerations_length; i0++) {
      target.accelerations[i0] = view.getFloat64(offset, false);
      offset += 8;
    }
    target.effort = new Array(target.effort_length);
    for (let i0 = 0; i0 < target.effort_length; i0++) {
      target.effort[i0] = view.getFloat64(offset, false);
      offset += 8;
    }
    target.time_from_start = new Duration();
    ({ offset } = Duration._decodeOne(view, offset, target.time_from_start));
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, JointTrajectoryPoint._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.positions_length, false);
    offset += 4;
    view.setInt32(offset, this.velocities_length, false);
    offset += 4;
    view.setInt32(offset, this.accelerations_length, false);
    offset += 4;
    view.setInt32(offset, this.effort_length, false);
    offset += 4;
    for (let i0 = 0; i0 < this.positions_length; i0++) {
      view.setFloat64(offset, this.positions[i0], false);
      offset += 8;
    }
    for (let i0 = 0; i0 < this.velocities_length; i0++) {
      view.setFloat64(offset, this.velocities[i0], false);
      offset += 8;
    }
    for (let i0 = 0; i0 < this.accelerations_length; i0++) {
      view.setFloat64(offset, this.accelerations[i0], false);
      offset += 8;
    }
    for (let i0 = 0; i0 < this.effort_length; i0++) {
      view.setFloat64(offset, this.effort[i0], false);
      offset += 8;
    }
    offset = this.time_from_start._encodeOne(view, offset);
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    size += 4;
    size += 4;
    size += this.positions_length * 8;
    size += this.velocities_length * 8;
    size += this.accelerations_length * 8;
    size += this.effort_length * 8;
    size += this.time_from_start._encodedSize();
    return size;
  }
}
