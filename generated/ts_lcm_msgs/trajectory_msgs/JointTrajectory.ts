// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { JointTrajectoryPoint } from "./JointTrajectoryPoint.ts";

export class JointTrajectory {
  static readonly _HASH = 0xf09db31afe774086n;
  static readonly _NAME = "trajectory_msgs.JointTrajectory";
  private static _packedFingerprint: bigint | null = null;

  joint_names_length: number;
  points_length: number;
  header: Header;
  joint_names: string[];
  points: JointTrajectoryPoint[];

  constructor(init?: Partial<JointTrajectory>) {
    this.joint_names_length = init?.joint_names_length ?? 0;
    this.points_length = init?.points_length ?? 0;
    this.header = init?.header ?? new Header();
    this.joint_names = init?.joint_names ?? [];
    this.points = init?.points ?? [];
  }

  static decode(data: Uint8Array): JointTrajectory {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = JointTrajectory._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new JointTrajectory();
    ({ offset } = JointTrajectory._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: JointTrajectory): { offset: number } {
    target.joint_names_length = view.getInt32(offset, false);
    offset += 4;
    target.points_length = view.getInt32(offset, false);
    offset += 4;
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.joint_names = new Array(target.joint_names_length);
    for (let i0 = 0; i0 < target.joint_names_length; i0++) {
      {
        const len = view.getUint32(offset, false);
        offset += 4;
        target.joint_names[i0] = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
        offset += len;
      }
    }
    target.points = new Array(target.points_length);
    for (let i0 = 0; i0 < target.points_length; i0++) {
      target.points[i0] = new JointTrajectoryPoint();
      ({ offset } = JointTrajectoryPoint._decodeOne(view, offset, target.points[i0]));
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, JointTrajectory._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.joint_names_length, false);
    offset += 4;
    view.setInt32(offset, this.points_length, false);
    offset += 4;
    offset = this.header._encodeOne(view, offset);
    for (let i0 = 0; i0 < this.joint_names_length; i0++) {
      {
        const encoded = new TextEncoder().encode(this.joint_names[i0]);
        view.setUint32(offset, encoded.length + 1, false);
        offset += 4;
        new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
        offset += encoded.length;
        view.setUint8(offset, 0); // null terminator
        offset += 1;
      }
    }
    for (let i0 = 0; i0 < this.points_length; i0++) {
      offset = this.points[i0]._encodeOne(view, offset);
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    size += this.header._encodedSize();
    for (let i0 = 0; i0 < this.joint_names_length; i0++) {
      size += 4 + new TextEncoder().encode(this.joint_names[i0]).length + 1;
    }
    for (let i0 = 0; i0 < this.points_length; i0++) {
      size += this.points[i0]._encodedSize();
    }
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(JointTrajectory)) return 0n;
    const newparents = [...parents, JointTrajectory];
    let tmphash = JointTrajectory._HASH;
    tmphash = (tmphash + Header._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + JointTrajectoryPoint._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (JointTrajectory._packedFingerprint === null) {
      JointTrajectory._packedFingerprint = JointTrajectory._getHashRecursive([]);
    }
    return JointTrajectory._packedFingerprint;
  }
}
