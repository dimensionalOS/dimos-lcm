// Auto-generated by lcm-ts. DO NOT EDIT.

import { InteractiveMarker } from "./InteractiveMarker.ts";
import { InteractiveMarkerPose } from "./InteractiveMarkerPose.ts";

export class InteractiveMarkerUpdate {
  static readonly _HASH = 0xf6eebb4c4a608b4cn;
  static readonly _NAME = "visualization_msgs.InteractiveMarkerUpdate";
  private static _packedFingerprint: bigint | null = null;

  static readonly KEEP_ALIVE = 0;
  static readonly UPDATE = 1;

  markers_length: number;
  poses_length: number;
  erases_length: number;
  server_id: string;
  seq_num: bigint;
  type: number;
  markers: InteractiveMarker[];
  poses: InteractiveMarkerPose[];
  erases: string[];

  constructor(init?: Partial<InteractiveMarkerUpdate>) {
    this.markers_length = init?.markers_length ?? 0;
    this.poses_length = init?.poses_length ?? 0;
    this.erases_length = init?.erases_length ?? 0;
    this.server_id = init?.server_id ?? "";
    this.seq_num = init?.seq_num ?? 0n;
    this.type = init?.type ?? 0;
    this.markers = init?.markers ?? [];
    this.poses = init?.poses ?? [];
    this.erases = init?.erases ?? [];
  }

  static decode(data: Uint8Array): InteractiveMarkerUpdate {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = InteractiveMarkerUpdate._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new InteractiveMarkerUpdate();
    ({ offset } = InteractiveMarkerUpdate._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: InteractiveMarkerUpdate): { offset: number } {
    target.markers_length = view.getInt32(offset, false);
    offset += 4;
    target.poses_length = view.getInt32(offset, false);
    offset += 4;
    target.erases_length = view.getInt32(offset, false);
    offset += 4;
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.server_id = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    target.seq_num = view.getBigInt64(offset, false);
    offset += 8;
    target.type = view.getUint8(offset);
    offset += 1;
    target.markers = new Array(target.markers_length);
    for (let i0 = 0; i0 < target.markers_length; i0++) {
      target.markers[i0] = new InteractiveMarker();
      ({ offset } = InteractiveMarker._decodeOne(view, offset, target.markers[i0]));
    }
    target.poses = new Array(target.poses_length);
    for (let i0 = 0; i0 < target.poses_length; i0++) {
      target.poses[i0] = new InteractiveMarkerPose();
      ({ offset } = InteractiveMarkerPose._decodeOne(view, offset, target.poses[i0]));
    }
    target.erases = new Array(target.erases_length);
    for (let i0 = 0; i0 < target.erases_length; i0++) {
      {
        const len = view.getUint32(offset, false);
        offset += 4;
        target.erases[i0] = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
        offset += len;
      }
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, InteractiveMarkerUpdate._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.markers_length, false);
    offset += 4;
    view.setInt32(offset, this.poses_length, false);
    offset += 4;
    view.setInt32(offset, this.erases_length, false);
    offset += 4;
    {
      const encoded = new TextEncoder().encode(this.server_id);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    view.setBigInt64(offset, this.seq_num, false);
    offset += 8;
    view.setUint8(offset, this.type);
    offset += 1;
    for (let i0 = 0; i0 < this.markers_length; i0++) {
      offset = this.markers[i0]._encodeOne(view, offset);
    }
    for (let i0 = 0; i0 < this.poses_length; i0++) {
      offset = this.poses[i0]._encodeOne(view, offset);
    }
    for (let i0 = 0; i0 < this.erases_length; i0++) {
      {
        const encoded = new TextEncoder().encode(this.erases[i0]);
        view.setUint32(offset, encoded.length + 1, false);
        offset += 4;
        new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
        offset += encoded.length;
        view.setUint8(offset, 0); // null terminator
        offset += 1;
      }
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    size += 4;
    size += 4 + new TextEncoder().encode(this.server_id).length + 1;
    size += 8;
    size += 1;
    for (let i0 = 0; i0 < this.markers_length; i0++) {
      size += this.markers[i0]._encodedSize();
    }
    for (let i0 = 0; i0 < this.poses_length; i0++) {
      size += this.poses[i0]._encodedSize();
    }
    for (let i0 = 0; i0 < this.erases_length; i0++) {
      size += 4 + new TextEncoder().encode(this.erases[i0]).length + 1;
    }
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(InteractiveMarkerUpdate)) return 0n;
    const newparents = [...parents, InteractiveMarkerUpdate];
    let tmphash = InteractiveMarkerUpdate._HASH;
    tmphash = (tmphash + InteractiveMarker._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + InteractiveMarkerPose._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (InteractiveMarkerUpdate._packedFingerprint === null) {
      InteractiveMarkerUpdate._packedFingerprint = InteractiveMarkerUpdate._getHashRecursive([]);
    }
    return InteractiveMarkerUpdate._packedFingerprint;
  }
}
