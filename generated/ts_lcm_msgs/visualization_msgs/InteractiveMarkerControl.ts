// Auto-generated by lcm-ts. DO NOT EDIT.

import { Quaternion } from "../geometry_msgs/Quaternion.ts";
import { Marker } from "./Marker.ts";

export class InteractiveMarkerControl {
  static readonly _HASH = 0xc6e4d2e0e8d2dedcn;
  static readonly _NAME = "visualization_msgs.InteractiveMarkerControl";

  static readonly INHERIT = 0;
  static readonly FIXED = 1;
  static readonly VIEW_FACING = 2;
  static readonly NONE = 0;
  static readonly MENU = 1;
  static readonly BUTTON = 2;
  static readonly MOVE_AXIS = 3;
  static readonly MOVE_PLANE = 4;
  static readonly ROTATE_AXIS = 5;
  static readonly MOVE_ROTATE = 6;
  static readonly MOVE_3D = 7;
  static readonly ROTATE_3D = 8;
  static readonly MOVE_ROTATE_3D = 9;

  markers_length: number;
  name: string;
  orientation: Quaternion;
  orientation_mode: number;
  interaction_mode: number;
  always_visible: boolean;
  markers: Marker[];
  independent_marker_orientation: boolean;
  description: string;

  constructor(init?: Partial<InteractiveMarkerControl>) {
    this.markers_length = init?.markers_length ?? 0;
    this.name = init?.name ?? "";
    this.orientation = init?.orientation ?? new Quaternion();
    this.orientation_mode = init?.orientation_mode ?? 0;
    this.interaction_mode = init?.interaction_mode ?? 0;
    this.always_visible = init?.always_visible ?? false;
    this.markers = init?.markers ?? [];
    this.independent_marker_orientation = init?.independent_marker_orientation ?? false;
    this.description = init?.description ?? "";
  }

  static decode(data: Uint8Array): InteractiveMarkerControl {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== InteractiveMarkerControl._HASH) {
      throw new Error(`Hash mismatch: expected ${InteractiveMarkerControl._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new InteractiveMarkerControl();
    ({ offset } = InteractiveMarkerControl._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: InteractiveMarkerControl): { offset: number } {
    target.markers_length = view.getInt32(offset, false);
    offset += 4;
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.name = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    target.orientation = new Quaternion();
    ({ offset } = Quaternion._decodeOne(view, offset, target.orientation));
    target.orientation_mode = view.getUint8(offset);
    offset += 1;
    target.interaction_mode = view.getUint8(offset);
    offset += 1;
    target.always_visible = view.getInt8(offset) !== 0;
    offset += 1;
    target.markers = new Array(target.markers_length);
    for (let i0 = 0; i0 < target.markers_length; i0++) {
      target.markers[i0] = new Marker();
      ({ offset } = Marker._decodeOne(view, offset, target.markers[i0]));
    }
    target.independent_marker_orientation = view.getInt8(offset) !== 0;
    offset += 1;
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.description = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, InteractiveMarkerControl._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.markers_length, false);
    offset += 4;
    {
      const encoded = new TextEncoder().encode(this.name);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    offset = this.orientation._encodeOne(view, offset);
    view.setUint8(offset, this.orientation_mode);
    offset += 1;
    view.setUint8(offset, this.interaction_mode);
    offset += 1;
    view.setInt8(offset, this.always_visible ? 1 : 0);
    offset += 1;
    for (let i0 = 0; i0 < this.markers_length; i0++) {
      offset = this.markers[i0]._encodeOne(view, offset);
    }
    view.setInt8(offset, this.independent_marker_orientation ? 1 : 0);
    offset += 1;
    {
      const encoded = new TextEncoder().encode(this.description);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4 + new TextEncoder().encode(this.name).length + 1;
    size += this.orientation._encodedSize();
    size += 1;
    size += 1;
    size += 1;
    for (let i0 = 0; i0 < this.markers_length; i0++) {
      size += this.markers[i0]._encodedSize();
    }
    size += 1;
    size += 4 + new TextEncoder().encode(this.description).length + 1;
    return size;
  }
}
