// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { Point } from "../geometry_msgs/Point.ts";
import { ColorRGBA } from "../std_msgs/ColorRGBA.ts";
import { Duration } from "../std_msgs/Duration.ts";

export class ImageMarker {
  static readonly _HASH = 0x3a3ea371b474d924n;
  static readonly _NAME = "visualization_msgs.ImageMarker";
  private static _packedFingerprint: bigint | null = null;

  static readonly CIRCLE = 0;
  static readonly LINE_STRIP = 1;
  static readonly LINE_LIST = 2;
  static readonly POLYGON = 3;
  static readonly POINTS = 4;
  static readonly ADD = 0;
  static readonly REMOVE = 1;

  points_length: number;
  outline_colors_length: number;
  header: Header;
  ns: string;
  id: number;
  type: number;
  action: number;
  position: Point;
  scale: number;
  outline_color: ColorRGBA;
  filled: number;
  fill_color: ColorRGBA;
  lifetime: Duration;
  points: Point[];
  outline_colors: ColorRGBA[];

  constructor(init?: Partial<ImageMarker>) {
    this.points_length = init?.points_length ?? 0;
    this.outline_colors_length = init?.outline_colors_length ?? 0;
    this.header = init?.header ?? new Header();
    this.ns = init?.ns ?? "";
    this.id = init?.id ?? 0;
    this.type = init?.type ?? 0;
    this.action = init?.action ?? 0;
    this.position = init?.position ?? new Point();
    this.scale = init?.scale ?? 0;
    this.outline_color = init?.outline_color ?? new ColorRGBA();
    this.filled = init?.filled ?? 0;
    this.fill_color = init?.fill_color ?? new ColorRGBA();
    this.lifetime = init?.lifetime ?? new Duration();
    this.points = init?.points ?? [];
    this.outline_colors = init?.outline_colors ?? [];
  }

  static decode(data: Uint8Array): ImageMarker {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = ImageMarker._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new ImageMarker();
    ({ offset } = ImageMarker._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: ImageMarker): { offset: number } {
    target.points_length = view.getInt32(offset, false);
    offset += 4;
    target.outline_colors_length = view.getInt32(offset, false);
    offset += 4;
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.ns = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    target.id = view.getInt32(offset, false);
    offset += 4;
    target.type = view.getInt32(offset, false);
    offset += 4;
    target.action = view.getInt32(offset, false);
    offset += 4;
    target.position = new Point();
    ({ offset } = Point._decodeOne(view, offset, target.position));
    target.scale = view.getFloat32(offset, false);
    offset += 4;
    target.outline_color = new ColorRGBA();
    ({ offset } = ColorRGBA._decodeOne(view, offset, target.outline_color));
    target.filled = view.getUint8(offset);
    offset += 1;
    target.fill_color = new ColorRGBA();
    ({ offset } = ColorRGBA._decodeOne(view, offset, target.fill_color));
    target.lifetime = new Duration();
    ({ offset } = Duration._decodeOne(view, offset, target.lifetime));
    target.points = new Array(target.points_length);
    for (let i0 = 0; i0 < target.points_length; i0++) {
      target.points[i0] = new Point();
      ({ offset } = Point._decodeOne(view, offset, target.points[i0]));
    }
    target.outline_colors = new Array(target.outline_colors_length);
    for (let i0 = 0; i0 < target.outline_colors_length; i0++) {
      target.outline_colors[i0] = new ColorRGBA();
      ({ offset } = ColorRGBA._decodeOne(view, offset, target.outline_colors[i0]));
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, ImageMarker._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.points_length, false);
    offset += 4;
    view.setInt32(offset, this.outline_colors_length, false);
    offset += 4;
    offset = this.header._encodeOne(view, offset);
    {
      const encoded = new TextEncoder().encode(this.ns);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    view.setInt32(offset, this.id, false);
    offset += 4;
    view.setInt32(offset, this.type, false);
    offset += 4;
    view.setInt32(offset, this.action, false);
    offset += 4;
    offset = this.position._encodeOne(view, offset);
    view.setFloat32(offset, this.scale, false);
    offset += 4;
    offset = this.outline_color._encodeOne(view, offset);
    view.setUint8(offset, this.filled);
    offset += 1;
    offset = this.fill_color._encodeOne(view, offset);
    offset = this.lifetime._encodeOne(view, offset);
    for (let i0 = 0; i0 < this.points_length; i0++) {
      offset = this.points[i0]._encodeOne(view, offset);
    }
    for (let i0 = 0; i0 < this.outline_colors_length; i0++) {
      offset = this.outline_colors[i0]._encodeOne(view, offset);
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    size += this.header._encodedSize();
    size += 4 + new TextEncoder().encode(this.ns).length + 1;
    size += 4;
    size += 4;
    size += 4;
    size += this.position._encodedSize();
    size += 4;
    size += this.outline_color._encodedSize();
    size += 1;
    size += this.fill_color._encodedSize();
    size += this.lifetime._encodedSize();
    for (let i0 = 0; i0 < this.points_length; i0++) {
      size += this.points[i0]._encodedSize();
    }
    for (let i0 = 0; i0 < this.outline_colors_length; i0++) {
      size += this.outline_colors[i0]._encodedSize();
    }
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(ImageMarker)) return 0n;
    const newparents = [...parents, ImageMarker];
    let tmphash = ImageMarker._HASH;
    tmphash = (tmphash + Header._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Point._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + ColorRGBA._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + ColorRGBA._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Duration._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Point._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + ColorRGBA._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (ImageMarker._packedFingerprint === null) {
      ImageMarker._packedFingerprint = ImageMarker._getHashRecursive([]);
    }
    return ImageMarker._packedFingerprint;
  }
}
