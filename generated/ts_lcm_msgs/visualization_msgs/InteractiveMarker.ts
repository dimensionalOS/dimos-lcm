// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { Pose } from "../geometry_msgs/Pose.ts";
import { MenuEntry } from "./MenuEntry.ts";
import { InteractiveMarkerControl } from "./InteractiveMarkerControl.ts";

export class InteractiveMarker {
  static readonly _HASH = 0xe9ed8e5f9a72545fn;
  static readonly _NAME = "visualization_msgs.InteractiveMarker";
  private static _packedFingerprint: bigint | null = null;

  menu_entries_length: number;
  controls_length: number;
  header: Header;
  pose: Pose;
  name: string;
  description: string;
  scale: number;
  menu_entries: MenuEntry[];
  controls: InteractiveMarkerControl[];

  constructor(init?: Partial<InteractiveMarker>) {
    this.menu_entries_length = init?.menu_entries_length ?? 0;
    this.controls_length = init?.controls_length ?? 0;
    this.header = init?.header ?? new Header();
    this.pose = init?.pose ?? new Pose();
    this.name = init?.name ?? "";
    this.description = init?.description ?? "";
    this.scale = init?.scale ?? 0;
    this.menu_entries = init?.menu_entries ?? [];
    this.controls = init?.controls ?? [];
  }

  static decode(data: Uint8Array): InteractiveMarker {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = InteractiveMarker._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new InteractiveMarker();
    ({ offset } = InteractiveMarker._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: InteractiveMarker): { offset: number } {
    target.menu_entries_length = view.getInt32(offset, false);
    offset += 4;
    target.controls_length = view.getInt32(offset, false);
    offset += 4;
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.pose = new Pose();
    ({ offset } = Pose._decodeOne(view, offset, target.pose));
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.name = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.description = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    target.scale = view.getFloat32(offset, false);
    offset += 4;
    target.menu_entries = new Array(target.menu_entries_length);
    for (let i0 = 0; i0 < target.menu_entries_length; i0++) {
      target.menu_entries[i0] = new MenuEntry();
      ({ offset } = MenuEntry._decodeOne(view, offset, target.menu_entries[i0]));
    }
    target.controls = new Array(target.controls_length);
    for (let i0 = 0; i0 < target.controls_length; i0++) {
      target.controls[i0] = new InteractiveMarkerControl();
      ({ offset } = InteractiveMarkerControl._decodeOne(view, offset, target.controls[i0]));
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, InteractiveMarker._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.menu_entries_length, false);
    offset += 4;
    view.setInt32(offset, this.controls_length, false);
    offset += 4;
    offset = this.header._encodeOne(view, offset);
    offset = this.pose._encodeOne(view, offset);
    {
      const encoded = new TextEncoder().encode(this.name);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    {
      const encoded = new TextEncoder().encode(this.description);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    view.setFloat32(offset, this.scale, false);
    offset += 4;
    for (let i0 = 0; i0 < this.menu_entries_length; i0++) {
      offset = this.menu_entries[i0]._encodeOne(view, offset);
    }
    for (let i0 = 0; i0 < this.controls_length; i0++) {
      offset = this.controls[i0]._encodeOne(view, offset);
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    size += this.header._encodedSize();
    size += this.pose._encodedSize();
    size += 4 + new TextEncoder().encode(this.name).length + 1;
    size += 4 + new TextEncoder().encode(this.description).length + 1;
    size += 4;
    for (let i0 = 0; i0 < this.menu_entries_length; i0++) {
      size += this.menu_entries[i0]._encodedSize();
    }
    for (let i0 = 0; i0 < this.controls_length; i0++) {
      size += this.controls[i0]._encodedSize();
    }
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(InteractiveMarker)) return 0n;
    const newparents = [...parents, InteractiveMarker];
    let tmphash = InteractiveMarker._HASH;
    tmphash = (tmphash + Header._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Pose._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + MenuEntry._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + InteractiveMarkerControl._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (InteractiveMarker._packedFingerprint === null) {
      InteractiveMarker._packedFingerprint = InteractiveMarker._getHashRecursive([]);
    }
    return InteractiveMarker._packedFingerprint;
  }
}
