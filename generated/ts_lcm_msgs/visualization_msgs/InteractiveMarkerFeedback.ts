// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { Pose } from "../geometry_msgs/Pose.ts";
import { Point } from "../geometry_msgs/Point.ts";

export class InteractiveMarkerFeedback {
  static readonly _HASH = 0xd44e20643fb0d4f7n;
  static readonly _NAME = "visualization_msgs.InteractiveMarkerFeedback";
  private static _packedFingerprint: bigint | null = null;

  static readonly KEEP_ALIVE = 0;
  static readonly POSE_UPDATE = 1;
  static readonly MENU_SELECT = 2;
  static readonly BUTTON_CLICK = 3;
  static readonly MOUSE_DOWN = 4;
  static readonly MOUSE_UP = 5;

  header: Header;
  client_id: string;
  marker_name: string;
  control_name: string;
  event_type: number;
  pose: Pose;
  menu_entry_id: number;
  mouse_point: Point;
  mouse_point_valid: boolean;

  constructor(init?: Partial<InteractiveMarkerFeedback>) {
    this.header = init?.header ?? new Header();
    this.client_id = init?.client_id ?? "";
    this.marker_name = init?.marker_name ?? "";
    this.control_name = init?.control_name ?? "";
    this.event_type = init?.event_type ?? 0;
    this.pose = init?.pose ?? new Pose();
    this.menu_entry_id = init?.menu_entry_id ?? 0;
    this.mouse_point = init?.mouse_point ?? new Point();
    this.mouse_point_valid = init?.mouse_point_valid ?? false;
  }

  static decode(data: Uint8Array): InteractiveMarkerFeedback {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = InteractiveMarkerFeedback._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new InteractiveMarkerFeedback();
    ({ offset } = InteractiveMarkerFeedback._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: InteractiveMarkerFeedback): { offset: number } {
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.client_id = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.marker_name = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.control_name = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    target.event_type = view.getUint8(offset);
    offset += 1;
    target.pose = new Pose();
    ({ offset } = Pose._decodeOne(view, offset, target.pose));
    target.menu_entry_id = view.getInt32(offset, false);
    offset += 4;
    target.mouse_point = new Point();
    ({ offset } = Point._decodeOne(view, offset, target.mouse_point));
    target.mouse_point_valid = view.getInt8(offset) !== 0;
    offset += 1;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, InteractiveMarkerFeedback._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.header._encodeOne(view, offset);
    {
      const encoded = new TextEncoder().encode(this.client_id);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    {
      const encoded = new TextEncoder().encode(this.marker_name);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    {
      const encoded = new TextEncoder().encode(this.control_name);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    view.setUint8(offset, this.event_type);
    offset += 1;
    offset = this.pose._encodeOne(view, offset);
    view.setInt32(offset, this.menu_entry_id, false);
    offset += 4;
    offset = this.mouse_point._encodeOne(view, offset);
    view.setInt8(offset, this.mouse_point_valid ? 1 : 0);
    offset += 1;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.header._encodedSize();
    size += 4 + new TextEncoder().encode(this.client_id).length + 1;
    size += 4 + new TextEncoder().encode(this.marker_name).length + 1;
    size += 4 + new TextEncoder().encode(this.control_name).length + 1;
    size += 1;
    size += this.pose._encodedSize();
    size += 4;
    size += this.mouse_point._encodedSize();
    size += 1;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(InteractiveMarkerFeedback)) return 0n;
    const newparents = [...parents, InteractiveMarkerFeedback];
    let tmphash = InteractiveMarkerFeedback._HASH;
    tmphash = (tmphash + Header._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Pose._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Point._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (InteractiveMarkerFeedback._packedFingerprint === null) {
      InteractiveMarkerFeedback._packedFingerprint = InteractiveMarkerFeedback._getHashRecursive([]);
    }
    return InteractiveMarkerFeedback._packedFingerprint;
  }
}
