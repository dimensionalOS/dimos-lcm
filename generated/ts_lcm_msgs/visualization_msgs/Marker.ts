// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { Pose } from "../geometry_msgs/Pose.ts";
import { Vector3 } from "../geometry_msgs/Vector3.ts";
import { ColorRGBA } from "../std_msgs/ColorRGBA.ts";
import { Duration } from "../std_msgs/Duration.ts";
import { Point } from "../geometry_msgs/Point.ts";

export class Marker {
  static readonly _HASH = 0xc2e8cae4d2c2d8e6n;
  static readonly _NAME = "visualization_msgs.Marker";

  static readonly ARROW = 0;
  static readonly CUBE = 1;
  static readonly SPHERE = 2;
  static readonly CYLINDER = 3;
  static readonly LINE_STRIP = 4;
  static readonly LINE_LIST = 5;
  static readonly CUBE_LIST = 6;
  static readonly SPHERE_LIST = 7;
  static readonly POINTS = 8;
  static readonly TEXT_VIEW_FACING = 9;
  static readonly MESH_RESOURCE = 10;
  static readonly TRIANGLE_LIST = 11;
  static readonly ADD = 0;
  static readonly MODIFY = 0;
  static readonly DELETE = 2;
  static readonly DELETEALL = 3;

  points_length: number;
  colors_length: number;
  header: Header;
  ns: string;
  id: number;
  type: number;
  action: number;
  pose: Pose;
  scale: Vector3;
  color: ColorRGBA;
  lifetime: Duration;
  frame_locked: boolean;
  points: Point[];
  colors: ColorRGBA[];
  text: string;
  mesh_resource: string;
  mesh_use_embedded_materials: boolean;

  constructor(init?: Partial<Marker>) {
    this.points_length = init?.points_length ?? 0;
    this.colors_length = init?.colors_length ?? 0;
    this.header = init?.header ?? new Header();
    this.ns = init?.ns ?? "";
    this.id = init?.id ?? 0;
    this.type = init?.type ?? 0;
    this.action = init?.action ?? 0;
    this.pose = init?.pose ?? new Pose();
    this.scale = init?.scale ?? new Vector3();
    this.color = init?.color ?? new ColorRGBA();
    this.lifetime = init?.lifetime ?? new Duration();
    this.frame_locked = init?.frame_locked ?? false;
    this.points = init?.points ?? [];
    this.colors = init?.colors ?? [];
    this.text = init?.text ?? "";
    this.mesh_resource = init?.mesh_resource ?? "";
    this.mesh_use_embedded_materials = init?.mesh_use_embedded_materials ?? false;
  }

  static decode(data: Uint8Array): Marker {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== Marker._HASH) {
      throw new Error(`Hash mismatch: expected ${Marker._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Marker();
    ({ offset } = Marker._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Marker): { offset: number } {
    target.points_length = view.getInt32(offset, false);
    offset += 4;
    target.colors_length = view.getInt32(offset, false);
    offset += 4;
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.ns = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    target.id = view.getInt32(offset, false);
    offset += 4;
    target.type = view.getInt32(offset, false);
    offset += 4;
    target.action = view.getInt32(offset, false);
    offset += 4;
    target.pose = new Pose();
    ({ offset } = Pose._decodeOne(view, offset, target.pose));
    target.scale = new Vector3();
    ({ offset } = Vector3._decodeOne(view, offset, target.scale));
    target.color = new ColorRGBA();
    ({ offset } = ColorRGBA._decodeOne(view, offset, target.color));
    target.lifetime = new Duration();
    ({ offset } = Duration._decodeOne(view, offset, target.lifetime));
    target.frame_locked = view.getInt8(offset) !== 0;
    offset += 1;
    target.points = new Array(target.points_length);
    for (let i0 = 0; i0 < target.points_length; i0++) {
      target.points[i0] = new Point();
      ({ offset } = Point._decodeOne(view, offset, target.points[i0]));
    }
    target.colors = new Array(target.colors_length);
    for (let i0 = 0; i0 < target.colors_length; i0++) {
      target.colors[i0] = new ColorRGBA();
      ({ offset } = ColorRGBA._decodeOne(view, offset, target.colors[i0]));
    }
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.text = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.mesh_resource = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    target.mesh_use_embedded_materials = view.getInt8(offset) !== 0;
    offset += 1;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, Marker._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.points_length, false);
    offset += 4;
    view.setInt32(offset, this.colors_length, false);
    offset += 4;
    offset = this.header._encodeOne(view, offset);
    {
      const encoded = new TextEncoder().encode(this.ns);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    view.setInt32(offset, this.id, false);
    offset += 4;
    view.setInt32(offset, this.type, false);
    offset += 4;
    view.setInt32(offset, this.action, false);
    offset += 4;
    offset = this.pose._encodeOne(view, offset);
    offset = this.scale._encodeOne(view, offset);
    offset = this.color._encodeOne(view, offset);
    offset = this.lifetime._encodeOne(view, offset);
    view.setInt8(offset, this.frame_locked ? 1 : 0);
    offset += 1;
    for (let i0 = 0; i0 < this.points_length; i0++) {
      offset = this.points[i0]._encodeOne(view, offset);
    }
    for (let i0 = 0; i0 < this.colors_length; i0++) {
      offset = this.colors[i0]._encodeOne(view, offset);
    }
    {
      const encoded = new TextEncoder().encode(this.text);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    {
      const encoded = new TextEncoder().encode(this.mesh_resource);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    view.setInt8(offset, this.mesh_use_embedded_materials ? 1 : 0);
    offset += 1;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    size += this.header._encodedSize();
    size += 4 + new TextEncoder().encode(this.ns).length + 1;
    size += 4;
    size += 4;
    size += 4;
    size += this.pose._encodedSize();
    size += this.scale._encodedSize();
    size += this.color._encodedSize();
    size += this.lifetime._encodedSize();
    size += 1;
    for (let i0 = 0; i0 < this.points_length; i0++) {
      size += this.points[i0]._encodedSize();
    }
    for (let i0 = 0; i0 < this.colors_length; i0++) {
      size += this.colors[i0]._encodedSize();
    }
    size += 4 + new TextEncoder().encode(this.text).length + 1;
    size += 4 + new TextEncoder().encode(this.mesh_resource).length + 1;
    size += 1;
    return size;
  }
}
