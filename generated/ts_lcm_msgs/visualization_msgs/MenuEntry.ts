// Auto-generated by lcm-ts. DO NOT EDIT.

export class MenuEntry {
  static readonly _HASH = 0x667b2d15ef03e972n;
  static readonly _NAME = "visualization_msgs.MenuEntry";
  private static _packedFingerprint: bigint | null = null;

  static readonly FEEDBACK = 0;
  static readonly ROSRUN = 1;
  static readonly ROSLAUNCH = 2;

  id: number;
  parent_id: number;
  title: string;
  command: string;
  command_type: number;

  constructor(init?: Partial<MenuEntry>) {
    this.id = init?.id ?? 0;
    this.parent_id = init?.parent_id ?? 0;
    this.title = init?.title ?? "";
    this.command = init?.command ?? "";
    this.command_type = init?.command_type ?? 0;
  }

  static decode(data: Uint8Array): MenuEntry {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = MenuEntry._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new MenuEntry();
    ({ offset } = MenuEntry._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: MenuEntry): { offset: number } {
    target.id = view.getInt32(offset, false);
    offset += 4;
    target.parent_id = view.getInt32(offset, false);
    offset += 4;
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.title = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.command = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    target.command_type = view.getUint8(offset);
    offset += 1;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, MenuEntry._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.id, false);
    offset += 4;
    view.setInt32(offset, this.parent_id, false);
    offset += 4;
    {
      const encoded = new TextEncoder().encode(this.title);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    {
      const encoded = new TextEncoder().encode(this.command);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    view.setUint8(offset, this.command_type);
    offset += 1;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    size += 4 + new TextEncoder().encode(this.title).length + 1;
    size += 4 + new TextEncoder().encode(this.command).length + 1;
    size += 1;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(MenuEntry)) return 0n;
    const newparents = [...parents, MenuEntry];
    let tmphash = MenuEntry._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (MenuEntry._packedFingerprint === null) {
      MenuEntry._packedFingerprint = MenuEntry._getHashRecursive([]);
    }
    return MenuEntry._packedFingerprint;
  }
}
