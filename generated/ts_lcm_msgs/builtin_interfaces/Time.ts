// Auto-generated by lcm-ts. DO NOT EDIT.

export class Time {
  static readonly _HASH = 0x263f39efa2a9af63n;
  static readonly _NAME = "builtin_interfaces.Time";
  private static _packedFingerprint: bigint | null = null;

  sec: number;
  nanosec: number;

  constructor(init?: Partial<Time>) {
    this.sec = init?.sec ?? 0;
    this.nanosec = init?.nanosec ?? 0;
  }

  static decode(data: Uint8Array): Time {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = Time._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Time();
    ({ offset } = Time._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Time): { offset: number } {
    target.sec = view.getInt32(offset, false);
    offset += 4;
    target.nanosec = view.getInt32(offset, false);
    offset += 4;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, Time._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.sec, false);
    offset += 4;
    view.setInt32(offset, this.nanosec, false);
    offset += 4;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(Time)) return 0n;
    const newparents = [...parents, Time];
    let tmphash = Time._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (Time._packedFingerprint === null) {
      Time._packedFingerprint = Time._getHashRecursive([]);
    }
    return Time._packedFingerprint;
  }
}
