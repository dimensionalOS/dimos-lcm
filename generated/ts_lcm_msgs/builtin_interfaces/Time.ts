// Auto-generated by lcm-ts. DO NOT EDIT.

export class Time {
  static readonly _HASH = 0xe8dcc2dcdee6cac6n;
  static readonly _NAME = "builtin_interfaces.Time";

  sec: number;
  nanosec: number;

  constructor(init?: Partial<Time>) {
    this.sec = init?.sec ?? 0;
    this.nanosec = init?.nanosec ?? 0;
  }

  static decode(data: Uint8Array): Time {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== Time._HASH) {
      throw new Error(`Hash mismatch: expected ${Time._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Time();
    ({ offset } = Time._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Time): { offset: number } {
    target.sec = view.getInt32(offset, false);
    offset += 4;
    target.nanosec = view.getInt32(offset, false);
    offset += 4;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, Time._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.sec, false);
    offset += 4;
    view.setInt32(offset, this.nanosec, false);
    offset += 4;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    return size;
  }
}
