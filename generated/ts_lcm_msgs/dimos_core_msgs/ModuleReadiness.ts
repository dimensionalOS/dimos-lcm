// Auto-generated by lcm-ts. DO NOT EDIT.

export class ModuleReadiness {
  static readonly _HASH = 0x150d1bfa7d87808dn;
  static readonly _NAME = "dimos_core_msgs.ModuleReadiness";
  private static _packedFingerprint: bigint | null = null;

  source: string;
  generation: bigint;
  sequence: bigint;
  source_ts: number;
  ready: boolean;
  reason: string;

  constructor(init?: Partial<ModuleReadiness>) {
    this.source = init?.source ?? "";
    this.generation = init?.generation ?? 0n;
    this.sequence = init?.sequence ?? 0n;
    this.source_ts = init?.source_ts ?? 0;
    this.ready = init?.ready ?? false;
    this.reason = init?.reason ?? "";
  }

  static decode(data: Uint8Array): ModuleReadiness {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = ModuleReadiness._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new ModuleReadiness();
    ({ offset } = ModuleReadiness._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: ModuleReadiness): { offset: number } {
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.source = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    target.generation = view.getBigInt64(offset, false);
    offset += 8;
    target.sequence = view.getBigInt64(offset, false);
    offset += 8;
    target.source_ts = view.getFloat64(offset, false);
    offset += 8;
    target.ready = view.getInt8(offset) !== 0;
    offset += 1;
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.reason = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, ModuleReadiness._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    {
      const encoded = new TextEncoder().encode(this.source);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    view.setBigInt64(offset, this.generation, false);
    offset += 8;
    view.setBigInt64(offset, this.sequence, false);
    offset += 8;
    view.setFloat64(offset, this.source_ts, false);
    offset += 8;
    view.setInt8(offset, this.ready ? 1 : 0);
    offset += 1;
    {
      const encoded = new TextEncoder().encode(this.reason);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4 + new TextEncoder().encode(this.source).length + 1;
    size += 8;
    size += 8;
    size += 8;
    size += 1;
    size += 4 + new TextEncoder().encode(this.reason).length + 1;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(ModuleReadiness)) return 0n;
    const newparents = [...parents, ModuleReadiness];
    let tmphash = ModuleReadiness._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (ModuleReadiness._packedFingerprint === null) {
      ModuleReadiness._packedFingerprint = ModuleReadiness._getHashRecursive([]);
    }
    return ModuleReadiness._packedFingerprint;
  }
}
