// Auto-generated by lcm-ts. DO NOT EDIT.

import { ResourceDescription } from "./ResourceDescription.ts";
import { InterfaceDescription } from "./InterfaceDescription.ts";
import { CommandProfile } from "./CommandProfile.ts";

export class ConnectionDescription {
  static readonly _HASH = 0x31d30851f65c7d8cn;
  static readonly _NAME = "dimos_control_msgs.ConnectionDescription";
  private static _packedFingerprint: bigint | null = null;

  static readonly DIRECT = 1;
  static readonly OPERATOR_CONFIRMED = 2;
  static readonly APPLY_OMISSION = 1;
  static readonly ARMED_IDLE_SAFE_STOP = 2;
  static readonly NATIVE_WATCHDOG = 1;
  static readonly EXTERNAL_SUPERVISOR = 2;
  static readonly INTRINSICALLY_SAFE = 3;
  static readonly SIMULATION = 4;
  static readonly UNPROTECTED = 5;

  source: string;
  description_epoch: bigint;
  resources_length: number;
  interfaces_length: number;
  command_profiles_length: number;
  resources: ResourceDescription[];
  interfaces: InterfaceDescription[];
  command_profiles: CommandProfile[];
  expected_state_rate_hz: number;
  state_stale_timeout_s: number;
  command_watchdog_timeout_s: number;
  activation_policy: number;
  armed_idle_policy: number;
  safe_stop_behavior: string;
  process_loss_classification: number;
  process_loss_evidence: string;

  constructor(init?: Partial<ConnectionDescription>) {
    this.source = init?.source ?? "";
    this.description_epoch = init?.description_epoch ?? 0n;
    this.resources_length = init?.resources_length ?? 0;
    this.interfaces_length = init?.interfaces_length ?? 0;
    this.command_profiles_length = init?.command_profiles_length ?? 0;
    this.resources = init?.resources ?? [];
    this.interfaces = init?.interfaces ?? [];
    this.command_profiles = init?.command_profiles ?? [];
    this.expected_state_rate_hz = init?.expected_state_rate_hz ?? 0;
    this.state_stale_timeout_s = init?.state_stale_timeout_s ?? 0;
    this.command_watchdog_timeout_s = init?.command_watchdog_timeout_s ?? 0;
    this.activation_policy = init?.activation_policy ?? 0;
    this.armed_idle_policy = init?.armed_idle_policy ?? 0;
    this.safe_stop_behavior = init?.safe_stop_behavior ?? "";
    this.process_loss_classification = init?.process_loss_classification ?? 0;
    this.process_loss_evidence = init?.process_loss_evidence ?? "";
  }

  static decode(data: Uint8Array): ConnectionDescription {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = ConnectionDescription._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new ConnectionDescription();
    ({ offset } = ConnectionDescription._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: ConnectionDescription): { offset: number } {
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.source = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    target.description_epoch = view.getBigInt64(offset, false);
    offset += 8;
    target.resources_length = view.getInt32(offset, false);
    offset += 4;
    target.interfaces_length = view.getInt32(offset, false);
    offset += 4;
    target.command_profiles_length = view.getInt32(offset, false);
    offset += 4;
    target.resources = new Array(target.resources_length);
    for (let i0 = 0; i0 < target.resources_length; i0++) {
      target.resources[i0] = new ResourceDescription();
      ({ offset } = ResourceDescription._decodeOne(view, offset, target.resources[i0]));
    }
    target.interfaces = new Array(target.interfaces_length);
    for (let i0 = 0; i0 < target.interfaces_length; i0++) {
      target.interfaces[i0] = new InterfaceDescription();
      ({ offset } = InterfaceDescription._decodeOne(view, offset, target.interfaces[i0]));
    }
    target.command_profiles = new Array(target.command_profiles_length);
    for (let i0 = 0; i0 < target.command_profiles_length; i0++) {
      target.command_profiles[i0] = new CommandProfile();
      ({ offset } = CommandProfile._decodeOne(view, offset, target.command_profiles[i0]));
    }
    target.expected_state_rate_hz = view.getFloat64(offset, false);
    offset += 8;
    target.state_stale_timeout_s = view.getFloat64(offset, false);
    offset += 8;
    target.command_watchdog_timeout_s = view.getFloat64(offset, false);
    offset += 8;
    target.activation_policy = view.getInt8(offset);
    offset += 1;
    target.armed_idle_policy = view.getInt8(offset);
    offset += 1;
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.safe_stop_behavior = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    target.process_loss_classification = view.getInt8(offset);
    offset += 1;
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.process_loss_evidence = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, ConnectionDescription._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    {
      const encoded = new TextEncoder().encode(this.source);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    view.setBigInt64(offset, this.description_epoch, false);
    offset += 8;
    view.setInt32(offset, this.resources_length, false);
    offset += 4;
    view.setInt32(offset, this.interfaces_length, false);
    offset += 4;
    view.setInt32(offset, this.command_profiles_length, false);
    offset += 4;
    for (let i0 = 0; i0 < this.resources_length; i0++) {
      offset = this.resources[i0]._encodeOne(view, offset);
    }
    for (let i0 = 0; i0 < this.interfaces_length; i0++) {
      offset = this.interfaces[i0]._encodeOne(view, offset);
    }
    for (let i0 = 0; i0 < this.command_profiles_length; i0++) {
      offset = this.command_profiles[i0]._encodeOne(view, offset);
    }
    view.setFloat64(offset, this.expected_state_rate_hz, false);
    offset += 8;
    view.setFloat64(offset, this.state_stale_timeout_s, false);
    offset += 8;
    view.setFloat64(offset, this.command_watchdog_timeout_s, false);
    offset += 8;
    view.setInt8(offset, this.activation_policy);
    offset += 1;
    view.setInt8(offset, this.armed_idle_policy);
    offset += 1;
    {
      const encoded = new TextEncoder().encode(this.safe_stop_behavior);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    view.setInt8(offset, this.process_loss_classification);
    offset += 1;
    {
      const encoded = new TextEncoder().encode(this.process_loss_evidence);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4 + new TextEncoder().encode(this.source).length + 1;
    size += 8;
    size += 4;
    size += 4;
    size += 4;
    for (let i0 = 0; i0 < this.resources_length; i0++) {
      size += this.resources[i0]._encodedSize();
    }
    for (let i0 = 0; i0 < this.interfaces_length; i0++) {
      size += this.interfaces[i0]._encodedSize();
    }
    for (let i0 = 0; i0 < this.command_profiles_length; i0++) {
      size += this.command_profiles[i0]._encodedSize();
    }
    size += 8;
    size += 8;
    size += 8;
    size += 1;
    size += 1;
    size += 4 + new TextEncoder().encode(this.safe_stop_behavior).length + 1;
    size += 1;
    size += 4 + new TextEncoder().encode(this.process_loss_evidence).length + 1;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(ConnectionDescription)) return 0n;
    const newparents = [...parents, ConnectionDescription];
    let tmphash = ConnectionDescription._HASH;
    tmphash = (tmphash + ResourceDescription._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + InterfaceDescription._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + CommandProfile._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (ConnectionDescription._packedFingerprint === null) {
      ConnectionDescription._packedFingerprint = ConnectionDescription._getHashRecursive([]);
    }
    return ConnectionDescription._packedFingerprint;
  }
}
