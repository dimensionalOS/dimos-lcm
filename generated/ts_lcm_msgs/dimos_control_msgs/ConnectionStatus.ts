// Auto-generated by lcm-ts. DO NOT EDIT.

export class ConnectionStatus {
  static readonly _HASH = 0xc90f95ebfad6e276n;
  static readonly _NAME = "dimos_control_msgs.ConnectionStatus";
  private static _packedFingerprint: bigint | null = null;

  static readonly STANDBY = 1;
  static readonly PREPARING = 2;
  static readonly PREPARED = 3;
  static readonly COMMITTING = 4;
  static readonly ARMED = 5;
  static readonly SAFE_STOPPED = 6;
  static readonly ESTOPPED = 7;
  static readonly NONE = 0;
  static readonly PENDING = 1;
  static readonly SUCCEEDED = 2;
  static readonly REJECTED = 3;

  source: string;
  source_ts: number;
  lifecycle_state: number;
  description_epoch: bigint;
  control_epoch: bigint;
  last_operation_id: bigint;
  last_operation: number;
  acknowledgement: number;
  fault: string;

  constructor(init?: Partial<ConnectionStatus>) {
    this.source = init?.source ?? "";
    this.source_ts = init?.source_ts ?? 0;
    this.lifecycle_state = init?.lifecycle_state ?? 0;
    this.description_epoch = init?.description_epoch ?? 0n;
    this.control_epoch = init?.control_epoch ?? 0n;
    this.last_operation_id = init?.last_operation_id ?? 0n;
    this.last_operation = init?.last_operation ?? 0;
    this.acknowledgement = init?.acknowledgement ?? 0;
    this.fault = init?.fault ?? "";
  }

  static decode(data: Uint8Array): ConnectionStatus {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = ConnectionStatus._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new ConnectionStatus();
    ({ offset } = ConnectionStatus._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: ConnectionStatus): { offset: number } {
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.source = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    target.source_ts = view.getFloat64(offset, false);
    offset += 8;
    target.lifecycle_state = view.getInt8(offset);
    offset += 1;
    target.description_epoch = view.getBigInt64(offset, false);
    offset += 8;
    target.control_epoch = view.getBigInt64(offset, false);
    offset += 8;
    target.last_operation_id = view.getBigInt64(offset, false);
    offset += 8;
    target.last_operation = view.getInt8(offset);
    offset += 1;
    target.acknowledgement = view.getInt8(offset);
    offset += 1;
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.fault = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, ConnectionStatus._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    {
      const encoded = new TextEncoder().encode(this.source);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    view.setFloat64(offset, this.source_ts, false);
    offset += 8;
    view.setInt8(offset, this.lifecycle_state);
    offset += 1;
    view.setBigInt64(offset, this.description_epoch, false);
    offset += 8;
    view.setBigInt64(offset, this.control_epoch, false);
    offset += 8;
    view.setBigInt64(offset, this.last_operation_id, false);
    offset += 8;
    view.setInt8(offset, this.last_operation);
    offset += 1;
    view.setInt8(offset, this.acknowledgement);
    offset += 1;
    {
      const encoded = new TextEncoder().encode(this.fault);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4 + new TextEncoder().encode(this.source).length + 1;
    size += 8;
    size += 1;
    size += 8;
    size += 8;
    size += 8;
    size += 1;
    size += 1;
    size += 4 + new TextEncoder().encode(this.fault).length + 1;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(ConnectionStatus)) return 0n;
    const newparents = [...parents, ConnectionStatus];
    let tmphash = ConnectionStatus._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (ConnectionStatus._packedFingerprint === null) {
      ConnectionStatus._packedFingerprint = ConnectionStatus._getHashRecursive([]);
    }
    return ConnectionStatus._packedFingerprint;
  }
}
