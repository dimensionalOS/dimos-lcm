// Auto-generated by lcm-ts. DO NOT EDIT.

export class InterfaceDescription {
  static readonly _HASH = 0xbfb21e12c9a9bd0dn;
  static readonly _NAME = "dimos_control_msgs.InterfaceDescription";
  private static _packedFingerprint: bigint | null = null;

  static readonly HOLD_LAST = 1;
  static readonly ZERO = 2;
  static readonly SAFE_STOP = 3;

  name: string;
  resource: string;
  field: string;
  unit: string;
  state: boolean;
  command: boolean;
  has_hard_min: boolean;
  hard_min: number;
  has_hard_max: boolean;
  hard_max: number;
  has_soft_min: boolean;
  soft_min: number;
  has_soft_max: boolean;
  soft_max: number;
  omission_policy: number;

  constructor(init?: Partial<InterfaceDescription>) {
    this.name = init?.name ?? "";
    this.resource = init?.resource ?? "";
    this.field = init?.field ?? "";
    this.unit = init?.unit ?? "";
    this.state = init?.state ?? false;
    this.command = init?.command ?? false;
    this.has_hard_min = init?.has_hard_min ?? false;
    this.hard_min = init?.hard_min ?? 0;
    this.has_hard_max = init?.has_hard_max ?? false;
    this.hard_max = init?.hard_max ?? 0;
    this.has_soft_min = init?.has_soft_min ?? false;
    this.soft_min = init?.soft_min ?? 0;
    this.has_soft_max = init?.has_soft_max ?? false;
    this.soft_max = init?.soft_max ?? 0;
    this.omission_policy = init?.omission_policy ?? 0;
  }

  static decode(data: Uint8Array): InterfaceDescription {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = InterfaceDescription._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new InterfaceDescription();
    ({ offset } = InterfaceDescription._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: InterfaceDescription): { offset: number } {
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.name = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.resource = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.field = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.unit = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    target.state = view.getInt8(offset) !== 0;
    offset += 1;
    target.command = view.getInt8(offset) !== 0;
    offset += 1;
    target.has_hard_min = view.getInt8(offset) !== 0;
    offset += 1;
    target.hard_min = view.getFloat64(offset, false);
    offset += 8;
    target.has_hard_max = view.getInt8(offset) !== 0;
    offset += 1;
    target.hard_max = view.getFloat64(offset, false);
    offset += 8;
    target.has_soft_min = view.getInt8(offset) !== 0;
    offset += 1;
    target.soft_min = view.getFloat64(offset, false);
    offset += 8;
    target.has_soft_max = view.getInt8(offset) !== 0;
    offset += 1;
    target.soft_max = view.getFloat64(offset, false);
    offset += 8;
    target.omission_policy = view.getInt8(offset);
    offset += 1;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, InterfaceDescription._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    {
      const encoded = new TextEncoder().encode(this.name);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    {
      const encoded = new TextEncoder().encode(this.resource);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    {
      const encoded = new TextEncoder().encode(this.field);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    {
      const encoded = new TextEncoder().encode(this.unit);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    view.setInt8(offset, this.state ? 1 : 0);
    offset += 1;
    view.setInt8(offset, this.command ? 1 : 0);
    offset += 1;
    view.setInt8(offset, this.has_hard_min ? 1 : 0);
    offset += 1;
    view.setFloat64(offset, this.hard_min, false);
    offset += 8;
    view.setInt8(offset, this.has_hard_max ? 1 : 0);
    offset += 1;
    view.setFloat64(offset, this.hard_max, false);
    offset += 8;
    view.setInt8(offset, this.has_soft_min ? 1 : 0);
    offset += 1;
    view.setFloat64(offset, this.soft_min, false);
    offset += 8;
    view.setInt8(offset, this.has_soft_max ? 1 : 0);
    offset += 1;
    view.setFloat64(offset, this.soft_max, false);
    offset += 8;
    view.setInt8(offset, this.omission_policy);
    offset += 1;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4 + new TextEncoder().encode(this.name).length + 1;
    size += 4 + new TextEncoder().encode(this.resource).length + 1;
    size += 4 + new TextEncoder().encode(this.field).length + 1;
    size += 4 + new TextEncoder().encode(this.unit).length + 1;
    size += 1;
    size += 1;
    size += 1;
    size += 8;
    size += 1;
    size += 8;
    size += 1;
    size += 8;
    size += 1;
    size += 8;
    size += 1;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(InterfaceDescription)) return 0n;
    const newparents = [...parents, InterfaceDescription];
    let tmphash = InterfaceDescription._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (InterfaceDescription._packedFingerprint === null) {
      InterfaceDescription._packedFingerprint = InterfaceDescription._getHashRecursive([]);
    }
    return InterfaceDescription._packedFingerprint;
  }
}
