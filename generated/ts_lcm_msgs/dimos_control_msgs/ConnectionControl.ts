// Auto-generated by lcm-ts. DO NOT EDIT.

export class ConnectionControl {
  static readonly _HASH = 0xb82d9643472f465dn;
  static readonly _NAME = "dimos_control_msgs.ConnectionControl";
  private static _packedFingerprint: bigint | null = null;

  static readonly PREPARE_ARM = 1;
  static readonly COMMIT_ARM = 2;
  static readonly ABORT_ARM = 3;
  static readonly SAFE_STOP = 4;
  static readonly CLEAR_SAFE_STOP = 5;
  static readonly ESTOP = 6;
  static readonly CLEAR_ESTOP = 7;

  target_sources_length: number;
  target_sources: string[];
  operation_id: bigint;
  operation: number;
  proposed_control_epoch: bigint;
  reason: string;

  constructor(init?: Partial<ConnectionControl>) {
    this.target_sources_length = init?.target_sources_length ?? 0;
    this.target_sources = init?.target_sources ?? [];
    this.operation_id = init?.operation_id ?? 0n;
    this.operation = init?.operation ?? 0;
    this.proposed_control_epoch = init?.proposed_control_epoch ?? 0n;
    this.reason = init?.reason ?? "";
  }

  static decode(data: Uint8Array): ConnectionControl {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = ConnectionControl._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new ConnectionControl();
    ({ offset } = ConnectionControl._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: ConnectionControl): { offset: number } {
    target.target_sources_length = view.getInt32(offset, false);
    offset += 4;
    target.target_sources = new Array(target.target_sources_length);
    for (let i0 = 0; i0 < target.target_sources_length; i0++) {
      {
        const len = view.getUint32(offset, false);
        offset += 4;
        target.target_sources[i0] = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
        offset += len;
      }
    }
    target.operation_id = view.getBigInt64(offset, false);
    offset += 8;
    target.operation = view.getInt8(offset);
    offset += 1;
    target.proposed_control_epoch = view.getBigInt64(offset, false);
    offset += 8;
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.reason = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, ConnectionControl._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.target_sources_length, false);
    offset += 4;
    for (let i0 = 0; i0 < this.target_sources_length; i0++) {
      {
        const encoded = new TextEncoder().encode(this.target_sources[i0]);
        view.setUint32(offset, encoded.length + 1, false);
        offset += 4;
        new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
        offset += encoded.length;
        view.setUint8(offset, 0); // null terminator
        offset += 1;
      }
    }
    view.setBigInt64(offset, this.operation_id, false);
    offset += 8;
    view.setInt8(offset, this.operation);
    offset += 1;
    view.setBigInt64(offset, this.proposed_control_epoch, false);
    offset += 8;
    {
      const encoded = new TextEncoder().encode(this.reason);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    for (let i0 = 0; i0 < this.target_sources_length; i0++) {
      size += 4 + new TextEncoder().encode(this.target_sources[i0]).length + 1;
    }
    size += 8;
    size += 1;
    size += 8;
    size += 4 + new TextEncoder().encode(this.reason).length + 1;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(ConnectionControl)) return 0n;
    const newparents = [...parents, ConnectionControl];
    let tmphash = ConnectionControl._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (ConnectionControl._packedFingerprint === null) {
      ConnectionControl._packedFingerprint = ConnectionControl._getHashRecursive([]);
    }
    return ConnectionControl._packedFingerprint;
  }
}
