// Auto-generated by lcm-ts. DO NOT EDIT.

export class CommandProfile {
  static readonly _HASH = 0x38538e7a9d86eb4an;
  static readonly _NAME = "dimos_control_msgs.CommandProfile";
  private static _packedFingerprint: bigint | null = null;

  name: string;
  required_fields_length: number;
  optional_fields_length: number;
  required_fields: string[];
  optional_fields: string[];
  uniform_across_resources: boolean;

  constructor(init?: Partial<CommandProfile>) {
    this.name = init?.name ?? "";
    this.required_fields_length = init?.required_fields_length ?? 0;
    this.optional_fields_length = init?.optional_fields_length ?? 0;
    this.required_fields = init?.required_fields ?? [];
    this.optional_fields = init?.optional_fields ?? [];
    this.uniform_across_resources = init?.uniform_across_resources ?? false;
  }

  static decode(data: Uint8Array): CommandProfile {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = CommandProfile._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new CommandProfile();
    ({ offset } = CommandProfile._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: CommandProfile): { offset: number } {
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.name = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    target.required_fields_length = view.getInt32(offset, false);
    offset += 4;
    target.optional_fields_length = view.getInt32(offset, false);
    offset += 4;
    target.required_fields = new Array(target.required_fields_length);
    for (let i0 = 0; i0 < target.required_fields_length; i0++) {
      {
        const len = view.getUint32(offset, false);
        offset += 4;
        target.required_fields[i0] = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
        offset += len;
      }
    }
    target.optional_fields = new Array(target.optional_fields_length);
    for (let i0 = 0; i0 < target.optional_fields_length; i0++) {
      {
        const len = view.getUint32(offset, false);
        offset += 4;
        target.optional_fields[i0] = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
        offset += len;
      }
    }
    target.uniform_across_resources = view.getInt8(offset) !== 0;
    offset += 1;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, CommandProfile._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    {
      const encoded = new TextEncoder().encode(this.name);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    view.setInt32(offset, this.required_fields_length, false);
    offset += 4;
    view.setInt32(offset, this.optional_fields_length, false);
    offset += 4;
    for (let i0 = 0; i0 < this.required_fields_length; i0++) {
      {
        const encoded = new TextEncoder().encode(this.required_fields[i0]);
        view.setUint32(offset, encoded.length + 1, false);
        offset += 4;
        new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
        offset += encoded.length;
        view.setUint8(offset, 0); // null terminator
        offset += 1;
      }
    }
    for (let i0 = 0; i0 < this.optional_fields_length; i0++) {
      {
        const encoded = new TextEncoder().encode(this.optional_fields[i0]);
        view.setUint32(offset, encoded.length + 1, false);
        offset += 4;
        new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
        offset += encoded.length;
        view.setUint8(offset, 0); // null terminator
        offset += 1;
      }
    }
    view.setInt8(offset, this.uniform_across_resources ? 1 : 0);
    offset += 1;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4 + new TextEncoder().encode(this.name).length + 1;
    size += 4;
    size += 4;
    for (let i0 = 0; i0 < this.required_fields_length; i0++) {
      size += 4 + new TextEncoder().encode(this.required_fields[i0]).length + 1;
    }
    for (let i0 = 0; i0 < this.optional_fields_length; i0++) {
      size += 4 + new TextEncoder().encode(this.optional_fields[i0]).length + 1;
    }
    size += 1;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(CommandProfile)) return 0n;
    const newparents = [...parents, CommandProfile];
    let tmphash = CommandProfile._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (CommandProfile._packedFingerprint === null) {
      CommandProfile._packedFingerprint = CommandProfile._getHashRecursive([]);
    }
    return CommandProfile._packedFingerprint;
  }
}
