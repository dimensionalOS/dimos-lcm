// Auto-generated by lcm-ts. DO NOT EDIT.

export class ResourceDescription {
  static readonly _HASH = 0x802e2d1f1cd1df73n;
  static readonly _NAME = "dimos_control_msgs.ResourceDescription";
  private static _packedFingerprint: bigint | null = null;

  static readonly JOINT = 1;
  static readonly GRIPPER = 2;
  static readonly BASE = 3;
  static readonly IMU = 4;

  name: string;
  resource_type: number;
  group_name: string;

  constructor(init?: Partial<ResourceDescription>) {
    this.name = init?.name ?? "";
    this.resource_type = init?.resource_type ?? 0;
    this.group_name = init?.group_name ?? "";
  }

  static decode(data: Uint8Array): ResourceDescription {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = ResourceDescription._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new ResourceDescription();
    ({ offset } = ResourceDescription._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: ResourceDescription): { offset: number } {
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.name = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    target.resource_type = view.getInt8(offset);
    offset += 1;
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.group_name = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, ResourceDescription._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    {
      const encoded = new TextEncoder().encode(this.name);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    view.setInt8(offset, this.resource_type);
    offset += 1;
    {
      const encoded = new TextEncoder().encode(this.group_name);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4 + new TextEncoder().encode(this.name).length + 1;
    size += 1;
    size += 4 + new TextEncoder().encode(this.group_name).length + 1;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(ResourceDescription)) return 0n;
    const newparents = [...parents, ResourceDescription];
    let tmphash = ResourceDescription._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (ResourceDescription._packedFingerprint === null) {
      ResourceDescription._packedFingerprint = ResourceDescription._getHashRecursive([]);
    }
    return ResourceDescription._packedFingerprint;
  }
}
