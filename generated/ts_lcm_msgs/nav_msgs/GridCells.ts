// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { Point } from "../geometry_msgs/Point.ts";

export class GridCells {
  static readonly _HASH = 0xb855326533062750n;
  static readonly _NAME = "nav_msgs.GridCells";
  private static _packedFingerprint: bigint | null = null;

  cells_length: number;
  header: Header;
  cell_width: number;
  cell_height: number;
  cells: Point[];

  constructor(init?: Partial<GridCells>) {
    this.cells_length = init?.cells_length ?? 0;
    this.header = init?.header ?? new Header();
    this.cell_width = init?.cell_width ?? 0;
    this.cell_height = init?.cell_height ?? 0;
    this.cells = init?.cells ?? [];
  }

  static decode(data: Uint8Array): GridCells {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = GridCells._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new GridCells();
    ({ offset } = GridCells._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: GridCells): { offset: number } {
    target.cells_length = view.getInt32(offset, false);
    offset += 4;
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.cell_width = view.getFloat32(offset, false);
    offset += 4;
    target.cell_height = view.getFloat32(offset, false);
    offset += 4;
    target.cells = new Array(target.cells_length);
    for (let i0 = 0; i0 < target.cells_length; i0++) {
      target.cells[i0] = new Point();
      ({ offset } = Point._decodeOne(view, offset, target.cells[i0]));
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, GridCells._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.cells_length, false);
    offset += 4;
    offset = this.header._encodeOne(view, offset);
    view.setFloat32(offset, this.cell_width, false);
    offset += 4;
    view.setFloat32(offset, this.cell_height, false);
    offset += 4;
    for (let i0 = 0; i0 < this.cells_length; i0++) {
      offset = this.cells[i0]._encodeOne(view, offset);
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += this.header._encodedSize();
    size += 4;
    size += 4;
    for (let i0 = 0; i0 < this.cells_length; i0++) {
      size += this.cells[i0]._encodedSize();
    }
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(GridCells)) return 0n;
    const newparents = [...parents, GridCells];
    let tmphash = GridCells._HASH;
    tmphash = (tmphash + Header._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Point._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (GridCells._packedFingerprint === null) {
      GridCells._packedFingerprint = GridCells._getHashRecursive([]);
    }
    return GridCells._packedFingerprint;
  }
}
