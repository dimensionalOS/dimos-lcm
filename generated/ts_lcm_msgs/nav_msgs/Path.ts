// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { PoseStamped } from "../geometry_msgs/PoseStamped.ts";

export class Path {
  static readonly _HASH = 0xcac8e0dee6cae600n;
  static readonly _NAME = "nav_msgs.Path";

  poses_length: number;
  header: Header;
  poses: PoseStamped[];

  constructor(init?: Partial<Path>) {
    this.poses_length = init?.poses_length ?? 0;
    this.header = init?.header ?? new Header();
    this.poses = init?.poses ?? [];
  }

  static decode(data: Uint8Array): Path {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== Path._HASH) {
      throw new Error(`Hash mismatch: expected ${Path._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Path();
    ({ offset } = Path._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Path): { offset: number } {
    target.poses_length = view.getInt32(offset, false);
    offset += 4;
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.poses = new Array(target.poses_length);
    for (let i0 = 0; i0 < target.poses_length; i0++) {
      target.poses[i0] = new PoseStamped();
      ({ offset } = PoseStamped._decodeOne(view, offset, target.poses[i0]));
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, Path._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.poses_length, false);
    offset += 4;
    offset = this.header._encodeOne(view, offset);
    for (let i0 = 0; i0 < this.poses_length; i0++) {
      offset = this.poses[i0]._encodeOne(view, offset);
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += this.header._encodedSize();
    for (let i0 = 0; i0 < this.poses_length; i0++) {
      size += this.poses[i0]._encodedSize();
    }
    return size;
  }
}
