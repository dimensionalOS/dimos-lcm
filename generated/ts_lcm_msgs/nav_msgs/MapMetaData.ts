// Auto-generated by lcm-ts. DO NOT EDIT.

import { Time } from "../std_msgs/Time.ts";
import { Pose } from "../geometry_msgs/Pose.ts";

export class MapMetaData {
  static readonly _HASH = 0x3245f3cdb468ba93n;
  static readonly _NAME = "nav_msgs.MapMetaData";
  private static _packedFingerprint: bigint | null = null;

  map_load_time: Time;
  resolution: number;
  width: number;
  height: number;
  origin: Pose;

  constructor(init?: Partial<MapMetaData>) {
    this.map_load_time = init?.map_load_time ?? new Time();
    this.resolution = init?.resolution ?? 0;
    this.width = init?.width ?? 0;
    this.height = init?.height ?? 0;
    this.origin = init?.origin ?? new Pose();
  }

  static decode(data: Uint8Array): MapMetaData {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = MapMetaData._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new MapMetaData();
    ({ offset } = MapMetaData._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: MapMetaData): { offset: number } {
    target.map_load_time = new Time();
    ({ offset } = Time._decodeOne(view, offset, target.map_load_time));
    target.resolution = view.getFloat32(offset, false);
    offset += 4;
    target.width = view.getInt32(offset, false);
    offset += 4;
    target.height = view.getInt32(offset, false);
    offset += 4;
    target.origin = new Pose();
    ({ offset } = Pose._decodeOne(view, offset, target.origin));
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, MapMetaData._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.map_load_time._encodeOne(view, offset);
    view.setFloat32(offset, this.resolution, false);
    offset += 4;
    view.setInt32(offset, this.width, false);
    offset += 4;
    view.setInt32(offset, this.height, false);
    offset += 4;
    offset = this.origin._encodeOne(view, offset);
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.map_load_time._encodedSize();
    size += 4;
    size += 4;
    size += 4;
    size += this.origin._encodedSize();
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(MapMetaData)) return 0n;
    const newparents = [...parents, MapMetaData];
    let tmphash = MapMetaData._HASH;
    tmphash = (tmphash + Time._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Pose._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (MapMetaData._packedFingerprint === null) {
      MapMetaData._packedFingerprint = MapMetaData._getHashRecursive([]);
    }
    return MapMetaData._packedFingerprint;
  }
}
