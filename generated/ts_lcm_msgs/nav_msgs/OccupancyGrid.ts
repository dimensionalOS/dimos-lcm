// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { MapMetaData } from "./MapMetaData.ts";

export class OccupancyGrid {
  static readonly _HASH = 0x70bee8c8c2e8c200n;
  static readonly _NAME = "nav_msgs.OccupancyGrid";

  data_length: number;
  header: Header;
  info: MapMetaData;
  data: number[];

  constructor(init?: Partial<OccupancyGrid>) {
    this.data_length = init?.data_length ?? 0;
    this.header = init?.header ?? new Header();
    this.info = init?.info ?? new MapMetaData();
    this.data = init?.data ?? [];
  }

  static decode(data: Uint8Array): OccupancyGrid {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== OccupancyGrid._HASH) {
      throw new Error(`Hash mismatch: expected ${OccupancyGrid._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new OccupancyGrid();
    ({ offset } = OccupancyGrid._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: OccupancyGrid): { offset: number } {
    target.data_length = view.getInt32(offset, false);
    offset += 4;
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.info = new MapMetaData();
    ({ offset } = MapMetaData._decodeOne(view, offset, target.info));
    target.data = new Array(target.data_length);
    for (let i0 = 0; i0 < target.data_length; i0++) {
      target.data[i0] = view.getInt8(offset);
      offset += 1;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, OccupancyGrid._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.data_length, false);
    offset += 4;
    offset = this.header._encodeOne(view, offset);
    offset = this.info._encodeOne(view, offset);
    for (let i0 = 0; i0 < this.data_length; i0++) {
      view.setInt8(offset, this.data[i0]);
      offset += 1;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += this.header._encodedSize();
    size += this.info._encodedSize();
    size += this.data_length * 1;
    return size;
  }
}
