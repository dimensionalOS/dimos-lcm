// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { BoundingBox2D } from "./BoundingBox2D.ts";

export class BoundingBox2DArray {
  static readonly _HASH = 0xb279b6ac710340a2n;
  static readonly _NAME = "vision_msgs.BoundingBox2DArray";
  private static _packedFingerprint: bigint | null = null;

  boxes_length: number;
  header: Header;
  boxes: BoundingBox2D[];

  constructor(init?: Partial<BoundingBox2DArray>) {
    this.boxes_length = init?.boxes_length ?? 0;
    this.header = init?.header ?? new Header();
    this.boxes = init?.boxes ?? [];
  }

  static decode(data: Uint8Array): BoundingBox2DArray {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = BoundingBox2DArray._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new BoundingBox2DArray();
    ({ offset } = BoundingBox2DArray._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: BoundingBox2DArray): { offset: number } {
    target.boxes_length = view.getInt32(offset, false);
    offset += 4;
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.boxes = new Array(target.boxes_length);
    for (let i0 = 0; i0 < target.boxes_length; i0++) {
      target.boxes[i0] = new BoundingBox2D();
      ({ offset } = BoundingBox2D._decodeOne(view, offset, target.boxes[i0]));
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, BoundingBox2DArray._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.boxes_length, false);
    offset += 4;
    offset = this.header._encodeOne(view, offset);
    for (let i0 = 0; i0 < this.boxes_length; i0++) {
      offset = this.boxes[i0]._encodeOne(view, offset);
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += this.header._encodedSize();
    for (let i0 = 0; i0 < this.boxes_length; i0++) {
      size += this.boxes[i0]._encodedSize();
    }
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(BoundingBox2DArray)) return 0n;
    const newparents = [...parents, BoundingBox2DArray];
    let tmphash = BoundingBox2DArray._HASH;
    tmphash = (tmphash + Header._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + BoundingBox2D._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (BoundingBox2DArray._packedFingerprint === null) {
      BoundingBox2DArray._packedFingerprint = BoundingBox2DArray._getHashRecursive([]);
    }
    return BoundingBox2DArray._packedFingerprint;
  }
}
