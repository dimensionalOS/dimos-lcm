// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { Detection3D } from "./Detection3D.ts";

export class Detection3DArray {
  static readonly _HASH = 0x85b4a076ba01be3cn;
  static readonly _NAME = "vision_msgs.Detection3DArray";
  private static _packedFingerprint: bigint | null = null;

  detections_length: number;
  header: Header;
  detections: Detection3D[];

  constructor(init?: Partial<Detection3DArray>) {
    this.detections_length = init?.detections_length ?? 0;
    this.header = init?.header ?? new Header();
    this.detections = init?.detections ?? [];
  }

  static decode(data: Uint8Array): Detection3DArray {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = Detection3DArray._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Detection3DArray();
    ({ offset } = Detection3DArray._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Detection3DArray): { offset: number } {
    target.detections_length = view.getInt32(offset, false);
    offset += 4;
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.detections = new Array(target.detections_length);
    for (let i0 = 0; i0 < target.detections_length; i0++) {
      target.detections[i0] = new Detection3D();
      ({ offset } = Detection3D._decodeOne(view, offset, target.detections[i0]));
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, Detection3DArray._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.detections_length, false);
    offset += 4;
    offset = this.header._encodeOne(view, offset);
    for (let i0 = 0; i0 < this.detections_length; i0++) {
      offset = this.detections[i0]._encodeOne(view, offset);
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += this.header._encodedSize();
    for (let i0 = 0; i0 < this.detections_length; i0++) {
      size += this.detections[i0]._encodedSize();
    }
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(Detection3DArray)) return 0n;
    const newparents = [...parents, Detection3DArray];
    let tmphash = Detection3DArray._HASH;
    tmphash = (tmphash + Header._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Detection3D._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (Detection3DArray._packedFingerprint === null) {
      Detection3DArray._packedFingerprint = Detection3DArray._getHashRecursive([]);
    }
    return Detection3DArray._packedFingerprint;
  }
}
