// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { VisionClass } from "./VisionClass.ts";

export class LabelInfo {
  static readonly _HASH = 0xd0e4cae6d0ded8c8n;
  static readonly _NAME = "vision_msgs.LabelInfo";

  class_map_length: number;
  header: Header;
  class_map: VisionClass[];
  threshold: number;

  constructor(init?: Partial<LabelInfo>) {
    this.class_map_length = init?.class_map_length ?? 0;
    this.header = init?.header ?? new Header();
    this.class_map = init?.class_map ?? [];
    this.threshold = init?.threshold ?? 0;
  }

  static decode(data: Uint8Array): LabelInfo {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== LabelInfo._HASH) {
      throw new Error(`Hash mismatch: expected ${LabelInfo._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new LabelInfo();
    ({ offset } = LabelInfo._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: LabelInfo): { offset: number } {
    target.class_map_length = view.getInt32(offset, false);
    offset += 4;
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.class_map = new Array(target.class_map_length);
    for (let i0 = 0; i0 < target.class_map_length; i0++) {
      target.class_map[i0] = new VisionClass();
      ({ offset } = VisionClass._decodeOne(view, offset, target.class_map[i0]));
    }
    target.threshold = view.getFloat32(offset, false);
    offset += 4;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, LabelInfo._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.class_map_length, false);
    offset += 4;
    offset = this.header._encodeOne(view, offset);
    for (let i0 = 0; i0 < this.class_map_length; i0++) {
      offset = this.class_map[i0]._encodeOne(view, offset);
    }
    view.setFloat32(offset, this.threshold, false);
    offset += 4;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += this.header._encodedSize();
    for (let i0 = 0; i0 < this.class_map_length; i0++) {
      size += this.class_map[i0]._encodedSize();
    }
    size += 4;
    return size;
  }
}
