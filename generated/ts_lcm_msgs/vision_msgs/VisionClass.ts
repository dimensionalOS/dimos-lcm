// Auto-generated by lcm-ts. DO NOT EDIT.

export class VisionClass {
  static readonly _HASH = 0xdc055c3dd46c2633n;
  static readonly _NAME = "vision_msgs.VisionClass";
  private static _packedFingerprint: bigint | null = null;

  class_id: number;
  class_name: string;

  constructor(init?: Partial<VisionClass>) {
    this.class_id = init?.class_id ?? 0;
    this.class_name = init?.class_name ?? "";
  }

  static decode(data: Uint8Array): VisionClass {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = VisionClass._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new VisionClass();
    ({ offset } = VisionClass._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: VisionClass): { offset: number } {
    target.class_id = view.getInt16(offset, false);
    offset += 2;
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.class_name = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, VisionClass._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt16(offset, this.class_id, false);
    offset += 2;
    {
      const encoded = new TextEncoder().encode(this.class_name);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 2;
    size += 4 + new TextEncoder().encode(this.class_name).length + 1;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(VisionClass)) return 0n;
    const newparents = [...parents, VisionClass];
    let tmphash = VisionClass._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (VisionClass._packedFingerprint === null) {
      VisionClass._packedFingerprint = VisionClass._getHashRecursive([]);
    }
    return VisionClass._packedFingerprint;
  }
}
