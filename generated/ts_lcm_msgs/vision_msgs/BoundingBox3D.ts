// Auto-generated by lcm-ts. DO NOT EDIT.

import { Pose } from "../geometry_msgs/Pose.ts";
import { Vector3 } from "../geometry_msgs/Vector3.ts";

export class BoundingBox3D {
  static readonly _HASH = 0xe8dee466e6d2f4can;
  static readonly _NAME = "vision_msgs.BoundingBox3D";

  center: Pose;
  size: Vector3;

  constructor(init?: Partial<BoundingBox3D>) {
    this.center = init?.center ?? new Pose();
    this.size = init?.size ?? new Vector3();
  }

  static decode(data: Uint8Array): BoundingBox3D {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== BoundingBox3D._HASH) {
      throw new Error(`Hash mismatch: expected ${BoundingBox3D._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new BoundingBox3D();
    ({ offset } = BoundingBox3D._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: BoundingBox3D): { offset: number } {
    target.center = new Pose();
    ({ offset } = Pose._decodeOne(view, offset, target.center));
    target.size = new Vector3();
    ({ offset } = Vector3._decodeOne(view, offset, target.size));
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, BoundingBox3D._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.center._encodeOne(view, offset);
    offset = this.size._encodeOne(view, offset);
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.center._encodedSize();
    size += this.size._encodedSize();
    return size;
  }
}
