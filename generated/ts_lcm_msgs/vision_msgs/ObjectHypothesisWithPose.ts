// Auto-generated by lcm-ts. DO NOT EDIT.

import { ObjectHypothesis } from "./ObjectHypothesis.ts";
import { PoseWithCovariance } from "../geometry_msgs/PoseWithCovariance.ts";

export class ObjectHypothesisWithPose {
  static readonly _HASH = 0x65e1d44b451e8a8bn;
  static readonly _NAME = "vision_msgs.ObjectHypothesisWithPose";
  private static _packedFingerprint: bigint | null = null;

  hypothesis: ObjectHypothesis;
  pose: PoseWithCovariance;

  constructor(init?: Partial<ObjectHypothesisWithPose>) {
    this.hypothesis = init?.hypothesis ?? new ObjectHypothesis();
    this.pose = init?.pose ?? new PoseWithCovariance();
  }

  static decode(data: Uint8Array): ObjectHypothesisWithPose {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = ObjectHypothesisWithPose._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new ObjectHypothesisWithPose();
    ({ offset } = ObjectHypothesisWithPose._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: ObjectHypothesisWithPose): { offset: number } {
    target.hypothesis = new ObjectHypothesis();
    ({ offset } = ObjectHypothesis._decodeOne(view, offset, target.hypothesis));
    target.pose = new PoseWithCovariance();
    ({ offset } = PoseWithCovariance._decodeOne(view, offset, target.pose));
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, ObjectHypothesisWithPose._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.hypothesis._encodeOne(view, offset);
    offset = this.pose._encodeOne(view, offset);
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.hypothesis._encodedSize();
    size += this.pose._encodedSize();
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(ObjectHypothesisWithPose)) return 0n;
    const newparents = [...parents, ObjectHypothesisWithPose];
    let tmphash = ObjectHypothesisWithPose._HASH;
    tmphash = (tmphash + ObjectHypothesis._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + PoseWithCovariance._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (ObjectHypothesisWithPose._packedFingerprint === null) {
      ObjectHypothesisWithPose._packedFingerprint = ObjectHypothesisWithPose._getHashRecursive([]);
    }
    return ObjectHypothesisWithPose._packedFingerprint;
  }
}
