// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { ObjectHypothesis } from "./ObjectHypothesis.ts";

export class Classification {
  static readonly _HASH = 0x60719354aae1b22an;
  static readonly _NAME = "vision_msgs.Classification";
  private static _packedFingerprint: bigint | null = null;

  results_length: number;
  header: Header;
  results: ObjectHypothesis[];

  constructor(init?: Partial<Classification>) {
    this.results_length = init?.results_length ?? 0;
    this.header = init?.header ?? new Header();
    this.results = init?.results ?? [];
  }

  static decode(data: Uint8Array): Classification {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = Classification._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Classification();
    ({ offset } = Classification._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Classification): { offset: number } {
    target.results_length = view.getInt32(offset, false);
    offset += 4;
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.results = new Array(target.results_length);
    for (let i0 = 0; i0 < target.results_length; i0++) {
      target.results[i0] = new ObjectHypothesis();
      ({ offset } = ObjectHypothesis._decodeOne(view, offset, target.results[i0]));
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, Classification._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.results_length, false);
    offset += 4;
    offset = this.header._encodeOne(view, offset);
    for (let i0 = 0; i0 < this.results_length; i0++) {
      offset = this.results[i0]._encodeOne(view, offset);
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += this.header._encodedSize();
    for (let i0 = 0; i0 < this.results_length; i0++) {
      size += this.results[i0]._encodedSize();
    }
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(Classification)) return 0n;
    const newparents = [...parents, Classification];
    let tmphash = Classification._HASH;
    tmphash = (tmphash + Header._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + ObjectHypothesis._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (Classification._packedFingerprint === null) {
      Classification._packedFingerprint = Classification._getHashRecursive([]);
    }
    return Classification._packedFingerprint;
  }
}
