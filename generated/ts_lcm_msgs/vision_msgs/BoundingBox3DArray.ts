// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { BoundingBox3D } from "./BoundingBox3D.ts";

export class BoundingBox3DArray {
  static readonly _HASH = 0x6688c4def0cae600n;
  static readonly _NAME = "vision_msgs.BoundingBox3DArray";

  boxes_length: number;
  header: Header;
  boxes: BoundingBox3D[];

  constructor(init?: Partial<BoundingBox3DArray>) {
    this.boxes_length = init?.boxes_length ?? 0;
    this.header = init?.header ?? new Header();
    this.boxes = init?.boxes ?? [];
  }

  static decode(data: Uint8Array): BoundingBox3DArray {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== BoundingBox3DArray._HASH) {
      throw new Error(`Hash mismatch: expected ${BoundingBox3DArray._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new BoundingBox3DArray();
    ({ offset } = BoundingBox3DArray._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: BoundingBox3DArray): { offset: number } {
    target.boxes_length = view.getInt32(offset, false);
    offset += 4;
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.boxes = new Array(target.boxes_length);
    for (let i0 = 0; i0 < target.boxes_length; i0++) {
      target.boxes[i0] = new BoundingBox3D();
      ({ offset } = BoundingBox3D._decodeOne(view, offset, target.boxes[i0]));
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, BoundingBox3DArray._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.boxes_length, false);
    offset += 4;
    offset = this.header._encodeOne(view, offset);
    for (let i0 = 0; i0 < this.boxes_length; i0++) {
      offset = this.boxes[i0]._encodeOne(view, offset);
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += this.header._encodedSize();
    for (let i0 = 0; i0 < this.boxes_length; i0++) {
      size += this.boxes[i0]._encodedSize();
    }
    return size;
  }
}
