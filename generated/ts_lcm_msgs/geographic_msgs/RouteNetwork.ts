// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { UniqueID } from "../uuid_msgs/UniqueID.ts";
import { BoundingBox } from "./BoundingBox.ts";
import { WayPoint } from "./WayPoint.ts";
import { RouteSegment } from "./RouteSegment.ts";
import { KeyValue } from "./KeyValue.ts";

export class RouteNetwork {
  static readonly _HASH = 0xf50b14f90fc9a672n;
  static readonly _NAME = "geographic_msgs.RouteNetwork";
  private static _packedFingerprint: bigint | null = null;

  points_length: number;
  segments_length: number;
  props_length: number;
  header: Header;
  id: UniqueID;
  bounds: BoundingBox;
  points: WayPoint[];
  segments: RouteSegment[];
  props: KeyValue[];

  constructor(init?: Partial<RouteNetwork>) {
    this.points_length = init?.points_length ?? 0;
    this.segments_length = init?.segments_length ?? 0;
    this.props_length = init?.props_length ?? 0;
    this.header = init?.header ?? new Header();
    this.id = init?.id ?? new UniqueID();
    this.bounds = init?.bounds ?? new BoundingBox();
    this.points = init?.points ?? [];
    this.segments = init?.segments ?? [];
    this.props = init?.props ?? [];
  }

  static decode(data: Uint8Array): RouteNetwork {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = RouteNetwork._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new RouteNetwork();
    ({ offset } = RouteNetwork._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: RouteNetwork): { offset: number } {
    target.points_length = view.getInt32(offset, false);
    offset += 4;
    target.segments_length = view.getInt32(offset, false);
    offset += 4;
    target.props_length = view.getInt32(offset, false);
    offset += 4;
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.id = new UniqueID();
    ({ offset } = UniqueID._decodeOne(view, offset, target.id));
    target.bounds = new BoundingBox();
    ({ offset } = BoundingBox._decodeOne(view, offset, target.bounds));
    target.points = new Array(target.points_length);
    for (let i0 = 0; i0 < target.points_length; i0++) {
      target.points[i0] = new WayPoint();
      ({ offset } = WayPoint._decodeOne(view, offset, target.points[i0]));
    }
    target.segments = new Array(target.segments_length);
    for (let i0 = 0; i0 < target.segments_length; i0++) {
      target.segments[i0] = new RouteSegment();
      ({ offset } = RouteSegment._decodeOne(view, offset, target.segments[i0]));
    }
    target.props = new Array(target.props_length);
    for (let i0 = 0; i0 < target.props_length; i0++) {
      target.props[i0] = new KeyValue();
      ({ offset } = KeyValue._decodeOne(view, offset, target.props[i0]));
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, RouteNetwork._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.points_length, false);
    offset += 4;
    view.setInt32(offset, this.segments_length, false);
    offset += 4;
    view.setInt32(offset, this.props_length, false);
    offset += 4;
    offset = this.header._encodeOne(view, offset);
    offset = this.id._encodeOne(view, offset);
    offset = this.bounds._encodeOne(view, offset);
    for (let i0 = 0; i0 < this.points_length; i0++) {
      offset = this.points[i0]._encodeOne(view, offset);
    }
    for (let i0 = 0; i0 < this.segments_length; i0++) {
      offset = this.segments[i0]._encodeOne(view, offset);
    }
    for (let i0 = 0; i0 < this.props_length; i0++) {
      offset = this.props[i0]._encodeOne(view, offset);
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    size += 4;
    size += this.header._encodedSize();
    size += this.id._encodedSize();
    size += this.bounds._encodedSize();
    for (let i0 = 0; i0 < this.points_length; i0++) {
      size += this.points[i0]._encodedSize();
    }
    for (let i0 = 0; i0 < this.segments_length; i0++) {
      size += this.segments[i0]._encodedSize();
    }
    for (let i0 = 0; i0 < this.props_length; i0++) {
      size += this.props[i0]._encodedSize();
    }
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(RouteNetwork)) return 0n;
    const newparents = [...parents, RouteNetwork];
    let tmphash = RouteNetwork._HASH;
    tmphash = (tmphash + Header._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + UniqueID._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + BoundingBox._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + WayPoint._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + RouteSegment._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + KeyValue._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (RouteNetwork._packedFingerprint === null) {
      RouteNetwork._packedFingerprint = RouteNetwork._getHashRecursive([]);
    }
    return RouteNetwork._packedFingerprint;
  }
}
