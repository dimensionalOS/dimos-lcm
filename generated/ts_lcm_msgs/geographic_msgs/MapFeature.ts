// Auto-generated by lcm-ts. DO NOT EDIT.

import { UniqueID } from "../uuid_msgs/UniqueID.ts";
import { KeyValue } from "./KeyValue.ts";

export class MapFeature {
  static readonly _HASH = 0x75975993f8dd2d22n;
  static readonly _NAME = "geographic_msgs.MapFeature";
  private static _packedFingerprint: bigint | null = null;

  components_length: number;
  props_length: number;
  id: UniqueID;
  components: UniqueID[];
  props: KeyValue[];

  constructor(init?: Partial<MapFeature>) {
    this.components_length = init?.components_length ?? 0;
    this.props_length = init?.props_length ?? 0;
    this.id = init?.id ?? new UniqueID();
    this.components = init?.components ?? [];
    this.props = init?.props ?? [];
  }

  static decode(data: Uint8Array): MapFeature {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = MapFeature._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new MapFeature();
    ({ offset } = MapFeature._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: MapFeature): { offset: number } {
    target.components_length = view.getInt32(offset, false);
    offset += 4;
    target.props_length = view.getInt32(offset, false);
    offset += 4;
    target.id = new UniqueID();
    ({ offset } = UniqueID._decodeOne(view, offset, target.id));
    target.components = new Array(target.components_length);
    for (let i0 = 0; i0 < target.components_length; i0++) {
      target.components[i0] = new UniqueID();
      ({ offset } = UniqueID._decodeOne(view, offset, target.components[i0]));
    }
    target.props = new Array(target.props_length);
    for (let i0 = 0; i0 < target.props_length; i0++) {
      target.props[i0] = new KeyValue();
      ({ offset } = KeyValue._decodeOne(view, offset, target.props[i0]));
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, MapFeature._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.components_length, false);
    offset += 4;
    view.setInt32(offset, this.props_length, false);
    offset += 4;
    offset = this.id._encodeOne(view, offset);
    for (let i0 = 0; i0 < this.components_length; i0++) {
      offset = this.components[i0]._encodeOne(view, offset);
    }
    for (let i0 = 0; i0 < this.props_length; i0++) {
      offset = this.props[i0]._encodeOne(view, offset);
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    size += this.id._encodedSize();
    for (let i0 = 0; i0 < this.components_length; i0++) {
      size += this.components[i0]._encodedSize();
    }
    for (let i0 = 0; i0 < this.props_length; i0++) {
      size += this.props[i0]._encodedSize();
    }
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(MapFeature)) return 0n;
    const newparents = [...parents, MapFeature];
    let tmphash = MapFeature._HASH;
    tmphash = (tmphash + UniqueID._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + UniqueID._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + KeyValue._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (MapFeature._packedFingerprint === null) {
      MapFeature._packedFingerprint = MapFeature._getHashRecursive([]);
    }
    return MapFeature._packedFingerprint;
  }
}
