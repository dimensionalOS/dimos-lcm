// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { GeoPoint } from "./GeoPoint.ts";

export class GeoPointStamped {
  static readonly _HASH = 0x3a3685292ab14382n;
  static readonly _NAME = "geographic_msgs.GeoPointStamped";
  private static _packedFingerprint: bigint | null = null;

  header: Header;
  position: GeoPoint;

  constructor(init?: Partial<GeoPointStamped>) {
    this.header = init?.header ?? new Header();
    this.position = init?.position ?? new GeoPoint();
  }

  static decode(data: Uint8Array): GeoPointStamped {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = GeoPointStamped._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new GeoPointStamped();
    ({ offset } = GeoPointStamped._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: GeoPointStamped): { offset: number } {
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.position = new GeoPoint();
    ({ offset } = GeoPoint._decodeOne(view, offset, target.position));
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, GeoPointStamped._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.header._encodeOne(view, offset);
    offset = this.position._encodeOne(view, offset);
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.header._encodedSize();
    size += this.position._encodedSize();
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(GeoPointStamped)) return 0n;
    const newparents = [...parents, GeoPointStamped];
    let tmphash = GeoPointStamped._HASH;
    tmphash = (tmphash + Header._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + GeoPoint._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (GeoPointStamped._packedFingerprint === null) {
      GeoPointStamped._packedFingerprint = GeoPointStamped._getHashRecursive([]);
    }
    return GeoPointStamped._packedFingerprint;
  }
}
