// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { GeographicMap } from "./GeographicMap.ts";
import { UniqueID } from "../uuid_msgs/UniqueID.ts";

export class GeographicMapChanges {
  static readonly _HASH = 0x5d92adbb4d9db2cbn;
  static readonly _NAME = "geographic_msgs.GeographicMapChanges";
  private static _packedFingerprint: bigint | null = null;

  deletes_length: number;
  header: Header;
  diffs: GeographicMap;
  deletes: UniqueID[];

  constructor(init?: Partial<GeographicMapChanges>) {
    this.deletes_length = init?.deletes_length ?? 0;
    this.header = init?.header ?? new Header();
    this.diffs = init?.diffs ?? new GeographicMap();
    this.deletes = init?.deletes ?? [];
  }

  static decode(data: Uint8Array): GeographicMapChanges {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = GeographicMapChanges._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new GeographicMapChanges();
    ({ offset } = GeographicMapChanges._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: GeographicMapChanges): { offset: number } {
    target.deletes_length = view.getInt32(offset, false);
    offset += 4;
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.diffs = new GeographicMap();
    ({ offset } = GeographicMap._decodeOne(view, offset, target.diffs));
    target.deletes = new Array(target.deletes_length);
    for (let i0 = 0; i0 < target.deletes_length; i0++) {
      target.deletes[i0] = new UniqueID();
      ({ offset } = UniqueID._decodeOne(view, offset, target.deletes[i0]));
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, GeographicMapChanges._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.deletes_length, false);
    offset += 4;
    offset = this.header._encodeOne(view, offset);
    offset = this.diffs._encodeOne(view, offset);
    for (let i0 = 0; i0 < this.deletes_length; i0++) {
      offset = this.deletes[i0]._encodeOne(view, offset);
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += this.header._encodedSize();
    size += this.diffs._encodedSize();
    for (let i0 = 0; i0 < this.deletes_length; i0++) {
      size += this.deletes[i0]._encodedSize();
    }
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(GeographicMapChanges)) return 0n;
    const newparents = [...parents, GeographicMapChanges];
    let tmphash = GeographicMapChanges._HASH;
    tmphash = (tmphash + Header._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + GeographicMap._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + UniqueID._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (GeographicMapChanges._packedFingerprint === null) {
      GeographicMapChanges._packedFingerprint = GeographicMapChanges._getHashRecursive([]);
    }
    return GeographicMapChanges._packedFingerprint;
  }
}
