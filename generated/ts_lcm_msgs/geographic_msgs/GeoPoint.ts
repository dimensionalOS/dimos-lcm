// Auto-generated by lcm-ts. DO NOT EDIT.

export class GeoPoint {
  static readonly _HASH = 0xb84ee2b89179c84en;
  static readonly _NAME = "geographic_msgs.GeoPoint";
  private static _packedFingerprint: bigint | null = null;

  latitude: number;
  longitude: number;
  altitude: number;

  constructor(init?: Partial<GeoPoint>) {
    this.latitude = init?.latitude ?? 0;
    this.longitude = init?.longitude ?? 0;
    this.altitude = init?.altitude ?? 0;
  }

  static decode(data: Uint8Array): GeoPoint {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = GeoPoint._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new GeoPoint();
    ({ offset } = GeoPoint._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: GeoPoint): { offset: number } {
    target.latitude = view.getFloat64(offset, false);
    offset += 8;
    target.longitude = view.getFloat64(offset, false);
    offset += 8;
    target.altitude = view.getFloat64(offset, false);
    offset += 8;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, GeoPoint._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setFloat64(offset, this.latitude, false);
    offset += 8;
    view.setFloat64(offset, this.longitude, false);
    offset += 8;
    view.setFloat64(offset, this.altitude, false);
    offset += 8;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 8;
    size += 8;
    size += 8;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(GeoPoint)) return 0n;
    const newparents = [...parents, GeoPoint];
    let tmphash = GeoPoint._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (GeoPoint._packedFingerprint === null) {
      GeoPoint._packedFingerprint = GeoPoint._getHashRecursive([]);
    }
    return GeoPoint._packedFingerprint;
  }
}
