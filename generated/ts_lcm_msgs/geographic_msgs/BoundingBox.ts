// Auto-generated by lcm-ts. DO NOT EDIT.

import { GeoPoint } from "./GeoPoint.ts";

export class BoundingBox {
  static readonly _HASH = 0xecb7cbaa66bccde0n;
  static readonly _NAME = "geographic_msgs.BoundingBox";
  private static _packedFingerprint: bigint | null = null;

  min_pt: GeoPoint;
  max_pt: GeoPoint;

  constructor(init?: Partial<BoundingBox>) {
    this.min_pt = init?.min_pt ?? new GeoPoint();
    this.max_pt = init?.max_pt ?? new GeoPoint();
  }

  static decode(data: Uint8Array): BoundingBox {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = BoundingBox._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new BoundingBox();
    ({ offset } = BoundingBox._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: BoundingBox): { offset: number } {
    target.min_pt = new GeoPoint();
    ({ offset } = GeoPoint._decodeOne(view, offset, target.min_pt));
    target.max_pt = new GeoPoint();
    ({ offset } = GeoPoint._decodeOne(view, offset, target.max_pt));
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, BoundingBox._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.min_pt._encodeOne(view, offset);
    offset = this.max_pt._encodeOne(view, offset);
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.min_pt._encodedSize();
    size += this.max_pt._encodedSize();
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(BoundingBox)) return 0n;
    const newparents = [...parents, BoundingBox];
    let tmphash = BoundingBox._HASH;
    tmphash = (tmphash + GeoPoint._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + GeoPoint._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (BoundingBox._packedFingerprint === null) {
      BoundingBox._packedFingerprint = BoundingBox._getHashRecursive([]);
    }
    return BoundingBox._packedFingerprint;
  }
}
