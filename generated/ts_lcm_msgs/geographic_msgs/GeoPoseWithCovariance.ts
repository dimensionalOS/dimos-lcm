// Auto-generated by lcm-ts. DO NOT EDIT.

import { GeoPose } from "./GeoPose.ts";

export class GeoPoseWithCovariance {
  static readonly _HASH = 0x42dbdfaa69371237n;
  static readonly _NAME = "geographic_msgs.GeoPoseWithCovariance";
  private static _packedFingerprint: bigint | null = null;

  pose: GeoPose;
  covariance: number[];

  constructor(init?: Partial<GeoPoseWithCovariance>) {
    this.pose = init?.pose ?? new GeoPose();
    this.covariance = init?.covariance ?? Array.from({ length: 36 }, () => 0);
  }

  static decode(data: Uint8Array): GeoPoseWithCovariance {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = GeoPoseWithCovariance._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new GeoPoseWithCovariance();
    ({ offset } = GeoPoseWithCovariance._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: GeoPoseWithCovariance): { offset: number } {
    target.pose = new GeoPose();
    ({ offset } = GeoPose._decodeOne(view, offset, target.pose));
    target.covariance = new Array(36);
    for (let i0 = 0; i0 < 36; i0++) {
      target.covariance[i0] = view.getFloat64(offset, false);
      offset += 8;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, GeoPoseWithCovariance._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.pose._encodeOne(view, offset);
    for (let i0 = 0; i0 < 36; i0++) {
      view.setFloat64(offset, this.covariance[i0], false);
      offset += 8;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.pose._encodedSize();
    size += 36 * 8;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(GeoPoseWithCovariance)) return 0n;
    const newparents = [...parents, GeoPoseWithCovariance];
    let tmphash = GeoPoseWithCovariance._HASH;
    tmphash = (tmphash + GeoPose._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (GeoPoseWithCovariance._packedFingerprint === null) {
      GeoPoseWithCovariance._packedFingerprint = GeoPoseWithCovariance._getHashRecursive([]);
    }
    return GeoPoseWithCovariance._packedFingerprint;
  }
}
