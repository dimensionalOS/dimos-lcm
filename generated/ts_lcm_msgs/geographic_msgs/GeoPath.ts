// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { GeoPoseStamped } from "./GeoPoseStamped.ts";

export class GeoPath {
  static readonly _HASH = 0xc779b6acc503055an;
  static readonly _NAME = "geographic_msgs.GeoPath";
  private static _packedFingerprint: bigint | null = null;

  poses_length: number;
  header: Header;
  poses: GeoPoseStamped[];

  constructor(init?: Partial<GeoPath>) {
    this.poses_length = init?.poses_length ?? 0;
    this.header = init?.header ?? new Header();
    this.poses = init?.poses ?? [];
  }

  static decode(data: Uint8Array): GeoPath {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = GeoPath._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new GeoPath();
    ({ offset } = GeoPath._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: GeoPath): { offset: number } {
    target.poses_length = view.getInt32(offset, false);
    offset += 4;
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.poses = new Array(target.poses_length);
    for (let i0 = 0; i0 < target.poses_length; i0++) {
      target.poses[i0] = new GeoPoseStamped();
      ({ offset } = GeoPoseStamped._decodeOne(view, offset, target.poses[i0]));
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, GeoPath._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.poses_length, false);
    offset += 4;
    offset = this.header._encodeOne(view, offset);
    for (let i0 = 0; i0 < this.poses_length; i0++) {
      offset = this.poses[i0]._encodeOne(view, offset);
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += this.header._encodedSize();
    for (let i0 = 0; i0 < this.poses_length; i0++) {
      size += this.poses[i0]._encodedSize();
    }
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(GeoPath)) return 0n;
    const newparents = [...parents, GeoPath];
    let tmphash = GeoPath._HASH;
    tmphash = (tmphash + Header._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + GeoPoseStamped._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (GeoPath._packedFingerprint === null) {
      GeoPath._packedFingerprint = GeoPath._getHashRecursive([]);
    }
    return GeoPath._packedFingerprint;
  }
}
