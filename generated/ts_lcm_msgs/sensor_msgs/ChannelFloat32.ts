// Auto-generated by lcm-ts. DO NOT EDIT.

export class ChannelFloat32 {
  static readonly _HASH = 0x8cbe448953ebf073n;
  static readonly _NAME = "sensor_msgs.ChannelFloat32";
  private static _packedFingerprint: bigint | null = null;

  values_length: number;
  name: string;
  values: number[];

  constructor(init?: Partial<ChannelFloat32>) {
    this.values_length = init?.values_length ?? 0;
    this.name = init?.name ?? "";
    this.values = init?.values ?? [];
  }

  static decode(data: Uint8Array): ChannelFloat32 {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = ChannelFloat32._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new ChannelFloat32();
    ({ offset } = ChannelFloat32._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: ChannelFloat32): { offset: number } {
    target.values_length = view.getInt32(offset, false);
    offset += 4;
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.name = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    target.values = new Array(target.values_length);
    for (let i0 = 0; i0 < target.values_length; i0++) {
      target.values[i0] = view.getFloat32(offset, false);
      offset += 4;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, ChannelFloat32._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.values_length, false);
    offset += 4;
    {
      const encoded = new TextEncoder().encode(this.name);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    for (let i0 = 0; i0 < this.values_length; i0++) {
      view.setFloat32(offset, this.values[i0], false);
      offset += 4;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4 + new TextEncoder().encode(this.name).length + 1;
    size += this.values_length * 4;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(ChannelFloat32)) return 0n;
    const newparents = [...parents, ChannelFloat32];
    let tmphash = ChannelFloat32._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (ChannelFloat32._packedFingerprint === null) {
      ChannelFloat32._packedFingerprint = ChannelFloat32._getHashRecursive([]);
    }
    return ChannelFloat32._packedFingerprint;
  }
}
