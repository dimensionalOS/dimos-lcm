// Auto-generated by lcm-ts. DO NOT EDIT.

export class PointField {
  static readonly _HASH = 0x702e0e03f04285d7n;
  static readonly _NAME = "sensor_msgs.PointField";
  private static _packedFingerprint: bigint | null = null;

  static readonly INT8 = 1;
  static readonly UINT8 = 2;
  static readonly INT16 = 3;
  static readonly UINT16 = 4;
  static readonly INT32 = 5;
  static readonly UINT32 = 6;
  static readonly FLOAT32 = 7;
  static readonly FLOAT64 = 8;

  name: string;
  offset: number;
  datatype: number;
  count: number;

  constructor(init?: Partial<PointField>) {
    this.name = init?.name ?? "";
    this.offset = init?.offset ?? 0;
    this.datatype = init?.datatype ?? 0;
    this.count = init?.count ?? 0;
  }

  static decode(data: Uint8Array): PointField {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = PointField._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new PointField();
    ({ offset } = PointField._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: PointField): { offset: number } {
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.name = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    target.offset = view.getInt32(offset, false);
    offset += 4;
    target.datatype = view.getUint8(offset);
    offset += 1;
    target.count = view.getInt32(offset, false);
    offset += 4;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, PointField._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    {
      const encoded = new TextEncoder().encode(this.name);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    view.setInt32(offset, this.offset, false);
    offset += 4;
    view.setUint8(offset, this.datatype);
    offset += 1;
    view.setInt32(offset, this.count, false);
    offset += 4;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4 + new TextEncoder().encode(this.name).length + 1;
    size += 4;
    size += 1;
    size += 4;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(PointField)) return 0n;
    const newparents = [...parents, PointField];
    let tmphash = PointField._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (PointField._packedFingerprint === null) {
      PointField._packedFingerprint = PointField._getHashRecursive([]);
    }
    return PointField._packedFingerprint;
  }
}
