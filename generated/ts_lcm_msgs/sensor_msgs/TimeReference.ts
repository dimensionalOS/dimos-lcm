// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { Time } from "../std_msgs/Time.ts";

export class TimeReference {
  static readonly _HASH = 0xdccee6deeae4c6can;
  static readonly _NAME = "sensor_msgs.TimeReference";

  header: Header;
  time_ref: Time;
  source: string;

  constructor(init?: Partial<TimeReference>) {
    this.header = init?.header ?? new Header();
    this.time_ref = init?.time_ref ?? new Time();
    this.source = init?.source ?? "";
  }

  static decode(data: Uint8Array): TimeReference {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== TimeReference._HASH) {
      throw new Error(`Hash mismatch: expected ${TimeReference._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new TimeReference();
    ({ offset } = TimeReference._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: TimeReference): { offset: number } {
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.time_ref = new Time();
    ({ offset } = Time._decodeOne(view, offset, target.time_ref));
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.source = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, TimeReference._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.header._encodeOne(view, offset);
    offset = this.time_ref._encodeOne(view, offset);
    {
      const encoded = new TextEncoder().encode(this.source);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.header._encodedSize();
    size += this.time_ref._encodedSize();
    size += 4 + new TextEncoder().encode(this.source).length + 1;
    return size;
  }
}
