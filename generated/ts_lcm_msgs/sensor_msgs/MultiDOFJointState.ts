// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { Transform } from "../geometry_msgs/Transform.ts";
import { Twist } from "../geometry_msgs/Twist.ts";
import { Wrench } from "../geometry_msgs/Wrench.ts";

export class MultiDOFJointState {
  static readonly _HASH = 0xd0eee4cadcc6d000n;
  static readonly _NAME = "sensor_msgs.MultiDOFJointState";

  joint_names_length: number;
  transforms_length: number;
  twist_length: number;
  wrench_length: number;
  header: Header;
  joint_names: string[];
  transforms: Transform[];
  twist: Twist[];
  wrench: Wrench[];

  constructor(init?: Partial<MultiDOFJointState>) {
    this.joint_names_length = init?.joint_names_length ?? 0;
    this.transforms_length = init?.transforms_length ?? 0;
    this.twist_length = init?.twist_length ?? 0;
    this.wrench_length = init?.wrench_length ?? 0;
    this.header = init?.header ?? new Header();
    this.joint_names = init?.joint_names ?? [];
    this.transforms = init?.transforms ?? [];
    this.twist = init?.twist ?? [];
    this.wrench = init?.wrench ?? [];
  }

  static decode(data: Uint8Array): MultiDOFJointState {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== MultiDOFJointState._HASH) {
      throw new Error(`Hash mismatch: expected ${MultiDOFJointState._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new MultiDOFJointState();
    ({ offset } = MultiDOFJointState._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: MultiDOFJointState): { offset: number } {
    target.joint_names_length = view.getInt32(offset, false);
    offset += 4;
    target.transforms_length = view.getInt32(offset, false);
    offset += 4;
    target.twist_length = view.getInt32(offset, false);
    offset += 4;
    target.wrench_length = view.getInt32(offset, false);
    offset += 4;
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.joint_names = new Array(target.joint_names_length);
    for (let i0 = 0; i0 < target.joint_names_length; i0++) {
      {
        const len = view.getUint32(offset, false);
        offset += 4;
        target.joint_names[i0] = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
        offset += len;
      }
    }
    target.transforms = new Array(target.transforms_length);
    for (let i0 = 0; i0 < target.transforms_length; i0++) {
      target.transforms[i0] = new Transform();
      ({ offset } = Transform._decodeOne(view, offset, target.transforms[i0]));
    }
    target.twist = new Array(target.twist_length);
    for (let i0 = 0; i0 < target.twist_length; i0++) {
      target.twist[i0] = new Twist();
      ({ offset } = Twist._decodeOne(view, offset, target.twist[i0]));
    }
    target.wrench = new Array(target.wrench_length);
    for (let i0 = 0; i0 < target.wrench_length; i0++) {
      target.wrench[i0] = new Wrench();
      ({ offset } = Wrench._decodeOne(view, offset, target.wrench[i0]));
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, MultiDOFJointState._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.joint_names_length, false);
    offset += 4;
    view.setInt32(offset, this.transforms_length, false);
    offset += 4;
    view.setInt32(offset, this.twist_length, false);
    offset += 4;
    view.setInt32(offset, this.wrench_length, false);
    offset += 4;
    offset = this.header._encodeOne(view, offset);
    for (let i0 = 0; i0 < this.joint_names_length; i0++) {
      {
        const encoded = new TextEncoder().encode(this.joint_names[i0]);
        view.setUint32(offset, encoded.length + 1, false);
        offset += 4;
        new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
        offset += encoded.length;
        view.setUint8(offset, 0); // null terminator
        offset += 1;
      }
    }
    for (let i0 = 0; i0 < this.transforms_length; i0++) {
      offset = this.transforms[i0]._encodeOne(view, offset);
    }
    for (let i0 = 0; i0 < this.twist_length; i0++) {
      offset = this.twist[i0]._encodeOne(view, offset);
    }
    for (let i0 = 0; i0 < this.wrench_length; i0++) {
      offset = this.wrench[i0]._encodeOne(view, offset);
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    size += 4;
    size += 4;
    size += this.header._encodedSize();
    for (let i0 = 0; i0 < this.joint_names_length; i0++) {
      size += 4 + new TextEncoder().encode(this.joint_names[i0]).length + 1;
    }
    for (let i0 = 0; i0 < this.transforms_length; i0++) {
      size += this.transforms[i0]._encodedSize();
    }
    for (let i0 = 0; i0 < this.twist_length; i0++) {
      size += this.twist[i0]._encodedSize();
    }
    for (let i0 = 0; i0 < this.wrench_length; i0++) {
      size += this.wrench[i0]._encodedSize();
    }
    return size;
  }
}
