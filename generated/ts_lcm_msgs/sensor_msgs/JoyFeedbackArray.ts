// Auto-generated by lcm-ts. DO NOT EDIT.

import { JoyFeedback } from "./JoyFeedback.ts";

export class JoyFeedbackArray {
  static readonly _HASH = 0xc6d6c2e4e4c2f200n;
  static readonly _NAME = "sensor_msgs.JoyFeedbackArray";

  array_length: number;
  array: JoyFeedback[];

  constructor(init?: Partial<JoyFeedbackArray>) {
    this.array_length = init?.array_length ?? 0;
    this.array = init?.array ?? [];
  }

  static decode(data: Uint8Array): JoyFeedbackArray {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== JoyFeedbackArray._HASH) {
      throw new Error(`Hash mismatch: expected ${JoyFeedbackArray._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new JoyFeedbackArray();
    ({ offset } = JoyFeedbackArray._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: JoyFeedbackArray): { offset: number } {
    target.array_length = view.getInt32(offset, false);
    offset += 4;
    target.array = new Array(target.array_length);
    for (let i0 = 0; i0 < target.array_length; i0++) {
      target.array[i0] = new JoyFeedback();
      ({ offset } = JoyFeedback._decodeOne(view, offset, target.array[i0]));
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, JoyFeedbackArray._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.array_length, false);
    offset += 4;
    for (let i0 = 0; i0 < this.array_length; i0++) {
      offset = this.array[i0]._encodeOne(view, offset);
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    for (let i0 = 0; i0 < this.array_length; i0++) {
      size += this.array[i0]._encodedSize();
    }
    return size;
  }
}
