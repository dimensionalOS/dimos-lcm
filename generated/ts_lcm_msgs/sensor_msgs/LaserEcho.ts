// Auto-generated by lcm-ts. DO NOT EDIT.

export class LaserEcho {
  static readonly _HASH = 0xc45702116c7d0a2en;
  static readonly _NAME = "sensor_msgs.LaserEcho";
  private static _packedFingerprint: bigint | null = null;

  echoes_length: number;
  echoes: number[];

  constructor(init?: Partial<LaserEcho>) {
    this.echoes_length = init?.echoes_length ?? 0;
    this.echoes = init?.echoes ?? [];
  }

  static decode(data: Uint8Array): LaserEcho {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = LaserEcho._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new LaserEcho();
    ({ offset } = LaserEcho._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: LaserEcho): { offset: number } {
    target.echoes_length = view.getInt32(offset, false);
    offset += 4;
    target.echoes = new Array(target.echoes_length);
    for (let i0 = 0; i0 < target.echoes_length; i0++) {
      target.echoes[i0] = view.getFloat32(offset, false);
      offset += 4;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, LaserEcho._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.echoes_length, false);
    offset += 4;
    for (let i0 = 0; i0 < this.echoes_length; i0++) {
      view.setFloat32(offset, this.echoes[i0], false);
      offset += 4;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += this.echoes_length * 4;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(LaserEcho)) return 0n;
    const newparents = [...parents, LaserEcho];
    let tmphash = LaserEcho._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (LaserEcho._packedFingerprint === null) {
      LaserEcho._packedFingerprint = LaserEcho._getHashRecursive([]);
    }
    return LaserEcho._packedFingerprint;
  }
}
