// Auto-generated by lcm-ts. DO NOT EDIT.

export class NavSatStatus {
  static readonly _HASH = 0x76b236592075c1dbn;
  static readonly _NAME = "sensor_msgs.NavSatStatus";
  private static _packedFingerprint: bigint | null = null;

  static readonly STATUS_NO_FIX = 1;
  static readonly STATUS_FIX = 0;
  static readonly STATUS_SBAS_FIX = 1;
  static readonly STATUS_GBAS_FIX = 2;
  static readonly SERVICE_GPS = 1;
  static readonly SERVICE_GLONASS = 2;
  static readonly SERVICE_COMPASS = 4;
  static readonly SERVICE_GALILEO = 8;

  status: number;
  service: number;

  constructor(init?: Partial<NavSatStatus>) {
    this.status = init?.status ?? 0;
    this.service = init?.service ?? 0;
  }

  static decode(data: Uint8Array): NavSatStatus {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = NavSatStatus._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new NavSatStatus();
    ({ offset } = NavSatStatus._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: NavSatStatus): { offset: number } {
    target.status = view.getInt8(offset);
    offset += 1;
    target.service = view.getInt16(offset, false);
    offset += 2;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, NavSatStatus._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt8(offset, this.status);
    offset += 1;
    view.setInt16(offset, this.service, false);
    offset += 2;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 1;
    size += 2;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(NavSatStatus)) return 0n;
    const newparents = [...parents, NavSatStatus];
    let tmphash = NavSatStatus._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (NavSatStatus._packedFingerprint === null) {
      NavSatStatus._packedFingerprint = NavSatStatus._getHashRecursive([]);
    }
    return NavSatStatus._packedFingerprint;
  }
}
