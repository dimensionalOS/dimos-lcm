// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { Point32 } from "../geometry_msgs/Point32.ts";
import { ChannelFloat32 } from "./ChannelFloat32.ts";

export class PointCloud {
  static readonly _HASH = 0x3ff7dc99e345164en;
  static readonly _NAME = "sensor_msgs.PointCloud";
  private static _packedFingerprint: bigint | null = null;

  points_length: number;
  channels_length: number;
  header: Header;
  points: Point32[];
  channels: ChannelFloat32[];

  constructor(init?: Partial<PointCloud>) {
    this.points_length = init?.points_length ?? 0;
    this.channels_length = init?.channels_length ?? 0;
    this.header = init?.header ?? new Header();
    this.points = init?.points ?? [];
    this.channels = init?.channels ?? [];
  }

  static decode(data: Uint8Array): PointCloud {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = PointCloud._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new PointCloud();
    ({ offset } = PointCloud._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: PointCloud): { offset: number } {
    target.points_length = view.getInt32(offset, false);
    offset += 4;
    target.channels_length = view.getInt32(offset, false);
    offset += 4;
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.points = new Array(target.points_length);
    for (let i0 = 0; i0 < target.points_length; i0++) {
      target.points[i0] = new Point32();
      ({ offset } = Point32._decodeOne(view, offset, target.points[i0]));
    }
    target.channels = new Array(target.channels_length);
    for (let i0 = 0; i0 < target.channels_length; i0++) {
      target.channels[i0] = new ChannelFloat32();
      ({ offset } = ChannelFloat32._decodeOne(view, offset, target.channels[i0]));
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, PointCloud._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.points_length, false);
    offset += 4;
    view.setInt32(offset, this.channels_length, false);
    offset += 4;
    offset = this.header._encodeOne(view, offset);
    for (let i0 = 0; i0 < this.points_length; i0++) {
      offset = this.points[i0]._encodeOne(view, offset);
    }
    for (let i0 = 0; i0 < this.channels_length; i0++) {
      offset = this.channels[i0]._encodeOne(view, offset);
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    size += this.header._encodedSize();
    for (let i0 = 0; i0 < this.points_length; i0++) {
      size += this.points[i0]._encodedSize();
    }
    for (let i0 = 0; i0 < this.channels_length; i0++) {
      size += this.channels[i0]._encodedSize();
    }
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(PointCloud)) return 0n;
    const newparents = [...parents, PointCloud];
    let tmphash = PointCloud._HASH;
    tmphash = (tmphash + Header._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Point32._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + ChannelFloat32._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (PointCloud._packedFingerprint === null) {
      PointCloud._packedFingerprint = PointCloud._getHashRecursive([]);
    }
    return PointCloud._packedFingerprint;
  }
}
