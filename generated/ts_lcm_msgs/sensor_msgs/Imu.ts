// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { Quaternion } from "../geometry_msgs/Quaternion.ts";
import { Vector3 } from "../geometry_msgs/Vector3.ts";

export class Imu {
  static readonly _HASH = 0xe4d2c2dcc6ca0212n;
  static readonly _NAME = "sensor_msgs.Imu";

  header: Header;
  orientation: Quaternion;
  orientation_covariance: number[];
  angular_velocity: Vector3;
  angular_velocity_covariance: number[];
  linear_acceleration: Vector3;
  linear_acceleration_covariance: number[];

  constructor(init?: Partial<Imu>) {
    this.header = init?.header ?? new Header();
    this.orientation = init?.orientation ?? new Quaternion();
    this.orientation_covariance = init?.orientation_covariance ?? Array.from({ length: 9 }, () => 0);
    this.angular_velocity = init?.angular_velocity ?? new Vector3();
    this.angular_velocity_covariance = init?.angular_velocity_covariance ?? Array.from({ length: 9 }, () => 0);
    this.linear_acceleration = init?.linear_acceleration ?? new Vector3();
    this.linear_acceleration_covariance = init?.linear_acceleration_covariance ?? Array.from({ length: 9 }, () => 0);
  }

  static decode(data: Uint8Array): Imu {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== Imu._HASH) {
      throw new Error(`Hash mismatch: expected ${Imu._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Imu();
    ({ offset } = Imu._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Imu): { offset: number } {
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.orientation = new Quaternion();
    ({ offset } = Quaternion._decodeOne(view, offset, target.orientation));
    target.orientation_covariance = new Array(9);
    for (let i0 = 0; i0 < 9; i0++) {
      target.orientation_covariance[i0] = view.getFloat64(offset, false);
      offset += 8;
    }
    target.angular_velocity = new Vector3();
    ({ offset } = Vector3._decodeOne(view, offset, target.angular_velocity));
    target.angular_velocity_covariance = new Array(9);
    for (let i0 = 0; i0 < 9; i0++) {
      target.angular_velocity_covariance[i0] = view.getFloat64(offset, false);
      offset += 8;
    }
    target.linear_acceleration = new Vector3();
    ({ offset } = Vector3._decodeOne(view, offset, target.linear_acceleration));
    target.linear_acceleration_covariance = new Array(9);
    for (let i0 = 0; i0 < 9; i0++) {
      target.linear_acceleration_covariance[i0] = view.getFloat64(offset, false);
      offset += 8;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, Imu._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.header._encodeOne(view, offset);
    offset = this.orientation._encodeOne(view, offset);
    for (let i0 = 0; i0 < 9; i0++) {
      view.setFloat64(offset, this.orientation_covariance[i0], false);
      offset += 8;
    }
    offset = this.angular_velocity._encodeOne(view, offset);
    for (let i0 = 0; i0 < 9; i0++) {
      view.setFloat64(offset, this.angular_velocity_covariance[i0], false);
      offset += 8;
    }
    offset = this.linear_acceleration._encodeOne(view, offset);
    for (let i0 = 0; i0 < 9; i0++) {
      view.setFloat64(offset, this.linear_acceleration_covariance[i0], false);
      offset += 8;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.header._encodedSize();
    size += this.orientation._encodedSize();
    size += 9 * 8;
    size += this.angular_velocity._encodedSize();
    size += 9 * 8;
    size += this.linear_acceleration._encodedSize();
    size += 9 * 8;
    return size;
  }
}
