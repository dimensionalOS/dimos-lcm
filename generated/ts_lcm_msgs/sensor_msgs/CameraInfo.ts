// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { RegionOfInterest } from "./RegionOfInterest.ts";

export class CameraInfo {
  static readonly _HASH = 0xcae4cae6e8e4ded2n;
  static readonly _NAME = "sensor_msgs.CameraInfo";

  D_length: number;
  header: Header;
  height: number;
  width: number;
  distortion_model: string;
  D: number[];
  K: number[];
  R: number[];
  P: number[];
  binning_x: number;
  binning_y: number;
  roi: RegionOfInterest;

  constructor(init?: Partial<CameraInfo>) {
    this.D_length = init?.D_length ?? 0;
    this.header = init?.header ?? new Header();
    this.height = init?.height ?? 0;
    this.width = init?.width ?? 0;
    this.distortion_model = init?.distortion_model ?? "";
    this.D = init?.D ?? [];
    this.K = init?.K ?? Array.from({ length: 9 }, () => 0);
    this.R = init?.R ?? Array.from({ length: 9 }, () => 0);
    this.P = init?.P ?? Array.from({ length: 12 }, () => 0);
    this.binning_x = init?.binning_x ?? 0;
    this.binning_y = init?.binning_y ?? 0;
    this.roi = init?.roi ?? new RegionOfInterest();
  }

  static decode(data: Uint8Array): CameraInfo {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== CameraInfo._HASH) {
      throw new Error(`Hash mismatch: expected ${CameraInfo._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new CameraInfo();
    ({ offset } = CameraInfo._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: CameraInfo): { offset: number } {
    target.D_length = view.getInt32(offset, false);
    offset += 4;
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.height = view.getInt32(offset, false);
    offset += 4;
    target.width = view.getInt32(offset, false);
    offset += 4;
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.distortion_model = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    target.D = new Array(target.D_length);
    for (let i0 = 0; i0 < target.D_length; i0++) {
      target.D[i0] = view.getFloat64(offset, false);
      offset += 8;
    }
    target.K = new Array(9);
    for (let i0 = 0; i0 < 9; i0++) {
      target.K[i0] = view.getFloat64(offset, false);
      offset += 8;
    }
    target.R = new Array(9);
    for (let i0 = 0; i0 < 9; i0++) {
      target.R[i0] = view.getFloat64(offset, false);
      offset += 8;
    }
    target.P = new Array(12);
    for (let i0 = 0; i0 < 12; i0++) {
      target.P[i0] = view.getFloat64(offset, false);
      offset += 8;
    }
    target.binning_x = view.getInt32(offset, false);
    offset += 4;
    target.binning_y = view.getInt32(offset, false);
    offset += 4;
    target.roi = new RegionOfInterest();
    ({ offset } = RegionOfInterest._decodeOne(view, offset, target.roi));
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, CameraInfo._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.D_length, false);
    offset += 4;
    offset = this.header._encodeOne(view, offset);
    view.setInt32(offset, this.height, false);
    offset += 4;
    view.setInt32(offset, this.width, false);
    offset += 4;
    {
      const encoded = new TextEncoder().encode(this.distortion_model);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    for (let i0 = 0; i0 < this.D_length; i0++) {
      view.setFloat64(offset, this.D[i0], false);
      offset += 8;
    }
    for (let i0 = 0; i0 < 9; i0++) {
      view.setFloat64(offset, this.K[i0], false);
      offset += 8;
    }
    for (let i0 = 0; i0 < 9; i0++) {
      view.setFloat64(offset, this.R[i0], false);
      offset += 8;
    }
    for (let i0 = 0; i0 < 12; i0++) {
      view.setFloat64(offset, this.P[i0], false);
      offset += 8;
    }
    view.setInt32(offset, this.binning_x, false);
    offset += 4;
    view.setInt32(offset, this.binning_y, false);
    offset += 4;
    offset = this.roi._encodeOne(view, offset);
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += this.header._encodedSize();
    size += 4;
    size += 4;
    size += 4 + new TextEncoder().encode(this.distortion_model).length + 1;
    size += this.D_length * 8;
    size += 9 * 8;
    size += 9 * 8;
    size += 12 * 8;
    size += 4;
    size += 4;
    size += this.roi._encodedSize();
    return size;
  }
}
