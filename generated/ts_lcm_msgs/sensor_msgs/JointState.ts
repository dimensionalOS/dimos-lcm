// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";

export class JointState {
  static readonly _HASH = 0xb69cc30d0d7668fan;
  static readonly _NAME = "sensor_msgs.JointState";
  private static _packedFingerprint: bigint | null = null;

  name_length: number;
  position_length: number;
  velocity_length: number;
  effort_length: number;
  header: Header;
  name: string[];
  position: number[];
  velocity: number[];
  effort: number[];

  constructor(init?: Partial<JointState>) {
    this.name_length = init?.name_length ?? 0;
    this.position_length = init?.position_length ?? 0;
    this.velocity_length = init?.velocity_length ?? 0;
    this.effort_length = init?.effort_length ?? 0;
    this.header = init?.header ?? new Header();
    this.name = init?.name ?? [];
    this.position = init?.position ?? [];
    this.velocity = init?.velocity ?? [];
    this.effort = init?.effort ?? [];
  }

  static decode(data: Uint8Array): JointState {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = JointState._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new JointState();
    ({ offset } = JointState._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: JointState): { offset: number } {
    target.name_length = view.getInt32(offset, false);
    offset += 4;
    target.position_length = view.getInt32(offset, false);
    offset += 4;
    target.velocity_length = view.getInt32(offset, false);
    offset += 4;
    target.effort_length = view.getInt32(offset, false);
    offset += 4;
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.name = new Array(target.name_length);
    for (let i0 = 0; i0 < target.name_length; i0++) {
      {
        const len = view.getUint32(offset, false);
        offset += 4;
        target.name[i0] = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
        offset += len;
      }
    }
    target.position = new Array(target.position_length);
    for (let i0 = 0; i0 < target.position_length; i0++) {
      target.position[i0] = view.getFloat64(offset, false);
      offset += 8;
    }
    target.velocity = new Array(target.velocity_length);
    for (let i0 = 0; i0 < target.velocity_length; i0++) {
      target.velocity[i0] = view.getFloat64(offset, false);
      offset += 8;
    }
    target.effort = new Array(target.effort_length);
    for (let i0 = 0; i0 < target.effort_length; i0++) {
      target.effort[i0] = view.getFloat64(offset, false);
      offset += 8;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, JointState._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.name_length, false);
    offset += 4;
    view.setInt32(offset, this.position_length, false);
    offset += 4;
    view.setInt32(offset, this.velocity_length, false);
    offset += 4;
    view.setInt32(offset, this.effort_length, false);
    offset += 4;
    offset = this.header._encodeOne(view, offset);
    for (let i0 = 0; i0 < this.name_length; i0++) {
      {
        const encoded = new TextEncoder().encode(this.name[i0]);
        view.setUint32(offset, encoded.length + 1, false);
        offset += 4;
        new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
        offset += encoded.length;
        view.setUint8(offset, 0); // null terminator
        offset += 1;
      }
    }
    for (let i0 = 0; i0 < this.position_length; i0++) {
      view.setFloat64(offset, this.position[i0], false);
      offset += 8;
    }
    for (let i0 = 0; i0 < this.velocity_length; i0++) {
      view.setFloat64(offset, this.velocity[i0], false);
      offset += 8;
    }
    for (let i0 = 0; i0 < this.effort_length; i0++) {
      view.setFloat64(offset, this.effort[i0], false);
      offset += 8;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    size += 4;
    size += 4;
    size += this.header._encodedSize();
    for (let i0 = 0; i0 < this.name_length; i0++) {
      size += 4 + new TextEncoder().encode(this.name[i0]).length + 1;
    }
    size += this.position_length * 8;
    size += this.velocity_length * 8;
    size += this.effort_length * 8;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(JointState)) return 0n;
    const newparents = [...parents, JointState];
    let tmphash = JointState._HASH;
    tmphash = (tmphash + Header._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (JointState._packedFingerprint === null) {
      JointState._packedFingerprint = JointState._getHashRecursive([]);
    }
    return JointState._packedFingerprint;
  }
}
