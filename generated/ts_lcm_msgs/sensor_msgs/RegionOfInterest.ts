// Auto-generated by lcm-ts. DO NOT EDIT.

export class RegionOfInterest {
  static readonly _HASH = 0x398a869d05983f0en;
  static readonly _NAME = "sensor_msgs.RegionOfInterest";
  private static _packedFingerprint: bigint | null = null;

  x_offset: number;
  y_offset: number;
  height: number;
  width: number;
  do_rectify: boolean;

  constructor(init?: Partial<RegionOfInterest>) {
    this.x_offset = init?.x_offset ?? 0;
    this.y_offset = init?.y_offset ?? 0;
    this.height = init?.height ?? 0;
    this.width = init?.width ?? 0;
    this.do_rectify = init?.do_rectify ?? false;
  }

  static decode(data: Uint8Array): RegionOfInterest {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = RegionOfInterest._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new RegionOfInterest();
    ({ offset } = RegionOfInterest._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: RegionOfInterest): { offset: number } {
    target.x_offset = view.getInt32(offset, false);
    offset += 4;
    target.y_offset = view.getInt32(offset, false);
    offset += 4;
    target.height = view.getInt32(offset, false);
    offset += 4;
    target.width = view.getInt32(offset, false);
    offset += 4;
    target.do_rectify = view.getInt8(offset) !== 0;
    offset += 1;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, RegionOfInterest._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.x_offset, false);
    offset += 4;
    view.setInt32(offset, this.y_offset, false);
    offset += 4;
    view.setInt32(offset, this.height, false);
    offset += 4;
    view.setInt32(offset, this.width, false);
    offset += 4;
    view.setInt8(offset, this.do_rectify ? 1 : 0);
    offset += 1;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    size += 4;
    size += 4;
    size += 1;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(RegionOfInterest)) return 0n;
    const newparents = [...parents, RegionOfInterest];
    let tmphash = RegionOfInterest._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (RegionOfInterest._packedFingerprint === null) {
      RegionOfInterest._packedFingerprint = RegionOfInterest._getHashRecursive([]);
    }
    return RegionOfInterest._packedFingerprint;
  }
}
