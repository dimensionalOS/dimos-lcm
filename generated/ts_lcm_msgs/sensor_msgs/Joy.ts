// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";

export class Joy {
  static readonly _HASH = 0x209d2c19077a692fn;
  static readonly _NAME = "sensor_msgs.Joy";
  private static _packedFingerprint: bigint | null = null;

  axes_length: number;
  buttons_length: number;
  header: Header;
  axes: number[];
  buttons: number[];

  constructor(init?: Partial<Joy>) {
    this.axes_length = init?.axes_length ?? 0;
    this.buttons_length = init?.buttons_length ?? 0;
    this.header = init?.header ?? new Header();
    this.axes = init?.axes ?? [];
    this.buttons = init?.buttons ?? [];
  }

  static decode(data: Uint8Array): Joy {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = Joy._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Joy();
    ({ offset } = Joy._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Joy): { offset: number } {
    target.axes_length = view.getInt32(offset, false);
    offset += 4;
    target.buttons_length = view.getInt32(offset, false);
    offset += 4;
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.axes = new Array(target.axes_length);
    for (let i0 = 0; i0 < target.axes_length; i0++) {
      target.axes[i0] = view.getFloat32(offset, false);
      offset += 4;
    }
    target.buttons = new Array(target.buttons_length);
    for (let i0 = 0; i0 < target.buttons_length; i0++) {
      target.buttons[i0] = view.getInt32(offset, false);
      offset += 4;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, Joy._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.axes_length, false);
    offset += 4;
    view.setInt32(offset, this.buttons_length, false);
    offset += 4;
    offset = this.header._encodeOne(view, offset);
    for (let i0 = 0; i0 < this.axes_length; i0++) {
      view.setFloat32(offset, this.axes[i0], false);
      offset += 4;
    }
    for (let i0 = 0; i0 < this.buttons_length; i0++) {
      view.setInt32(offset, this.buttons[i0], false);
      offset += 4;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    size += this.header._encodedSize();
    size += this.axes_length * 4;
    size += this.buttons_length * 4;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(Joy)) return 0n;
    const newparents = [...parents, Joy];
    let tmphash = Joy._HASH;
    tmphash = (tmphash + Header._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (Joy._packedFingerprint === null) {
      Joy._packedFingerprint = Joy._getHashRecursive([]);
    }
    return Joy._packedFingerprint;
  }
}
