// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { LaserEcho } from "./LaserEcho.ts";

export class MultiEchoLaserScan {
  static readonly _HASH = 0xf02a6253849d18e2n;
  static readonly _NAME = "sensor_msgs.MultiEchoLaserScan";
  private static _packedFingerprint: bigint | null = null;

  ranges_length: number;
  intensities_length: number;
  header: Header;
  angle_min: number;
  angle_max: number;
  angle_increment: number;
  time_increment: number;
  scan_time: number;
  range_min: number;
  range_max: number;
  ranges: LaserEcho[];
  intensities: LaserEcho[];

  constructor(init?: Partial<MultiEchoLaserScan>) {
    this.ranges_length = init?.ranges_length ?? 0;
    this.intensities_length = init?.intensities_length ?? 0;
    this.header = init?.header ?? new Header();
    this.angle_min = init?.angle_min ?? 0;
    this.angle_max = init?.angle_max ?? 0;
    this.angle_increment = init?.angle_increment ?? 0;
    this.time_increment = init?.time_increment ?? 0;
    this.scan_time = init?.scan_time ?? 0;
    this.range_min = init?.range_min ?? 0;
    this.range_max = init?.range_max ?? 0;
    this.ranges = init?.ranges ?? [];
    this.intensities = init?.intensities ?? [];
  }

  static decode(data: Uint8Array): MultiEchoLaserScan {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = MultiEchoLaserScan._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new MultiEchoLaserScan();
    ({ offset } = MultiEchoLaserScan._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: MultiEchoLaserScan): { offset: number } {
    target.ranges_length = view.getInt32(offset, false);
    offset += 4;
    target.intensities_length = view.getInt32(offset, false);
    offset += 4;
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.angle_min = view.getFloat32(offset, false);
    offset += 4;
    target.angle_max = view.getFloat32(offset, false);
    offset += 4;
    target.angle_increment = view.getFloat32(offset, false);
    offset += 4;
    target.time_increment = view.getFloat32(offset, false);
    offset += 4;
    target.scan_time = view.getFloat32(offset, false);
    offset += 4;
    target.range_min = view.getFloat32(offset, false);
    offset += 4;
    target.range_max = view.getFloat32(offset, false);
    offset += 4;
    target.ranges = new Array(target.ranges_length);
    for (let i0 = 0; i0 < target.ranges_length; i0++) {
      target.ranges[i0] = new LaserEcho();
      ({ offset } = LaserEcho._decodeOne(view, offset, target.ranges[i0]));
    }
    target.intensities = new Array(target.intensities_length);
    for (let i0 = 0; i0 < target.intensities_length; i0++) {
      target.intensities[i0] = new LaserEcho();
      ({ offset } = LaserEcho._decodeOne(view, offset, target.intensities[i0]));
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, MultiEchoLaserScan._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.ranges_length, false);
    offset += 4;
    view.setInt32(offset, this.intensities_length, false);
    offset += 4;
    offset = this.header._encodeOne(view, offset);
    view.setFloat32(offset, this.angle_min, false);
    offset += 4;
    view.setFloat32(offset, this.angle_max, false);
    offset += 4;
    view.setFloat32(offset, this.angle_increment, false);
    offset += 4;
    view.setFloat32(offset, this.time_increment, false);
    offset += 4;
    view.setFloat32(offset, this.scan_time, false);
    offset += 4;
    view.setFloat32(offset, this.range_min, false);
    offset += 4;
    view.setFloat32(offset, this.range_max, false);
    offset += 4;
    for (let i0 = 0; i0 < this.ranges_length; i0++) {
      offset = this.ranges[i0]._encodeOne(view, offset);
    }
    for (let i0 = 0; i0 < this.intensities_length; i0++) {
      offset = this.intensities[i0]._encodeOne(view, offset);
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    size += this.header._encodedSize();
    size += 4;
    size += 4;
    size += 4;
    size += 4;
    size += 4;
    size += 4;
    size += 4;
    for (let i0 = 0; i0 < this.ranges_length; i0++) {
      size += this.ranges[i0]._encodedSize();
    }
    for (let i0 = 0; i0 < this.intensities_length; i0++) {
      size += this.intensities[i0]._encodedSize();
    }
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(MultiEchoLaserScan)) return 0n;
    const newparents = [...parents, MultiEchoLaserScan];
    let tmphash = MultiEchoLaserScan._HASH;
    tmphash = (tmphash + Header._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + LaserEcho._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + LaserEcho._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (MultiEchoLaserScan._packedFingerprint === null) {
      MultiEchoLaserScan._packedFingerprint = MultiEchoLaserScan._getHashRecursive([]);
    }
    return MultiEchoLaserScan._packedFingerprint;
  }
}
