// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { Vector3 } from "../geometry_msgs/Vector3.ts";

export class MagneticField {
  static readonly _HASH = 0xbfeb021e4751ed88n;
  static readonly _NAME = "sensor_msgs.MagneticField";
  private static _packedFingerprint: bigint | null = null;

  header: Header;
  magnetic_field: Vector3;
  magnetic_field_covariance: number[];

  constructor(init?: Partial<MagneticField>) {
    this.header = init?.header ?? new Header();
    this.magnetic_field = init?.magnetic_field ?? new Vector3();
    this.magnetic_field_covariance = init?.magnetic_field_covariance ?? Array.from({ length: 9 }, () => 0);
  }

  static decode(data: Uint8Array): MagneticField {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = MagneticField._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new MagneticField();
    ({ offset } = MagneticField._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: MagneticField): { offset: number } {
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.magnetic_field = new Vector3();
    ({ offset } = Vector3._decodeOne(view, offset, target.magnetic_field));
    target.magnetic_field_covariance = new Array(9);
    for (let i0 = 0; i0 < 9; i0++) {
      target.magnetic_field_covariance[i0] = view.getFloat64(offset, false);
      offset += 8;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, MagneticField._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.header._encodeOne(view, offset);
    offset = this.magnetic_field._encodeOne(view, offset);
    for (let i0 = 0; i0 < 9; i0++) {
      view.setFloat64(offset, this.magnetic_field_covariance[i0], false);
      offset += 8;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.header._encodedSize();
    size += this.magnetic_field._encodedSize();
    size += 9 * 8;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(MagneticField)) return 0n;
    const newparents = [...parents, MagneticField];
    let tmphash = MagneticField._HASH;
    tmphash = (tmphash + Header._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Vector3._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (MagneticField._packedFingerprint === null) {
      MagneticField._packedFingerprint = MagneticField._getHashRecursive([]);
    }
    return MagneticField._packedFingerprint;
  }
}
