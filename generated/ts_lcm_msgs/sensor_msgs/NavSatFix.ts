// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { NavSatStatus } from "./NavSatStatus.ts";

export class NavSatFix {
  static readonly _HASH = 0x4a84d20526d9067an;
  static readonly _NAME = "sensor_msgs.NavSatFix";
  private static _packedFingerprint: bigint | null = null;

  static readonly COVARIANCE_TYPE_UNKNOWN = 0;
  static readonly COVARIANCE_TYPE_APPROXIMATED = 1;
  static readonly COVARIANCE_TYPE_DIAGONAL_KNOWN = 2;
  static readonly COVARIANCE_TYPE_KNOWN = 3;

  header: Header;
  status: NavSatStatus;
  latitude: number;
  longitude: number;
  altitude: number;
  position_covariance: number[];
  position_covariance_type: number;

  constructor(init?: Partial<NavSatFix>) {
    this.header = init?.header ?? new Header();
    this.status = init?.status ?? new NavSatStatus();
    this.latitude = init?.latitude ?? 0;
    this.longitude = init?.longitude ?? 0;
    this.altitude = init?.altitude ?? 0;
    this.position_covariance = init?.position_covariance ?? Array.from({ length: 9 }, () => 0);
    this.position_covariance_type = init?.position_covariance_type ?? 0;
  }

  static decode(data: Uint8Array): NavSatFix {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = NavSatFix._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new NavSatFix();
    ({ offset } = NavSatFix._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: NavSatFix): { offset: number } {
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.status = new NavSatStatus();
    ({ offset } = NavSatStatus._decodeOne(view, offset, target.status));
    target.latitude = view.getFloat64(offset, false);
    offset += 8;
    target.longitude = view.getFloat64(offset, false);
    offset += 8;
    target.altitude = view.getFloat64(offset, false);
    offset += 8;
    target.position_covariance = new Array(9);
    for (let i0 = 0; i0 < 9; i0++) {
      target.position_covariance[i0] = view.getFloat64(offset, false);
      offset += 8;
    }
    target.position_covariance_type = view.getUint8(offset);
    offset += 1;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, NavSatFix._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.header._encodeOne(view, offset);
    offset = this.status._encodeOne(view, offset);
    view.setFloat64(offset, this.latitude, false);
    offset += 8;
    view.setFloat64(offset, this.longitude, false);
    offset += 8;
    view.setFloat64(offset, this.altitude, false);
    offset += 8;
    for (let i0 = 0; i0 < 9; i0++) {
      view.setFloat64(offset, this.position_covariance[i0], false);
      offset += 8;
    }
    view.setUint8(offset, this.position_covariance_type);
    offset += 1;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.header._encodedSize();
    size += this.status._encodedSize();
    size += 8;
    size += 8;
    size += 8;
    size += 9 * 8;
    size += 1;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(NavSatFix)) return 0n;
    const newparents = [...parents, NavSatFix];
    let tmphash = NavSatFix._HASH;
    tmphash = (tmphash + Header._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + NavSatStatus._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (NavSatFix._packedFingerprint === null) {
      NavSatFix._packedFingerprint = NavSatFix._getHashRecursive([]);
    }
    return NavSatFix._packedFingerprint;
  }
}
