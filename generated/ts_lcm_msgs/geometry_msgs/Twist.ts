// Auto-generated by lcm-ts. DO NOT EDIT.

import { Vector3 } from "./Vector3.ts";

export class Twist {
  static readonly _HASH = 0x3a4144772922add7n;
  static readonly _NAME = "geometry_msgs.Twist";
  private static _packedFingerprint: bigint | null = null;

  linear: Vector3;
  angular: Vector3;

  constructor(init?: Partial<Twist>) {
    this.linear = init?.linear ?? new Vector3();
    this.angular = init?.angular ?? new Vector3();
  }

  static decode(data: Uint8Array): Twist {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = Twist._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Twist();
    ({ offset } = Twist._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Twist): { offset: number } {
    target.linear = new Vector3();
    ({ offset } = Vector3._decodeOne(view, offset, target.linear));
    target.angular = new Vector3();
    ({ offset } = Vector3._decodeOne(view, offset, target.angular));
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, Twist._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.linear._encodeOne(view, offset);
    offset = this.angular._encodeOne(view, offset);
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.linear._encodedSize();
    size += this.angular._encodedSize();
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(Twist)) return 0n;
    const newparents = [...parents, Twist];
    let tmphash = Twist._HASH;
    tmphash = (tmphash + Vector3._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Vector3._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (Twist._packedFingerprint === null) {
      Twist._packedFingerprint = Twist._getHashRecursive([]);
    }
    return Twist._packedFingerprint;
  }
}
