// Auto-generated by lcm-ts. DO NOT EDIT.

import { Twist } from "./Twist.ts";

export class TwistWithCovariance {
  static readonly _HASH = 0xaba0a9d55e98da5dn;
  static readonly _NAME = "geometry_msgs.TwistWithCovariance";
  private static _packedFingerprint: bigint | null = null;

  twist: Twist;
  covariance: number[];

  constructor(init?: Partial<TwistWithCovariance>) {
    this.twist = init?.twist ?? new Twist();
    this.covariance = init?.covariance ?? Array.from({ length: 36 }, () => 0);
  }

  static decode(data: Uint8Array): TwistWithCovariance {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = TwistWithCovariance._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new TwistWithCovariance();
    ({ offset } = TwistWithCovariance._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: TwistWithCovariance): { offset: number } {
    target.twist = new Twist();
    ({ offset } = Twist._decodeOne(view, offset, target.twist));
    target.covariance = new Array(36);
    for (let i0 = 0; i0 < 36; i0++) {
      target.covariance[i0] = view.getFloat64(offset, false);
      offset += 8;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, TwistWithCovariance._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.twist._encodeOne(view, offset);
    for (let i0 = 0; i0 < 36; i0++) {
      view.setFloat64(offset, this.covariance[i0], false);
      offset += 8;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.twist._encodedSize();
    size += 36 * 8;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(TwistWithCovariance)) return 0n;
    const newparents = [...parents, TwistWithCovariance];
    let tmphash = TwistWithCovariance._HASH;
    tmphash = (tmphash + Twist._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (TwistWithCovariance._packedFingerprint === null) {
      TwistWithCovariance._packedFingerprint = TwistWithCovariance._getHashRecursive([]);
    }
    return TwistWithCovariance._packedFingerprint;
  }
}
