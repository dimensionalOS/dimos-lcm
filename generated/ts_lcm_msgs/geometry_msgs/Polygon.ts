// Auto-generated by lcm-ts. DO NOT EDIT.

import { Point32 } from "./Point32.ts";

export class Polygon {
  static readonly _HASH = 0x64e0ded2dce8e600n;
  static readonly _NAME = "geometry_msgs.Polygon";

  points_length: number;
  points: Point32[];

  constructor(init?: Partial<Polygon>) {
    this.points_length = init?.points_length ?? 0;
    this.points = init?.points ?? [];
  }

  static decode(data: Uint8Array): Polygon {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== Polygon._HASH) {
      throw new Error(`Hash mismatch: expected ${Polygon._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Polygon();
    ({ offset } = Polygon._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Polygon): { offset: number } {
    target.points_length = view.getInt32(offset, false);
    offset += 4;
    target.points = new Array(target.points_length);
    for (let i0 = 0; i0 < target.points_length; i0++) {
      target.points[i0] = new Point32();
      ({ offset } = Point32._decodeOne(view, offset, target.points[i0]));
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, Polygon._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.points_length, false);
    offset += 4;
    for (let i0 = 0; i0 < this.points_length; i0++) {
      offset = this.points[i0]._encodeOne(view, offset);
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    for (let i0 = 0; i0 < this.points_length; i0++) {
      size += this.points[i0]._encodedSize();
    }
    return size;
  }
}
