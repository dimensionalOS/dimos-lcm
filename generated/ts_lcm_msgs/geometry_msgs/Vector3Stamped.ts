// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { Vector3 } from "./Vector3.ts";

export class Vector3Stamped {
  static readonly _HASH = 0xecb8cfd985d1cbe0n;
  static readonly _NAME = "geometry_msgs.Vector3Stamped";
  private static _packedFingerprint: bigint | null = null;

  header: Header;
  vector: Vector3;

  constructor(init?: Partial<Vector3Stamped>) {
    this.header = init?.header ?? new Header();
    this.vector = init?.vector ?? new Vector3();
  }

  static decode(data: Uint8Array): Vector3Stamped {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = Vector3Stamped._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Vector3Stamped();
    ({ offset } = Vector3Stamped._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Vector3Stamped): { offset: number } {
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.vector = new Vector3();
    ({ offset } = Vector3._decodeOne(view, offset, target.vector));
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, Vector3Stamped._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.header._encodeOne(view, offset);
    offset = this.vector._encodeOne(view, offset);
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.header._encodedSize();
    size += this.vector._encodedSize();
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(Vector3Stamped)) return 0n;
    const newparents = [...parents, Vector3Stamped];
    let tmphash = Vector3Stamped._HASH;
    tmphash = (tmphash + Header._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Vector3._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (Vector3Stamped._packedFingerprint === null) {
      Vector3Stamped._packedFingerprint = Vector3Stamped._getHashRecursive([]);
    }
    return Vector3Stamped._packedFingerprint;
  }
}
