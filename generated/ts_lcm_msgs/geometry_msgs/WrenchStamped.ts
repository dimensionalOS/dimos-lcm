// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { Wrench } from "./Wrench.ts";

export class WrenchStamped {
  static readonly _HASH = 0xc6d0eee4cadcc6d0n;
  static readonly _NAME = "geometry_msgs.WrenchStamped";

  header: Header;
  wrench: Wrench;

  constructor(init?: Partial<WrenchStamped>) {
    this.header = init?.header ?? new Header();
    this.wrench = init?.wrench ?? new Wrench();
  }

  static decode(data: Uint8Array): WrenchStamped {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== WrenchStamped._HASH) {
      throw new Error(`Hash mismatch: expected ${WrenchStamped._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new WrenchStamped();
    ({ offset } = WrenchStamped._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: WrenchStamped): { offset: number } {
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.wrench = new Wrench();
    ({ offset } = Wrench._decodeOne(view, offset, target.wrench));
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, WrenchStamped._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.header._encodeOne(view, offset);
    offset = this.wrench._encodeOne(view, offset);
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.header._encodedSize();
    size += this.wrench._encodedSize();
    return size;
  }
}
