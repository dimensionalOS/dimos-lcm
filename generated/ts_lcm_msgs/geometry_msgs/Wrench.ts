// Auto-generated by lcm-ts. DO NOT EDIT.

import { Vector3 } from "./Vector3.ts";

export class Wrench {
  static readonly _HASH = 0xe466e8dee4e2eacan;
  static readonly _NAME = "geometry_msgs.Wrench";

  force: Vector3;
  torque: Vector3;

  constructor(init?: Partial<Wrench>) {
    this.force = init?.force ?? new Vector3();
    this.torque = init?.torque ?? new Vector3();
  }

  static decode(data: Uint8Array): Wrench {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== Wrench._HASH) {
      throw new Error(`Hash mismatch: expected ${Wrench._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Wrench();
    ({ offset } = Wrench._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Wrench): { offset: number } {
    target.force = new Vector3();
    ({ offset } = Vector3._decodeOne(view, offset, target.force));
    target.torque = new Vector3();
    ({ offset } = Vector3._decodeOne(view, offset, target.torque));
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, Wrench._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.force._encodeOne(view, offset);
    offset = this.torque._encodeOne(view, offset);
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.force._encodedSize();
    size += this.torque._encodedSize();
    return size;
  }
}
