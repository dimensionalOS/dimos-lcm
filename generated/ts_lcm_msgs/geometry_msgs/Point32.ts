// Auto-generated by lcm-ts. DO NOT EDIT.

export class Point32 {
  static readonly _HASH = 0x2a14f112c253ac0cn;
  static readonly _NAME = "geometry_msgs.Point32";
  private static _packedFingerprint: bigint | null = null;

  x: number;
  y: number;
  z: number;

  constructor(init?: Partial<Point32>) {
    this.x = init?.x ?? 0;
    this.y = init?.y ?? 0;
    this.z = init?.z ?? 0;
  }

  static decode(data: Uint8Array): Point32 {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = Point32._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Point32();
    ({ offset } = Point32._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Point32): { offset: number } {
    target.x = view.getFloat32(offset, false);
    offset += 4;
    target.y = view.getFloat32(offset, false);
    offset += 4;
    target.z = view.getFloat32(offset, false);
    offset += 4;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, Point32._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setFloat32(offset, this.x, false);
    offset += 4;
    view.setFloat32(offset, this.y, false);
    offset += 4;
    view.setFloat32(offset, this.z, false);
    offset += 4;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    size += 4;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(Point32)) return 0n;
    const newparents = [...parents, Point32];
    let tmphash = Point32._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (Point32._packedFingerprint === null) {
      Point32._packedFingerprint = Point32._getHashRecursive([]);
    }
    return Point32._packedFingerprint;
  }
}
