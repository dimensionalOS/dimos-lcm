// Auto-generated by lcm-ts. DO NOT EDIT.

import { Vector3 } from "./Vector3.ts";

export class Accel {
  static readonly _HASH = 0x66c2dcceead8c2e4n;
  static readonly _NAME = "geometry_msgs.Accel";

  linear: Vector3;
  angular: Vector3;

  constructor(init?: Partial<Accel>) {
    this.linear = init?.linear ?? new Vector3();
    this.angular = init?.angular ?? new Vector3();
  }

  static decode(data: Uint8Array): Accel {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== Accel._HASH) {
      throw new Error(`Hash mismatch: expected ${Accel._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Accel();
    ({ offset } = Accel._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Accel): { offset: number } {
    target.linear = new Vector3();
    ({ offset } = Vector3._decodeOne(view, offset, target.linear));
    target.angular = new Vector3();
    ({ offset } = Vector3._decodeOne(view, offset, target.angular));
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, Accel._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.linear._encodeOne(view, offset);
    offset = this.angular._encodeOne(view, offset);
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.linear._encodedSize();
    size += this.angular._encodedSize();
    return size;
  }
}
