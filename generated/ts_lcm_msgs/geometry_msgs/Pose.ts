// Auto-generated by lcm-ts. DO NOT EDIT.

import { Point } from "./Point.ts";
import { Quaternion } from "./Quaternion.ts";

export class Pose {
  static readonly _HASH = 0x2d70dd60bd541272n;
  static readonly _NAME = "geometry_msgs.Pose";
  private static _packedFingerprint: bigint | null = null;

  position: Point;
  orientation: Quaternion;

  constructor(init?: Partial<Pose>) {
    this.position = init?.position ?? new Point();
    this.orientation = init?.orientation ?? new Quaternion();
  }

  static decode(data: Uint8Array): Pose {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = Pose._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Pose();
    ({ offset } = Pose._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Pose): { offset: number } {
    target.position = new Point();
    ({ offset } = Point._decodeOne(view, offset, target.position));
    target.orientation = new Quaternion();
    ({ offset } = Quaternion._decodeOne(view, offset, target.orientation));
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, Pose._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.position._encodeOne(view, offset);
    offset = this.orientation._encodeOne(view, offset);
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.position._encodedSize();
    size += this.orientation._encodedSize();
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(Pose)) return 0n;
    const newparents = [...parents, Pose];
    let tmphash = Pose._HASH;
    tmphash = (tmphash + Point._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Quaternion._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (Pose._packedFingerprint === null) {
      Pose._packedFingerprint = Pose._getHashRecursive([]);
    }
    return Pose._packedFingerprint;
  }
}
