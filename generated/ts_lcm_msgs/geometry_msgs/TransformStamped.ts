// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { Transform } from "./Transform.ts";

export class TransformStamped {
  static readonly _HASH = 0xf694f4a6d8779002n;
  static readonly _NAME = "geometry_msgs.TransformStamped";
  private static _packedFingerprint: bigint | null = null;

  header: Header;
  child_frame_id: string;
  transform: Transform;

  constructor(init?: Partial<TransformStamped>) {
    this.header = init?.header ?? new Header();
    this.child_frame_id = init?.child_frame_id ?? "";
    this.transform = init?.transform ?? new Transform();
  }

  static decode(data: Uint8Array): TransformStamped {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = TransformStamped._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new TransformStamped();
    ({ offset } = TransformStamped._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: TransformStamped): { offset: number } {
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.child_frame_id = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    target.transform = new Transform();
    ({ offset } = Transform._decodeOne(view, offset, target.transform));
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, TransformStamped._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.header._encodeOne(view, offset);
    {
      const encoded = new TextEncoder().encode(this.child_frame_id);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    offset = this.transform._encodeOne(view, offset);
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.header._encodedSize();
    size += 4 + new TextEncoder().encode(this.child_frame_id).length + 1;
    size += this.transform._encodedSize();
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(TransformStamped)) return 0n;
    const newparents = [...parents, TransformStamped];
    let tmphash = TransformStamped._HASH;
    tmphash = (tmphash + Header._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Transform._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (TransformStamped._packedFingerprint === null) {
      TransformStamped._packedFingerprint = TransformStamped._getHashRecursive([]);
    }
    return TransformStamped._packedFingerprint;
  }
}
