// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { Twist } from "./Twist.ts";

export class TwistStamped {
  static readonly _HASH = 0xd2e6e8e8eed2e6e8n;
  static readonly _NAME = "geometry_msgs.TwistStamped";

  header: Header;
  twist: Twist;

  constructor(init?: Partial<TwistStamped>) {
    this.header = init?.header ?? new Header();
    this.twist = init?.twist ?? new Twist();
  }

  static decode(data: Uint8Array): TwistStamped {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== TwistStamped._HASH) {
      throw new Error(`Hash mismatch: expected ${TwistStamped._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new TwistStamped();
    ({ offset } = TwistStamped._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: TwistStamped): { offset: number } {
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.twist = new Twist();
    ({ offset } = Twist._decodeOne(view, offset, target.twist));
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, TwistStamped._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.header._encodeOne(view, offset);
    offset = this.twist._encodeOne(view, offset);
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.header._encodedSize();
    size += this.twist._encodedSize();
    return size;
  }
}
