// Auto-generated by lcm-ts. DO NOT EDIT.

export class Pose2D {
  static readonly _HASH = 0xc4d8cae8d0cae8c2n;
  static readonly _NAME = "geometry_msgs.Pose2D";

  x: number;
  y: number;
  theta: number;

  constructor(init?: Partial<Pose2D>) {
    this.x = init?.x ?? 0;
    this.y = init?.y ?? 0;
    this.theta = init?.theta ?? 0;
  }

  static decode(data: Uint8Array): Pose2D {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== Pose2D._HASH) {
      throw new Error(`Hash mismatch: expected ${Pose2D._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Pose2D();
    ({ offset } = Pose2D._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Pose2D): { offset: number } {
    target.x = view.getFloat64(offset, false);
    offset += 8;
    target.y = view.getFloat64(offset, false);
    offset += 8;
    target.theta = view.getFloat64(offset, false);
    offset += 8;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, Pose2D._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setFloat64(offset, this.x, false);
    offset += 8;
    view.setFloat64(offset, this.y, false);
    offset += 8;
    view.setFloat64(offset, this.theta, false);
    offset += 8;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 8;
    size += 8;
    size += 8;
    return size;
  }
}
