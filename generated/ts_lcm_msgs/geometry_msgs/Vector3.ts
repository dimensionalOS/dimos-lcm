// Auto-generated by lcm-ts. DO NOT EDIT.

export class Vector3 {
  static readonly _HASH = 0x573f2fdd2f76508fn;
  static readonly _NAME = "geometry_msgs.Vector3";
  private static _packedFingerprint: bigint | null = null;

  x: number;
  y: number;
  z: number;

  constructor(init?: Partial<Vector3>) {
    this.x = init?.x ?? 0;
    this.y = init?.y ?? 0;
    this.z = init?.z ?? 0;
  }

  static decode(data: Uint8Array): Vector3 {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = Vector3._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Vector3();
    ({ offset } = Vector3._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Vector3): { offset: number } {
    target.x = view.getFloat64(offset, false);
    offset += 8;
    target.y = view.getFloat64(offset, false);
    offset += 8;
    target.z = view.getFloat64(offset, false);
    offset += 8;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, Vector3._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setFloat64(offset, this.x, false);
    offset += 8;
    view.setFloat64(offset, this.y, false);
    offset += 8;
    view.setFloat64(offset, this.z, false);
    offset += 8;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 8;
    size += 8;
    size += 8;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(Vector3)) return 0n;
    const newparents = [...parents, Vector3];
    let tmphash = Vector3._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (Vector3._packedFingerprint === null) {
      Vector3._packedFingerprint = Vector3._getHashRecursive([]);
    }
    return Vector3._packedFingerprint;
  }
}
