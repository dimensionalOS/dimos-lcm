// Auto-generated by lcm-ts. DO NOT EDIT.

export class Vector3 {
  static readonly _HASH = 0xf2c8deeac4d8caf4n;
  static readonly _NAME = "geometry_msgs.Vector3";

  x: number;
  y: number;
  z: number;

  constructor(init?: Partial<Vector3>) {
    this.x = init?.x ?? 0;
    this.y = init?.y ?? 0;
    this.z = init?.z ?? 0;
  }

  static decode(data: Uint8Array): Vector3 {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== Vector3._HASH) {
      throw new Error(`Hash mismatch: expected ${Vector3._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Vector3();
    ({ offset } = Vector3._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Vector3): { offset: number } {
    target.x = view.getFloat64(offset, false);
    offset += 8;
    target.y = view.getFloat64(offset, false);
    offset += 8;
    target.z = view.getFloat64(offset, false);
    offset += 8;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, Vector3._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setFloat64(offset, this.x, false);
    offset += 8;
    view.setFloat64(offset, this.y, false);
    offset += 8;
    view.setFloat64(offset, this.z, false);
    offset += 8;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 8;
    size += 8;
    size += 8;
    return size;
  }
}
