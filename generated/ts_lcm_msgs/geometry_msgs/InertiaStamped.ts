// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { Inertia } from "./Inertia.ts";

export class InertiaStamped {
  static readonly _HASH = 0x3a39287c292abed7n;
  static readonly _NAME = "geometry_msgs.InertiaStamped";
  private static _packedFingerprint: bigint | null = null;

  header: Header;
  inertia: Inertia;

  constructor(init?: Partial<InertiaStamped>) {
    this.header = init?.header ?? new Header();
    this.inertia = init?.inertia ?? new Inertia();
  }

  static decode(data: Uint8Array): InertiaStamped {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = InertiaStamped._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new InertiaStamped();
    ({ offset } = InertiaStamped._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: InertiaStamped): { offset: number } {
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.inertia = new Inertia();
    ({ offset } = Inertia._decodeOne(view, offset, target.inertia));
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, InertiaStamped._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.header._encodeOne(view, offset);
    offset = this.inertia._encodeOne(view, offset);
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.header._encodedSize();
    size += this.inertia._encodedSize();
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(InertiaStamped)) return 0n;
    const newparents = [...parents, InertiaStamped];
    let tmphash = InertiaStamped._HASH;
    tmphash = (tmphash + Header._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Inertia._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (InertiaStamped._packedFingerprint === null) {
      InertiaStamped._packedFingerprint = InertiaStamped._getHashRecursive([]);
    }
    return InertiaStamped._packedFingerprint;
  }
}
