// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { Accel } from "./Accel.ts";

export class AccelStamped {
  static readonly _HASH = 0xf012322e268930c2n;
  static readonly _NAME = "geometry_msgs.AccelStamped";
  private static _packedFingerprint: bigint | null = null;

  header: Header;
  accel: Accel;

  constructor(init?: Partial<AccelStamped>) {
    this.header = init?.header ?? new Header();
    this.accel = init?.accel ?? new Accel();
  }

  static decode(data: Uint8Array): AccelStamped {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = AccelStamped._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new AccelStamped();
    ({ offset } = AccelStamped._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: AccelStamped): { offset: number } {
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.accel = new Accel();
    ({ offset } = Accel._decodeOne(view, offset, target.accel));
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, AccelStamped._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.header._encodeOne(view, offset);
    offset = this.accel._encodeOne(view, offset);
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.header._encodedSize();
    size += this.accel._encodedSize();
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(AccelStamped)) return 0n;
    const newparents = [...parents, AccelStamped];
    let tmphash = AccelStamped._HASH;
    tmphash = (tmphash + Header._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Accel._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (AccelStamped._packedFingerprint === null) {
      AccelStamped._packedFingerprint = AccelStamped._getHashRecursive([]);
    }
    return AccelStamped._packedFingerprint;
  }
}
