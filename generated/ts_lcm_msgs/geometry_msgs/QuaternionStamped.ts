// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { Quaternion } from "./Quaternion.ts";

export class QuaternionStamped {
  static readonly _HASH = 0xc2e8cae4dcd2dedcn;
  static readonly _NAME = "geometry_msgs.QuaternionStamped";

  header: Header;
  quaternion: Quaternion;

  constructor(init?: Partial<QuaternionStamped>) {
    this.header = init?.header ?? new Header();
    this.quaternion = init?.quaternion ?? new Quaternion();
  }

  static decode(data: Uint8Array): QuaternionStamped {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== QuaternionStamped._HASH) {
      throw new Error(`Hash mismatch: expected ${QuaternionStamped._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new QuaternionStamped();
    ({ offset } = QuaternionStamped._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: QuaternionStamped): { offset: number } {
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.quaternion = new Quaternion();
    ({ offset } = Quaternion._decodeOne(view, offset, target.quaternion));
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, QuaternionStamped._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.header._encodeOne(view, offset);
    offset = this.quaternion._encodeOne(view, offset);
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.header._encodedSize();
    size += this.quaternion._encodedSize();
    return size;
  }
}
