// Auto-generated by lcm-ts. DO NOT EDIT.

import { Pose } from "./Pose.ts";

export class PoseWithCovariance {
  static readonly _HASH = 0xe4d2c2dcc6ca0248n;
  static readonly _NAME = "geometry_msgs.PoseWithCovariance";

  pose: Pose;
  covariance: number[];

  constructor(init?: Partial<PoseWithCovariance>) {
    this.pose = init?.pose ?? new Pose();
    this.covariance = init?.covariance ?? Array.from({ length: 36 }, () => 0);
  }

  static decode(data: Uint8Array): PoseWithCovariance {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== PoseWithCovariance._HASH) {
      throw new Error(`Hash mismatch: expected ${PoseWithCovariance._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new PoseWithCovariance();
    ({ offset } = PoseWithCovariance._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: PoseWithCovariance): { offset: number } {
    target.pose = new Pose();
    ({ offset } = Pose._decodeOne(view, offset, target.pose));
    target.covariance = new Array(36);
    for (let i0 = 0; i0 < 36; i0++) {
      target.covariance[i0] = view.getFloat64(offset, false);
      offset += 8;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, PoseWithCovariance._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.pose._encodeOne(view, offset);
    for (let i0 = 0; i0 < 36; i0++) {
      view.setFloat64(offset, this.covariance[i0], false);
      offset += 8;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.pose._encodedSize();
    size += 36 * 8;
    return size;
  }
}
