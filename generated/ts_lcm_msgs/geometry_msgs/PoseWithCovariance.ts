// Auto-generated by lcm-ts. DO NOT EDIT.

import { Pose } from "./Pose.ts";

export class PoseWithCovariance {
  static readonly _HASH = 0x42dbdfaa69371237n;
  static readonly _NAME = "geometry_msgs.PoseWithCovariance";
  private static _packedFingerprint: bigint | null = null;

  pose: Pose;
  covariance: number[];

  constructor(init?: Partial<PoseWithCovariance>) {
    this.pose = init?.pose ?? new Pose();
    this.covariance = init?.covariance ?? Array.from({ length: 36 }, () => 0);
  }

  static decode(data: Uint8Array): PoseWithCovariance {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = PoseWithCovariance._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new PoseWithCovariance();
    ({ offset } = PoseWithCovariance._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: PoseWithCovariance): { offset: number } {
    target.pose = new Pose();
    ({ offset } = Pose._decodeOne(view, offset, target.pose));
    target.covariance = new Array(36);
    for (let i0 = 0; i0 < 36; i0++) {
      target.covariance[i0] = view.getFloat64(offset, false);
      offset += 8;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, PoseWithCovariance._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.pose._encodeOne(view, offset);
    for (let i0 = 0; i0 < 36; i0++) {
      view.setFloat64(offset, this.covariance[i0], false);
      offset += 8;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.pose._encodedSize();
    size += 36 * 8;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(PoseWithCovariance)) return 0n;
    const newparents = [...parents, PoseWithCovariance];
    let tmphash = PoseWithCovariance._HASH;
    tmphash = (tmphash + Pose._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (PoseWithCovariance._packedFingerprint === null) {
      PoseWithCovariance._packedFingerprint = PoseWithCovariance._getHashRecursive([]);
    }
    return PoseWithCovariance._packedFingerprint;
  }
}
