// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { Point } from "./Point.ts";

export class PointStamped {
  static readonly _HASH = 0xd2dce8e0ded2dce8n;
  static readonly _NAME = "geometry_msgs.PointStamped";

  header: Header;
  point: Point;

  constructor(init?: Partial<PointStamped>) {
    this.header = init?.header ?? new Header();
    this.point = init?.point ?? new Point();
  }

  static decode(data: Uint8Array): PointStamped {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== PointStamped._HASH) {
      throw new Error(`Hash mismatch: expected ${PointStamped._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new PointStamped();
    ({ offset } = PointStamped._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: PointStamped): { offset: number } {
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.point = new Point();
    ({ offset } = Point._decodeOne(view, offset, target.point));
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, PointStamped._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.header._encodeOne(view, offset);
    offset = this.point._encodeOne(view, offset);
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.header._encodedSize();
    size += this.point._encodedSize();
    return size;
  }
}
