// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { Polygon } from "./Polygon.ts";

export class PolygonStamped {
  static readonly _HASH = 0x413a2f753630b1d7n;
  static readonly _NAME = "geometry_msgs.PolygonStamped";
  private static _packedFingerprint: bigint | null = null;

  header: Header;
  polygon: Polygon;

  constructor(init?: Partial<PolygonStamped>) {
    this.header = init?.header ?? new Header();
    this.polygon = init?.polygon ?? new Polygon();
  }

  static decode(data: Uint8Array): PolygonStamped {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = PolygonStamped._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new PolygonStamped();
    ({ offset } = PolygonStamped._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: PolygonStamped): { offset: number } {
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.polygon = new Polygon();
    ({ offset } = Polygon._decodeOne(view, offset, target.polygon));
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, PolygonStamped._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.header._encodeOne(view, offset);
    offset = this.polygon._encodeOne(view, offset);
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.header._encodedSize();
    size += this.polygon._encodedSize();
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(PolygonStamped)) return 0n;
    const newparents = [...parents, PolygonStamped];
    let tmphash = PolygonStamped._HASH;
    tmphash = (tmphash + Header._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Polygon._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (PolygonStamped._packedFingerprint === null) {
      PolygonStamped._packedFingerprint = PolygonStamped._getHashRecursive([]);
    }
    return PolygonStamped._packedFingerprint;
  }
}
