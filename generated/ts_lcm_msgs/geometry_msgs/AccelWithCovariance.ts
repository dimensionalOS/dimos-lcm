// Auto-generated by lcm-ts. DO NOT EDIT.

import { Accel } from "./Accel.ts";

export class AccelWithCovariance {
  static readonly _HASH = 0xe4d2c2dcc6ca0248n;
  static readonly _NAME = "geometry_msgs.AccelWithCovariance";

  accel: Accel;
  covariance: number[];

  constructor(init?: Partial<AccelWithCovariance>) {
    this.accel = init?.accel ?? new Accel();
    this.covariance = init?.covariance ?? Array.from({ length: 36 }, () => 0);
  }

  static decode(data: Uint8Array): AccelWithCovariance {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== AccelWithCovariance._HASH) {
      throw new Error(`Hash mismatch: expected ${AccelWithCovariance._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new AccelWithCovariance();
    ({ offset } = AccelWithCovariance._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: AccelWithCovariance): { offset: number } {
    target.accel = new Accel();
    ({ offset } = Accel._decodeOne(view, offset, target.accel));
    target.covariance = new Array(36);
    for (let i0 = 0; i0 < 36; i0++) {
      target.covariance[i0] = view.getFloat64(offset, false);
      offset += 8;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, AccelWithCovariance._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.accel._encodeOne(view, offset);
    for (let i0 = 0; i0 < 36; i0++) {
      view.setFloat64(offset, this.covariance[i0], false);
      offset += 8;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.accel._encodedSize();
    size += 36 * 8;
    return size;
  }
}
