// Auto-generated by lcm-ts. DO NOT EDIT.

export class Quaternion {
  static readonly _HASH = 0x9b1dee9dfc8c0515n;
  static readonly _NAME = "geometry_msgs.Quaternion";
  private static _packedFingerprint: bigint | null = null;

  x: number;
  y: number;
  z: number;
  w: number;

  constructor(init?: Partial<Quaternion>) {
    this.x = init?.x ?? 0;
    this.y = init?.y ?? 0;
    this.z = init?.z ?? 0;
    this.w = init?.w ?? 0;
  }

  static decode(data: Uint8Array): Quaternion {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = Quaternion._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Quaternion();
    ({ offset } = Quaternion._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Quaternion): { offset: number } {
    target.x = view.getFloat64(offset, false);
    offset += 8;
    target.y = view.getFloat64(offset, false);
    offset += 8;
    target.z = view.getFloat64(offset, false);
    offset += 8;
    target.w = view.getFloat64(offset, false);
    offset += 8;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, Quaternion._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setFloat64(offset, this.x, false);
    offset += 8;
    view.setFloat64(offset, this.y, false);
    offset += 8;
    view.setFloat64(offset, this.z, false);
    offset += 8;
    view.setFloat64(offset, this.w, false);
    offset += 8;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 8;
    size += 8;
    size += 8;
    size += 8;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(Quaternion)) return 0n;
    const newparents = [...parents, Quaternion];
    let tmphash = Quaternion._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (Quaternion._packedFingerprint === null) {
      Quaternion._packedFingerprint = Quaternion._getHashRecursive([]);
    }
    return Quaternion._packedFingerprint;
  }
}
