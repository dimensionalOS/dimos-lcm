// Auto-generated by lcm-ts. DO NOT EDIT.

export class KeyValue {
  static readonly _HASH = 0xd2dcceecc2d8eacan;
  static readonly _NAME = "diagnostic_msgs.KeyValue";

  key: string;
  value: string;

  constructor(init?: Partial<KeyValue>) {
    this.key = init?.key ?? "";
    this.value = init?.value ?? "";
  }

  static decode(data: Uint8Array): KeyValue {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== KeyValue._HASH) {
      throw new Error(`Hash mismatch: expected ${KeyValue._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new KeyValue();
    ({ offset } = KeyValue._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: KeyValue): { offset: number } {
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.key = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.value = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, KeyValue._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    {
      const encoded = new TextEncoder().encode(this.key);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    {
      const encoded = new TextEncoder().encode(this.value);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4 + new TextEncoder().encode(this.key).length + 1;
    size += 4 + new TextEncoder().encode(this.value).length + 1;
    return size;
  }
}
