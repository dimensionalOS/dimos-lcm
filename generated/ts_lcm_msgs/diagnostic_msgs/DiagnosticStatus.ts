// Auto-generated by lcm-ts. DO NOT EDIT.

import { KeyValue } from "./KeyValue.ts";

export class DiagnosticStatus {
  static readonly _HASH = 0xcaecc2d8eacae600n;
  static readonly _NAME = "diagnostic_msgs.DiagnosticStatus";

  static readonly OK = 0;
  static readonly WARN = 1;
  static readonly ERROR = 2;
  static readonly STALE = 3;

  values_length: number;
  level: number;
  name: string;
  message: string;
  hardware_id: string;
  values: KeyValue[];

  constructor(init?: Partial<DiagnosticStatus>) {
    this.values_length = init?.values_length ?? 0;
    this.level = init?.level ?? 0;
    this.name = init?.name ?? "";
    this.message = init?.message ?? "";
    this.hardware_id = init?.hardware_id ?? "";
    this.values = init?.values ?? [];
  }

  static decode(data: Uint8Array): DiagnosticStatus {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== DiagnosticStatus._HASH) {
      throw new Error(`Hash mismatch: expected ${DiagnosticStatus._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new DiagnosticStatus();
    ({ offset } = DiagnosticStatus._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: DiagnosticStatus): { offset: number } {
    target.values_length = view.getInt32(offset, false);
    offset += 4;
    target.level = view.getInt8(offset);
    offset += 1;
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.name = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.message = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.hardware_id = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    target.values = new Array(target.values_length);
    for (let i0 = 0; i0 < target.values_length; i0++) {
      target.values[i0] = new KeyValue();
      ({ offset } = KeyValue._decodeOne(view, offset, target.values[i0]));
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, DiagnosticStatus._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.values_length, false);
    offset += 4;
    view.setInt8(offset, this.level);
    offset += 1;
    {
      const encoded = new TextEncoder().encode(this.name);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    {
      const encoded = new TextEncoder().encode(this.message);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    {
      const encoded = new TextEncoder().encode(this.hardware_id);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    for (let i0 = 0; i0 < this.values_length; i0++) {
      offset = this.values[i0]._encodeOne(view, offset);
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 1;
    size += 4 + new TextEncoder().encode(this.name).length + 1;
    size += 4 + new TextEncoder().encode(this.message).length + 1;
    size += 4 + new TextEncoder().encode(this.hardware_id).length + 1;
    for (let i0 = 0; i0 < this.values_length; i0++) {
      size += this.values[i0]._encodedSize();
    }
    return size;
  }
}
