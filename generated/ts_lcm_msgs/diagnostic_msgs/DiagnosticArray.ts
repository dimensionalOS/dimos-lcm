// Auto-generated by lcm-ts. DO NOT EDIT.

import { Header } from "../std_msgs/Header.ts";
import { DiagnosticStatus } from "./DiagnosticStatus.ts";

export class DiagnosticArray {
  static readonly _HASH = 0xe6e6e8c2e8eae600n;
  static readonly _NAME = "diagnostic_msgs.DiagnosticArray";

  status_length: number;
  header: Header;
  status: DiagnosticStatus[];

  constructor(init?: Partial<DiagnosticArray>) {
    this.status_length = init?.status_length ?? 0;
    this.header = init?.header ?? new Header();
    this.status = init?.status ?? [];
  }

  static decode(data: Uint8Array): DiagnosticArray {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== DiagnosticArray._HASH) {
      throw new Error(`Hash mismatch: expected ${DiagnosticArray._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new DiagnosticArray();
    ({ offset } = DiagnosticArray._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: DiagnosticArray): { offset: number } {
    target.status_length = view.getInt32(offset, false);
    offset += 4;
    target.header = new Header();
    ({ offset } = Header._decodeOne(view, offset, target.header));
    target.status = new Array(target.status_length);
    for (let i0 = 0; i0 < target.status_length; i0++) {
      target.status[i0] = new DiagnosticStatus();
      ({ offset } = DiagnosticStatus._decodeOne(view, offset, target.status[i0]));
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, DiagnosticArray._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.status_length, false);
    offset += 4;
    offset = this.header._encodeOne(view, offset);
    for (let i0 = 0; i0 < this.status_length; i0++) {
      offset = this.status[i0]._encodeOne(view, offset);
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += this.header._encodedSize();
    for (let i0 = 0; i0 < this.status_length; i0++) {
      size += this.status[i0]._encodedSize();
    }
    return size;
  }
}
