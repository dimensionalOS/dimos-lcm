// Auto-generated by lcm-ts. DO NOT EDIT.

export class UniqueID {
  static readonly _HASH = 0x697d4febd3ed5654n;
  static readonly _NAME = "uuid_msgs.UniqueID";
  private static _packedFingerprint: bigint | null = null;

  uuid: Uint8Array;

  constructor(init?: Partial<UniqueID>) {
    this.uuid = init?.uuid ?? new Uint8Array(16);
  }

  static decode(data: Uint8Array): UniqueID {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = UniqueID._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new UniqueID();
    ({ offset } = UniqueID._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: UniqueID): { offset: number } {
    target.uuid = new Uint8Array(view.buffer, view.byteOffset + offset, 16);
    offset += 16;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, UniqueID._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    new Uint8Array(view.buffer, view.byteOffset + offset).set(this.uuid.subarray(0, 16));
    offset += 16;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 16 * 1;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(UniqueID)) return 0n;
    const newparents = [...parents, UniqueID];
    let tmphash = UniqueID._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (UniqueID._packedFingerprint === null) {
      UniqueID._packedFingerprint = UniqueID._getHashRecursive([]);
    }
    return UniqueID._packedFingerprint;
  }
}
