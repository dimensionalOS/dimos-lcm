// Auto-generated by lcm-ts. DO NOT EDIT.

export class TF2Error {
  static readonly _HASH = 0xe4bee6e8e4d2dccen;
  static readonly _NAME = "tf2_msgs.TF2Error";

  static readonly NO_ERROR = 0;
  static readonly LOOKUP_ERROR = 1;
  static readonly CONNECTIVITY_ERROR = 2;
  static readonly EXTRAPOLATION_ERROR = 3;
  static readonly INVALID_ARGUMENT_ERROR = 4;
  static readonly TIMEOUT_ERROR = 5;
  static readonly TRANSFORM_ERROR = 6;

  error: number;
  error_string: string;

  constructor(init?: Partial<TF2Error>) {
    this.error = init?.error ?? 0;
    this.error_string = init?.error_string ?? "";
  }

  static decode(data: Uint8Array): TF2Error {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== TF2Error._HASH) {
      throw new Error(`Hash mismatch: expected ${TF2Error._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new TF2Error();
    ({ offset } = TF2Error._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: TF2Error): { offset: number } {
    target.error = view.getUint8(offset);
    offset += 1;
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.error_string = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, TF2Error._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setUint8(offset, this.error);
    offset += 1;
    {
      const encoded = new TextEncoder().encode(this.error_string);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 1;
    size += 4 + new TextEncoder().encode(this.error_string).length + 1;
    return size;
  }
}
