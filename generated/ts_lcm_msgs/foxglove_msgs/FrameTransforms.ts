// Auto-generated by lcm-ts. DO NOT EDIT.

import { FrameTransform } from "./FrameTransform.ts";

export class FrameTransforms {
  static readonly _HASH = 0x37bc5cbce50a5ce2n;
  static readonly _NAME = "foxglove_msgs.FrameTransforms";
  private static _packedFingerprint: bigint | null = null;

  transforms_length: number;
  transforms: FrameTransform[];

  constructor(init?: Partial<FrameTransforms>) {
    this.transforms_length = init?.transforms_length ?? 0;
    this.transforms = init?.transforms ?? [];
  }

  static decode(data: Uint8Array): FrameTransforms {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = FrameTransforms._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new FrameTransforms();
    ({ offset } = FrameTransforms._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: FrameTransforms): { offset: number } {
    target.transforms_length = view.getInt32(offset, false);
    offset += 4;
    target.transforms = new Array(target.transforms_length);
    for (let i0 = 0; i0 < target.transforms_length; i0++) {
      target.transforms[i0] = new FrameTransform();
      ({ offset } = FrameTransform._decodeOne(view, offset, target.transforms[i0]));
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, FrameTransforms._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.transforms_length, false);
    offset += 4;
    for (let i0 = 0; i0 < this.transforms_length; i0++) {
      offset = this.transforms[i0]._encodeOne(view, offset);
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    for (let i0 = 0; i0 < this.transforms_length; i0++) {
      size += this.transforms[i0]._encodedSize();
    }
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(FrameTransforms)) return 0n;
    const newparents = [...parents, FrameTransforms];
    let tmphash = FrameTransforms._HASH;
    tmphash = (tmphash + FrameTransform._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (FrameTransforms._packedFingerprint === null) {
      FrameTransforms._packedFingerprint = FrameTransforms._getHashRecursive([]);
    }
    return FrameTransforms._packedFingerprint;
  }
}
