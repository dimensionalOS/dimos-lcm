// Auto-generated by lcm-ts. DO NOT EDIT.

import { Pose } from "../geometry_msgs/Pose.ts";
import { Vector3 } from "../geometry_msgs/Vector3.ts";
import { Color } from "./Color.ts";

export class CylinderPrimitive {
  static readonly _HASH = 0xa52103034bfe0bacn;
  static readonly _NAME = "foxglove_msgs.CylinderPrimitive";
  private static _packedFingerprint: bigint | null = null;

  pose: Pose;
  size: Vector3;
  bottom_scale: number;
  top_scale: number;
  color: Color;

  constructor(init?: Partial<CylinderPrimitive>) {
    this.pose = init?.pose ?? new Pose();
    this.size = init?.size ?? new Vector3();
    this.bottom_scale = init?.bottom_scale ?? 0;
    this.top_scale = init?.top_scale ?? 0;
    this.color = init?.color ?? new Color();
  }

  static decode(data: Uint8Array): CylinderPrimitive {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = CylinderPrimitive._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new CylinderPrimitive();
    ({ offset } = CylinderPrimitive._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: CylinderPrimitive): { offset: number } {
    target.pose = new Pose();
    ({ offset } = Pose._decodeOne(view, offset, target.pose));
    target.size = new Vector3();
    ({ offset } = Vector3._decodeOne(view, offset, target.size));
    target.bottom_scale = view.getFloat64(offset, false);
    offset += 8;
    target.top_scale = view.getFloat64(offset, false);
    offset += 8;
    target.color = new Color();
    ({ offset } = Color._decodeOne(view, offset, target.color));
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, CylinderPrimitive._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.pose._encodeOne(view, offset);
    offset = this.size._encodeOne(view, offset);
    view.setFloat64(offset, this.bottom_scale, false);
    offset += 8;
    view.setFloat64(offset, this.top_scale, false);
    offset += 8;
    offset = this.color._encodeOne(view, offset);
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.pose._encodedSize();
    size += this.size._encodedSize();
    size += 8;
    size += 8;
    size += this.color._encodedSize();
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(CylinderPrimitive)) return 0n;
    const newparents = [...parents, CylinderPrimitive];
    let tmphash = CylinderPrimitive._HASH;
    tmphash = (tmphash + Pose._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Vector3._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Color._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (CylinderPrimitive._packedFingerprint === null) {
      CylinderPrimitive._packedFingerprint = CylinderPrimitive._getHashRecursive([]);
    }
    return CylinderPrimitive._packedFingerprint;
  }
}
