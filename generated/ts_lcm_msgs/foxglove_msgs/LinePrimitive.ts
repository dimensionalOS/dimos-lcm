// Auto-generated by lcm-ts. DO NOT EDIT.

import { Pose } from "../geometry_msgs/Pose.ts";
import { Point } from "../geometry_msgs/Point.ts";
import { Color } from "./Color.ts";

export class LinePrimitive {
  static readonly _HASH = 0x3b39f8eb653b3cd3n;
  static readonly _NAME = "foxglove_msgs.LinePrimitive";
  private static _packedFingerprint: bigint | null = null;

  static readonly LINE_STRIP = 0;
  static readonly LINE_LOOP = 1;
  static readonly LINE_LIST = 2;

  points_length: number;
  colors_length: number;
  indices_length: number;
  type: number;
  pose: Pose;
  thickness: number;
  scale_invariant: boolean;
  points: Point[];
  color: Color;
  colors: Color[];
  indices: number[];

  constructor(init?: Partial<LinePrimitive>) {
    this.points_length = init?.points_length ?? 0;
    this.colors_length = init?.colors_length ?? 0;
    this.indices_length = init?.indices_length ?? 0;
    this.type = init?.type ?? 0;
    this.pose = init?.pose ?? new Pose();
    this.thickness = init?.thickness ?? 0;
    this.scale_invariant = init?.scale_invariant ?? false;
    this.points = init?.points ?? [];
    this.color = init?.color ?? new Color();
    this.colors = init?.colors ?? [];
    this.indices = init?.indices ?? [];
  }

  static decode(data: Uint8Array): LinePrimitive {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = LinePrimitive._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new LinePrimitive();
    ({ offset } = LinePrimitive._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: LinePrimitive): { offset: number } {
    target.points_length = view.getInt32(offset, false);
    offset += 4;
    target.colors_length = view.getInt32(offset, false);
    offset += 4;
    target.indices_length = view.getInt32(offset, false);
    offset += 4;
    target.type = view.getUint8(offset);
    offset += 1;
    target.pose = new Pose();
    ({ offset } = Pose._decodeOne(view, offset, target.pose));
    target.thickness = view.getFloat64(offset, false);
    offset += 8;
    target.scale_invariant = view.getInt8(offset) !== 0;
    offset += 1;
    target.points = new Array(target.points_length);
    for (let i0 = 0; i0 < target.points_length; i0++) {
      target.points[i0] = new Point();
      ({ offset } = Point._decodeOne(view, offset, target.points[i0]));
    }
    target.color = new Color();
    ({ offset } = Color._decodeOne(view, offset, target.color));
    target.colors = new Array(target.colors_length);
    for (let i0 = 0; i0 < target.colors_length; i0++) {
      target.colors[i0] = new Color();
      ({ offset } = Color._decodeOne(view, offset, target.colors[i0]));
    }
    target.indices = new Array(target.indices_length);
    for (let i0 = 0; i0 < target.indices_length; i0++) {
      target.indices[i0] = view.getInt32(offset, false);
      offset += 4;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, LinePrimitive._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.points_length, false);
    offset += 4;
    view.setInt32(offset, this.colors_length, false);
    offset += 4;
    view.setInt32(offset, this.indices_length, false);
    offset += 4;
    view.setUint8(offset, this.type);
    offset += 1;
    offset = this.pose._encodeOne(view, offset);
    view.setFloat64(offset, this.thickness, false);
    offset += 8;
    view.setInt8(offset, this.scale_invariant ? 1 : 0);
    offset += 1;
    for (let i0 = 0; i0 < this.points_length; i0++) {
      offset = this.points[i0]._encodeOne(view, offset);
    }
    offset = this.color._encodeOne(view, offset);
    for (let i0 = 0; i0 < this.colors_length; i0++) {
      offset = this.colors[i0]._encodeOne(view, offset);
    }
    for (let i0 = 0; i0 < this.indices_length; i0++) {
      view.setInt32(offset, this.indices[i0], false);
      offset += 4;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    size += 4;
    size += 1;
    size += this.pose._encodedSize();
    size += 8;
    size += 1;
    for (let i0 = 0; i0 < this.points_length; i0++) {
      size += this.points[i0]._encodedSize();
    }
    size += this.color._encodedSize();
    for (let i0 = 0; i0 < this.colors_length; i0++) {
      size += this.colors[i0]._encodedSize();
    }
    size += this.indices_length * 4;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(LinePrimitive)) return 0n;
    const newparents = [...parents, LinePrimitive];
    let tmphash = LinePrimitive._HASH;
    tmphash = (tmphash + Pose._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Point._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Color._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Color._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (LinePrimitive._packedFingerprint === null) {
      LinePrimitive._packedFingerprint = LinePrimitive._getHashRecursive([]);
    }
    return LinePrimitive._packedFingerprint;
  }
}
