// Auto-generated by lcm-ts. DO NOT EDIT.

import { Pose } from "../geometry_msgs/Pose.ts";
import { Color } from "./Color.ts";

export class ArrowPrimitive {
  static readonly _HASH = 0xd8dee4c6ded8dee4n;
  static readonly _NAME = "foxglove_msgs.ArrowPrimitive";

  pose: Pose;
  shaft_length: number;
  shaft_diameter: number;
  head_length: number;
  head_diameter: number;
  color: Color;

  constructor(init?: Partial<ArrowPrimitive>) {
    this.pose = init?.pose ?? new Pose();
    this.shaft_length = init?.shaft_length ?? 0;
    this.shaft_diameter = init?.shaft_diameter ?? 0;
    this.head_length = init?.head_length ?? 0;
    this.head_diameter = init?.head_diameter ?? 0;
    this.color = init?.color ?? new Color();
  }

  static decode(data: Uint8Array): ArrowPrimitive {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== ArrowPrimitive._HASH) {
      throw new Error(`Hash mismatch: expected ${ArrowPrimitive._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new ArrowPrimitive();
    ({ offset } = ArrowPrimitive._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: ArrowPrimitive): { offset: number } {
    target.pose = new Pose();
    ({ offset } = Pose._decodeOne(view, offset, target.pose));
    target.shaft_length = view.getFloat64(offset, false);
    offset += 8;
    target.shaft_diameter = view.getFloat64(offset, false);
    offset += 8;
    target.head_length = view.getFloat64(offset, false);
    offset += 8;
    target.head_diameter = view.getFloat64(offset, false);
    offset += 8;
    target.color = new Color();
    ({ offset } = Color._decodeOne(view, offset, target.color));
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, ArrowPrimitive._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.pose._encodeOne(view, offset);
    view.setFloat64(offset, this.shaft_length, false);
    offset += 8;
    view.setFloat64(offset, this.shaft_diameter, false);
    offset += 8;
    view.setFloat64(offset, this.head_length, false);
    offset += 8;
    view.setFloat64(offset, this.head_diameter, false);
    offset += 8;
    offset = this.color._encodeOne(view, offset);
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.pose._encodedSize();
    size += 8;
    size += 8;
    size += 8;
    size += 8;
    size += this.color._encodedSize();
    return size;
  }
}
