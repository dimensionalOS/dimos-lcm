// Auto-generated by lcm-ts. DO NOT EDIT.

import { Time } from "../builtin_interfaces/Time.ts";
import { Pose } from "../geometry_msgs/Pose.ts";

export class LaserScan {
  static readonly _HASH = 0xdce6d2e8d2cae600n;
  static readonly _NAME = "foxglove_msgs.LaserScan";

  ranges_length: number;
  intensities_length: number;
  timestamp: Time;
  frame_id: string;
  pose: Pose;
  start_angle: number;
  end_angle: number;
  ranges: number[];
  intensities: number[];

  constructor(init?: Partial<LaserScan>) {
    this.ranges_length = init?.ranges_length ?? 0;
    this.intensities_length = init?.intensities_length ?? 0;
    this.timestamp = init?.timestamp ?? new Time();
    this.frame_id = init?.frame_id ?? "";
    this.pose = init?.pose ?? new Pose();
    this.start_angle = init?.start_angle ?? 0;
    this.end_angle = init?.end_angle ?? 0;
    this.ranges = init?.ranges ?? [];
    this.intensities = init?.intensities ?? [];
  }

  static decode(data: Uint8Array): LaserScan {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== LaserScan._HASH) {
      throw new Error(`Hash mismatch: expected ${LaserScan._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new LaserScan();
    ({ offset } = LaserScan._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: LaserScan): { offset: number } {
    target.ranges_length = view.getInt32(offset, false);
    offset += 4;
    target.intensities_length = view.getInt32(offset, false);
    offset += 4;
    target.timestamp = new Time();
    ({ offset } = Time._decodeOne(view, offset, target.timestamp));
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.frame_id = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    target.pose = new Pose();
    ({ offset } = Pose._decodeOne(view, offset, target.pose));
    target.start_angle = view.getFloat64(offset, false);
    offset += 8;
    target.end_angle = view.getFloat64(offset, false);
    offset += 8;
    target.ranges = new Array(target.ranges_length);
    for (let i0 = 0; i0 < target.ranges_length; i0++) {
      target.ranges[i0] = view.getFloat64(offset, false);
      offset += 8;
    }
    target.intensities = new Array(target.intensities_length);
    for (let i0 = 0; i0 < target.intensities_length; i0++) {
      target.intensities[i0] = view.getFloat64(offset, false);
      offset += 8;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, LaserScan._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.ranges_length, false);
    offset += 4;
    view.setInt32(offset, this.intensities_length, false);
    offset += 4;
    offset = this.timestamp._encodeOne(view, offset);
    {
      const encoded = new TextEncoder().encode(this.frame_id);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    offset = this.pose._encodeOne(view, offset);
    view.setFloat64(offset, this.start_angle, false);
    offset += 8;
    view.setFloat64(offset, this.end_angle, false);
    offset += 8;
    for (let i0 = 0; i0 < this.ranges_length; i0++) {
      view.setFloat64(offset, this.ranges[i0], false);
      offset += 8;
    }
    for (let i0 = 0; i0 < this.intensities_length; i0++) {
      view.setFloat64(offset, this.intensities[i0], false);
      offset += 8;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    size += this.timestamp._encodedSize();
    size += 4 + new TextEncoder().encode(this.frame_id).length + 1;
    size += this.pose._encodedSize();
    size += 8;
    size += 8;
    size += this.ranges_length * 8;
    size += this.intensities_length * 8;
    return size;
  }
}
