// Auto-generated by lcm-ts. DO NOT EDIT.

import { Time } from "../builtin_interfaces/Time.ts";
import { Point2 } from "./Point2.ts";
import { Color } from "./Color.ts";

export class PointsAnnotation {
  static readonly _HASH = 0xd0d2c6d6dccae6e6n;
  static readonly _NAME = "foxglove_msgs.PointsAnnotation";

  static readonly UNKNOWN = 0;
  static readonly POINTS = 1;
  static readonly LINE_LOOP = 2;
  static readonly LINE_STRIP = 3;
  static readonly LINE_LIST = 4;

  points_length: number;
  outline_colors_length: number;
  timestamp: Time;
  type: number;
  points: Point2[];
  outline_color: Color;
  outline_colors: Color[];
  fill_color: Color;
  thickness: number;

  constructor(init?: Partial<PointsAnnotation>) {
    this.points_length = init?.points_length ?? 0;
    this.outline_colors_length = init?.outline_colors_length ?? 0;
    this.timestamp = init?.timestamp ?? new Time();
    this.type = init?.type ?? 0;
    this.points = init?.points ?? [];
    this.outline_color = init?.outline_color ?? new Color();
    this.outline_colors = init?.outline_colors ?? [];
    this.fill_color = init?.fill_color ?? new Color();
    this.thickness = init?.thickness ?? 0;
  }

  static decode(data: Uint8Array): PointsAnnotation {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== PointsAnnotation._HASH) {
      throw new Error(`Hash mismatch: expected ${PointsAnnotation._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new PointsAnnotation();
    ({ offset } = PointsAnnotation._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: PointsAnnotation): { offset: number } {
    target.points_length = view.getInt32(offset, false);
    offset += 4;
    target.outline_colors_length = view.getInt32(offset, false);
    offset += 4;
    target.timestamp = new Time();
    ({ offset } = Time._decodeOne(view, offset, target.timestamp));
    target.type = view.getUint8(offset);
    offset += 1;
    target.points = new Array(target.points_length);
    for (let i0 = 0; i0 < target.points_length; i0++) {
      target.points[i0] = new Point2();
      ({ offset } = Point2._decodeOne(view, offset, target.points[i0]));
    }
    target.outline_color = new Color();
    ({ offset } = Color._decodeOne(view, offset, target.outline_color));
    target.outline_colors = new Array(target.outline_colors_length);
    for (let i0 = 0; i0 < target.outline_colors_length; i0++) {
      target.outline_colors[i0] = new Color();
      ({ offset } = Color._decodeOne(view, offset, target.outline_colors[i0]));
    }
    target.fill_color = new Color();
    ({ offset } = Color._decodeOne(view, offset, target.fill_color));
    target.thickness = view.getFloat64(offset, false);
    offset += 8;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, PointsAnnotation._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.points_length, false);
    offset += 4;
    view.setInt32(offset, this.outline_colors_length, false);
    offset += 4;
    offset = this.timestamp._encodeOne(view, offset);
    view.setUint8(offset, this.type);
    offset += 1;
    for (let i0 = 0; i0 < this.points_length; i0++) {
      offset = this.points[i0]._encodeOne(view, offset);
    }
    offset = this.outline_color._encodeOne(view, offset);
    for (let i0 = 0; i0 < this.outline_colors_length; i0++) {
      offset = this.outline_colors[i0]._encodeOne(view, offset);
    }
    offset = this.fill_color._encodeOne(view, offset);
    view.setFloat64(offset, this.thickness, false);
    offset += 8;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    size += this.timestamp._encodedSize();
    size += 1;
    for (let i0 = 0; i0 < this.points_length; i0++) {
      size += this.points[i0]._encodedSize();
    }
    size += this.outline_color._encodedSize();
    for (let i0 = 0; i0 < this.outline_colors_length; i0++) {
      size += this.outline_colors[i0]._encodedSize();
    }
    size += this.fill_color._encodedSize();
    size += 8;
    return size;
  }
}
