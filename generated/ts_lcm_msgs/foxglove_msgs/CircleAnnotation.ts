// Auto-generated by lcm-ts. DO NOT EDIT.

import { Time } from "../builtin_interfaces/Time.ts";
import { Point2 } from "./Point2.ts";
import { Color } from "./Color.ts";

export class CircleAnnotation {
  static readonly _HASH = 0xdccabec6ded8dee4n;
  static readonly _NAME = "foxglove_msgs.CircleAnnotation";

  timestamp: Time;
  position: Point2;
  diameter: number;
  thickness: number;
  fill_color: Color;
  outline_color: Color;

  constructor(init?: Partial<CircleAnnotation>) {
    this.timestamp = init?.timestamp ?? new Time();
    this.position = init?.position ?? new Point2();
    this.diameter = init?.diameter ?? 0;
    this.thickness = init?.thickness ?? 0;
    this.fill_color = init?.fill_color ?? new Color();
    this.outline_color = init?.outline_color ?? new Color();
  }

  static decode(data: Uint8Array): CircleAnnotation {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== CircleAnnotation._HASH) {
      throw new Error(`Hash mismatch: expected ${CircleAnnotation._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new CircleAnnotation();
    ({ offset } = CircleAnnotation._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: CircleAnnotation): { offset: number } {
    target.timestamp = new Time();
    ({ offset } = Time._decodeOne(view, offset, target.timestamp));
    target.position = new Point2();
    ({ offset } = Point2._decodeOne(view, offset, target.position));
    target.diameter = view.getFloat64(offset, false);
    offset += 8;
    target.thickness = view.getFloat64(offset, false);
    offset += 8;
    target.fill_color = new Color();
    ({ offset } = Color._decodeOne(view, offset, target.fill_color));
    target.outline_color = new Color();
    ({ offset } = Color._decodeOne(view, offset, target.outline_color));
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, CircleAnnotation._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.timestamp._encodeOne(view, offset);
    offset = this.position._encodeOne(view, offset);
    view.setFloat64(offset, this.diameter, false);
    offset += 8;
    view.setFloat64(offset, this.thickness, false);
    offset += 8;
    offset = this.fill_color._encodeOne(view, offset);
    offset = this.outline_color._encodeOne(view, offset);
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.timestamp._encodedSize();
    size += this.position._encodedSize();
    size += 8;
    size += 8;
    size += this.fill_color._encodedSize();
    size += this.outline_color._encodedSize();
    return size;
  }
}
