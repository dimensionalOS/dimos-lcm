// Auto-generated by lcm-ts. DO NOT EDIT.

export class Color {
  static readonly _HASH = 0xc4c8deeac4d8cac2n;
  static readonly _NAME = "foxglove_msgs.Color";

  r: number;
  g: number;
  b: number;
  a: number;

  constructor(init?: Partial<Color>) {
    this.r = init?.r ?? 0;
    this.g = init?.g ?? 0;
    this.b = init?.b ?? 0;
    this.a = init?.a ?? 0;
  }

  static decode(data: Uint8Array): Color {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== Color._HASH) {
      throw new Error(`Hash mismatch: expected ${Color._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Color();
    ({ offset } = Color._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Color): { offset: number } {
    target.r = view.getFloat64(offset, false);
    offset += 8;
    target.g = view.getFloat64(offset, false);
    offset += 8;
    target.b = view.getFloat64(offset, false);
    offset += 8;
    target.a = view.getFloat64(offset, false);
    offset += 8;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, Color._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setFloat64(offset, this.r, false);
    offset += 8;
    view.setFloat64(offset, this.g, false);
    offset += 8;
    view.setFloat64(offset, this.b, false);
    offset += 8;
    view.setFloat64(offset, this.a, false);
    offset += 8;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 8;
    size += 8;
    size += 8;
    size += 8;
    return size;
  }
}
