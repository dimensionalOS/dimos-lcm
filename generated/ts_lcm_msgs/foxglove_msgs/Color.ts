// Auto-generated by lcm-ts. DO NOT EDIT.

export class Color {
  static readonly _HASH = 0x998136169753c441n;
  static readonly _NAME = "foxglove_msgs.Color";
  private static _packedFingerprint: bigint | null = null;

  r: number;
  g: number;
  b: number;
  a: number;

  constructor(init?: Partial<Color>) {
    this.r = init?.r ?? 0;
    this.g = init?.g ?? 0;
    this.b = init?.b ?? 0;
    this.a = init?.a ?? 0;
  }

  static decode(data: Uint8Array): Color {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = Color._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Color();
    ({ offset } = Color._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Color): { offset: number } {
    target.r = view.getFloat64(offset, false);
    offset += 8;
    target.g = view.getFloat64(offset, false);
    offset += 8;
    target.b = view.getFloat64(offset, false);
    offset += 8;
    target.a = view.getFloat64(offset, false);
    offset += 8;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, Color._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setFloat64(offset, this.r, false);
    offset += 8;
    view.setFloat64(offset, this.g, false);
    offset += 8;
    view.setFloat64(offset, this.b, false);
    offset += 8;
    view.setFloat64(offset, this.a, false);
    offset += 8;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 8;
    size += 8;
    size += 8;
    size += 8;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(Color)) return 0n;
    const newparents = [...parents, Color];
    let tmphash = Color._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (Color._packedFingerprint === null) {
      Color._packedFingerprint = Color._getHashRecursive([]);
    }
    return Color._packedFingerprint;
  }
}
