// Auto-generated by lcm-ts. DO NOT EDIT.

import { Time } from "../builtin_interfaces/Time.ts";
import { Pose } from "../geometry_msgs/Pose.ts";

export class PosesInFrame {
  static readonly _HASH = 0x65f6cee7d8076f4bn;
  static readonly _NAME = "foxglove_msgs.PosesInFrame";
  private static _packedFingerprint: bigint | null = null;

  poses_length: number;
  timestamp: Time;
  frame_id: string;
  poses: Pose[];

  constructor(init?: Partial<PosesInFrame>) {
    this.poses_length = init?.poses_length ?? 0;
    this.timestamp = init?.timestamp ?? new Time();
    this.frame_id = init?.frame_id ?? "";
    this.poses = init?.poses ?? [];
  }

  static decode(data: Uint8Array): PosesInFrame {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = PosesInFrame._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new PosesInFrame();
    ({ offset } = PosesInFrame._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: PosesInFrame): { offset: number } {
    target.poses_length = view.getInt32(offset, false);
    offset += 4;
    target.timestamp = new Time();
    ({ offset } = Time._decodeOne(view, offset, target.timestamp));
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.frame_id = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    target.poses = new Array(target.poses_length);
    for (let i0 = 0; i0 < target.poses_length; i0++) {
      target.poses[i0] = new Pose();
      ({ offset } = Pose._decodeOne(view, offset, target.poses[i0]));
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, PosesInFrame._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.poses_length, false);
    offset += 4;
    offset = this.timestamp._encodeOne(view, offset);
    {
      const encoded = new TextEncoder().encode(this.frame_id);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    for (let i0 = 0; i0 < this.poses_length; i0++) {
      offset = this.poses[i0]._encodeOne(view, offset);
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += this.timestamp._encodedSize();
    size += 4 + new TextEncoder().encode(this.frame_id).length + 1;
    for (let i0 = 0; i0 < this.poses_length; i0++) {
      size += this.poses[i0]._encodedSize();
    }
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(PosesInFrame)) return 0n;
    const newparents = [...parents, PosesInFrame];
    let tmphash = PosesInFrame._HASH;
    tmphash = (tmphash + Time._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (tmphash + Pose._getHashRecursive(newparents)) & 0xffffffffffffffffn;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (PosesInFrame._packedFingerprint === null) {
      PosesInFrame._packedFingerprint = PosesInFrame._getHashRecursive([]);
    }
    return PosesInFrame._packedFingerprint;
  }
}
