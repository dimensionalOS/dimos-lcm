// Auto-generated by lcm-ts. DO NOT EDIT.

import { SceneEntityDeletion } from "./SceneEntityDeletion.ts";
import { SceneEntity } from "./SceneEntity.ts";

export class SceneUpdate {
  static readonly _HASH = 0xdce8d2e8d2cae600n;
  static readonly _NAME = "foxglove_msgs.SceneUpdate";

  deletions_length: number;
  entities_length: number;
  deletions: SceneEntityDeletion[];
  entities: SceneEntity[];

  constructor(init?: Partial<SceneUpdate>) {
    this.deletions_length = init?.deletions_length ?? 0;
    this.entities_length = init?.entities_length ?? 0;
    this.deletions = init?.deletions ?? [];
    this.entities = init?.entities ?? [];
  }

  static decode(data: Uint8Array): SceneUpdate {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== SceneUpdate._HASH) {
      throw new Error(`Hash mismatch: expected ${SceneUpdate._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new SceneUpdate();
    ({ offset } = SceneUpdate._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: SceneUpdate): { offset: number } {
    target.deletions_length = view.getInt32(offset, false);
    offset += 4;
    target.entities_length = view.getInt32(offset, false);
    offset += 4;
    target.deletions = new Array(target.deletions_length);
    for (let i0 = 0; i0 < target.deletions_length; i0++) {
      target.deletions[i0] = new SceneEntityDeletion();
      ({ offset } = SceneEntityDeletion._decodeOne(view, offset, target.deletions[i0]));
    }
    target.entities = new Array(target.entities_length);
    for (let i0 = 0; i0 < target.entities_length; i0++) {
      target.entities[i0] = new SceneEntity();
      ({ offset } = SceneEntity._decodeOne(view, offset, target.entities[i0]));
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, SceneUpdate._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.deletions_length, false);
    offset += 4;
    view.setInt32(offset, this.entities_length, false);
    offset += 4;
    for (let i0 = 0; i0 < this.deletions_length; i0++) {
      offset = this.deletions[i0]._encodeOne(view, offset);
    }
    for (let i0 = 0; i0 < this.entities_length; i0++) {
      offset = this.entities[i0]._encodeOne(view, offset);
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += 4;
    for (let i0 = 0; i0 < this.deletions_length; i0++) {
      size += this.deletions[i0]._encodedSize();
    }
    for (let i0 = 0; i0 < this.entities_length; i0++) {
      size += this.entities[i0]._encodedSize();
    }
    return size;
  }
}
