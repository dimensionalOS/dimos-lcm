// Auto-generated by lcm-ts. DO NOT EDIT.

import { Pose } from "../geometry_msgs/Pose.ts";
import { Vector3 } from "../geometry_msgs/Vector3.ts";
import { Color } from "./Color.ts";

export class ModelPrimitive {
  static readonly _HASH = 0xf2e8cac8c2e8c200n;
  static readonly _NAME = "foxglove_msgs.ModelPrimitive";

  data_length: number;
  pose: Pose;
  scale: Vector3;
  color: Color;
  override_color: boolean;
  url: string;
  media_type: string;
  data: Uint8Array;

  constructor(init?: Partial<ModelPrimitive>) {
    this.data_length = init?.data_length ?? 0;
    this.pose = init?.pose ?? new Pose();
    this.scale = init?.scale ?? new Vector3();
    this.color = init?.color ?? new Color();
    this.override_color = init?.override_color ?? false;
    this.url = init?.url ?? "";
    this.media_type = init?.media_type ?? "";
    this.data = init?.data ?? new Uint8Array(0);
  }

  static decode(data: Uint8Array): ModelPrimitive {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== ModelPrimitive._HASH) {
      throw new Error(`Hash mismatch: expected ${ModelPrimitive._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new ModelPrimitive();
    ({ offset } = ModelPrimitive._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: ModelPrimitive): { offset: number } {
    target.data_length = view.getInt32(offset, false);
    offset += 4;
    target.pose = new Pose();
    ({ offset } = Pose._decodeOne(view, offset, target.pose));
    target.scale = new Vector3();
    ({ offset } = Vector3._decodeOne(view, offset, target.scale));
    target.color = new Color();
    ({ offset } = Color._decodeOne(view, offset, target.color));
    target.override_color = view.getInt8(offset) !== 0;
    offset += 1;
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.url = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.media_type = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    target.data = new Uint8Array(view.buffer, view.byteOffset + offset, target.data_length);
    offset += target.data_length;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, ModelPrimitive._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setInt32(offset, this.data_length, false);
    offset += 4;
    offset = this.pose._encodeOne(view, offset);
    offset = this.scale._encodeOne(view, offset);
    offset = this.color._encodeOne(view, offset);
    view.setInt8(offset, this.override_color ? 1 : 0);
    offset += 1;
    {
      const encoded = new TextEncoder().encode(this.url);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    {
      const encoded = new TextEncoder().encode(this.media_type);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    new Uint8Array(view.buffer, view.byteOffset + offset).set(this.data.subarray(0, this.data_length));
    offset += this.data_length;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4;
    size += this.pose._encodedSize();
    size += this.scale._encodedSize();
    size += this.color._encodedSize();
    size += 1;
    size += 4 + new TextEncoder().encode(this.url).length + 1;
    size += 4 + new TextEncoder().encode(this.media_type).length + 1;
    size += this.data_length * 1;
    return size;
  }
}
