// Auto-generated by lcm-ts. DO NOT EDIT.

export class GeoJSON {
  static readonly _HASH = 0x15423f51794b2ebcn;
  static readonly _NAME = "foxglove_msgs.GeoJSON";
  private static _packedFingerprint: bigint | null = null;

  geojson: string;

  constructor(init?: Partial<GeoJSON>) {
    this.geojson = init?.geojson ?? "";
  }

  static decode(data: Uint8Array): GeoJSON {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint (recursive hash including nested types)
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    const expectedHash = GeoJSON._getPackedFingerprint();
    if (hash !== expectedHash) {
      throw new Error(`Hash mismatch: expected ${expectedHash.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new GeoJSON();
    ({ offset } = GeoJSON._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: GeoJSON): { offset: number } {
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.geojson = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint (recursive hash including nested types)
    view.setBigUint64(offset, GeoJSON._getPackedFingerprint(), false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    {
      const encoded = new TextEncoder().encode(this.geojson);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 4 + new TextEncoder().encode(this.geojson).length + 1;
    return size;
  }

  // deno-lint-ignore no-explicit-any
  static _getHashRecursive(parents: any[]): bigint {
    if (parents.includes(GeoJSON)) return 0n;
    const newparents = [...parents, GeoJSON];
    let tmphash = GeoJSON._HASH;
    tmphash = (((tmphash << 1n) & 0xffffffffffffffffn) + (tmphash >> 63n)) & 0xffffffffffffffffn;
    return tmphash;
  }

  static _getPackedFingerprint(): bigint {
    if (GeoJSON._packedFingerprint === null) {
      GeoJSON._packedFingerprint = GeoJSON._getHashRecursive([]);
    }
    return GeoJSON._packedFingerprint;
  }
}
