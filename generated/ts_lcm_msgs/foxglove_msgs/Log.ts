// Auto-generated by lcm-ts. DO NOT EDIT.

import { Time } from "../builtin_interfaces/Time.ts";

export class Log {
  static readonly _HASH = 0x6664bee8d8d2dccan;
  static readonly _NAME = "foxglove_msgs.Log";

  static readonly UNKNOWN = 0;
  static readonly DEBUG = 1;
  static readonly INFO = 2;
  static readonly WARNING = 3;
  static readonly ERROR = 4;
  static readonly FATAL = 5;

  timestamp: Time;
  level: number;
  message: string;
  name: string;
  file: string;
  line: number;

  constructor(init?: Partial<Log>) {
    this.timestamp = init?.timestamp ?? new Time();
    this.level = init?.level ?? 0;
    this.message = init?.message ?? "";
    this.name = init?.name ?? "";
    this.file = init?.file ?? "";
    this.line = init?.line ?? 0;
  }

  static decode(data: Uint8Array): Log {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== Log._HASH) {
      throw new Error(`Hash mismatch: expected ${Log._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Log();
    ({ offset } = Log._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Log): { offset: number } {
    target.timestamp = new Time();
    ({ offset } = Time._decodeOne(view, offset, target.timestamp));
    target.level = view.getUint8(offset);
    offset += 1;
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.message = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.name = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    {
      const len = view.getUint32(offset, false);
      offset += 4;
      target.file = new TextDecoder().decode(new Uint8Array(view.buffer, view.byteOffset + offset, len - 1));
      offset += len;
    }
    target.line = view.getInt32(offset, false);
    offset += 4;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, Log._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    offset = this.timestamp._encodeOne(view, offset);
    view.setUint8(offset, this.level);
    offset += 1;
    {
      const encoded = new TextEncoder().encode(this.message);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    {
      const encoded = new TextEncoder().encode(this.name);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    {
      const encoded = new TextEncoder().encode(this.file);
      view.setUint32(offset, encoded.length + 1, false);
      offset += 4;
      new Uint8Array(view.buffer, view.byteOffset + offset).set(encoded);
      offset += encoded.length;
      view.setUint8(offset, 0); // null terminator
      offset += 1;
    }
    view.setInt32(offset, this.line, false);
    offset += 4;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += this.timestamp._encodedSize();
    size += 1;
    size += 4 + new TextEncoder().encode(this.message).length + 1;
    size += 4 + new TextEncoder().encode(this.name).length + 1;
    size += 4 + new TextEncoder().encode(this.file).length + 1;
    size += 4;
    return size;
  }
}
