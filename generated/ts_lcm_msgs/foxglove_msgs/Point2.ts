// Auto-generated by lcm-ts. DO NOT EDIT.

export class Point2 {
  static readonly _HASH = 0xf0c8deeac4d8caf2n;
  static readonly _NAME = "foxglove_msgs.Point2";

  x: number;
  y: number;

  constructor(init?: Partial<Point2>) {
    this.x = init?.x ?? 0;
    this.y = init?.y ?? 0;
  }

  static decode(data: Uint8Array): Point2 {
    const view = new DataView(data.buffer, data.byteOffset, data.byteLength);
    let offset = 0;

    // Verify fingerprint
    const hash = view.getBigUint64(offset, false);
    offset += 8;
    if (hash !== Point2._HASH) {
      throw new Error(`Hash mismatch: expected ${Point2._HASH.toString(16)}, got ${hash.toString(16)}`);
    }

    const result = new Point2();
    ({ offset } = Point2._decodeOne(view, offset, result));
    return result;
  }

  static _decodeOne(view: DataView, offset: number, target: Point2): { offset: number } {
    target.x = view.getFloat64(offset, false);
    offset += 8;
    target.y = view.getFloat64(offset, false);
    offset += 8;
    return { offset };
  }

  encode(): Uint8Array {
    const size = 8 + this._encodedSize();
    const data = new Uint8Array(size);
    const view = new DataView(data.buffer);
    let offset = 0;

    // Write fingerprint
    view.setBigUint64(offset, Point2._HASH, false);
    offset += 8;

    offset = this._encodeOne(view, offset);
    return data;
  }

  _encodeOne(view: DataView, offset: number): number {
    view.setFloat64(offset, this.x, false);
    offset += 8;
    view.setFloat64(offset, this.y, false);
    offset += 8;
    return offset;
  }

  _encodedSize(): number {
    let size = 0;
    size += 8;
    size += 8;
    return size;
  }
}
