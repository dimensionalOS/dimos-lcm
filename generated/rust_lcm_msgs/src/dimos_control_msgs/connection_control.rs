// Auto-generated by lcm-rust-gen. DO NOT EDIT.

use byteorder::{BigEndian, ReadBytesExt, WriteBytesExt};
use std::io::{self, Read, Write, Cursor};
use std::sync::OnceLock;

#[derive(Debug, Clone, Default, PartialEq)]
pub struct ConnectionControl {
    pub target_sources: Vec<std::string::String>,
    pub operation_id: i64,
    pub operation: i8,
    pub proposed_control_epoch: i64,
    pub reason: std::string::String,
}

impl ConnectionControl {
    pub const HASH: i64 = 0xB82D9643472F465Du64 as i64;
    pub const NAME: &str = "dimos_control_msgs.ConnectionControl";

    pub const PREPARE_ARM: i8 = 1;
    pub const COMMIT_ARM: i8 = 2;
    pub const ABORT_ARM: i8 = 3;
    pub const SAFE_STOP: i8 = 4;
    pub const CLEAR_SAFE_STOP: i8 = 5;
    pub const ESTOP: i8 = 6;
    pub const CLEAR_ESTOP: i8 = 7;

    fn packed_fingerprint() -> u64 {
        static CACHE: OnceLock<u64> = OnceLock::new();
        *CACHE.get_or_init(|| Self::hash_recursive(&mut Vec::new()))
    }

    pub(crate) fn hash_recursive(parents: &mut Vec<u64>) -> u64 {
        let self_hash = Self::HASH as u64;
        if parents.contains(&self_hash) {
            return 0;
        }
        parents.push(self_hash);
        let tmphash = self_hash as u64;
        parents.pop();
        // rotate left by 1
        tmphash << 1 | tmphash >> 63
    }

    pub fn encode(&self) -> Vec<u8> {
        let mut buf = Vec::with_capacity(8 + self.encoded_size());
        buf.write_u64::<BigEndian>(Self::packed_fingerprint()).unwrap();
        self.encode_one(&mut buf).unwrap();
        buf
    }

    pub fn decode(data: &[u8]) -> io::Result<Self> {
        let mut cursor = Cursor::new(data);
        let hash = cursor.read_u64::<BigEndian>()?;
        let expected = Self::packed_fingerprint();
        if hash != expected {
            return Err(io::Error::new(io::ErrorKind::InvalidData,
                format!("Hash mismatch: expected {:016x}, got {:016x}", expected, hash)));
        }
        Self::decode_one(&mut cursor)
    }

    pub fn encode_one<W: Write>(&self, buf: &mut W) -> io::Result<()> {
        buf.write_i32::<BigEndian>(self.target_sources.len() as i32)?;
        for v0 in self.target_sources.iter() {
            {
                let bytes = v0.as_bytes();
                buf.write_u32::<BigEndian>((bytes.len() + 1) as u32)?;
                buf.write_all(bytes)?;
                buf.write_u8(0)?;
            }
        }
        buf.write_i64::<BigEndian>(self.operation_id)?;
        buf.write_i8(self.operation)?;
        buf.write_i64::<BigEndian>(self.proposed_control_epoch)?;
        {
            let bytes = self.reason.as_bytes();
            buf.write_u32::<BigEndian>((bytes.len() + 1) as u32)?;
            buf.write_all(bytes)?;
            buf.write_u8(0)?;
        }
        Ok(())
    }

    pub fn decode_one<R: Read>(buf: &mut R) -> io::Result<Self> {
        let target_sources_length = buf.read_i32::<BigEndian>()? as usize;
        let target_sources = {
            let mut v = Vec::with_capacity(target_sources_length);
            for _ in 0..target_sources_length {
                let _elem_0 = {
                    let len = buf.read_u32::<BigEndian>()? as usize;
                    let mut bytes = vec![0u8; len];
                    buf.read_exact(&mut bytes)?;
                    std::string::String::from_utf8(bytes[..len - 1].to_vec())
                        .map_err(|e| io::Error::new(io::ErrorKind::InvalidData, e))?
                };
                v.push(_elem_0);
            }
            v
        };
        let operation_id = buf.read_i64::<BigEndian>()?;
        let operation = buf.read_i8()?;
        let proposed_control_epoch = buf.read_i64::<BigEndian>()?;
        let reason = {
            let len = buf.read_u32::<BigEndian>()? as usize;
            let mut bytes = vec![0u8; len];
            buf.read_exact(&mut bytes)?;
            std::string::String::from_utf8(bytes[..len - 1].to_vec())
                .map_err(|e| io::Error::new(io::ErrorKind::InvalidData, e))?
        };
        Ok(Self {
            target_sources,
            operation_id,
            operation,
            proposed_control_epoch,
            reason,
        })
    }

    pub fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 4;
        for s0 in self.target_sources.iter() {
            size += 4 + s0.len() + 1;
        }
        size += 8;
        size += 1;
        size += 8;
        size += 4 + self.reason.len() + 1;
        size
    }

}
