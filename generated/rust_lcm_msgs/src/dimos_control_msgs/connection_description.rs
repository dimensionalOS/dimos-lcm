// Auto-generated by lcm-rust-gen. DO NOT EDIT.

use byteorder::{BigEndian, ReadBytesExt, WriteBytesExt};
use std::io::{self, Read, Write, Cursor};
use std::sync::OnceLock;

#[derive(Debug, Clone, Default, PartialEq)]
pub struct ConnectionDescription {
    pub source: std::string::String,
    pub description_epoch: i64,
    pub resources: Vec<crate::dimos_control_msgs::ResourceDescription>,
    pub interfaces: Vec<crate::dimos_control_msgs::InterfaceDescription>,
    pub command_profiles: Vec<crate::dimos_control_msgs::CommandProfile>,
    pub expected_state_rate_hz: f64,
    pub state_stale_timeout_s: f64,
    pub command_watchdog_timeout_s: f64,
    pub activation_policy: i8,
    pub armed_idle_policy: i8,
    pub safe_stop_behavior: std::string::String,
    pub process_loss_classification: i8,
    pub process_loss_evidence: std::string::String,
}

impl ConnectionDescription {
    pub const HASH: i64 = 0x31D30851F65C7D8Cu64 as i64;
    pub const NAME: &str = "dimos_control_msgs.ConnectionDescription";

    pub const DIRECT: i8 = 1;
    pub const OPERATOR_CONFIRMED: i8 = 2;
    pub const APPLY_OMISSION: i8 = 1;
    pub const ARMED_IDLE_SAFE_STOP: i8 = 2;
    pub const NATIVE_WATCHDOG: i8 = 1;
    pub const EXTERNAL_SUPERVISOR: i8 = 2;
    pub const INTRINSICALLY_SAFE: i8 = 3;
    pub const SIMULATION: i8 = 4;
    pub const UNPROTECTED: i8 = 5;

    fn packed_fingerprint() -> u64 {
        static CACHE: OnceLock<u64> = OnceLock::new();
        *CACHE.get_or_init(|| Self::hash_recursive(&mut Vec::new()))
    }

    pub(crate) fn hash_recursive(parents: &mut Vec<u64>) -> u64 {
        let self_hash = Self::HASH as u64;
        if parents.contains(&self_hash) {
            return 0;
        }
        parents.push(self_hash);
        let mut tmphash = self_hash as u64;
        tmphash = tmphash.wrapping_add(crate::dimos_control_msgs::ResourceDescription::hash_recursive(parents));
        tmphash = tmphash.wrapping_add(crate::dimos_control_msgs::InterfaceDescription::hash_recursive(parents));
        tmphash = tmphash.wrapping_add(crate::dimos_control_msgs::CommandProfile::hash_recursive(parents));
        parents.pop();
        // rotate left by 1
        tmphash << 1 | tmphash >> 63
    }

    pub fn encode(&self) -> Vec<u8> {
        let mut buf = Vec::with_capacity(8 + self.encoded_size());
        buf.write_u64::<BigEndian>(Self::packed_fingerprint()).unwrap();
        self.encode_one(&mut buf).unwrap();
        buf
    }

    pub fn decode(data: &[u8]) -> io::Result<Self> {
        let mut cursor = Cursor::new(data);
        let hash = cursor.read_u64::<BigEndian>()?;
        let expected = Self::packed_fingerprint();
        if hash != expected {
            return Err(io::Error::new(io::ErrorKind::InvalidData,
                format!("Hash mismatch: expected {:016x}, got {:016x}", expected, hash)));
        }
        Self::decode_one(&mut cursor)
    }

    pub fn encode_one<W: Write>(&self, buf: &mut W) -> io::Result<()> {
        {
            let bytes = self.source.as_bytes();
            buf.write_u32::<BigEndian>((bytes.len() + 1) as u32)?;
            buf.write_all(bytes)?;
            buf.write_u8(0)?;
        }
        buf.write_i64::<BigEndian>(self.description_epoch)?;
        buf.write_i32::<BigEndian>(self.resources.len() as i32)?;
        buf.write_i32::<BigEndian>(self.interfaces.len() as i32)?;
        buf.write_i32::<BigEndian>(self.command_profiles.len() as i32)?;
        for v0 in self.resources.iter() {
            v0.encode_one(buf)?;
        }
        for v0 in self.interfaces.iter() {
            v0.encode_one(buf)?;
        }
        for v0 in self.command_profiles.iter() {
            v0.encode_one(buf)?;
        }
        buf.write_f64::<BigEndian>(self.expected_state_rate_hz)?;
        buf.write_f64::<BigEndian>(self.state_stale_timeout_s)?;
        buf.write_f64::<BigEndian>(self.command_watchdog_timeout_s)?;
        buf.write_i8(self.activation_policy)?;
        buf.write_i8(self.armed_idle_policy)?;
        {
            let bytes = self.safe_stop_behavior.as_bytes();
            buf.write_u32::<BigEndian>((bytes.len() + 1) as u32)?;
            buf.write_all(bytes)?;
            buf.write_u8(0)?;
        }
        buf.write_i8(self.process_loss_classification)?;
        {
            let bytes = self.process_loss_evidence.as_bytes();
            buf.write_u32::<BigEndian>((bytes.len() + 1) as u32)?;
            buf.write_all(bytes)?;
            buf.write_u8(0)?;
        }
        Ok(())
    }

    pub fn decode_one<R: Read>(buf: &mut R) -> io::Result<Self> {
        let source = {
            let len = buf.read_u32::<BigEndian>()? as usize;
            let mut bytes = vec![0u8; len];
            buf.read_exact(&mut bytes)?;
            std::string::String::from_utf8(bytes[..len - 1].to_vec())
                .map_err(|e| io::Error::new(io::ErrorKind::InvalidData, e))?
        };
        let description_epoch = buf.read_i64::<BigEndian>()?;
        let resources_length = buf.read_i32::<BigEndian>()? as usize;
        let interfaces_length = buf.read_i32::<BigEndian>()? as usize;
        let command_profiles_length = buf.read_i32::<BigEndian>()? as usize;
        let resources = {
            let mut v = Vec::with_capacity(resources_length);
            for _ in 0..resources_length {
                let _elem_0 = crate::dimos_control_msgs::ResourceDescription::decode_one(buf)?;
                v.push(_elem_0);
            }
            v
        };
        let interfaces = {
            let mut v = Vec::with_capacity(interfaces_length);
            for _ in 0..interfaces_length {
                let _elem_0 = crate::dimos_control_msgs::InterfaceDescription::decode_one(buf)?;
                v.push(_elem_0);
            }
            v
        };
        let command_profiles = {
            let mut v = Vec::with_capacity(command_profiles_length);
            for _ in 0..command_profiles_length {
                let _elem_0 = crate::dimos_control_msgs::CommandProfile::decode_one(buf)?;
                v.push(_elem_0);
            }
            v
        };
        let expected_state_rate_hz = buf.read_f64::<BigEndian>()?;
        let state_stale_timeout_s = buf.read_f64::<BigEndian>()?;
        let command_watchdog_timeout_s = buf.read_f64::<BigEndian>()?;
        let activation_policy = buf.read_i8()?;
        let armed_idle_policy = buf.read_i8()?;
        let safe_stop_behavior = {
            let len = buf.read_u32::<BigEndian>()? as usize;
            let mut bytes = vec![0u8; len];
            buf.read_exact(&mut bytes)?;
            std::string::String::from_utf8(bytes[..len - 1].to_vec())
                .map_err(|e| io::Error::new(io::ErrorKind::InvalidData, e))?
        };
        let process_loss_classification = buf.read_i8()?;
        let process_loss_evidence = {
            let len = buf.read_u32::<BigEndian>()? as usize;
            let mut bytes = vec![0u8; len];
            buf.read_exact(&mut bytes)?;
            std::string::String::from_utf8(bytes[..len - 1].to_vec())
                .map_err(|e| io::Error::new(io::ErrorKind::InvalidData, e))?
        };
        Ok(Self {
            source,
            description_epoch,
            resources,
            interfaces,
            command_profiles,
            expected_state_rate_hz,
            state_stale_timeout_s,
            command_watchdog_timeout_s,
            activation_policy,
            armed_idle_policy,
            safe_stop_behavior,
            process_loss_classification,
            process_loss_evidence,
        })
    }

    pub fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 4 + self.source.len() + 1;
        size += 8;
        size += 4;
        size += 4;
        size += 4;
        for v0 in self.resources.iter() {
            size += v0.encoded_size();
        }
        for v0 in self.interfaces.iter() {
            size += v0.encoded_size();
        }
        for v0 in self.command_profiles.iter() {
            size += v0.encoded_size();
        }
        size += 8;
        size += 8;
        size += 8;
        size += 1;
        size += 1;
        size += 4 + self.safe_stop_behavior.len() + 1;
        size += 1;
        size += 4 + self.process_loss_evidence.len() + 1;
        size
    }

}
