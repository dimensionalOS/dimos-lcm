// Auto-generated by lcm-rust-gen. DO NOT EDIT.

use byteorder::{BigEndian, ReadBytesExt, WriteBytesExt};
use std::io::{self, Read, Write, Cursor};
use std::sync::OnceLock;

#[derive(Debug, Clone, Default, PartialEq)]
pub struct InterfaceDescription {
    pub name: std::string::String,
    pub resource: std::string::String,
    pub field: std::string::String,
    pub unit: std::string::String,
    pub state: bool,
    pub command: bool,
    pub has_hard_min: bool,
    pub hard_min: f64,
    pub has_hard_max: bool,
    pub hard_max: f64,
    pub has_soft_min: bool,
    pub soft_min: f64,
    pub has_soft_max: bool,
    pub soft_max: f64,
    pub omission_policy: i8,
}

impl InterfaceDescription {
    pub const HASH: i64 = 0xBFB21E12C9A9BD0Du64 as i64;
    pub const NAME: &str = "dimos_control_msgs.InterfaceDescription";

    pub const HOLD_LAST: i8 = 1;
    pub const ZERO: i8 = 2;
    pub const SAFE_STOP: i8 = 3;

    fn packed_fingerprint() -> u64 {
        static CACHE: OnceLock<u64> = OnceLock::new();
        *CACHE.get_or_init(|| Self::hash_recursive(&mut Vec::new()))
    }

    pub(crate) fn hash_recursive(parents: &mut Vec<u64>) -> u64 {
        let self_hash = Self::HASH as u64;
        if parents.contains(&self_hash) {
            return 0;
        }
        parents.push(self_hash);
        let tmphash = self_hash as u64;
        parents.pop();
        // rotate left by 1
        tmphash << 1 | tmphash >> 63
    }

    pub fn encode(&self) -> Vec<u8> {
        let mut buf = Vec::with_capacity(8 + self.encoded_size());
        buf.write_u64::<BigEndian>(Self::packed_fingerprint()).unwrap();
        self.encode_one(&mut buf).unwrap();
        buf
    }

    pub fn decode(data: &[u8]) -> io::Result<Self> {
        let mut cursor = Cursor::new(data);
        let hash = cursor.read_u64::<BigEndian>()?;
        let expected = Self::packed_fingerprint();
        if hash != expected {
            return Err(io::Error::new(io::ErrorKind::InvalidData,
                format!("Hash mismatch: expected {:016x}, got {:016x}", expected, hash)));
        }
        Self::decode_one(&mut cursor)
    }

    pub fn encode_one<W: Write>(&self, buf: &mut W) -> io::Result<()> {
        {
            let bytes = self.name.as_bytes();
            buf.write_u32::<BigEndian>((bytes.len() + 1) as u32)?;
            buf.write_all(bytes)?;
            buf.write_u8(0)?;
        }
        {
            let bytes = self.resource.as_bytes();
            buf.write_u32::<BigEndian>((bytes.len() + 1) as u32)?;
            buf.write_all(bytes)?;
            buf.write_u8(0)?;
        }
        {
            let bytes = self.field.as_bytes();
            buf.write_u32::<BigEndian>((bytes.len() + 1) as u32)?;
            buf.write_all(bytes)?;
            buf.write_u8(0)?;
        }
        {
            let bytes = self.unit.as_bytes();
            buf.write_u32::<BigEndian>((bytes.len() + 1) as u32)?;
            buf.write_all(bytes)?;
            buf.write_u8(0)?;
        }
        buf.write_i8(if self.state { 1 } else { 0 })?;
        buf.write_i8(if self.command { 1 } else { 0 })?;
        buf.write_i8(if self.has_hard_min { 1 } else { 0 })?;
        buf.write_f64::<BigEndian>(self.hard_min)?;
        buf.write_i8(if self.has_hard_max { 1 } else { 0 })?;
        buf.write_f64::<BigEndian>(self.hard_max)?;
        buf.write_i8(if self.has_soft_min { 1 } else { 0 })?;
        buf.write_f64::<BigEndian>(self.soft_min)?;
        buf.write_i8(if self.has_soft_max { 1 } else { 0 })?;
        buf.write_f64::<BigEndian>(self.soft_max)?;
        buf.write_i8(self.omission_policy)?;
        Ok(())
    }

    pub fn decode_one<R: Read>(buf: &mut R) -> io::Result<Self> {
        let name = {
            let len = buf.read_u32::<BigEndian>()? as usize;
            let mut bytes = vec![0u8; len];
            buf.read_exact(&mut bytes)?;
            std::string::String::from_utf8(bytes[..len - 1].to_vec())
                .map_err(|e| io::Error::new(io::ErrorKind::InvalidData, e))?
        };
        let resource = {
            let len = buf.read_u32::<BigEndian>()? as usize;
            let mut bytes = vec![0u8; len];
            buf.read_exact(&mut bytes)?;
            std::string::String::from_utf8(bytes[..len - 1].to_vec())
                .map_err(|e| io::Error::new(io::ErrorKind::InvalidData, e))?
        };
        let field = {
            let len = buf.read_u32::<BigEndian>()? as usize;
            let mut bytes = vec![0u8; len];
            buf.read_exact(&mut bytes)?;
            std::string::String::from_utf8(bytes[..len - 1].to_vec())
                .map_err(|e| io::Error::new(io::ErrorKind::InvalidData, e))?
        };
        let unit = {
            let len = buf.read_u32::<BigEndian>()? as usize;
            let mut bytes = vec![0u8; len];
            buf.read_exact(&mut bytes)?;
            std::string::String::from_utf8(bytes[..len - 1].to_vec())
                .map_err(|e| io::Error::new(io::ErrorKind::InvalidData, e))?
        };
        let state = buf.read_i8()? != 0;
        let command = buf.read_i8()? != 0;
        let has_hard_min = buf.read_i8()? != 0;
        let hard_min = buf.read_f64::<BigEndian>()?;
        let has_hard_max = buf.read_i8()? != 0;
        let hard_max = buf.read_f64::<BigEndian>()?;
        let has_soft_min = buf.read_i8()? != 0;
        let soft_min = buf.read_f64::<BigEndian>()?;
        let has_soft_max = buf.read_i8()? != 0;
        let soft_max = buf.read_f64::<BigEndian>()?;
        let omission_policy = buf.read_i8()?;
        Ok(Self {
            name,
            resource,
            field,
            unit,
            state,
            command,
            has_hard_min,
            hard_min,
            has_hard_max,
            hard_max,
            has_soft_min,
            soft_min,
            has_soft_max,
            soft_max,
            omission_policy,
        })
    }

    pub fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 4 + self.name.len() + 1;
        size += 4 + self.resource.len() + 1;
        size += 4 + self.field.len() + 1;
        size += 4 + self.unit.len() + 1;
        size += 1;
        size += 1;
        size += 1;
        size += 8;
        size += 1;
        size += 8;
        size += 1;
        size += 8;
        size += 1;
        size += 8;
        size += 1;
        size
    }

}
