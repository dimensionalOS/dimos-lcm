// Auto-generated by lcm-rust-gen. DO NOT EDIT.

use byteorder::{BigEndian, ReadBytesExt, WriteBytesExt};
use std::io::{self, Read, Write, Cursor};
use std::sync::OnceLock;

#[derive(Debug, Clone, Default, PartialEq)]
pub struct ConnectionStatus {
    pub source: std::string::String,
    pub source_ts: f64,
    pub lifecycle_state: i8,
    pub description_epoch: i64,
    pub control_epoch: i64,
    pub last_operation_id: i64,
    pub last_operation: i8,
    pub acknowledgement: i8,
    pub fault: std::string::String,
}

impl ConnectionStatus {
    pub const HASH: i64 = 0xC90F95EBFAD6E276u64 as i64;
    pub const NAME: &str = "dimos_control_msgs.ConnectionStatus";

    pub const STANDBY: i8 = 1;
    pub const PREPARING: i8 = 2;
    pub const PREPARED: i8 = 3;
    pub const COMMITTING: i8 = 4;
    pub const ARMED: i8 = 5;
    pub const SAFE_STOPPED: i8 = 6;
    pub const ESTOPPED: i8 = 7;
    pub const NONE: i8 = 0;
    pub const PENDING: i8 = 1;
    pub const SUCCEEDED: i8 = 2;
    pub const REJECTED: i8 = 3;

    fn packed_fingerprint() -> u64 {
        static CACHE: OnceLock<u64> = OnceLock::new();
        *CACHE.get_or_init(|| Self::hash_recursive(&mut Vec::new()))
    }

    pub(crate) fn hash_recursive(parents: &mut Vec<u64>) -> u64 {
        let self_hash = Self::HASH as u64;
        if parents.contains(&self_hash) {
            return 0;
        }
        parents.push(self_hash);
        let tmphash = self_hash as u64;
        parents.pop();
        // rotate left by 1
        tmphash << 1 | tmphash >> 63
    }

    pub fn encode(&self) -> Vec<u8> {
        let mut buf = Vec::with_capacity(8 + self.encoded_size());
        buf.write_u64::<BigEndian>(Self::packed_fingerprint()).unwrap();
        self.encode_one(&mut buf).unwrap();
        buf
    }

    pub fn decode(data: &[u8]) -> io::Result<Self> {
        let mut cursor = Cursor::new(data);
        let hash = cursor.read_u64::<BigEndian>()?;
        let expected = Self::packed_fingerprint();
        if hash != expected {
            return Err(io::Error::new(io::ErrorKind::InvalidData,
                format!("Hash mismatch: expected {:016x}, got {:016x}", expected, hash)));
        }
        Self::decode_one(&mut cursor)
    }

    pub fn encode_one<W: Write>(&self, buf: &mut W) -> io::Result<()> {
        {
            let bytes = self.source.as_bytes();
            buf.write_u32::<BigEndian>((bytes.len() + 1) as u32)?;
            buf.write_all(bytes)?;
            buf.write_u8(0)?;
        }
        buf.write_f64::<BigEndian>(self.source_ts)?;
        buf.write_i8(self.lifecycle_state)?;
        buf.write_i64::<BigEndian>(self.description_epoch)?;
        buf.write_i64::<BigEndian>(self.control_epoch)?;
        buf.write_i64::<BigEndian>(self.last_operation_id)?;
        buf.write_i8(self.last_operation)?;
        buf.write_i8(self.acknowledgement)?;
        {
            let bytes = self.fault.as_bytes();
            buf.write_u32::<BigEndian>((bytes.len() + 1) as u32)?;
            buf.write_all(bytes)?;
            buf.write_u8(0)?;
        }
        Ok(())
    }

    pub fn decode_one<R: Read>(buf: &mut R) -> io::Result<Self> {
        let source = {
            let len = buf.read_u32::<BigEndian>()? as usize;
            let mut bytes = vec![0u8; len];
            buf.read_exact(&mut bytes)?;
            std::string::String::from_utf8(bytes[..len - 1].to_vec())
                .map_err(|e| io::Error::new(io::ErrorKind::InvalidData, e))?
        };
        let source_ts = buf.read_f64::<BigEndian>()?;
        let lifecycle_state = buf.read_i8()?;
        let description_epoch = buf.read_i64::<BigEndian>()?;
        let control_epoch = buf.read_i64::<BigEndian>()?;
        let last_operation_id = buf.read_i64::<BigEndian>()?;
        let last_operation = buf.read_i8()?;
        let acknowledgement = buf.read_i8()?;
        let fault = {
            let len = buf.read_u32::<BigEndian>()? as usize;
            let mut bytes = vec![0u8; len];
            buf.read_exact(&mut bytes)?;
            std::string::String::from_utf8(bytes[..len - 1].to_vec())
                .map_err(|e| io::Error::new(io::ErrorKind::InvalidData, e))?
        };
        Ok(Self {
            source,
            source_ts,
            lifecycle_state,
            description_epoch,
            control_epoch,
            last_operation_id,
            last_operation,
            acknowledgement,
            fault,
        })
    }

    pub fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 4 + self.source.len() + 1;
        size += 8;
        size += 1;
        size += 8;
        size += 8;
        size += 8;
        size += 1;
        size += 1;
        size += 4 + self.fault.len() + 1;
        size
    }

}
